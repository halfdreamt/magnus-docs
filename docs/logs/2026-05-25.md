# 05/25/26 - 05/31/26

Week Goals:
- [ ] Magnus3: Formalize a food-source model that supports alternatives to gathering (farming, hunting, herding, fishing) without baking any of them in

Accomplished:
-

Monday - 05/25/26
Daily Goals:
- [ ] Magnus3: Land an initial design sketch for the generalized food-source model
Log:
- Magnus3: Made family-member names in the new minimap side panel clickable. Reused the existing `.inspect-link` styling so they match the rest of the viewer's navigation links. Wired `MinimapPanel.onNavigate` in `main.js` to the same flow used by the community panel — scrolls the timeline to the person's row, sets `selectedSegmentId`, opens the right-side inspector with the person, and updates the family highlight overlay. The minimap stays open so the user can keep exploring the spatial map alongside the inspector. Two-file change: `viewer/js/minimap.js` (add data-entity-id + click listener), `viewer/js/main.js` (wire onNavigate).
- Magnus3: Brainstorming session on generalizing the food system to admit alternatives to gathering
  - Premise carried over from last week's rewrite: we unbaked the "farming communities" assumption, generalized to one labor-vs-land formula, and shipped gathering as the v1 default. Alternatives (Farmer/Herder/Fisher/Hunter) are parked as design notes at the bottom of the ecology design doc, not scaffolded — there is no strategy/plugin surface yet.
  - Verified the unbaking is real: only residual "grain" in the codebase is the unrelated word "finer-grained" in a comment in `EcologyComponents.hpp`. `GatheringSystem.hpp` is the sole food-production system and its header explicitly says "Default food-production behavior."
- Magnus3: First-pass framing — formalize per-mode parameters that plug into the existing equation
  - Equation today: `yield = min(labor × efficiency, tract_cap × (1 + leader_bonus)) × variance`
  - Proposed dial set so any "mode" is just a named configuration of dials: `efficiency`, `labor_curve` (linear/sub-linear/threshold), `tract_cap_fn` (how the mode reads tract attributes), `biome_affinity` (multiplier or hard gate), `tract_radius`, `depletion/recovery`, `variance_band`, `capital_state`, `cadence` (per-season / yearly cycle / seasonal peak), `leader_channel`
  - Walked five candidate modes (Gatherer / Hunter / Fisher / Pastoralist / Farmer) against the dials. Stress points that emerged: `tract_cap_fn` was doing a lot of work because three of five modes want the cap derived from a *different* tract attribute than the gatherable-food cap; `capital` and `depletion` are the two dials with no precedent in current code (both add persistent inter-season state); `cadence` is the one dial that genuinely breaks the equation's shape because Farmer's annual plant-then-harvest doesn't fit "compute every season."
- Magnus3: Ryan reframed the model — modes were the wrong abstraction
  - **The reframe:** communities don't *be* a mode; they *allocate labor* across whatever sources their land affords. Sources naturally form a 2×2 grid: **Flora/Fauna × Wild/Domesticated**. Hunting and fishing collapse into Fauna-Wild (gating differences become biome/sub-type variation, not separate modes). Gathering is Flora-Wild. Farming is Flora-Domesticated. Herding is Fauna-Domesticated.
  - **The deeper move** was the layer separation: properties of the *sources themselves* (intrinsic to land) vs. capacity of the *community and its members* to process those sources (intrinsic to the community). Each layer owns different state; the yield equation factors across both.
- Magnus3: Worked out the two-layer model in more detail
  - **Source layer (on the tract):** each tract carries a vector of sources, one per active cell of the 2×2 it can support. Per source: `potential` (ceiling the land could support), `standing_stock` (current state — game density, established cropland, available grazing; wild stocks replenish naturally, domesticated stocks need labor to exist), `biome_multiplier`. A coastal forest might have all four cells active; a bog might have only Flora-Wild; tundra only Fauna-Wild. The land *affords* a configuration; it doesn't dictate behavior.
  - **Community capability layer (on the community):** `labor_pool` (eventually age-weighted), `per-cell processing_capacity` (efficiency × skill × leader_bonus × tool_stock for each cell — a community can be good at hunting but poor at farming on land that supports both), `allocation_policy` (how labor splits across available sources each season), `capital_state` per domesticated cell (herd size, cleared/planted acreage — the tract holds *potential* cropland/grazing; the community holds *the actual herd / the actual field*).
  - **Per-source yield:** `yield_s = min(labor_s × efficiency_s, stock_s × harvest_rate_s) × variance_s`. Total community food = Σ yield_s. Same `min(labor, land)` shape as today, just per-source and summed.
- Magnus3: What this factoring unlocks
  - **Mixed economies emerge naturally** — no mode-switching, no either/or. A community fishes in spring, gathers in summer, harvests in autumn from the same allocator.
  - **Identical land, different communities** — two settlements on the same coastal forest can specialize differently based on accumulated capital and skill.
  - **Transitions are continuous** — adopting agriculture isn't a mode flip; it's the community starting to invest labor into Flora-Domesticated capital on a tract that has the potential. Wild→domesticated plays out as capital accumulation over generations.
  - **Tract value becomes legible** — a tract's "carrying capacity" stops being one number; it's a vector of source potentials. Migration and fission decisions can read which cells are unsaturated.
  - **Leader bonuses sharpen** — instead of one `gathering_bonus`, a leader has affinities per cell.
- Magnus3: Open design questions surfaced, parked for the next session
  - **Capital ownership on transitions:** herds and fields are *of* the community but *on* the tract. Leaning toward community-owns-capital + references-tract, so fission/migration forces a deliberate choice about what to take, leave, or rebuild.
  - **Sub-types within a cell:** Fauna-Wild bundles hunting and fishing; mechanically large game / small game / fish behave differently. First-class entries in the source vector vs. biome-modifier variation within one Fauna-Wild entry — undecided.
  - **Allocation policy is its own design surface.** Proportional-to-expected-return is fine for v1; cultural inertia and risk preference get interesting later. Budgeting note: this is per-community per-season — same hot path that the archetype-churn fix just rescued — so it has to stay cheap arithmetic.
  - **Wild ↔ Domesticated binary vs. continuum:** voting binary for v1. The gradual story emerges from capital accumulating in domesticated cells over generations; we don't need a continuous coordinate.
  - **What replaces the "default mode" today:** every community starts with allocation toward the wild cells the land affords (Domesticated cells start at zero capital, inert until labor is invested). Domestication emerges from sustained investment rather than being chosen at founding.
- Magnus3: Where to push next — the load-bearing threads are allocation policy and capital-on-fission. The broader open question is whether to step further back first and reconsider how this changes the existing Tract/Plot data model and the Famine event shape before going deeper on any single piece.
- Magnus3: Added a "Starvation" section to the Famine event inspector — clickable per-person rows showing year range, duration, and a "died" tag when the interval ended at the person's death tick. Sorted by duration desc, reuses the existing `.inspect-list` 200px scroll cap. Single-file change in `viewer/js/inspect.js`.
- Magnus3: Ryan flagged that most short famines showed zero entries in the new section, and one showed `#1382` instead of a name. Investigated and found two distinct bugs:
  - **Data loss bug.** Instrumented `ConsumptionSystem`: sim counter said `intervals_opened=53282`, but only 952 landed in exported FamineParams — 1.8% retention. Added more counters and confirmed `fp_null_after_ensure=2172` (exactly equal to `famines_opened`), meaning **every freshly-created famine's `get_mut<FamineParams>()` returned null**. Root cause: Flecs systems run inside an implicit defer scope where `entity.set<T>()` is queued until end-of-frame. So `open_famine_event()` would `.set<FamineParams>({})` (deferred), then the immediate `get_mut<FamineParams>()` in `ensure_famine_open` returned null because the deferred set hadn't been flushed yet. Result: every famine lost its *first-season* intervals. For famines that closed after one season (1,784 of 2,172 = 82% of all famines), that meant *all* intervals were lost.
  - **ID remapping bug.** The `iv.person_id` recorded in `ConsumptionSystem` is the raw Flecs entity id (`person.id()`), but `ExportCommand` remaps every other entity reference through a `get_id()` lambda that assigns stable sequential IDs starting at 1. The starvation-interval export bypassed that remap, so the viewer's `entityMap.get(rawFlecsId)` never matched and fell through to the `#NNNN` fallback.
- Magnus3: Fixed both
  - `ConsumptionSystem.hpp`: wrap famine creation in `ecs.defer_suspend()` / `defer_resume()` so `set<FamineParams>` takes effect immediately and the subsequent `get_mut` returns a valid pointer the same season.
  - `ExportCommand.hpp`: route `iv.person_id` through `get_id(world.ecs().entity(iv.person_id))` so the exported id matches the viewer's entity map.
  - Verified: 53,282 intervals retained (vs 952), 0 zero-interval famines (vs 1,784), sample person_ids resolve in the entities array.
- Magnus3: Perf check — defer_suspend/resume during famine creation is essentially free. Three runs each pre-fix and post-fix, 1000y seed 42 default config: pre-fix mean tick_total ~186ms (range 173–202), post-fix ~172ms (range 163–184). Within noise; if anything slightly faster post-fix, likely because immediate archetype resolution avoids cascading deferred-state lookups. The legitimate "167ms" baseline from last week's resolution still holds.
- Magnus3: Moved famine rendering off the main timeline and into the per-community food/population chart in the bottom drawer. Three-file change: emptied `EVENT_TYPE_ORDER` in `constants.js` so no event tracks render on the main timeline at all (Famine was the only entry); `inspect.js` `_drawFoodChart` now filters famines by community and paints them as translucent red bands (~18% alpha) spanning each famine's duration, behind the food/pop curves, with a "Famine (N)" legend entry and click-to-inspect via a new `onShowEvent` callback; `main.js` wires `communityPanel.onShowEvent` to `inspect.showEvent`. Net effect: main timeline is quieter (no event tracks at all anymore), and famines now show in their natural context — overlaid on the food curve that triggered them and the population curve that they shaped — with the new starvation drill-down a click away.
- Magnus3: Ryan asked whether the current toolset can dial famine fraction down from "feels like always" to ≤50% of the timeline without splitting the single fertility knob into interbirth/menarche/menopause components. Walked the equilibrium math: with `tract.cap_base=40` × biome mult and `food_per_person_per_season=3`, carrying capacity is ~13 people/tract on grassland; with `birth.chance_per_adult_per_spring=0.15` applied per married pair, expected children/pair ≈ 7.5 over a 50-year fertile window — communities double in ~12y and slam the ceiling. Recommended dropping the birth knob; flagged that the deeper cause is near-zero non-famine mortality, so a future fertility split won't fix the dynamic without adding background mortality.
- Magnus3: Ryan reminded me the `tools/exponator/exponator.py` harness already exists for this kind of "what would different worlds look like?" sweep. Saved a `reference_exponator_tool.md` auto-memory so I reach for it first next time. Extended exponator with four famine metrics — `famine_fraction_mean` (per-community famine_ticks / lifespan, averaged across communities), `famine_fraction_weighted` (time-weighted), `total_famines`, `mean_famine_duration_y`. Famine events have a Victim-role community participant, so the extraction sums per-community famine durations and divides by community lifespan.
- Magnus3: Ran the `birth-fine` group (7 birth rates × 10 seeds × 1000y) plus the existing `birth-0.030` / `birth-0.080..0.200` sweeps. Total runtime ~5 minutes. Result: clean monotonic curve.
  - 0.030: 0.4% famine, communities go extinct (0/3 alive)
  - 0.040: 1.2%, still extinct (0/3)
  - 0.045: 2.2%, marginal (0.5/3)
  - 0.050: 6.3%, struggling but viable (1.4/3)
  - 0.055: 9.5%, stable+low famine (1.5/3, pop 31)
  - 0.060: 15.9% (2.1/3, pop 39)
  - 0.065: 21.1% (2.4/3, pop 46)
  - 0.070: 26.5% (2.5/3, pop 50)
  - 0.080: 36.4% (3/3, pop 59)
  - 0.100: 46.8% — **50% crossing point** (3/3, pop 63)
  - 0.120: 54.7% (3/3, pop 64)
  - 0.150: 62.1% — **current default** (3/3, pop 69)
  - 0.200: 69.7% (3/3, pop 73)
- Magnus3: Two surprises worth flagging.
  - The "98% famine" perception didn't match aggregate data — the per-community mean at the default settings tops out at ~62%, not 98%. The 98% read must come from eyeballing a single long-lived community in the viewer, where a community that's hit its carrying ceiling stays in chronic famine indefinitely; the aggregate is pulled down by short-lived/recovering communities.
  - The tract-cap knob *doesn't* meaningfully reduce famine fraction the way I'd first suggested. At `birth=0.055`, sweeping `cap_base` 30→40→50 left famine flat (10.0% → 9.5% → 12.1%); it just changed the size communities grow to before hitting the ceiling. Birth rate is doing essentially all the work.
- Magnus3: Picked `birth=0.07` as the new default — upper end of "comfortable" (slightly smaller communities than default 0.15, all 3 alive, famine ~27%). Edited `config/ecology.json` (live) and the matching default in `src/config/EcologyConfig.hpp` for consistency (no rebuild needed — JSON wins at runtime, the hpp default is fallback only). Restarted `serve.py` so the `/api/config` cache refreshes the new value into the viewer's settings panel.
- Magnus3: Reviewed birth/death granularity. Mapped the actual cadence: in-sim births fire once/year on the Spring boundary tick (every newborn in a given year shares that tick); starvation deaths land on one of the 4 seasonal boundaries; "natural" death isn't a system at all — it's just `Life.duration_ticks` set at birth as a whole-year multiple, so `active_at(tick)` flips false on the same Spring-of-year tick as births. Net effect: nearly every life event in the sim lands on a Spring boundary. Famines (sub-yearly already via season boundaries) are the only existing sub-yearly events on a person's timeline.
- Magnus3: Brainstormed staggered seasonal births
  - Ryan proposed checking fertility 4×/year and staggering each birth somewhere in the coming season, so people get unique birth days. Scoped this session to birth only — natural-death cleanup deferred.
  - My first sketch reached for a `PendingBirths` queue + daily drain. Ryan pushed back: why a queue at all? Realized `active_at(tick)` already does the work — create the entity at the season boundary with `birth_tick = boundary + random_in_season`, and nothing observes it until the next boundary (Summer is a no-op season and the orchestrator only fires on season boundaries). No queue, no daily pass.
  - Accepted simplification: a baby born on day 80 of summer still gets charged a full season of food at the autumn boundary, since consumption is per-person-per-season and we don't distinguish infants from adults anyway.
- Magnus3: Worked through the date-display format with Ryan. Settled on `Year N, Season day D (X/64)` for the inspect panel — sub-yearly viewer formats today are just `Year 50.5` fractional. Caught and fixed two things along the way:
  - The desktop CLAUDE.md and Magnus3 CLAUDE.md both said "4 seasons × 90 days × 256 ticks/day". The total (92,160) is right but the breakdown is wrong — actual is `4 seasons × 16 days × 1,440 ticks/day` (DAYS_PER_SEASON=16, TICKS_PER_DAY=1,440 from `TICKS_PER_HOUR=60 × 24`). Fixed the Magnus3 one; the desktop CLAUDE.md doesn't actually contain that line, only the Magnus3 one did.
  - A Magnus3 year is 64 days, not 365 — so the format is e.g. `Year 123, Summer day 5 (21/64)`. Scoped this session to person Born/Died only; will revisit other event timestamps (famines, marriages) next session.
- Magnus3: Implemented staggered seasonal births
  - **`EcologySystem.hpp`**: pulled `process_births` call out of the `case Season::Spring` branch, now runs unconditionally each season before the season switch.
  - **`BirthSystem.hpp`**: renamed the config knob `chance_per_adult_per_spring` → `chance_per_adult_per_year` (the field name was actively misleading now), and divide by `SEASONS_PER_YEAR` inside `process_births` to preserve the tuned yearly rate of 0.07. On a successful roll, `birth_tick = current_tick + deterministic_select(seed, TICKS_PER_SEASON)` distributes the child somewhere across the coming season. Updated the header comment block to describe the new model.
  - **`EcologyConfig.hpp` + `config/ecology.json`**: renamed field + JSON key (glaze reflects field names directly).
  - **`viewer/index.html`**: updated the settings input `data-path` and label ("Birth Rate (per year)").
  - **`tools/exponator/exponator.py`**: 4 references renamed.
  - Built clean (Release). Calibration preserved — same expected children/pair/year as before.
- Magnus3: Implemented viewer date format
  - **`viewer/js/constants.js`**: added `TICKS_PER_DAY=1440`, `DAYS_PER_SEASON=16`, `SEASONS_PER_YEAR=4`, derived `TICKS_PER_SEASON` and `DAYS_PER_YEAR`, plus `SEASON_NAMES=['Spring','Summer','Autumn','Winter']`.
  - **`viewer/js/layout.js`**: added `formatDate(tick)` returning `Year N, Season day D (X/64)`. Day-of-season and day-of-year are 1-indexed for display (real-world calendar convention).
  - **`viewer/js/inspect.js`**: person rows now use `formatDate` for Born/Died labels; other entity types (community, family, deity) keep the existing fractional-year format. Duration stays in years for all types.
- Magnus3: Perf check after staggered-birth change — no regression, possibly an improvement. Four runs, 1000y seed 42 default config: `tick_total` = 103.5, 92.3, 91.9, 83.6 ms (mean ~93ms, range 84-104). Last week's post-fix baseline at this seed/config was ~172ms (163-184). Sim ran the full 1000y with 660 persons (625 in-sim births), 1,229 famines, 44 alive at end — healthy workload, not an early-extinction artifact. Not claiming a causal mechanism; could be hardware state, could be the spread of archetype churn across 4 seasons instead of one. The relevant signal is "no regression," which is confirmed.

Tuesday - 05/26/26
Daily Goals:
- [ ] Magnus3: Land natural-death staggering without breaking the recent birth-rate calibration
Log:
- Magnus3: Started on natural-death staggering. Today every in-sim person has end_tick = birth_tick + N*TICKS_PER_YEAR (lifespan is in whole years), so death day-of-year is identical to birth day-of-year. Ryan picked option (1) from three sketches: add a sub-year offset to lifespan using the same death_seed rng. Implemented as `+ rng.next(TICKS_PER_YEAR)`. Verified the cosmetic decoupling works (9/9 in-sim-born deaths in 100y test had different day-of-year from birth).
- Magnus3: Perf check after the death-offset change came back ~3x slower. Investigated and found per-person work was stable (~140-160µs across all runs) — the absolute time grew because population grew 3x. Seed 42 went from 714 entities / 44 alive / 3 communities → 2228 / 216 / 7. All 4 seeds I tested (42, 7, 13, 99) showed the same ~3x equilibrium shift.
- Magnus3: My first theory was "+0.5y mean lifespan shift drives more reproduction." Ryan correctly pushed back that 0.77% lifespan increase shouldn't 3x the population. The arithmetic doesn't support it (extra +0.035 children/pair lifetime). Stepped back, reverted death-offset, re-verified baseline equilibrium (714 entities seed 42, identical to before — confirms it's reproducible and the death-offset really is the trigger).
- Magnus3: Ran a diagnostic sweep to isolate the cause. Four variants, 4 seeds each, 1000y, default config:
  - **V1 baseline** (no offset, lifespan = N*TPY): mean across 4 seeds = 832 entities
  - **V2 my original** (`+ rng.next(TPY)`, range [0, TPY), mean +0.5y): mean = 2366 entities — **+184% vs baseline**
  - **V3 centered** (`+ rng.next(TPY) - TPY/2`, range [-TPY/2, +TPY/2), mean 0): mean = 867 entities — **+4% vs baseline**
  - **V4 constant** (`+ TPY/2`, no rng): mean = 894 entities — **+7% vs baseline**
- Magnus3: Conclusion — neither pure mean-shift (V4) nor pure smoothing (V3) breaks equilibrium. Only V2 (strictly non-negative random extension, mean +6mo) does. I don't have a clean mechanistic theory; it appears to be a nonlinear interaction between the asymmetric (one-sided) lifespan extension and the fission threshold check. The centered ±6mo variant sidesteps the issue entirely while still achieving the cosmetic decoupling — 98.6% of deaths have different day-of-year from their birth in a 1000y seed-42 run. Mean lifespan stays at 65.4y (baseline 65.2y), so the recent 0.07 birth-rate calibration holds.
- Magnus3: Implemented V3 in PersonGenerator::estimate_lifespan, but Ryan pushed harder before committing — wanted a real mechanistic theory.
- Magnus3: Audited every system that reads `active_at` or `end_tick`. Confirmed the orchestrator only fires at season boundaries (single Flecs system with `current_season_idx` early-return). All simulation logic — consumption, marriage, birth rolls, leadership, fission — happens at season boundaries. Births and deaths are the only entity timings that don't sit on season boundaries, and both are decided at boundaries with the off-boundary tick being purely cosmetic.
  - Of all the systems, the **fission threshold check is the only step function on living counts**. Family fissions when `living_count >= 11` (default + personality modifier). Every 10y in autumn. Continuous checks (food per person, marriage eligibility) degrade smoothly under sub-yearly death ticks.
  - Direct evidence: fission events per century — V1 baseline 4, V2 (broken) 9, V3 4, V4 4. V2's 2.5× fission rate maps directly to its 3× population growth via cascade: each extra fission creates a new family that grows independently and contributes its own fission events. Some fissions create new communities (V2: 4 created mid-sim; V1/V3/V4: 0).
  - **Mechanism for V2's explosion**: lifespans are strictly ≥ baseline. Some members who would have died are still alive at the next autumn check. Families hovering at 9-10 cross 11 occasionally → fission triggers earlier than baseline. V3 (symmetric) has crossings UP and DOWN that balance; V4 (constant shift) has predictable uniform shift insufficient to cross threshold. Only V2's strictly-positive variance creates one-sided threshold crossings.
- Magnus3: Ryan also flagged a cleaner mental model — births and deaths are analogous (both decided at boundaries, displayed sub-boundary). Suggested I should use the same scatter shape for both. Implemented V5 (1-season scatter constrained to birth season, refactored `estimate_lifespan` to take birth_tick). V5 verified zero behavioral change in 3 of 4 seeds; seed 7 still grew +23% — surprising and worth flagging but didn't fully investigate.
- Magnus3: Ryan rejected V5 — having every person die in the same season they were born is unrealistic. Reverted to V3. The asymmetric scatter (birth: 1-season, death: 1-year centered) is intentional and gives the realism (death uniformly across all 4 seasons of year N) without the mean-shift / one-sided-variance trap. Verified at 4 seeds:
  - Day-of-year decoupling: 1.1-1.7% accidental matches (expected ~1.6% for true random — perfect)
  - Season-of-year distribution: 24-26% matches per season (perfectly uniform across 4 seasons)
  - Entity count: +0.9% to +16% vs baseline (mean +4.2%, within noise)
  - Mean lifespan: ±0.2y of baseline across all seeds (vs V2's +0.5y systematic shift)
  - Perf: tick_total 84-243ms across 4 seeds, in line with baseline 65-236ms — no regression
- Magnus3: Separately noticed in the investigation that V2's run ended with 6 alive communities even though `world.json` says `max_communities_per_region: 3`. The C++ default in WorldConfig.hpp is 12; might be a JSON load issue or the cap check is wrong. Filed for separate investigation — unrelated to the death-offset work.
- Magnus3: Added "All time / At playhead" toggle to the family + community inspector member lists.
  - Data layer already had `getLivingFamilyMembersAt(simData, familyId, tick)` (tenure-based). Mirrored it with a new `getLivingCommunityMembersAt(simData, communityId, tick)` that filters families by lifespan-contains-tick and persons by living-at-tick whose tenure-at-tick family is in this community.
  - `InspectPanel` gained `playheadTick`, `memberView`, and `_currentEntityId` state plus a `setPlayhead(tick)` method that re-renders only when the visible entity is a family/community AND mode is `'playhead'` — so scrubbing the playhead while inspecting a person costs nothing.
  - Toggle is rendered inline on the right side of the "Members (N)" section title (small select, matches the existing `#bottom-view-toggle` style). Section title also gets an "at Yxxx" suffix in playhead mode so it's obvious which year is being filtered to.
  - **Behavior change worth noting**: the "All time" list for families now uses tenure data (anyone who was ever a tenured member) rather than the old `personsByFamily` (parent_id, i.e. *current* members only). The new definition matches the user's mental model better — "all time" = "ever" — but it's a definition shift, not a bug fix. Same for communities (was: current families + their current persons; now: every family that ever existed in the community + every person whose tenure ever placed them there).
- Magnus3: Fixed playhead-scrub releasing into an inspect-select. Releasing a playhead drag was triggering the `canvasWrapper` pointerup handler at `main.js:316` — which runs `hitTest` against the release position and opens whatever entity (or area-header minimap) happened to be under the cursor. The scrub handler was added in capture phase on pointerdown but did nothing about pointerup, so the inspect path ran on release. Added a `scrubbingPlayhead` flag set on scrub-pointerdown, cleared via `setTimeout(..., 0)` on scrub-pointerup so the bubble-phase inspect listener sees the flag and bails early — same shape as the existing `zoom._dragging` guard immediately above it. Two-line edit in `viewer/js/main.js`.
- Magnus3: Expanded the family inspector's marriage rows the same way we did for persons. Each marriage in the events list now renders two sub-items: "&lt;Stayer&gt; married &lt;Mover&gt;" and "&lt;Mover first name&gt; moved in" — mirrors the person-view two-line structure, just from the destination family's perspective. The family always appears as the marriage event's Target participant, so FamilyMover is the new arrival and the non-mover of Primary/Secondary is the existing member.
- Magnus3: Extended the existing "All time / At playhead" toggle to also gate the events list (previously only filtered members). When "At playhead" is active, events are filtered to those whose duration spans the playhead tick (`start_tick ≤ tick ≤ start_tick + duration_ticks`). Section title also gains the same "at Yxxx" suffix as the members header. Updated the toggle's title attribute and the `memberView` comment to reflect the broader scope.
- Magnus3: Open question parked — should a fission-born family's events list show the founders' marriages? Ryan noticed a brand-new family had an empty marriage list at the very start. Mechanism: marriage events record only the destination family (Target) at marriage time. When a person later fissions off to start a new family, that marriage event still points at the *old* Target, not the new family, so it never lands in `eventsByEntity[newFamily]`. Two reasonable framings: (1) literal — the marriage didn't happen "to" this family because the family didn't exist yet, leave as-is; (2) genealogical — the founders' marriage is the family's origin story, surface it. Not deciding now; if we pick (2) the fix probably lives in the data layer (synthesize a derived "founders' marriages" list per family from the fission event + each founder's marriage history) rather than the inspector view itself, so a future "family tree" feature can reuse it.
- Magnus3: Major design discussion — decentering reproduction from marriage, centering it on the woman. Ryan flagged two coupled problems with the current model: (1) fertility is gated on community-aggregate food and adult-pair existence, which doesn't reflect the actual mechanism behind the gather→farm population shift — that was driven by diet density and weaning practices acting through women's bodies (lactation refractory period, fertility-window length, age-at-first-birth), not raw calorie availability; (2) there is currently zero mechanical difference between men and women in reproduction (no pregnancy state, no maternal mortality, no per-person fertility window), which both flattens the biology and thematically decenters women from the one process they're physiologically essential to. Bonus problem: gating on community/family hunger also means *male* hunger blocks reproduction, which inverts whose body the mechanism actually runs through.
- Magnus3: Proposed direction — move fertility onto the woman entity: age window (menarche → menopause), pregnancy state, post-birth refractory period (tied to lactation duration, which is itself tied to diet composition), her own nutritional state, maternal mortality. The lactation/refractory dial is the key mechanic — low-density gatherer diet → ~3-4y lactation → ~4y inter-birth interval; high-density agricultural carbs → fast weaning → ~1.5-2y interval, which roughly accounts for the post-agriculture explosion without tuning. Menarche, age-at-first-birth, and maternal mortality come along for free once fertility lives on her. This is a "rich seam" not a single mechanic.
- Magnus3: Marriage's role in the rewrite. Currently marriage does two jobs: (a) reproduction gate, (b) lineage transfer via FamilyMover. (a) goes away under the proposal. (b) is already cultural — the matrilineal/patrilineal config is the hint that marriage was always more of an order-system concept than ecology, just without a home yet. Plan: when the order/rule system arrives (next phase after ecology), marriage moves there as a cultural-variation surface — communities can have monogamy, polygyny, or no formal marriage, and the reproductive mechanic doesn't care.
- Magnus3: Open question — paternity tracking without marriage = parents. Three options: (a) drop paternity, maternal lineage only; (b) lighter "pair-bond" concept separable from marriage-as-institution; (c) let the order/rule system assign paternity by social convention (most thematically honest — paternity has always been a social construct families enforce — but most work, and only meaningful once cultural variation exists to make assignment meaningful).
- Magnus3: Sequencing — per-woman fertility rewrite probably belongs with the current ecology work (fertility *is* an ecological response to diet, needs to talk to food allocation). Marriage-as-order waits for the order system. Awkward intermediate: marriage stays a nominal reproductive gate until the order system arrives, but that's easier than coordinating both rewrites simultaneously.
- Magnus3: Talked Ryan down from a more elaborate paternity proposal (weighted potential-fathers list + encounter rolls + per-person fertility) into a simpler version: roll fertility into a single per-woman rate that diet/lifestyle modifies, keep marriage as the "encounter" step that pairs people up, and use remarriage/divorce to get lifelong paternity variance for free. The simplification is good — but the key thing to preserve is that the roll runs on **the woman**, not the couple. Her age window, her hunger, her diet. Without that, "fertility rate per marriage" puts us back at the same flattened mechanic we started complaining about. Tradeoffs accepted: no death-in-childbirth, no mechanical pregnancy state, no emergent lactation-driven inter-birth interval (lactation becomes a tuned dial in the diet→rate curve rather than its own state machine). All promotable to real mechanics later if any of them becomes the thing we care about.

Wednesday - 05/27/26
Daily Goals:
- [ ]
Log:
-

Thursday - 05/28/26
Daily Goals:
- [x] Magnus3: Begin the multi-system architecture — extract leadership out of Ecology into a new Rule (Zeus) system
- [x] Magnus3: Fresh conceptual stock-take of the whole simulation loop (Ryan walk-through)
Log:
- Magnus3: Stepped back from code to settle the general architecture for the planned 7-layer system split (classical-planet inspired): Sol=Player, Luna=Narrative, Mars=War, Mercury=Trade/Magic, Zeus=Rule, Venus=Ecology, Saturn=time/space backplane. The leadership example exposed the core problem — sibling systems have cyclic dependencies (Ecology→Rule: population/family structure feeds leader selection; Rule→Ecology: leader personality modifies food yield). Naive "every system reads/writes everything" produces an ordering-sensitive tangle.
- Magnus3: Settled the architectural backbone (binding decisions, now in Magnus3/CLAUDE.md):
  - **Single-writer ownership** — each component has exactly one writer system; all others read-only. Makes cross-layer data flow auditable. (Rule owns CommunityLeader/LeadershipHistory; Ecology reads them.)
  - **Fixed-phase ordering breaks cycles with a one-tick lag** — systems run in a defined order; a system sees fresh state from earlier-in-tick systems and stale-by-one-tick state from later ones. Back-edges become the lagged reads. Deterministic.
  - **Communication model A (shared ECS state + ordering) for now**; model B (intent / SENSE→DECIDE→RESOLVE, already sketched in Simulation.hpp) deferred until War/Trade create resource contention needing arbitration.
- Magnus3: Ryan proposed tick order Sol→Luna→Mars→Mercury→Zeus→Venus→Saturn, then refined it into a cleaner framing: **Sol and Luna are both intent *sources*** (player intents / narrative intents), the four core layers are *resolvers* that own state and apply-or-reject, and Saturn is the host/substrate (the entirety of the app that isn't a domain system), not an active sibling. This gives a three-band tick: **Propose** (Sol, Luna) → **Resolve** (Mars, Mercury, Zeus, Venus) → **Settle** (Saturn) — which maps onto SENSE→DECIDE→RESOLVE, so model A is a clean on-ramp to B. Rules banked for the future intent bus: owning system is the sole arbiter + must drain intents deterministically; directors (Luna) need applied/rejected receipts read next tick; intents cascade downstream within a tick but lag one tick upstream; Sol before Luna so narrative weaves around player moves.
- Magnus3: Clarified the determinism directive with Ryan and updated the auto-memory. "Determinism within a version" was being read too literally — the axis is *the same build reproducing itself across re-runs*, NOT stability across changes. Any change (refactor, tweak) is free to alter outputs and that is not a cost; only nondeterminism or behaviorally-wrong output matters.
- Magnus3: Implemented the leadership→RuleSystem extraction (model A, the first concrete move toward the 7-layer split):
  - New `src/systems/RuleSystem.hpp` — Zeus orchestrator, sibling to EcologySystem, registered in World::register_systems *before* Ecology. Query-less `.run()` task (confirmed in Flecs v4.0.3 source: EcsQueryMatchNothing → run-once path), gated once per season via a RuleState singleton; collects communities then re-elects leaders (election is per-community independent so iteration order is irrelevant).
  - Reshaped `src/systems/LeadershipSystem.hpp` into Rule's first subsystem, split by ownership: write side `update_community_leader(community, tick)` (re-elects only when the cached leader has died; updates CommunityLeader + LeadershipHistory), read side `leader_modifiers_from_cache` / shared `modifiers_for_leader`. Deleted the old `get_cached_leader_modifiers` / `get_leader_modifiers`.
  - `EcologySystem` now only *reads* the leader cache (no longer selects/caches/records) — enacts single-writer. `InspectCommand` updated to the shared `modifiers_for_leader` helper.
  - Flecs note: the pipeline derives merge/sync points from declared query terms; both old and new code touch CommunityLeader only via get/set inside callbacks, so the scheduler never saw the dependency either way. Didn't fight it — registration order puts Rule before Ecology, and a one-tick lag would have been fine anyway.
  - Verification (seed 42, 1000y / 92,160,000 ticks): clean Release build; exported twice → byte-identical (deterministic); also byte-identical to the pre-refactor baseline (sha256 d7d6efc…), confirming a pure behavior-preserving relocation and that Ecology reads the leader cache same-tick in practice. `inspect community 0` shows leader + modifier correctly.
- Magnus3: `jq` was missing despite the desktop CLAUDE.md listing it at `C:\Users\Ryan\bin\jq.exe`. Installed via `winget install jqlang.jq` (v1.8.1) — winget put it on PATH + added a `jq` alias; real exe at `C:\Users\Ryan\AppData\Local\Microsoft\WinGet\Packages\jqlang.jq_Microsoft.Winget.Source_8wekyb3d8bbwe\jq.exe`. Updated the path in the desktop CLAUDE.md.
- Magnus3: Ryan asked for a fresh conceptual walk-through of the whole sim loop to take stock. Read the generation flow + all Ecology/Rule systems and cross-checked against his narrated mental model ("divine startup script → world+spaces → randomly-aged founders → gather → children → fission → death of old age → no children if hungry → marry & move family"). Findings:
  - **Model is faithful** — every beat he described is implemented in that order. Confirmed the 7-Act WorldGenerator (Creator→World→Ruler Deity→Ecology Deity→Regions/Tracts→Communities→Families→People) and the seasonal loop.
  - **Mechanics he omitted from his own narration:** starvation *death* (50%/season at zero food, not just the no-children soft gate — and it's the dominant population regulator per the birth sweeps); the Rule/Zeus leadership layer feeding food yield; personality traits driving fission threshold / exodus fraction / leader bonus; marriage exogamy + 1-generation incest avoidance.
  - **Concept↔code drift flagged:** "far away places" never crosses a region (regions are sealed; fission tops out at founding a new community a few cells over); founders are adults-only (18–45), not full-age-range "randomly aged"; the hunger→no-children gate runs on the *pair* (so male hunger blocks reproduction) — the exact thing the per-woman fertility rewrite targets; "drought" is in the docs but I couldn't find it as a live mechanic — flagged to verify (possible doc-drift).
  - **Missing concepts named:** background mortality (no disease/infant/accident death — Ryan's own logs already flag near-zero non-famine mortality as the root cause behind the famine dynamics), resource depletion/regen (the two-layer food model is the intended fix), gender-as-mechanic (per-woman rewrite target).
  - **Re-surfaced parked items:** `max_communities_per_region` cap bug (V2 ended with 6 alive vs world.json's 3; C++ default 12 — filed Tue, never chased); founders'-marriages-on-fission empty-list question. Noted natural-death staggering (V3) is closed.
  - Offered next steps: turn per-woman fertility into a build plan, chase the communities-cap bug, or verify drought. No code changed this session — analysis/stock-take only.
- Magnus3: Ran a baseline perf check (Ryan asked to confirm no recent change degraded the baseline). Build is current (no source newer than the Release exe, built today 22:21), config is the documented default (birth=0.07, cap_base=40). Seed 42 / 1000y, 5 runs: `tick_total` = 324.2, 294.9, 304.1, 298.8, 308.7 ms (mean ~306ms). Tightly clustered, not cold-start noise. Entity/famine counts byte-identical every run (deterministic): 6429 events, famines_opened=2375, flecs_tables=6757.
  - **This is well above the documented baselines** at the same seed/ticks/config: Monday staggered-birth run ~93ms (660 persons, 1,229 famines), last week post-fix ~167–172ms. Current ~306ms.
  - Key signal: the *workload itself* roughly doubled — famines_opened 1,229 (Mon) → 2,375 (now), a deterministic behavior shift, not machine noise. Elevated tick_total largely tracks the bigger population. Per-event cost is also up somewhat (~0.034ms → ~0.048ms), expected to be superlinear via archetype churn (6757 tables).
  - Committed code changes since Monday's 93ms measurement: V3 natural-death staggering (Tue) and the leadership→RuleSystem extraction (Thu). V3 was documented as only +4.2% mean entities and RuleSystem was verified byte-identical — *neither should ~2× the famine count*, so the equilibrium shift is unexplained and warrants a bisect (rebuild pre-V3 / pre-RuleSystem commits and re-measure famines_opened + tick_total at seed 42).
- Magnus3: Brainstormed "randomness as a divine lever" with Ryan — every contingent outcome routed through a deity that both arbitrates it and is its causal source ("Venus blessed us with a child" becomes a literal graph edge). Key moves worked out:
  - **Two separable ideas:** causal/narrative routing (cheap `CausedBy` tag, no math change) vs. centralized arbitration (entities apply, one arbiter grants — where exact tuning lives). Flavor benefit from the first, tuning benefit from the second; can ship independently.
  - **Reframed the prize:** not "exact 15%" but authoring the whole distribution — mean (exact, as a fraction of applicants so total still tracks population), variance (a per-stream dial, 0=quota), and correlation (regional drought = one roll, impossible with independent per-entity coin flips).
  - **Resolved the "god-object" worry** via awareness-vs-authority: a deity reads omnisciently but writes only its domain's outcome — single-writer was always about writes. Deity = maximally-informed decision function.
  - **Determinism taxonomy (the organizing result):** extra-diegetic (whole app is seed-pure) vs diegetic, and diegetic splits three ways — (1) divine act = a die rolled here (routes through a deity, gets a `providence` edge); (2) natural law = fixed constants + equation shapes (e.g. 3 food/season, the `min(labor,land)` form); (3) deterministic consequence / "fate" = no die here but inputs trace to past divine acts (old-age death just *arriving* on the lifespan Venus set at birth is the poster child). Categories are term-level, not system-level (showed the harvest equation carrying all three). Payoff: the intent bus only carries cat-1 (bounds scope + protects the hot path); the cat-1/cat-3 line is itself a design dial (promote succession/appetite to divine if you want contingency there); a deity exists exactly where a domain has cat-1 decisions.
  - **Fits the existing architecture:** the bus is model B (PROPOSE→RESOLVE→RECEIPT) with an earlier motivation; each planet-layer deity owns its domain's RNG (Venus: fertility/harvest/drought/ecological death; Zeus: succession + marriage — marriage is order/cultural, not Venus); single-writer holds; gods become replayable/reseedable/exponator-sweepable. Cost: determinism discipline moves from pure-functions to deterministic queue ordering (sort applicants by a stable key before granting).
  - **Synthesis with per-woman fertility:** her diet/age/nutrition become *weights in the lottery* rather than an independent probability — exact aggregate control + per-woman differentiation, strictly more expressive.
  - Confirmed decisions: variance is a per-stream dial (none or degree); drought returns soon and should reuse the fertility arbiter's shape; `CausedBy` gains a role/type (`lineage` vs `providence`). Parked (Ryan's call): personality drift / mid-life trait re-rolls — static traits accepted for now.
  - Wrote it up as `docs/research/2026-05-28-divine-determinism.md` (matched the existing research-doc format). Open questions captured there: lottery selection rule, per-stream variance defaults, receipts as Luna narrative hooks.
- Magnus3: Started implementing divine arbitration (maximal direction). Ryan's two calls: marriage moves *fully* to Zeus; pace = fertility-first → calibration check → then marriage. This entry covers Phase 1 (scaffolding) + Phase 2 (fertility through Venus).
- Magnus3: Built the causation scaffolding (no behavior change):
  - `BlessedBy` providence relationship in `CausalComponents.hpp`, parallel to `CausedBy` (lineage). A divinely-granted birth now carries both: `CausedBy(parents)` + `BlessedBy(deity)`. Registered in `World::register_relationships`.
  - `RuleDeity` tag in `EntityComponents.hpp` (mirror of `EcologyDeity`); tagged the ruler deity in WorldGenerator Act 3; registered in `World`. Scaffolding for the marriage→Zeus phase — nothing reads it yet.
  - New `systems/DivineArbiter.hpp` — the intent bus as a per-season in-memory batch (no per-intent ECS entities, per the design doc's hot-path note). `divine_grant(applicants, rate, variance, arbiter_seed)` returns the granted indices: count from `divine_grant_count`, selection by Efraimidis–Spirakis weighted-reservoir keys (uniform weight ⇒ uniform random K-subset; per-woman weights slot in here later). Order-independent (keys are pure functions of seeds) so determinism is preserved.
  - Added `birth.variance` dial to `EcologyConfig` + `ecology.json` (default 0).
- Magnus3: Routed fertility through Venus in `BirthSystem::process_births` — PROPOSE (non-starving married pairs apply) → RESOLVE (`divine_grant` at `chance_per_adult_per_year/4`) → RECEIPT (granted pairs get a child stamped `BlessedBy(ecology_deity)`). Passed the deity in from the orchestrator. Built clean Release.
- Magnus3: First calibration read looked alarming — seed 42 jumped 667→1013 persons / 45→59 alive (+48%/+31%) vs the logged baseline. Chased it properly instead of theorizing:
  - First hypothesis (variance reduction: exact-rate births sit steadier against the famine ceiling) — tested by running `variance=1.0`. Barely moved (66 alive). Discovered my v1 jitter model was *inert* at these scales: when the seasonal mean is ~0.1 births, a symmetric ±jitter never flips the 0/1 count. Rewrote `divine_grant_count`: `variance=0` = probabilistic rounding (exact mean, tightest spread); `variance=1` = a true binomial draw (n independent Bernoulli, == old per-pair behavior in distribution); in between, the binomial deviation is shrunk toward the mean. Unbiased at every setting.
  - Re-ran `variance=1` (true binomial) — *still* 1027/60, nowhere near baseline 667. So the shift is **not** a birth-variance effect at all; first hypothesis dead.
  - Took the rigorous path: `git stash`, built clean HEAD, measured the true baseline on *this exact config* across 4 seeds — 42:667/45, 7:648/40, 13:635/38, **99:1262/90**. The baseline spans ~2× across seeds, and seed-99 baseline (1262/90) is *higher* than my seed-42 change. Ran my change (variance=0) across the same seeds — 42:1013/59, 7:917/62, 13:666/42, **99:1129/83** (mine is *lower* than baseline at seed 99). Means: baseline 803/53, mine 931/62 → +16%/+15% aggregate, direction inconsistent per seed.
  - **Conclusion:** not a regression and not a bug. It's the documented fission-threshold step-function sensitivity (the V2/V3 episode) — perturbing *which* families get births reshuffles threshold crossings and lands the system at a different point in an already-huge cross-seed distribution. Determinism intact: seed-42 export is byte-identical across re-runs (`cee45cd…`). Perf fine: the arbiter is negligible (birth ~6ms of ~158ms); total time scales with population as always.
  - Open for Ryan's call before marriage: accept the aggregate (+16% is noise-level vs the 635–1262 baseline spread), or trim `chance_per_adult_per_year` slightly (~0.07→~0.06) to re-center the mean. Per-seed determinism and the variance dial both validated. Kept `variance=0` (exact) as the committed default.
- Magnus3: Ryan poked around the viewer, happy with the fertility result. Accepted calibration as-is (don't chase noise). Confirmed the new build works in an already-open viewer via serve.py (it shells out to the exe fresh on every Generate; just click Generate again — no serve.py restart or browser reload needed for new sim behavior; restart only refreshes the settings-panel config cache). Flagged that BlessedBy edges aren't surfaced in the export yet, so only the population behavior is observable, not the providence links.
- Magnus3: Design discussion — "can we plug shapes into Venus to drive outcomes (e.g. a 200-year population cycle)?" Worked out: very viable now, because exact-count arbitration flips the control relationship from descriptive (tune knobs → emergent outcome) to prescriptive (state a target → arbiter realizes it). Key distinctions surfaced: open-loop (precompute birth schedule, plays back — fragile, fights the sim's own death/famine/fission dynamics) vs closed-loop (precompute the target *curve*, Venus reads actual-vs-target each season and sets the grant to track it — the right way). Honest limits: births are an up-only lever (down-phase bounded by natural death rate unless Venus also pulls a mortality lever); per-community vs global scope is a seed-scope choice; tracking a reference is a control problem with a ~16y maturation lag (200y cycle is slow enough to be fine). Noted this IS the planned Luna→Venus directive channel (narrative biases, core layer owns realization), and the hard-track-vs-soft-bias spectrum is a deliberate choice. Added a "What centralization fixes — and what it doesn't" section to the design doc (noise vs structural amplification, with today's empirical proof that exact births didn't tame the cross-seed spread). Ryan wants to get to this control aspect soon.
- Magnus3: Committed + pushed fertility-through-Venus. Magnus3 `0dacc9a` (master → halfdreamt/Magnus3), magnus-docs `6379fb7` (main → halfdreamt/magnus-docs). Ryan pre-approved both commit messages.
- Magnus3: Routed marriage through the arbiter — scoped to a *direct parallel* of fertility per Ryan's condition ("only if it's more or less a direct parallel; I want to get to control soon"). **Walked back the earlier "move marriage fully to Zeus" decision** — the single-writer migration into RuleSystem isn't a parallel (defer/ordering/snapshot work) and would delay the control experiment, so it's deferred to whenever the Rule layer is actually built out. What landed: marriage still *executes* in the ecology orchestrator, but its roll is now arbitrated by the *ruler* deity (Zeus) — ruler-seeded arbiter (`marriage_arbiter_seed` derived from `combine_seeds(world_seed, 0x801E8)`, matching the ruler deity's own seed), `BlessedBy(ruler_deity)` stamped on each marriage event, and a `marriage.variance` dial mirroring `birth.variance`.
  - Pattern: pairing stays a deterministic social process; the divine roll only decides which viable courtships wed. Local phase forms adjacent candidate pairs (post-shuffle, relatedness-filtered) → `divine_grant` of an exact fraction. Exogamous phase greedily forms disjoint cross-community candidate pairs → `divine_grant`. Behavior change worth noting: exogamous matching went from "try multiple partners until a roll succeeds" to "first viable partner becomes a candidate, then grant a fraction" — slightly lower exogamous throughput, accepted for the clean parallel.
  - The ecology orchestrator looks up the ruler deity once (cached in `SystemState.ruler_deity`) via a `RuleDeity` query and threads it into both marriage phases. Hit a compile error first — `world.each<RuleDeity>` doesn't work for a zero-size tag (wants a component ref); switched to `world.query_builder<>().with<RuleDeity>().build().each(...)`.
  - Verified (seed 42, 1000y): clean Release build; export byte-identical across re-runs (`fa474ea…`, determinism holds); 909 marriage events (marriages clearly functioning); 992 persons / 54 alive (within the fertility-only envelope of 1013/59). `marry` cost ~2ms of ~132ms — negligible.
  - Not yet committed — pending Ryan's review / viewer test.

Friday - 05/29/26
Daily Goals:
- [x] Magnus3: Refine the famine concept; land the demographics/identity layer (sex, gender, orientation)
Log:
- Magnus3: Started famine lethality (severity+duration starvation mortality, `CauseOfDeath` record preserving fated-vs-actual death, `died` flag on intervals). Ryan stepped back before it killed anyone — realized "famine" conflates a *cause* with an *effect*. Reverted all mortality edits to a clean build.
- Magnus3: Refined the famine concept (verified against `GatheringSystem`/`ConsumptionSystem`). Famine is purely emergent: it opens when `stores < pop·need`, and yield is pinned at the tract ceiling (`min(labor·eff, tract_cap)·variance`). So famine ≡ population overshooting tract carrying capacity (~13 for a cap-40 grassland). Measured it: seed 42, 1000y → **0 famine deaths** despite 1587 famines (one ran 8.75y with everyone starving, nobody died). Three layers were being smeared: **cause** (only one today: CC overshoot), **state** (the diagnosis/Famine event), **effect** (mortality, ~nil). Also flagged the famine event currently lists Venus as `Primary` — implying she authored an overshoot she didn't.
  - Resolution: Venus's down-lever should act on the *cause* (suppress yield = drought), not fabricate a famine. Famine stays an emergent effect; the causal graph records cause (overshoot vs Venus's drought). No "manual famine" needed — we need a yield lever. Lethality (effect layer) is orthogonal and still valid.
- Magnus3: Discussed recognition/prediction of CC famine. Big realization: the system is now *legible* — CC is ~fixed (tract_cap/need), old-age deaths are fated-at-birth (the code already holds the full future death schedule of the living), births are Venus's exact grants. So pop trajectory is computable, not a black box. Recognition = compute CC + classify famines at onset (overshoot vs transient). Prediction = project pop vs CC + stores → famine ETA. Honest limits: fission is a relief valve + step-function chaos caps long-horizon multi-community forecasts; short-horizon single-community is solid.
- Magnus3: Prescience discussion. Mechanism = a deity forks the world and runs a sandboxed rollout (0.5s/1000y → 50y≈25ms), reads the outcome, acts. Infinite-loop fix: rolled-out deities use a flat policy (no nested lookahead) → depth-1. Rollout *sidesteps* the chaos that defeats analytic forecasting (just run the deterministic dynamics). Substrate already half-present (`Checkpoint`/`DivergencePoint`/`PlayerIntervention`). But on reflection: for Venus's *homeostasis*, lag-aware feedback beats prescience; prescience earns its keep for **Luna's** future-shape authorship, not setpoint-holding. Matches Ryan's "Luna has the most prescience" gradient. Also the control paradox: acting on a forecast invalidates it.
- Magnus3: Settled the up-lever. Venus's fertility is currently throttled by Zeus (no married pairs → no births). "Extramarital births" = the **per-woman fertility rewrite seen from the control angle**: move fertility onto the woman, marriage becomes a *factor in paternity* not the gate → Venus gets an actuator Zeus can't throttle. Double payoff (biological realism + control authority).
- Magnus3: **Discovered there is no sex/gender modeled at all** — persons have personality/physical/health but no sex; marriage pairs any two singles; "matrilineal" just picks person1. So "decentering women" was philosophy over a world with no women. Decided to build the identity layer first, inclusively.
- Magnus3: Built the **identity layer** (Piece 1). Three decoupled axes mapped to layers: `Sex` {Female,Male,Intersex} → fertility (Venus); `Gender` {Woman,Man,Nonbinary} → social (Zeus, recorded-not-yet-mechanical); `Orientation` {Heterosexual,Homosexual,Bisexual,Asexual} → marriage pairing (Zeus). Decoupling them is the *correct factoring*, not a tax — `Female`≠`Woman` by design. Files: `EntityComponents.hpp` (enums + `Demographics` + name helpers), `WorldConfig.hpp`+`world.json` (configurable distributions, rough modern-US defaults), `World.hpp` (register), `PersonGenerator.hpp` (`generate_demographics` in all 3 creation paths, seeded with distinct salts so existing rolls are unperturbed), `WorldGenerator.hpp`/`BirthSystem.hpp` (pass the config), `ExportCommand.hpp` (surface sex/gender/orientation per person).
- Magnus3: Wired **orientation into marriage** (Ryan wanted it working from the get-go). `attracted_to`/`mutually_compatible` on gender (asexual→none, bi→all, hetero→different, homo→same; Nonbinary as its own gender — a documented v1 simplification). Local phase switched from adjacent-pairing to **greedy compatible-matching** (an arbitrary adjacent pair is rarely compatible); exogamous phase gained the same compatibility gate.
  - Verified (seed 42, 1000y): clean Release build (fixed `world.each<RuleDeity>`-style issue earlier; here a clean first build after the enum work); export **byte-identical** across re-runs (`8550c14…`); distributions land on config (47.5%F/52.1%M/0.46% intersex; ~1.2% nonbinary; ~92/5.5/1.7/0.6 orientation); 519 marriages, 651 persons / 35 alive — within the wide cross-seed envelope (635–1262), down from the no-orientation run because orientation now genuinely constrains pairing. Not yet committed; viewer inspector display of the three axes still TODO (data is in the export).
- Magnus3: Committed/pushed the identity layer (`957bac0`) + viewer Identity section (`678367e`); docs `4209d9e`. Then asked whether export/viewer distinguish mother vs father — answer: no, and you can't infer it yet (reproduction wasn't gestator-based, so same-sex/any-sex pairs could "reproduce" → no defined mother). Role-labeling needs Piece 2 to be *meaningful*.
- Magnus3: Built **Piece 2 — gestator-focused fertility** (the up-lever) with role-labeled lineage. Fertility now runs on the woman: applicant pool = alive, natal `Female`, age ∈ [menarche 15, menopause 45), not-starving (HER OWN state — fixes the male-hunger inversion), past an inter-birth refractory (`Maternity.last_birth_tick`, v1 lactation stand-in). Venus grants an exact fraction → birth lever no longer throttled by Zeus/marriage. Paternity = husband if married-to-a-male, else a deterministically-assigned adult community male (extramarital); child always gets two `CausedBy` lineage edges (genealogy intact) PLUS explicit `Parentage{mother,father}`. New components `Parentage`/`Maternity`; fertility-window config (`menarche_years`/`menopause_years`/`min_interbirth_years`). Intersex gestation deferred (v1 gestates Females).
  - Surfaced in export (`mother_id`/`father_id`) and the viewer (Birth Family / Mother / Father rows in the person inspector).
  - **Calibration: per-woman 0.07 went extinct** (0 alive, ~120 births, 0 famines — sub-replacement, not a bug). Mechanism verified correct first: 120 births, 100% mothers female, 100% fathers male, 0 maternal-only. Swept the rate: 0.07→extinct, 0.15→extinct, 0.25→1144/69, 0.35→1081/84, 0.45→1280/93. Replacement cliff ~0.20 (a woman has a capped 30y window where a married pair reproduced indefinitely → needs ~3.5x the old per-pair rate). Set provisional default **0.25** (viable, near old equilibrium) in `ecology.json` + `EcologyConfig.hpp`; **final calibration deferred until the famine down-lever is in** (birth rate + lethal famine together set the equilibrium — tuning the up-lever in isolation is premature).
  - Verified (seed 42, 1000y, rate 0.25): clean build, export byte-identical across re-runs (`60def9c`), 1144 persons / 69 alive, all 1109 in-sim births carry a (female) mother. Committed `ef2cc64`; docs `d3314e8`.
- Magnus3: Stepped back on the viewer settings panel — Ryan wants it consolidated by domain (UI / Generation / Simulation-by-layer) as we add layers. Realized the real move is to **align the config structure with domain ownership first**, then the viewer mirrors it. Guiding principle landed: *a param's config home = the domain whose decision it parameterizes* — world/generation = the Creator's fixed startup, domain configs = each deity's levers; **where a param lives signals who can change it and when**. (That's why sex-ratio belongs in Ecology, not rigid world-gen — "Oops, all Women" = Venus modulating the sex ratio over generations, confirmed interpretation A; changing living people's gender is B/Hera, deferred.)
- Magnus3: **Pass 1 — config-domain realignment** (behavior-preserving). Moved: `marriage` ecology→Rule; `fission` world→Ecology; `founding` + initial `community` food ecology→World; demographics **split** — sex→`EcologyConfig::SexDistribution`, gender+orientation→`RulerConfig::IdentityDistribution`. Renamed `ruler.json`→`rule.json` + `--ruler-config`→`--rule-config` (kept the C++ struct `RulerConfig` to avoid an 11-file cosmetic sweep). Threaded `RulerConfig` into the person generators (`WorldGenerator` ctor + member; `process_births` param) so demographics can read gender/orientation from Rule. Updated all readers (`MarriageSystem`→rule.marriage, `FissionSystem`→eco.fission, `CommunityGenerator` swap, `PersonGenerator` founding→world + demographics split), CLI (`main.cpp`), `serve.py`, `exponator.py`, README.
  - Resulting three domain configs: **world.json** (structure/regions/names/deities/founding/initial-food), **ecology.json** (gathering/consumption/famine/fertility/lifespan/fission/sex-ratio), **rule.json** (leadership/marriage/gender+orientation). Scales: each future layer gets its own config + viewer section.
  - 20-file change. Verified **byte-identical to the pre-refactor hash** (`60def9c`) — pure relocation, zero behavior change. Committed `5c3919d`; docs `c8ee69c`.
- Magnus3: **Pass 2 — viewer settings consolidation** (tabs, per Ryan's call over accordion). Restructured `#settings-panel` into **Viewer / Generation / Simulation** tabs that mirror the config domains. Moved the display controls (zoom, connector toggles, family-display) **out of the toolbar** into the Viewer tab (declutter, per Ryan) — kept their element IDs so `main.js` wiring is untouched (getElementById finds them wherever they live; config inputs still collected via `[data-path]` within the panel, which is generic over the top-level config name so `rule.*` works end-to-end).
  - Repathed stale inputs to the realigned configs: marriage `ecology.marriage.*`→`rule.marriage.*`, fission `world.fission.*`→`ecology.fission.*`. Added inputs: fertility window (menarche/menopause/min-interbirth), sex ratio (`ecology.sex_distribution`), gender/orientation (`rule.identity_distribution`), founding ages + initial food (`world.*`), min marriage age. Relabeled "Birth Rate (per year)"→"(per woman/yr)". Simulation tab is sectioned by deity (Ecology · Fertility/Sex/Life&Land/Settlement; Rule · Marriage/Identity).
  - Tab-switching JS (`main.js`) + tab CSS (`timeline.css`, mirrors the settings grid). 3 viewer files. Verified statically: JS syntax clean, no duplicate IDs, all 28 data-paths match the Pass-1 config keys. **serve.py must be restarted** to refresh its startup config cache before the panel shows the new values. Committed `438887b`; docs `bc49db2`.
- Magnus3: Layout fix on the new panel — the zoom readouts (`0%`/`20px`) overflowed the Display grid column into Connectors. Theorized wrong twice (range width; cache); the tell was that the grid+tabs *rendered*, so CSS was loading and my range rule was active but insufficient. Real cause: `justify-content: space-between` pins the readout to the row's right edge, and the fieldset (grid item, default `min-width: auto`) was forced wider than its column by un-shrinkable content (the long `nowrap` "Family membership display" label). Fix: `.settings-section { min-width: 0 }` + label `white-space: normal` + range `min-width: 0` (+ `?v=` cache-bust on the stylesheet link). Lesson re-learned: observe (the rendered tell) before theorizing.
- Magnus3: Ryan reframed "tabs" → he wanted the categories as **toolbar buttons**, one per domain, not an in-panel tab bar. Refactored: removed the single Settings button + the `.settings-tabs` bar; added Viewer / Generation / Ecology / Rule buttons to the toolbar (splitting the former Simulation tab into Ecology + Rule). Each button opens the panel to its `data-tab` section and highlights; re-clicking the active one closes the panel. Generic category-button JS + `.settings-cat-btn` outline styling. Buttons map 1:1 to the four content sections; future layers (Mars/Mercury/Luna) each add a button. Committed `e6c5c83`.
- Magnus3: Full perf review after the week's changes (identity layer, gestator fertility, marriage-arbiter, config realignment). First found the committed exe (13:34) predated three commits — including the config-realignment (`5c3919d`, which renamed `ruler.json`→`rule.json` and moved params between configs) — so it wouldn't even read today's config layout cleanly. Rebuilt clean Release before measuring anything.
  - **Current baseline (seed 42, 1000y, default config, 3 runs):** `tick_total` = 217 / 207 / 209 ms (mean ~210ms). Deterministic workload: famines_opened=1605, intervals_opened=38910, flecs_tables=5921, 3809 events.
  - **Baseline trajectory in context:** last-week post-fix ~167–172ms → Mon staggered-birth ~93ms (low-pop equilibrium) → Thu stock-take ~306ms (famines=2375, tables=6757, the marriage-arbiter-era high-pop equilibrium) → now ~210ms (famines=1605, tables=5921). The Thursday 306ms "unexplained 2× famine count" concern is **superseded, not bisected**: the gestator-fertility rewrite (Fri, rate 0.25) reset the population equilibrium again, so seed 42 now sits at a different (lower) workload. Per-event cost is healthy; total tracks population/workload as it always has.
  - **Cross-seed (1 run each):** seed 7 → 194ms (1737 famines, 5662 tables); seed 13 → 229ms (2230, 6417); seed 99 → 292ms (2010, 7983). Spread tracks famine/table count, consistent with the long-standing "tick_total scales with equilibrium population" story.
  - **Profiler answer — where the time goes (seed 42, ms over the full run):** of ~210ms tick_total, the callback body is ~162ms (77%) and Flecs `progress()` overhead is ~55ms (23%, i.e. archetype churn across ~5900 tables). Within the callback the cost is **dominated by structural/bookkeeping iteration, not ecological math**: exogamy ~39 · cleanup ~32 · snapshot ~29 · birth ~19 · consume ~14 · scan ~5 · fission ~4.5 · marry ~3 · cache ~2 · leader ~0.8 · gather ~0.7 · hist/defer ~0.3. So the three biggest callback costs (exogamy + cleanup + snapshot ≈ 100ms) are all "count/iterate the members" work; the actual food/birth simulation (gather+consume+birth ≈ 34ms) is a minority. Same ranking holds across all four seeds.
  - **Notable inefficiency spotted (not yet fixed):** `cleanup_dead_communities` runs an `ecs.each` over *all* communities, *every region, every season* (~4000×), and for each community calls `refresh_living_cache` + `count_alive` — redundant with the cache refresh `process_season` already did that same season. `build_snapshot` is also rebuilt per-community-per-season (12003 times) plus again after any death. Both are candidate optimizations if we want to push the baseline down; flagged, not actioned. Profiling done via the always-on `EcologyTimings` breakdown (Tracy is `ON_DEMAND`, would need the GUI connected for a line-level capture).
- Magnus3: Verified extramarital births are actually firing, and measured the rate — Ryan wanted to be sure the up-lever's paternity-not-gated-by-marriage path is real.
  - BirthSystem records no marital/extramarital flag (only `Parentage{mother, father}`), but the flag isn't needed: the export already carries per-person birth `start_tick` + `mother_id` + `father_id`, and Marriage events carry the two spouse participants + interval. So extramarital birth = child whose `father_id` is NOT the mother's spouse at the child's birth tick. Pure post-hoc reconstruction, no code change.
  - Wrote a one-off `extramarital.py` (build couple→intervals from Marriage events, classify each born child via `married_at(mother, father, birth_tick)`) — used for the measurement below, then deleted (not committed). Re-derivable from the export if needed again.
  - Results (1000y): **seed 42 → 23.3% extramarital** (258/1109); seed 1001 → 35.4% (229/646, smaller pop = more variance, +3 maternal-only/no-father); seed 7777 → 24.9% (333/1336). Roughly a quarter of all births are extramarital. Confirms the lever works as designed.
  - "No father assigned" (maternal-only) path is real but rare (0–0.5%): unmarried mother in a community with no eligible adult male at that moment.
  - Gotcha: the committed `output/*.json` exports are stale (pre-demographics, no `mother_id`/`father_id`, `spouse_id` not `current_spouse_id`). Rebuilt Release and generated fresh exports before analyzing.
- Magnus3: Acted on the perf review's flagged inefficiencies. Captured a reference export hash first (seed 42, 1000y → `60def9c3…`) so every change could be checked byte-identical.
  - **Win — trimmed/autumn-only population snapshot (`build_snapshot`).** Discovered `pop.adults` and `pop.married_pairs` are *written every season but read nowhere*; only `pop.singles` (autumn marriage) and `pop.alive` (everyone) are consumed. So the snapshot's whole expensive body — the `get_spouse` calls + O(n²) married-pair dedup — was dead work run ~12,000× per run. Rewrote `build_snapshot(community, tick, adult_age, need_singles)`: always set `alive`; only when `need_singles` (autumn) scan for singles (adult-age + `!is_married`, same order so determinism holds); never build the unused `adults`/`married_pairs`. Both `EcologySystem` callers pass `need_singles = (season == Autumn)`.
    - Result: `snap` 29ms → ~4.3ms. seed-42 `tick_total` ~210ms → ~170–178ms (≈18%). Holds cross-seed: seed 7 194→171, seed 13 229→192, seed 99 292→271. Export **byte-identical** to the reference (`60def9c3…`) — behavior-preserving + determinism intact.
  - **Tried and reverted — memoizing marriage ancestor sets.** Hypothesis: `too_closely_related` re-traverses the lineage graph for the same person across the O(n²) greedy pairing loop, so precompute each single's ancestors once. Measured it: `marry` 3→8ms and `exogamy` unchanged — a net *loss*. Why: with `min_unrelated_generations=1` ancestor sets are tiny (~2 entries), and the check only runs *after* the orientation filter and stops at the greedy `break`, so few calls ever happen — precomputing ancestors for *all* singles unconditionally costs more than the lazy calls saved. Backed out cleanly (re-verified byte-identical). Lesson: the redundancy was real but the inputs (tiny sets, early exits) made it cheaper to leave lazy. Measure, don't assume.
  - **Where the time goes now (seed 42):** exogamy ~32–40 (now the single biggest) · cleanup ~26–30 · consume ~16–23 · birth ~16 · marry ~3 · snap ~4 · scan ~3 · fission ~4 · gather/leader/cache <2; callback ~132ms, Flecs overhead ~55ms. The two remaining big structural costs (exogamy, cleanup) both re-scan all communities/members per autumn/season; trimming them further needs maintaining an incremental singles list or event-driven cleanup (natural deaths make naive gating unsafe) — more invasive, parked unless we want to chase it. Not yet committed; pending Ryan's review.
- Magnus3: Investigated which family extramarital children enter (follow-up to the extramarital measurement). Code-traced `BirthSystem`: child always enters `mother.parent()` — the mother's family, taking that family's surname; father plays no role in placement. So extramarital children follow the mother (father stays in his own family); marital children also enter the mother's family, but that's already the couple's *shared* family because one spouse moved at marriage (`transfer_family`).
  - **Found a real inconsistency:** `pick_family_mover` wasn't sex/gender-aware — `"matrilineal"` just returned `person1` (the lower *shuffle index*), not the woman. So a married couple ended up in the wife's family only ~half the time; the rest carried the husband's line — contradicting the matrilineal/women-focused intent.
  - **Fix (Ryan's call):** made `lineage` genuinely gender-aware and added it as a real knob. Decisions via AskUserQuestion: axis = **Gender** (Woman↔Man, consistent with marriage pairing); same-sex/nonbinary/intersex couples → **deterministic coin-flip** fallback; **default flipped to `patrilineal`** (wife moves to husband's family). Values: `patrilineal` (Woman moves, default) / `matrilineal` (Man moves) / `random` (coin flip).
  - Touched: `MarriageSystem.hpp` (`pick_family_mover` rewrite — reads both partners' `Demographics.gender`), `RulerConfig.hpp` (default + comment), `rule.json` (value), viewer `index.html` (new lineage `<select>` in Rule·Marriage), `main.js` (config collection now handles `[data-path]` `<select>`/string values, not just numeric `input`s), `timeline.css` (`.setting-row select` style + cache-bust `?v=5`).
  - Verified (seed 42, 1000y, clean Release): determinism intact (two runs byte-identical, `51605d84…`); outputs shifted as expected (1175 persons vs old 1144 — the mover changed). Direct behavior check: **922/922 Woman+Man marriages now move the Woman** (0 exceptions), 38 non-Woman+Man couples fall back to coin flip. Extramarital rate steady ~23.6%. Not yet committed; pending review.
- Magnus3: **Carrying-capacity research** (prep for the famine down-lever — want CC to be salient/controllable before making famine lethal). Read the full seasonal loop (`EcologySystem`/`GatheringSystem`/`ConsumptionSystem`) + live configs, then measured a seed-42 1000y run.
  - **CC is cleanly computable** at the production=consumption point: `CC = cap_base · biome · (1+leader_bonus) · E[variance] / food_per_person`. Live values + the catch that **Heartland is Woodland (×1.2)** → `40·1.2·1.0·1.0/3 ≈ 16/community` (grassland would be 13.3; leader bonus → 16–17.6). Labor→land crossover at pop ≈ 13.7. World ≈ 3×16 = 48.
  - **Observed pop does NOT match.** Per-community pop oscillates ~6→33 (decade-mean ~24, max 36) — ~50% above CC. It's an **overshoot/collapse limit cycle**, not an equilibrium: low pop → food banks to 286–586 → fed → births → overshoot → buffer drains → starvation → births halt → die-back → rebuild → repeat.
  - **Why imprecise (root cause):** **famine is non-lethal** — 0 famine deaths in 1000y despite 52,183 person-seasons starving (peak deficit 67%). Death only rolls at *zero* allocation (needs pop >~48/comm, never reached). So pop isn't mortality-regulated at CC; the only brake is **fertility suppression** (a woman's own starvation blocks her births) + old age. Secondary: the **food buffer** decouples pop from current yield (pop tracks accumulated history); ±20% yield variance smears the threshold; leader bonus drifts the cap; integer flooring; **fission relief valve is effectively off** (starts at 3 communities, cap is also 3) and only runs every 10th autumn. Takeaway: today "famine" is a diagnosis with no consequence — lethal famine would pull realized pop toward the computable CC and tighten the cycle.
- Magnus3: Chased the "people dying at age 8" anomaly from the CC run (Ryan: no external mortality exists, shouldn't be possible). **Resolved — nobody dies young; it was a measurement + a real latent bug.**
  - The 32 "young deaths" are all **founders** (`start_tick=0`, no parents). `PersonGenerator::generate_in_family` sets a founder's Life event to `set_life_event(entity, seed, 0, lifespan − founding_age)` — start 0, duration = *remaining* years. So `(end−start)` counts only post-founding years: a founder who arrives at 45 and lives to 53 shows as "died at 8." Their true death age is correct (in [50,80]). The other 17 "<50" were sim-born people dying at 49.5–50.0 (rounding past my `<50` cutoff). Zero actual young/external deaths.
  - **Real latent bug surfaced:** age is computed everywhere as `tick − start_tick`, but founders are pinned to `start_tick=0`, so a 40-yo founder reads as **age 0 at tick 0**. The code comment (PersonGenerator ~L122-126) even describes the correct fix (birth_tick offset into the past, "dies at birth_tick + lifespan") but the implementation stores 0. Effect, confirmed by measurement: **first marriage at y15.5, first birth at y15.1** — exactly `adult_age`/`menarche` = 15. The entire founding generation (real ages 18–45) is frozen as "children" for 15 sim-years, and founder women's fertility window [15,45] is measured from tick 0 regardless of true age (a 44-yo founder woman gets a full 30y window). Distorts all early-sim demographics + leadership "oldest". Not the cause of any death; flagged for a fix decision (options: global epoch offset so founders get a real past birth_tick, vs. an age-offset component used by all age calcs).
- Magnus3: **Fixed the founder-age bug** (Ryan chose the age-offset-component approach over a global epoch shift — keep tick 0 = sim start, special-case only the founders who need it). New `FoundingAge { uint32_t ticks }` component (`EntityComponents.hpp`), set on each founder in `PersonGenerator::generate_in_family` (the founding age that was already computed for the lifespan math). New `components/AgeHelpers.hpp` with `effective_birth_tick(person, life)` (signed — founders predate tick 0) + `person_age_ticks(person, life, tick)`. Routed every age/birth-order site through it: `PopulationCache` (adult check in `build_snapshot`, `get_eligible_singles` min-age), `BirthSystem` (menarche/menopause/male-adult), `LeadershipSystem` (oldest-by-birth, now signed `int64` so founders rank by true age). Kept `start_tick=0` + death timing + `active_at()` untouched (only age & seniority need the offset). Registered `FoundingAge` in `World.hpp`. Fission unaffected (counts alive only, no age logic).
  - Verified (seed 42, 1000y, clean Release): determinism intact (two runs byte-identical, `abefcdbe…`); **the 15-year reproductive dead zone is gone — first marriage y15.5→y0.5, first birth y15.1→y0.26.** Founders now act their real age from tick 0 (a 44-yo founder woman correctly has ~1y of fertility left, not 30). Output shifted as expected (1198 persons vs 1175). Not committed; pending review.
  - Then did the display piece too (Ryan's call): export `founding_age_ticks` per person (`ExportCommand.hpp`) and the inspector now adds it back — shows "Born … (founder)", "Age at founding", and true "Age at death". Verified: 35 founders carry the offset; true age at death lands in [50,80] (mean 61.7); the earlier "died at 8.3" founder is correctly a 45-yo who died at 53.3. Committed `a9f22d1` ("Fix: Account for founding generation's pre-sim age").
- Magnus3: Built **Venus's first preemptive control of CC famines**, then reframed it with Ryan into a named architecture.
  - **Mechanism:** as a community nears a setpoint just below CC, fertility is throttled toward zero — so the overshoot that causes CC famine never happens. Per-community. CC computed in `EcologySystem` (leader-adjusted tract ceiling × mean variance ÷ need), passed to `process_births`, which scales the per-season grant rate by a `[0,1]` modifier.
  - **Calibration finding (the key result):** at full control + base fertility 0.25, famines → 0 but the population went **extinct** — throttling a lever only ~25% above replacement pulls the average realized rate below replacement. Swept strength × base fertility: the working point is **full control + base fertility 0.60** → 0 famines, stable pop ~15 (just under CC≈16), 32 alive. Partial strength (0.5–0.7) doesn't help — pop still overshoots and famines persist (it's more a regime than a smooth dial). So preemptive throttling *requires* raising base fertility to give Venus headroom — the up-lever and the (kind) down-pressure are coupled, exactly as anticipated.
  - **Mental-model / naming (Ryan):** this isn't another parameter beside the raw knobs — it's the layer that *takes the wheel on them*. Named **Divine Domain Administration (DDA)**: a deity consciously administers their domain's Layer-1 parameters season-to-season toward a goal (audio-automation analogy, but closed-loop/reactive). Unit = a **directive** (standing instruction). First directive = **Population Homeostasis**. Determinism untouched (a directive rolls no die — it sets parameters that feed the arbiters that do).
  - **Restructure:** `ecology.json` `population_control{strength,…}` → `administration{ enabled, directive, population_homeostasis{ setpoint_fraction:0.8, taper_fraction:0.4 } }`; strength dial dropped for a **binary on/off** (Ryan: tuning the directive isn't needed, binary fits its function). `BirthSystem` now has `population_homeostasis_modifier` + an `administration_fertility_modifier` dispatch (gated on enabled + directive; future fertility directives add cases). Base fertility default raised 0.25→0.60 (the "natural max" Venus administers down from). Viewer: new "Ecology · Administration (Venus)" fieldset with an **on/off toggle + Directive dropdown**; `main.js` config collection now also handles checkboxes (booleans).
  - Verified (seed 42, 1000y, clean Release): determinism intact (`ac7cf268`). Default (administer ON) → 0 famines, stable ~15, 32 alive. Toggle OFF → 1632 famines, boom/bust ~27 (the open-loop world). Committed `0c9b0b7` + README/CLAUDE.md mentions; docs log `236accb`. (serve.py needs a restart to serve the new config block to the viewer.)
- Magnus3: **Renamed the directive + defined the directive abstraction** (Ryan: "homeostasis" oversells it). Measured the tradeoff first: directive ON → only **2 of 3** communities persist to era's end vs **3 of 3** OFF (holding each community low makes it fragile to stochastic fade-out — settlements fade rather than starve). So renamed `population_homeostasis` → **`fertility_restraint`** (mechanism-forward; intent "famine aversion" will be shared by future directives). Pure rename — export byte-identical (`ac7cf268`).
  - Established a directive is defined by **Intent** (id · label · description = goal + mechanism + **tradeoff**) + **Implementation** (actuator it bends · world-state it senses · control law · params · scope), each directive bending only its own deity's knobs (single-writer). Surfaced the description in the viewer under the directive dropdown (lightweight JS map `DIRECTIVE_DESCRIPTIONS`, to be replaced by an exported C++ directive catalog once we have several). Decisions locked with Ryan: single-select for now (plan for multiple independently-toggleable directives later, multiply modifiers if two hit one knob); lightweight descriptions now, exported catalog when the 2nd/3rd directive makes duplication hurt.
  - Touched: `EcologyConfig.hpp` (struct/member/comment rename + tradeoff note), `BirthSystem.hpp` (`fertility_restraint_modifier` + dispatch), `ecology.json`, viewer (`index.html` option + `#directive-desc`, `main.js` desc map/wiring, `timeline.css` `.setting-desc` + cache-bust `?v=6`), README + CLAUDE.md (name + the intent/implementation definition). Not committed; pending review.
- Magnus3: **Designing gestation / pregnancy** (Ryan wants children born 3 seasons = 48 days after conception, with a small day-or-two jitter; a `Pregnancy` event preceding each non-founder `Life`; no viewer; no external mortality yet; Fertility Restraint and all other mechanics unchanged besides the delay). Stepped back to pressure-test coherence before coding.
  - **Most of it already exists:** sim-born children are already created *at conception* with a future `Life.start_tick`, and "alive" = `Life.active_at(tick)`. So the feature is mostly "push start_tick out by 3 seasons + record a Pregnancy interval." `EventType::Pregnancy` enum already exists, unused. Also noticed today's model isn't truly instant — the within-season `birth_offset` + season-boundary cache counting already means a child conceived in S is first counted at S+1, so the *marginal* change is ~2 seasons, not 3.
  - **The one vital gap (would silently corrupt populations):** `PopulationCache` has `next_death_tick` to schedule rescans but **no `next_birth_tick`**. Today a conception sets `dirty`, forcing the next-season rescan that surfaces the birth — coincidence that holds only because birth≈conception. Decoupling by 3 seasons breaks it: in a Fertility-Restraint setpoint lull (no new grants, nobody starving), the cache can stay clean for many seasons and **delayed babies never get counted**. Fix = mirror `next_death_tick`: add `next_birth_tick`, track min `start_tick` of conceived-but-unborn members during full rescan, force a full rescan when `tick >= next_birth_tick`. Wouldn't show up in a quick test (only bites in the lull).
  - **Verified safe (gate on `active_at`):** fission family-size/threshold/exodus/movers (`count_living_family_members`), marriage/singles/snapshots. Founders go through `generate_in_family` (no pregnancy) — correct.
  - **Decisions locked with Ryan:** (1) keep the intra-season offset as "when in the season she conceived," then gestate on top (preserves birth-day spread vs. clustering all of a cohort at the S+3 boundary); (2) **cancel the birth if the mother dies during gestation** — since v1 lifespans are fixed, resolve deterministically at conception (`mother.end_tick() > birth_tick`), with a prominent comment that this MUST become a runtime birth-realization check once external mortality lands; record a truncated `Pregnancy` event ending at her death (no child) for graph coherence; (3) config as `gestation_days = 48` + `gestation_variance_days = 2` in `EcologyConfig::Birth`. Jitter must be **centered** (cf. the `estimate_lifespan` lesson: a one-sided offset shifted equilibrium ~3×). Refractory stays anchored at conception so Fertility Restraint cadence is byte-unchanged (`Maternity.last_birth_tick` becomes a slight misnomer — note in comment).
  - **Population effect (confirmed Ryan's intuition + mechanism):** a standing pool of ~3 seasons of conceived-but-unborn people is uncounted → living count sits modestly lower; plus Fertility Restraint now senses pop on a 3-season delay vs. its effect → mild overshoot/oscillation possible (classic delayed feedback). Net "slightly lower + a bit wobblier."
  - **Implemented** (4 files): `EcologyConfig.hpp` (`gestation_days=48` + `gestation_variance_days=2`), `EntityComponents.hpp` (`PopulationCache::next_birth_tick`; `Maternity` comment re conception anchor), `PopulationCache.hpp` (`refresh_living_cache` schedules + honors `next_birth_tick`, full rescan when a gestating birth comes due), `BirthSystem.hpp` (conception offset → gestation + centered jitter, term check, Pregnancy event w/ mother=Primary, father=Secondary, child=Beneficiary; include `EcologyHelpers.hpp`). Not committed; pending review.
  - **Verified** (clean Release build): determinism intact — two 100y exports byte-identical (`fad8224d…`). Pregnancy events fire and precede Life events; durations 66258–71857 ticks = 69120 ± ~2 days, centered (matches 3-season gestation). Every term birth's `start_tick` == its pregnancy's `end_tick` (exact). 1000y/seed42: stable, 0 famines, pop healthy.
  - **Key finding — cancellation path is UNREACHABLE under v1 defaults.** 0 cancellations in 1000y, by construction: menopause (45) is below min lifespan (50) − max gestation (~0.75y), so a fertile woman always outlives any conception by ≥~5y; founder women are clamped to ≥5y remaining too. The "cancel if mother dies" branch is correct **forward-looking** code (live once external mortality lands — and at that point it must move to a runtime birth-realization check, per the in-code note). Proved the logic works by forcing `menopause_years=75` (overlapping the death window): 11/714 pregnancies cancelled (no child, no spurious birth), 10 truncate exactly at the mother's death; the 1 outlier is a harmless degenerate 1-tick Pregnancy when the mother dies within the conception season before her intra-season conception offset.
- Magnus3: **Recording + visualizing the Fertility Restraint directive's control signal** (Ryan's question: how are directive adjustments recorded, and can we see them on the community food graph?). Investigated first — the adjustment was **not recorded at all**: `administration_fertility_modifier` (the [0,1] throttle) is a transient local in `process_births`, multiplied into the season rate and discarded; the only per-community series reaching the viewer was `FoodHistory` = yearly `{tick, food, pop}`, drawn as two lines + famine bands. So this is adding the recording, not surfacing hidden data.
  - **Design (agreed with Ryan):** capture the control signal as a third series on the food graph, plus the carrying capacity that drives it (makes the throttle legible — you see pop climb toward CC and the fertility line dip). Continuous series over discrete events; restraint "bands" derive from the series in the viewer (mirrors famine bands), no new event type. Recompute the modifier in the Winter snapshot block (cheap pure fn, Venus owns both components — single-writer clean); yearly sample is fine for a trend. Render gated on `administration.enabled` so open-loop worlds don't get a flat-1.0 noise line.
  - **Implemented** (4 files): `EntityComponents.hpp` (`FoodSnapshot` += `carrying_capacity`, `fertility_modifier`); `EcologySystem.hpp` (Winter snapshot re-derives the modifier via `administration_fertility_modifier` — BirthSystem.hpp already included — and records both); `ExportCommand.hpp` (`food_history` widened `array<int64_t,3>`→`array<double,5>` = `[tick,food,pop,CC,fmod]`, positional so the viewer's destructure still works, doubles so the floats survive); `inspect.js` `_drawFoodChart` (amber fertility-modifier line on a [0,1] full-height axis, dashed CC reference on the pop axis, amber restraint shading opacity∝throttle, 2nd legend row — all gated on `adminActive` = any recorded modifier < 0.999, so open-loop worlds stay clean).
  - **Verified** (clean Release build, seed 42, 100y export). New fields present and the control law is exact: row pop=12/CC=17.72 → modifier **0.384** = (0.8·17.72−12)/(0.4·0.8·17.72) ✓; pop=15 (above setpoint) → **0** (fully clamped); pop=1 → **1** (unrestrained). So the recorded signal tracks pop-vs-CC precisely. Determinism intact — two exports byte-identical (`302beff5…`). `node --check inspect.js` clean. Not committed; pending review + a visual pass in the served viewer (serve.py restart needed to refresh its config/exe cache).
- Magnus3: **Exponator review of Fertility Restraint defaults — minimize community die-out while keeping populations low** + **CLI directive controls** (Ryan's ask). Starting point: observed the tradeoff directly — seed 42 / 1000y ends a community at pop=1 (fertility clamped to 0 once pop≈setpoint·CC), confirming Fertility Restraint trades famine for stochastic fade-out. Single 1000y export ≈ 0.26s, so a wide seed×param sweep is cheap. Plan: (1) add CLI flags to toggle DDA on/off and select a directive; (2) add exponator experiments sweeping `setpoint_fraction` × `taper_fraction` (+ admin on/off baseline) over 10 seeds; (3) read the CSVs and recommend defaults on the survival-vs-low-pop Pareto front.
  - **CLI controls (`main.cpp`):** added global `--directive <name>` (selects directive + enables admin; `off`/`none` disables) and `--no-admin` (force open-loop). Applied as overrides on top of the loaded `ecology.json` after load, with a new `Administration: enabled=… directive=…` log line. Verified all four paths (config default, `--no-admin`, `--directive off`, `--directive fertility_restraint`) report the expected state. Clean Release build.
  - **Exponator bug fix (pre-existing, blocked ALL runs):** today's `food_history` widening (3→5 cols: +CC, +fmod) broke `extract_metrics`'s `t,_f,p = row` unpack → every run failed `too many values to unpack`. Fixed to slice `row[0:3]` (+comment). The exponator hadn't been runnable since the widening.
  - **Experiments added:** `admin-off` baseline + `fr_exp(setpoint,taper)` family; registered a setpoint sweep (0.6–0.95 @ taper 0.4), taper sweep (0.2–1.0 @ sp 0.8), a frontier grid, and the `directive-sweep` group. 20 configs × 10 seeds, ~0.25s/run.
  - **Findings (10 seeds × 1000y; survival = communities alive @Y1000 / total, max 3/region):**
    - **admin-off (open-loop):** survival 0.97, meanPop **75**, famine fraction **0.78** (chronic CC-overshoot famine). This is what Fertility Restraint exists to fix.
    - **Fertility Restraint works as designed:** every FR config drives famine to ~0 (0.002–0.018) and halves+ population, at the cost of some community survival (fade-out, as expected).
    - **Two near-orthogonal levers:** `setpoint_fraction` is the **population dial** (0.6→0.95 raises both pop and survival monotonically); `taper_fraction` is the **survival dial** — **narrowing it 0.4→0.2 sharply raises survival** (communities reach a robust size fast instead of lingering small & fragile in a long low-fertility approach band).
    - **The current default (sp 0.8 / taper 0.4) is Pareto-dominated** — survival 0.50 / pop 31.9; e.g. sp0.9/tp1 beats it on both (0.60 / 30.3).
    - **Pareto frontier:** sp0.6/tp0.4 (pop 19.6, surv 0.27) → sp0.8/tp1 (24.8, 0.40) → sp0.8/tp0.8 (26.8, 0.47) → sp0.9/tp1 (30.3, 0.60) → sp0.85/tp0.2 (34.3, 0.63) → **sp0.8/tp0.2 (35.7, 0.73)** → **sp0.9/tp0.2 (42.0, 0.80)**.
    - **Structural survival ceiling ≈ 0.80:** seeds 1024 & 2026 are intrinsically fragile (peak pop ~36–41) and die out under *every* FR config — the directive can't rescue worlds that small, so FR survival plateaus at 0.80 (vs open-loop's 0.97).
  - **Recommendation (survival-first, per Ryan's framing — "die out as little as possible, then keep pop low"):** new default **setpoint 0.9 / taper 0.2** (survival 0.80 = the achievable ceiling, pop 42, famine ~0). Lower-pop alternative on the knee: **0.8 / 0.2** (survival 0.73, pop 36). Either way the **single highest-leverage change is narrowing taper 0.4→0.2.**
  - **Decision (Ryan):** adopt **setpoint 0.9 / taper 0.2** as the new default. Wrote it to both `config/ecology.json` and the `EcologyConfig.hpp` struct defaults, and rewrote the stale rationale comment (dropped the old "0.8 covers ±20% variance" note — no longer the driver — and documented setpoint=pop-dial / taper=survival-dial + the sweep provenance). Rebuilt clean; verified the exponator `baseline` (which loads config defaults) now reproduces the sweep point exactly: survival **0.80**, meanPop **42.0**, famine ~0. Nothing committed yet (awaiting approval).

Saturday - 05/30/26
Daily Goals:
- [ ]
Log:
- Magnus3: **Reviewed the Fertility Restraint recording against the export's event-sourcing invariant** (`ExportCommand.hpp` "4D / Event-Sourced Record": permanent fact / time-bounded existence / event / history array; anti-patterns = shadow "current value" + orphan mutable). Verdict: **fits the letter** (it's bucket 4, extends `food_history`, triggers neither anti-pattern) and, on the strongest reading, *serves* the philosophy — `ExportData` carries **no config**, so the modifier is **not recomputable from the export**; re-deriving it against serve.py's live `/api/config` would be wrong under config drift + duplicate the C++ control law in JS. So recording it captures an otherwise-unrecoverable run fact (the opposite of the redundant-projection smell). Honest tensions surfaced: (1) it's a *signal*, not an accumulated *stock* like food/pop — mild stretch of bucket 4; (2) **aliasing** — modifier acts per-season but is sampled yearly (Winter), so the trace can't recover in-year values (the real critique; fine for viz, weaker as "authoritative record"); (3) the effect is *already* event-sourced via birth grants (`BlessedBy(Venus)`), and a bucket-3 directive *transition* event (engaged/released) would be the most native "lowered here" form. Tightening options noted (clarifying comment / export the run config so it's derivable / transition events / per-season sampling) — none actioned.
- Magnus3: **Always show carrying capacity on the food graph** (Ryan), not just when the directive is active — it's the population ceiling, useful context regardless. Decoupled CC from the `adminActive` gate in `inspect.js _drawFoodChart`: new `hasCarry` flag (any row with col 3), CC always sizes the pop axis + draws (dashed) + gets a legend entry; the amber fertility line + restraint shading stay gated on `adminActive`. Gave CC its own identity — light blue `#90CAF9` (population-axis family) rather than the restraint amber, since it's a pop ceiling not a throttle signal. Viewer-only (C++ already records CC unconditionally, `7b6aab0`); `node --check` clean. Not committed; pending review.
- Magnus3: **Renamed the community-panel view dropdown to the deity-domain model** (Ryan: "Food" is really the Ecology viewer now, and "Leaders" → "Rule"). The "food" chart had grown to food/pop/CC/fertility = the whole Venus domain, and the leadership timeline is Zeus's. Renamed labels + option values (`leaders`→`rule`, `food`→`ecology`), `_currentView` default + dispatch, and the methods (`_drawLeaderTimeline`→`_drawRuleTimeline`, `_drawFoodChart`→`_drawEcologyChart`) + stale comments. Default view/order unchanged. Self-contained (`index.html` + `inspect.js`); `node --check` clean. Not committed; pending review.
- Magnus3: **Added food *types* + per-batch expiration to the ecology food system** (Ryan: well-tuned worlds let food accumulate to absurd degrees; wanted a yielded-food type and a shelf-life/spoilage check). Two halves: (1) gathering now yields a named type — **Berries**, the first entry in a new `FoodType` enum — laying the seam for future sources→types (hunting→meat, etc.); (2) each type has a shelf life and yielded food is held as **dated batches** that spoil once they age out.
  - **Design Q&A first** (entered plan mode, asked 4): storage = **typed dated batches** with a derived scalar total (vs. per-type avg-age aggregate); berry shelf life = **32 days / ~2 seasons** (autumn berries survive to winter consumption, but a year's stockpile can't); consumption = **oldest-first FIFO** (eat before it spoils); spoilage = **silent** + an `ecology_timings().food_spoiled` counter (mirrors how gathering yield is implicit/unrecorded). Plan file `nested-strolling-reddy.md`, approved.
  - **Core refactor:** `FoodStores` went from one `int64_t food` to `std::vector<FoodBatch>` (`{type, amount, yield_tick}`, kept oldest-first) + methods `total()/add()/consume()`. Spoilage (`expire_food`) lives in `EcologyConfig.hpp` not on the component, because it needs the per-type shelf-life config and `EntityComponents.hpp` must not depend on config (kept the include DAG acyclic: config→components, never the reverse). Touched every food site: gathering (now takes `tick`, yields a berry batch), seasonal orchestrator (new gather→**spoil**→consume order; fresh batches are age-0 so never spoil same-season), consumption (`total()`/`consume()` FIFO), generation (initial stores = one berry batch stamped tick 0), fission (split each batch proportionally so movers carry food of the same types *and ages*), inspect/main printouts (`total()` + per-type breakdown), and the `--profile` timings dump (`spoil=` + `food_spoiled=`).
  - **Verified** (Release build clean; seed 42, 100y): spoilage fires (`food_spoiled=5642`); **deterministic** (identical across two runs — no new RNG, spoilage is pure tick math); `inspect community 0` shows `Food: 21 (1 batches) / Berries: 21` — bounded, not the old unbounded pile. Shelf-life knob via `--config` (note: top-level opts go **before** the `run` subcommand) sweeps cleanly: 8d→32d→64d gives **188→27→2 famines** — short shelf rots berries before winter consumption, so they're wasted *and* people starve, exactly the "eat it before it rots" pressure we wanted. (Longer shelf shows more *absolute* spoilage only because communities survive, grow, and gather more over 100y — famine count is the clean signal.)
  - Not committed; pending review. Out of scope (noted in plan): only berries/gathering for now; no per-type nutrition or consumption preference; spoilage not yet a narrative Event.
- Magnus3: **Added per-layer toggles to the Ecology bottom-drawer chart** (Ryan) — the ecology view now stacks five overlays (Food / Population / Carrying capacity / Fertility-Venus / Famine bands) on one canvas, and it only gets busier as more domain signals land, so each layer needs a show/hide. Plan mode + one Q&A on placement → chose a **checkbox toolbar row above the chart** (over packing the header), shown only in the Ecology view. Viewer-only (`inspect.js` + `timeline.css`); data already carries every layer.
  - **Key constraint:** `main.js` re-runs `_render()`→`_drawEcologyChart()` on *every* zoom tick, so rebuilding the toolbar each frame would flicker + drop checkbox focus. Split the method into a thin **wrapper** (computes which layers have data → `hasCarry`/`adminActive`/`hasFamine`), `_buildEcologyScaffold` (injects the `.ecology-layers` checkbox row + canvas, wires listeners), and `_paintEcologyCanvas` (the old draw body). Guard: scaffold rebuilds only when `_ecologyMountedId !== currentCommunityId`; toggling a checkbox repaints the canvas *only*. Mount marker reset on rule-view switch + `close()`.
  - **State:** `this.layers = {food,population,carry,fertility,famine}` (all-on), persists across communities/views so choices stick. A toggle is emitted only for a layer that has data; each gates its line/fill/shading/band AND its legend entry AND (famine) the click hit-test. Two nice-to-haves fell out: hiding Carry rescales the pop axis to reclaim height (`showCarry = hasCarry && L.carry` drives `maxPop`), and the legend was rewritten to **flow + wrap** over visible entries (measure-text layout) instead of fixed x-offsets, so it never leaves gaps.
  - **Verified:** `node --check js/inspect.js` clean; `serve.py` up on :8090, assets 200. Visual confirmation (toggle each layer, zoom-persistence, cross-community persistence, Rule-view unaffected) is Ryan's to eyeball in the browser. Not committed; pending review. Out of scope: no localStorage persistence; Fertility line + restraint shading stay one combined toggle.

Sunday - 05/31/26
Daily Goals:
- [ ]
Log:
- Magnus3: **Designed the Rule/Zeus domain's first directive — mapping the Ecology DDA pattern onto governance** (long brainstorming session with Ryan; no code yet — the goal was to structure the system so it *needs* a directive, mirroring how the Malthusian baseline motivated Fertility Restraint).
  - **Keystone reframe:** a DDA directive *is* a feedback controller (sense → control law → actuate), and the three gunas Ryan is converging on map exactly onto the three damping regimes — **Sattva = critically damped** (reaches setpoint, no overshoot), **Rajas = under-damped** (overshoots, rings, can diverge into a limit cycle), **Tamas = over-damped** (sluggish, never arrives). This retroactively explains the Fertility Restraint setpoint×taper Pareto sweep as control-tuning toward critical damping.
  - **Gunas are domain-general but each domain damps a different *axis*.** Venus damps a *level* (population) on a symmetric axis → an equilibrium/fixed-point controller (the Empress). Zeus damps a *rate* (cohesion velocity) + later a *distribution* → a loop prone to a limit cycle (the Wheel of Fortune). Zeus's danger isn't "too much yield" (monotonically good in level) — it hides in too-fast growth + concentration.
  - **The boom-bust is emergent, not scripted.** Existing lags — gestation (~3 seasons), maturation to fertile (~15y), leader re-election only *on death*, the 1-tick Rule→Ecology back-edge — plus an *instantaneous* CC collapse when cohesion fails = a delayed-logistic oscillation. So Venus's directive averts a *static* trap (Malthus); Zeus's averts a *dynamic* one (oscillation). That's why it warrants a directive rather than a knob.
  - **Named the key state — three carrying capacities:** `natural` (no modifiers), `resultant` (all modifiers), and per-source *counterfactual* contribution ("what CC would be if this modifier vanished" — not an additive share, since modifiers compose multiplicatively). **Overhang = pop − natural CC** = the "dependent population" alive only on borrowed (cohesion) capacity. Publish as a read-only derived component so Venus & Zeus share one CC definition (dodges the two-capacities bug `EcologySystem.hpp:188` already warns about).
  - **Resolved the Venus×Zeus interaction:** feeding Venus the *resultant* CC is the right steady-state coupling, but a "naive" Venus that faithfully fills resultant-CC headroom is exactly what *builds* the overhang — and her fertility lever can only stop *adding* people, never clear an existing overhang (only mortality does). So the dangerous regime is Zeus-ramps-fast + Venus-permissive, and the famine on a cohesion collapse is unpreventable by Venus by construction. Keep naive Venus for v1 *on purpose* so the pathology surfaces; a risk-adjusted "prudent" Venus is a later fork.
  - **Settled vocabulary + dynamics (Ryan):** **Authority** = the per-season *rate* (the leader's; **constant for v1**, trait/tenure-driven later). **Cohesion** = the accumulating *stock* (the realm's). `cohesion += authority` each season — plain **additive**, not logistic; the boom-bust comes from tenure variance + the succession drop, not the growth rule. Authority(individual) → Cohesion(institutional) encodes the "personal becomes institutional" theme; cohesion drives the CC bonus → resultant CC. Clarified that fertility:population :: authority:cohesion — in both, the deity governs a *rate* and a *stock* results (Venus never sets population directly either).
  - **Directive shape (deferred — build the need first):** a [0,1] throttle on the authority→cohesion conversion, tapered as the overhang nears what succession volatility can safely carry — the *same* control-law shape as Fertility Restraint's throttle near CC. Intent pairing — Venus: *"don't make mouths you can't feed"*; Zeus: *"don't build a height you can't hold."* Instability is *derived* (overhang × cohesion volatility), not a separate stored value.
  - **Tyranny / distributional Rajas deferred to phase 2** — "a long/cruel reign concentrates consumption so the bottom starves first" needs replacing today's strictly-egalitarian allocation (`ConsumptionSystem.hpp:119`) with ordered/weighted allocation. Not load-bearing for v1 (a long reign is already dangerous via the dynamical overhang channel). Note: busts already kill *now* via the zero-allocation death roll (`ConsumptionSystem.hpp:169`); external (non-famine) mortality later sharpens it.
  - **Scope decisions (Ryan):** cohesion is **per-community** for now (moves to a relational/inter-community layer when annexation etc. arrive); v1 **authority is constant** (only lifespan→tenure varies).
  - **Agreed step 1 (no directive yet):** add `CommunityCohesion` stock (Zeus-owned; updated in RuleSystem: +authority/season, **fractional** drop at succession — self-bounding, no arbitrary ceiling); swap the Rule→Ecology yield bonus from instantaneous leader-conscientiousness to the cohesion stock (`LeaderModifiers` keeps its shape, only its source changes); publish natural/resultant CC + overhang as a Venus-owned derived component; keep Venus pointed at resultant CC; then run undirected and watch the boom-bust to confirm the directive is needed. New `RulerConfig.cohesion` knobs: `authority_per_season`, `succession_drop_fraction`, `cohesion_to_bonus_scale`, `founding_cohesion`. Plan presented; pending Ryan's go-ahead.
  - **Implemented step 1a (core sim; approved)** — 8 edit sites. New components `CommunityCohesion {value}` (Zeus-owned) + `CarryingCapacity {natural, resultant, overhang}` (Venus-owned), both registered in `World.hpp` and seeded at both community-creation sites (`CommunityGenerator`, `FissionSystem`) so they're always-present (dodges the deferred-`set`/`get_mut` trap). `RulerConfig.cohesion` block added (`authority_per_season=0.002`, `succession_drop_fraction=0.25`, `cohesion_to_bonus_scale=1.0` — placeholders pending a sweep; dropped `founding_cohesion`, unneeded for additive-from-zero) + `rule.json`; old `conscientiousness_gathering_bonus` marked dormant (becomes the authority source when authority goes dynamic). `LeadershipSystem`: new `update_community_cohesion` (succession detected via a fresh `LeadershipHistory` entry stamped this tick, size>1 to exclude founding → ×(1−drop), then +authority while a leader is seated) + read side swapped `modifiers_for_leader`/`leader_modifiers_from_cache` → `cohesion_modifiers` (bonus = cohesion × scale). `RuleSystem` calls it each season after leader election. `EcologySystem` now computes natural (bonus=0) + resultant CC, writes the `CarryingCapacity` component (overhang = max(0, pop−natural)); Venus still steers toward resultant (naive coupling, on purpose). `InspectCommand` shows cohesion + the CC decomposition.
  - **Verified** (clean Release build): `inspect community 0` seed 42 @150y → **Cohesion 0.397 → +40% gathering**, **CC natural 16.5 / resultant 23.1 / overhang 3.5** (resultant = natural×1.40 ✓; overhang = pop−natural ✓). Cohesion landing at ~0.40 (vs 1.2 if drops were absent over 600 seasons) confirms succession erosion balancing accumulation — the self-bounding equilibrium. **Determinism intact** — two 200y exports byte-identical (`2d3a7691…`). Not committed.
  - **Next (step 1b):** surface the dynamics over time — extend `FoodSnapshot`/export with cohesion + natural CC + overhang and plot them in the viewer so we can *watch* the boom-bust (long reign → cohesion/CC/overhang climb → succession collapse → famine of the overhang) and confirm the pathology that motivates the directive.
  - **Implemented step 1b (observe the boom-bust).** Extended the time series: `FoodSnapshot` += `natural_carrying_capacity` + `cohesion`; recorded in the Winter block (hoisted `natural_cc` out of the CC block; cohesion read from `CommunityCohesion`). Export `food_history` widened `array<double,5>`→`<double,7>` = `[tick,food,pop,resultantCC,fmod,naturalCC,cohesion]` — **new fields appended**, so existing positional readers (viewer 0-4, exponator `row[0:3]`) are untouched. Viewer (`inspect.js`): three new toggleable Ecology layers — **Natural CC** (blue-grey dashed floor, pop axis), **Overhang** (orange shaded band between natural CC and pop = the dependent cohort), **Cohesion (Zeus)** (purple, own normalized axis so the succession sawtooth reads). Slotted into the existing toggle/scaffold/paint split + flow-wrap legend; gated on presence+nonzero so open-loop worlds stay clean.
  - **Verified:** `node --check` clean; determinism intact (two 200y exports byte-identical). **The pathology is real & visible** — seed 7777, 1000y: cohesion **sawtooths 0.01–0.66** with ~70 succession drops (>0.05) per community, **overhang peaks at 11**, **16 famines** across 3 communities. So a long reign builds cohesion→CC→overhang and successions collapse it — the boom-bust the directive will tame, now plottable on one time axis (cohesion sawtooth + overhang band + famine bands aligned). Not committed; viewer needs a `serve.py` restart to refresh the exe/config cache, and the visual pass is Ryan's to eyeball.
  - **Merged the bottom-drawer Rule view into the Ecology view (Ryan)** — one chart, leaders as another toggleable layer; no more two-view dropdown (domain grouping of the toggles deferred). Removed `#bottom-view-toggle` (`index.html` + its `timeline.css` rules) and all view-switch wiring in `inspect.js` (`_currentView`, the toggle listener, `this.toggle` in close/showCommunity, the `_render` branch — `_render` now always draws the one chart). Deleted `_drawRuleTimeline`; folded leadership onto the ecology canvas as a `leaders` layer: faint full-height **succession divider lines** (so each leader change reads against the cohesion drop / famine it triggers) + a goldenrod **leader ribbon** along the top strip (alternating-opacity tenure bars with clipped first-name labels), and merged the click hit-test (ribbon → navigate to that leader; famine bands → open the famine inspector, leader band checked first). Legend shifts below the ribbon when the layer is on; gated on `community.leaders` presence. `node --check` clean; no dangling refs. Viewer-only; not committed; visual pass is Ryan's.
