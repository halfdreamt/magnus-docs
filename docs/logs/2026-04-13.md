# 04/13/26 - 04/19/26

Week Goals:
- [ ]

Accomplished:
-

Monday - 04/13/26
Daily Goals:
- [ ]
Log:
- Magnus3: Starvation/famine mortality model (closes food→population loop)
  - Per-person Starvation interval events fire when food allocation < need each season; per-community Famine event when any deficit
  - Zero-allocation per-person death roll, configurable via `ecology.starvation.zero_food_death_chance` (default 0.5)
  - Birth gate replaced: instead of `food_threshold_per_person`, just skip the birth roll if either parent is in `Starving` state
  - Gathering events dropped — silent process now; `food_history` already captures the per-year food curve
  - Stability result: 10/10 seeds keep all communities alive at Y1000 (was 0/10 before mortality feedback)
- Magnus3: Redesign — embed per-person starvation intervals inside the Famine event (drop separate Starvation event type)
  - At carrying capacity, mild starvation IS the routine, not the exception → per-person Starvation events were ~130k per 1000y, violating the "events for exceptions" principle
  - New shape: one Famine event per community per food-shortage spell; FamineParams carries `vector<StarvationInterval>{person_id, start_tick, end_tick}`
  - Starving component still exists (carries `since_tick` to find its open interval); CurrentStarvation removed
  - Behavior preserved exactly (deterministic Y1000 state for seed 42); event count 140k → 9.8k (14× reduction); sim runtime ~2.6s → ~1.2s
- Magnus3: Tune defaults for minimal stable population
  - `world.max_communities_per_region`: 12 → 3 (no fission expansion)
  - `ecology.tract.cap_base`: 60 → 40 (smaller carrying capacity per tract)
  - Result: mean pop ~63 (was ~163), per-community ~21 people, all 10/10 seeds stable at Y1000
- Magnus3: Performance profile — sim is 3x slower than pre-rewrite baseline despite similar entity counts
  - Pre-rewrite (`624d74a`): 138ms sim, 1293 entities, 5659 events
  - Current (`2906adb`): 410ms sim, 1304 entities, 3425 events (same scale, fewer events)
  - Of current 410ms: 162ms is ecology code, 248ms is "Flecs progress() outside callback" — 62μs/tick vs the previously-recorded 2.2μs/tick
  - Tried stubbing per-person consumption work as an A/B test → infinite population growth, 3GB memory, killed after 10 minutes (the stub broke food deduction; per-person work is load-bearing)
  - Detail and hypotheses written up in `docs/research/4-14-26-rewrite-regression.md`
  - Decision: pause investigation, current perf is acceptable for dev workflows (~10s sweeps, responsive viewer); revisit later

Tuesday - 04/14/26
Daily Goals:
- [ ]
Log:
-

Wednesday - 04/15/26
Daily Goals:
- [ ]
Log:
-

Thursday - 04/16/26
Daily Goals:
- [ ]
Log:
-

Friday - 04/17/26
Daily Goals:
- [ ]
Log:
-

Saturday - 04/18/26
Daily Goals:
- [ ]
Log:
-

Sunday - 04/19/26
Daily Goals:
- [ ]
Log:
-
