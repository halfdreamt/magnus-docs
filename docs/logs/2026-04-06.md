# 04/06/26 - 04/12/26

Week Goals:
- [ ]

Accomplished:
-

Monday - 04/06/26
Daily Goals:
- [ ]
Log:
- Magnus3: Social hierarchy + ecology system implementation
  - Added Community and Family entity types to the ECS
    - `CommunityIdentity` (seed, name, region, slot), `FoodStores` (grain)
    - `FamilyIdentity` (seed, surname, slot)
    - Hierarchy: Region -> Community (3) -> Family (3) -> Person (3-5), all child_of
  - New deterministic seed functions: `community_seed()`, `family_seed()`, `ecology_seed()`
  - Created CommunityGenerator, FamilyGenerator, WorldGenerator
    - Community names: syllabic root + place suffix (e.g. "Gurmholm", "Waemvale", "Frandale")
    - Families share surnames across members
    - WorldGenerator orchestrates full hierarchy from a single world seed
    - ~36 people per region (3 communities x 3 families x ~4 members)
  - Ecology Deity entity: singleton that drives all agricultural events
    - `EcologyState` component with per-region `RegionEcology` (soil fertility, rainfall, drought)
    - First Flecs system: `EcologySystem` fires at season boundaries (every 23,040 ticks)
    - Seasonal logic: Spring (weather), Summer (drought risk 10%), Autumn (harvest), Winter (consumption + famine check)
    - Food math: base 150 grain/community/year, modified by fertility x rainfall x variance
    - Consumption: 3 grain/person/season, ~12 people/community = ~144/year -> small surplus in good years
    - Drought reduces rainfall to 0.3, lasts 1-3 seasons -> harvests drop to ~40-50 -> famine
  - CLI inspect commands: complete game state visibility
    - `inspect world` - overview with communities, food stores, event counts
    - `inspect community <slot>` - families, members, events targeting this community
    - `inspect family <c> <f>` - members with personality traits and health
    - `inspect ecology` - deity state per region (fertility, rainfall, drought)
    - `inspect events` - chronological event log with details
    - All use `--seed` and `--tick` to regenerate deterministic world state
  - Flecs gotcha: empty tag structs can't use `.set<>()`, must use `.add<>()` or `.with<>()`
  - Flecs gotcha: `.each()` lambda can't take reference to empty tag component, use `.with<Tag>()` filter instead
  - Verified: determinism holds, 5-year run shows natural drought/recovery cycles
- Magnus3: Event model refactor — duration-based events + Life as Event
  - Removed rigid tier system (Epoch/Era/Episode/Moment tags + hierarchy)
  - Events are now intervals: `start_tick` + `duration_ticks`, not points in time
  - Temporal vocabulary is descriptive (day/season/year/decade), not structural
  - Spatial scope derived from participants, not stored on event
  - `EventParticipants` changed from flat struct to `vector<ParticipantEntry>` — events connect to any number of entities at any level
  - Added `ParticipantRole::Context` for spatial context (community, region)
  - Life as Event: `EventType::Life` with duration = lifespan
    - `PersonIdentity` no longer stores `birth_tick`/`death_tick` — derived from Life event
    - Person is "alive" at tick T if their Life event is active at T
    - Lifespan calculated from seed × community_health × family_health modifiers (50-80 base years)
  - New event types: `Life`, `Pregnancy`, `Drought` (replaces `Disaster` for droughts)
  - Removed: `Birth`, `Death`, `Aging` event types, `BirthParams`, tier structs, `TierStats`, tier duration constants, `AbsorptionThresholds`, `AbsorbedInto`
  - Updated EcologySystem: all events now have duration (Harvest=season, Drought=1-3 seasons, Famine=season)
  - Updated CLI inspect: shows lifespans (Y0-Y62, alive/dead), event durations (day/season/year/decade)
  - Architectural insight: events are neurons in a spatio-temporal graph, not slots in a tree
- Magnus3: Divine creation script
  - World generation is now a divine birth chain: Creator → World → Ruler/Ecology deities → people
  - Every entity traces back to the Creator through CausedBy relationships
  - 4 divine entities with 1000-year Life events (the "age")
  - `DeityIdentity` component (seed + name) keeps divine entities separate from PersonIdentity
  - Ruler deity creates regions and communities, Ecology deity births founding people
  - After founding generation, humans birth humans (ecology deity only influences conditions)
  - First real use of `CausedBy` relationship in the codebase
  - Added `AGE_YEARS` (1000) and `TICKS_PER_AGE` constants
  - Flecs gotcha: can't modify entities during `.each()` iteration — must collect targets first, then modify
- Magnus3: Merge Life events into entities
  - Entities that exist ARE events — person/deity entities carry `Event{Life}` component directly
  - Eliminated separate Life event entities and EventParticipants linking them
  - `person.get<Event>()` gives birth/death directly, no searching
  - `CausedBy` links go between entities: `person.add<CausedBy>(ecology_deity)`
  - Removed `find_life_event()` helper and `link_life_to_cause()` workaround
  - Principle: entities that exist are events, events that happen reference entities
- Magnus3: Birth/death lifecycle + active population counting
  - Population counting now uses active Life events — dead people stop consuming food
  - `count_alive(community, tick)` helper checks `event.active_at(tick)` for each person
  - Birth system runs each spring, gated by food surplus (`food_per_person > 15`)
  - Each living adult has 0.07 chance per spring of producing a child with a random partner
  - New `PersonGenerator::generate_birth()` for runtime births (exact birth tick, known parents)
  - Children get `CausedBy` links to both parents — first human-caused births in the system
  - Children join parent's family, inherit family surname
  - Tuning process: tested rates 0.04-0.08 across multiple seeds at 100 years
    - 0.06 → population declined (~28 alive from 35 founding)
    - 0.08 → population grew too fast (45-62 alive, food pressure)
    - 0.07 → stable equilibrium (~40 alive, food naturally caps growth)
  - 300-year run (seed 42): 204 total lives, 40 alive at Y300, population stable
    - Every person has full fidelity: name, family, community, exact birth/death year, traits
    - Determinism verified: two identical 300-year runs produce byte-identical output
    - Runtime: ~87 seconds for 27.6M ticks (Debug build)
  - Fuldlourmfraendtaent family dominated Gurmholm with 49 members across 300 years
- Magnus3: Performance profiling and tick-skip optimization
  - Profiled 300-year run: Debug 86s, Release 15s — most time spent in empty ticks
  - Root cause: `world.progress()` called every tick (27.6M times), but ecology system only fires 1,200 times (at season boundaries)
  - Fix: skip empty ticks, only call `progress()` at season boundaries
  - Added `World::skip_to()` for advancing tick counter without Flecs overhead
  - Ecology system reads world tick via pointer instead of Flecs `frame_count_total`
  - Results: 300 years in 5ms, full 1000-year age in 30ms — **2,800x speedup**
  - 1000-year age produces 597 lives, 5,433 total events, all deterministic
  - Observation: small populations (~12/community) go extinct over 1000 years due to stochastic collapse — realistic but may want migration or community interaction later
- Magnus3: Ecology config externalization
  - All hard-coded ecology constants extracted to `config/ecology.json` (harvest, drought, birth, lifespan, consumption, marriage)
  - All hard-coded world/naming constants extracted to `config/world.json` (syllable tables, place suffixes, deity names, structure)
  - `EcologyConfig` and `WorldConfig` structs loaded from JSON via glaze
  - CLI accepts `--config` flag for alternate config files
  - Verified: doubling harvest yield in config doubles population carrying capacity
- Magnus3: Divine creation script + entity-event merge
  - World generation follows divine birth chain: Creator → World → Ruler → Ecology Deity
  - Every entity traces back to Creator through `CausedBy` relationships
  - Entities that exist ARE their Life events — `person.get<Event>()` gives birth/death directly
  - Eliminated separate Life event entities and `find_life_event()` searches
- Magnus3: Marriage system
  - Eligible singles pair up in autumn, one partner transfers family (matrilineal/patrilineal/random configurable)
  - Only married pairs can have children — birth rate retuned to 0.20 per pair
  - Marriage is a duration-based event lasting until first spouse dies
  - Widows can remarry (detected via MarriedTo component)
- Magnus3: Community leaders with personality modifiers
  - Oldest person in largest family leads each community
  - Leader personality subtly modifies outcomes (configurable via `config/ruler.json`):
    - Conscientiousness → harvest bonus
    - Agreeableness → famine severity reduction
    - Stability → drought resilience
  - Leader cached on community, only recomputed when leader dies
- Magnus3: 1000-year population stability testing
  - Ran 10 seeds for 1000 years: 30% extinction rate from stochastic small-population collapse
  - Surviving communities concentrate population (realistic but motivates fission/migration)
  - Community fission implementation started but disabled pending performance work
- Magnus3: Performance optimization pass
  - Leader system caused 5x regression (141ms → from 30ms baseline) due to per-season full entity scans
  - O(1) marriage lookups: `MarriedTo` component replaces per-season all-events scan (141ms → 44ms)
  - Living population cache: `PopulationCache` with `next_death_tick` invalidation, only rescans on death/birth (44ms → 38ms)
  - Single-pass `build_snapshot`: population, adults, singles, married pairs computed in one iteration
  - Communities processed outside `ecs.each` to avoid Flecs locked storage issues
  - Profiling breakdown for 1000-year run (~40ms):
    - Flecs `progress()` overhead: 8.7ms (22%) — ~2.2μs per call
    - Ecology logic: ~30ms (78%) — ~7.5μs per season per community
    - At practical floor: within 5x of Flecs overhead
  - Full optimization journey: 50s → 30ms → 141ms (regression) → 40ms (recovered)
  - Flecs gotchas accumulated:
    - Can't `get_mut` on entities during `ecs.each` iteration (locked storage)
    - Must collect entities into vector first, then modify outside iteration
    - Empty tag structs need `.add<>()` not `.set<>()`

Tuesday - 04/07/26
Daily Goals:
- [ ]
Log:
-

Wednesday - 04/08/26
Daily Goals:
- [ ]
Log:
- Magnus3: Reviewed 040826-assessment.md — full architecture/logic assessment
  - Identified EcologySystem.hpp (1,165 lines) as the main maintainability concern
  - Assessment catalogs 7 bugs (critical: event ID collision, high: child seed fragility, no incest prevention, no famine mortality)
  - Feature roadmap in 4 tiers from hardening through new systems
- Magnus3: Decomposed EcologySystem.hpp monolith into 8 focused subsystem files
  - `EcologyHelpers.hpp` — shared `create_ecology_event()` and `add_ecology_participants()`
  - `PopulationCache.hpp` — living cache, snapshot building, lifespan maintenance, spouse/marriage helpers
  - `LeadershipSystem.hpp` — leader selection (oldest in largest family), cached modifiers
  - `MarriageSystem.hpp` — eligible singles pairing, family transfer by lineage config
  - `BirthSystem.hpp` — spring births from married pairs, food-gated
  - `WeatherSystem.hpp` — spring rain/drought-end, summer drought risk
  - `HarvestSystem.hpp` — autumn harvest with leader/weather modifiers
  - `FamineSystem.hpp` — winter consumption + famine severity calculation
  - `FissionSystem.hpp` — personality-driven family splitting, community founding
  - `EcologySystem.hpp` now ~130 lines: registers Flecs system + dispatches to subsystems
  - Pure refactor — no behavior changes, all systems fire on same seasonal cadence
- Magnus3: Reviewed food/famine system end-to-end
  - Traced full seasonal cycle: Spring(births) → Summer(drought) → Autumn(harvest) → Winter(consumption/famine)
  - Found famine is purely cosmetic — severity calculated but never read by any system
  - Food system only gates births (food_per_capita >= 15); no starvation death exists
  - Identified drought as the real disruptor: reduces rainfall to 0.3 → ~70% harvest loss
  - `drought_resilience` modifier computed but never applied anywhere
- Magnus3: Introduced region spatial grid system
  - Motivation: communities had no spatial position within regions, blocking future features like territory, gathering yields, migration
  - Added `AreaCell` and `RegionGrid` structs to `EcologyComponents.hpp` — each region is an 8×8 grid of cells
  - Each cell has `base_fertility` (0.5–1.5, deterministic from seed) for future food system use
  - Added `GridPosition` component to `EntityComponents.hpp` — communities now have (x, y) on the grid
  - Grid generated during world creation in `WorldGenerator.hpp`; communities placed on unoccupied cells
  - Updated `FissionSystem.hpp` with adjacent-first cell selection — new communities settle near parent, expanding outward in rings
  - Added dead community cleanup in `EcologySystem.hpp` — frees grid cells when all members die
  - Config-driven: `region_grid_width`/`region_grid_height` in `WorldConfig.hpp` and JSON configs
  - Verified: builds clean, simulation runs normally, grid positions are deterministic and non-overlapping
  - Updated InspectCommand callers: `EcologySystem::count_alive` → `count_alive` (free functions)
  - Added `/bigobj` to CMakeLists.txt — needed for header-heavy ECS template instantiations
  - Verified: Release build runs, inspect commands work, simulation produces same results
- Magnus3: Fixed pre-existing LOCKED_STORAGE Flecs assertion (Debug crash)
  - Root cause 1: `WorldGenerator.hpp` — `family.set<Event>()` called inside `community.children()` iteration, modifying entity archetype while table was locked
  - Fix: collect families + lifespans into vector during iteration, apply `set<Event>()` after iteration completes
  - Root cause 2 (preventive): `EcologySystem` — subsystems (births, marriages, fission) modify entities inside the deity `.each()` callback
  - Fix: wrapped all subsystem processing in `world.defer_begin()` / `world.defer_end()` to queue mutations
  - Verified: 1000-year Debug simulation runs clean, no assertion, no crash

Thursday - 04/09/26
Daily Goals:
- [ ]
Log:
- Magnus3: Viewer ↔ simulation bridge server (`serve.py`)
  - Python stdlib HTTP server serves viewer static files AND proxies `/api/generate` to `magnus3.exe export`
  - Viewer toolbar now has Seed/Years inputs + Generate button — no manual JSON export/import cycle
  - Extracted `displaySimulation()` from `loadFile()` in main.js so both paths share post-load logic
  - Existing Load JSON and drag-drop still work as fallback
- Magnus3: Generation settings panel with config overrides
  - Settings button toggles collapsible panel below toolbar with 4 fieldset groups: World Structure, Growth Limits, Population, Ecology
  - Settings auto-populate from defaults via new `/api/config` endpoint
  - Overrides sent as POST body to `/api/generate`, bridge deep-merges with defaults and writes temp config files
  - Added `--world-config` and `--ruler-config` CLI flags to `main.cpp` (CLI11 requires global flags before subcommand)
  - Bug: initially appended config flags after the `export` subcommand — CLI11 rejected them as unexpected args. Fix: build command with global flags first, subcommand last
- Magnus3: Biomes and named areas
  - 4 biomes: Grassland (farming), Woodland (hunting/foraging), Wetland (fishing), Highland (herding/mining)
  - Each region assigned a biome deterministically from seed (or from config override)
  - New `AreaGenerator.hpp`: 20 area types with biome-weighted probabilities (e.g. Woodland favors Forest/Grove/Glade)
  - Flood-fill algorithm clusters grid cells into 3-8 named areas per region using jittered seed points
  - Area names via existing `NameGenerator::generate_place_name()` with type prefix/suffix ("Forest of Aelund", "Korith Ridge")
  - Cell fertility now driven by area type instead of random — e.g. Field=1.4, Bog=0.5, Valley=1.1
  - Viewer groups communities under area sub-headers; region headers show biome
  - Areas span full region lifetime (geographic features, not tied to community lifespan)
- Magnus3: Region minimap with area visualization
  - Click region header or area bar → bottom panel shows interactive canvas minimap
  - Grid colored by area (golden-angle HSL distribution), community markers as green dots with labels
  - Click cells or legend entries to toggle area highlighting (dimming non-selected areas)
  - Exported grid dimensions (`grid_width`/`grid_height`) on regions and grid positions (`grid_x`/`grid_y`) on communities
  - Fixed `std::vector<std::pair>` serialization: glaze serialized pairs as JSON map objects, switched to `ExportCellCoord` struct
  - **Debugging lesson — drag filter swallowing clicks**: region/area header clicks produced zero response, no console output even from the first line of the pointerup handler. Spent significant time tracing through hit test logic, render paths, and data serialization before identifying the root cause: the `ZoomController` drag filter (`onDragFilter`) returned `true` for `hit.type === 'section'`, which included region/area headers. This initiated a drag on pointerdown, so `_dragging` was `true` when pointerup fired, causing the click handler's first guard (`if (zoom._dragging) return`) to abort silently. The fix was to exclude clickable headers from the drag filter. Key takeaway: when a click handler produces zero output (not even logging), the event isn't reaching it — look upstream at event interception (drag handlers, overlays, `preventDefault`), not downstream at the handler logic
  - Also fixed shared bottom panel bug: `minimapPanel.close()` removed the `open` class that `communityPanel.showCommunity()` just added (same DOM element). Fix: always close siblings before opening the target
- Magnus3: Fixed fission resurrecting dead communities
  - Bug: Persfell community in seed 42 had a 298-year gap with zero families (year 132–430), then families reappeared
  - Root cause: `FissionSystem.hpp` Option 2 ("room in another community") didn't check if the target community was alive — dead communities with 0 families looked like they had "room"
  - Fix 1: Added `is_community_alive(community, tick)` guard in the Option 2 loop
  - Fix 2: New communities created during fission weren't getting a `Life` event — export defaulted to year 0 start. Added Life event creation alongside the Settlement event
  - Verified: Persfell now properly dies at year 132, Hilddale founded at year 420 as a new community
- Magnus3: Fertility-based cell selection for community settlement
  - Previously, initial and fission-founded communities picked random unoccupied cells
  - Now communities settle on the highest-fertility available cells (e.g. Fields 1.4, Meadows 1.2 chosen before Bogs 0.5, Cliffs 0.3)
  - Three configurable settlement strategies per community (`SettlementStrategy` enum on `CommunityIdentity`):
    - `BestCell` — pick highest-fertility unoccupied cell in region (default)
    - `AdjacentFirst` — search outward in rings from parent, pick best within each ring
    - `Weighted` — score cells by `fertility - (distance * penalty)`, pick highest
  - Config: `default_settlement_strategy` and `weighted_distance_penalty` in `world.json` fission section
  - New communities inherit their parent community's strategy
  - `CommunityGenerator::pick_best_cell()` replaces `pick_unoccupied_cell()` for initial placement
  - `FissionSystem.hpp`: `pick_settlement_cell()` dispatcher routes to strategy-specific functions
  - Verified seed 42: all 4 communities (including fission-founded Hilddale) land on fertility 1.2 cells

Friday - 04/10/26
Daily Goals:
- [ ]
Log:
-
