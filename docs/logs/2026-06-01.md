# 06/01/26 - 06/07/26

Week Goals:
- [ ] Magnus3: Pre-expansion technical cleanup — pay down the structural debt that compounds per layer before adding the next divine layers

Accomplished:
- Pre-expansion technical review (research doc) + centralized config (SimConfig) landed
- Region→communities index refactor; viewer Performance report; famine distribution heatmap/scrubber + cause-of-death (Mortality)
- **Rule/Zeus redesign delivered:** Cohesion v2 (leader-authority accrual + gated appointment); hereditary integer **authority rank** (replaced the personality-% authority), with configurable inheritance, office-bump-on-victory, and **contested successions as clickable interregnum events** (predecessor/contenders/victor)
- **Confirmed the boom/bust chain** (interregnum → cohesion crash → overhang famine), cleanest with Venus on
- **Mercury layer seeded** — technology overlays; agriculture raises the cohesion payoff mid-run (a test for dramatic rule-dependence), wired into the viewer (config tab + discovery marker)
- **Zeus's first directive — Appoint Leaders (immediate appointment)** — the cure: with agriculture on, directive off → 87 interregnums / 207 deaths, directive on → 0 / 11. The Mercury→crisis→Zeus→cure arc is demonstrable from the UI.

Monday - 06/01/26
Daily Goals:
- [x] Magnus3: Critically review the project before adding more divine layers; enact the centralized config
Log:
- Magnus3: Ran a fresh summary of the simulation logic with the new Rule/Zeus cohesion mechanics, grounded in the source (RuleSystem orchestrator, LeadershipSystem `update_community_cohesion` + `cohesion_modifiers`, EcologySystem CC decomposition). Confirmed the model: Zeus accrues `authority → cohesion` (stock), eroded fractionally at each succession; cohesion drives the Rule→Ecology yield bonus → resultant CC; Venus steers fertility toward resultant CC. The emergent boom-bust is the dynamic trap Zeus's (deferred) directive will tame, mirroring how the Malthusian static trap motivated Venus's Fertility Restraint.
- Magnus3: Critical pre-expansion review — fanned out four parallel code audits (systems hot-path/seams, core/config/components, export/viewer seam, flagged-bug verification), all grounded in the actual source rather than the logs. Headline finding: the architecture's binding rules (single-writer, fixed-phase ordering, one-config-per-layer, the time-series contract) are sound in design but **enforced only by comments and naming discipline**, and the cost of complying with each scales with the layer count — now (2 of 7 layers) is the cheap moment to convert convention into structure.
  - **Tier 1 (compounds ~3×/layer):** (1) configs threaded individually through ~13 signatures + 4 class members, with param-order already drifting (`generate_in_family` wcfg,cfg,rcfg vs `generate_birth` cfg,wcfg,rcfg) — aggregate into one `SimConfig`; (2) Zeus-before-Venus order enforced only by source-line order in `register_systems` — make it explicit (Flecs phases); (3) 4× O(R×C) global scans per season (process_season/cleanup/exogamy/fission) — build a region→communities index once/season; (4) marriage executes in Venus's orchestrator but is Zeus's concern — move it to Rule's loop.
  - **Tier 2 (ownership):** (5) `CommunityCohesion` (Zeus-owned) written by Venus's FissionSystem/CommunityGenerator (benign zero-init today, real bug once founding cohesion ≠ 0); (6) family graph (`child_of`/`Parentage`/`MarriedTo`) has 3 writers across 2 layers, no owner — name one before Mars/Trade add more; (7) Fertility Restraint steers toward *resultant* CC, manufacturing the overhang it's meant to prevent — make the setpoint target explicit.
  - **Tier 3 (one live bug + traps):** (8) **REAL BUG** — FissionSystem Option-3 (new community) does deferred `.set<>()` then immediate `get<GridPosition>`/`get_mut<FoodStores>` in the same defer scope → new fission communities get **no plot and zero carried food** (same hazard ConsumptionSystem already solves with defer_suspend/resume); (9) event IDs from hand-partitioned file-static locals; (10) pairing determinism leans on Flecs table order.
  - **Tier 4 (export/viewer, one root cause):** (11) `food_history` positional array grown 3→5→7 cols on an array named "food"; (12) viewer per-layer copy-paste across 4 parallel lists; (13) `DIRECTIVE_DESCRIPTIONS` duplicated C++→JS; (14) event-sourcing doctrine bending (fertility_modifier is a reconstruction, cohesion yearly-aliased). Migrate to named fields + one `LAYER_SPECS` table now, while there are 3 readers not 7.
  - **Tier 5 (tidy):** dead `fission` block in world_3region.json silently ignored (the "6 alive vs cap 3" was a multi-region world, **not a bug**); JSON↔struct default drift (`cap_base` 60↔40); drought is doc-drift; dead `process_tick` SENSE/DECIDE/RESOLVE stub (the real model-B nucleus is `DivineArbiter`).
  - Wrote it up as `docs/research/2026-06-01-pre-expansion-technical-review.md`.
- Magnus3: Ryan picked the centralized config (#1) as the first thing to enact. **Implemented `SimConfig`** — a behavior-preserving aggregate of the three domain configs.
  - New `src/config/SimConfig.hpp`: `SimConfig { EcologyConfig ecology; WorldConfig world; RulerConfig rule; }` + `SimConfig::Paths` + `SimConfig::load(paths)`. Per-domain ownership stays in the individual XConfig structs (each keeps its own JSON file + `load()`); SimConfig only bundles them. Design rule applied: **collapse any signature/member carrying two-or-more configs to one `const SimConfig&`; leave single-config leaf functions (Gathering, Consumption, Leadership, Marriage, RuleSystem) taking their typed config** — they're not part of the plumbing problem.
  - Threaded `const SimConfig&` through every multi-config boundary (11 files): `World` (ctor + 3 members → 1; register_systems passes `m_config.rule` to RuleSystem, `m_config` to EcologySystem); `EcologySystem::register_system` + `SystemState` (3 config members → 1 SimConfig) + `process_season`; `BirthSystem::process_births`; `FissionSystem::process_fission`/`perform_fission`; `PersonGenerator::generate_in_family`/`generate_birth` (the order-drift pair); `CommunityGenerator::generate`; `WorldGenerator` (ctor + 3 members → 1); `InspectCommand`/`ExportCommand` (ctor + 3 members → 1); `main.cpp` (one `SimConfig::load`, DDA overrides now on `config.ecology.administration`, cmd_run/cmd_generate signatures, all 4 dispatch sites). Collapsed-function bodies kept their short local names via top-of-function aliases (`const auto& cfg = sc.ecology;` etc.) to minimize churn. Payoff: a new layer adds **one member to SimConfig and zero new parameters** to the existing plumbing.
  - **Verified** (clean Release build): export seed 42 / 100y is **byte-identical to the pre-refactor baseline** (`48a925e2…`) — pure relocation, zero behavior change — and identical across two runs (determinism intact). Smoke-tested the other touched CLI paths: `inspect community 0` shows cohesion/CC correctly; `generate` passes its determinism check. Not committed; pending review.

Wednesday - 06/03/26
Log:
- Magnus3: Light research / orientation — placed the project's emerging design ("set initial values → iterate simulated time → apply modifiers → repeat, plus feedback that senses and adjusts values for specific entities") against established complex-systems modeling traditions, to gauge how general the approach is and which fields could lend insight.
  - Identified four traditions the design braids together: (1) **System Dynamics** (Forrester) — the core loop is its stock/flow/feedback-loop primitives almost verbatim; lineage Industrial Dynamics → Urban Dynamics (→ SimCity) → World3/Limits to Growth; gives vocabulary (balancing vs reinforcing loops) and named failure archetypes. (2) **Agent-Based Modeling** — the per-person, emergence-from-micro-rules half; canonical ref Epstein & Axtell *Growing Artificial Societies* (Sugarscape, strikingly close to Magnus3); tooling NetLogo / Mesa. (3) **Control theory / Cybernetics** — flagged as the most underused match: the Divine Domain Administration directives (deity senses a variable, compares to a goal like Population Homeostasis, adjusts knobs) *are* closed-loop controllers with setpoints (PID / governor / homeostat). (4) **Microsimulation** — population of individuals stepped through discrete periods via per-entity transition rules; the literal econ/epi/policy analog, with practical answers for scale, calibration, and aggregate *alignment*.
  - Versatility: confirmed broadly general — same skeleton underlies epidemiology (SIR/SEIR = stock-and-flow), ecology (Lotka-Volterra predator-prey, relevant to the Ecology layer), ABM economics, traffic, supply chains, climate. Noted the [[reference_exponator_tool]] seed × config sweeps are, in this lineage, **Monte Carlo ensemble simulation**.
  - Headline takeaway / next body of knowledge to import: the design's distinctive move is layering an **agent-level control system** (directive-driven deities) on a **system-dynamics substrate**. That hybrid's signature risk is *a controller acting on a noisy, delayed aggregate* → overshoot, oscillation, overshoot-and-collapse, integral windup — precisely the regime control theory exists to stabilize. Directly relevant to the [[project_divine_domain_administration]] directives (e.g. Fertility Restraint steering toward *resultant* CC, already flagged in Monday's review as manufacturing its own overhang). Recommended control theory (PID tuning, damping, delay/feedback coupling) as the next field to mine; offered a follow-up to map the actual layers (config knobs → modifiers → directives → ecology) onto stock/flow/controller vocabulary, or a focused brief on control-theoretic stability for the directive system.
- Magnus3: Started review item #3 — **region→communities index once per season** (replace the 4× O(R×C) global `ecs.each` scans in process_season / cleanup_dead_communities / exogamy / fission with one O(C) index sliced into each consumer).
  - **Baseline measured first** (Release, seed 42, 1000y = 92,160,000 ticks; 3 runs each, warmup dropped). Built two perf-test configs since the existing `world_3region.json` silently reverts to 1 region (its `fission` block is an unknown key → glaze parse fails → default config; tidy-up #5 confirmed live): `world_perftest.json` (5 regions, 4 communities/region, 4 families) + `ecology_perftest.json` (community cap 12, admin off) so fission proliferates communities.
    - **S1 default** (1 region, 3 communities, admin on): cleanup=25ms, exogamy=31ms, scan=3ms, fission=4ms; callback_total=117ms, tick_total=170ms. Fingerprint: community-seasons=12003, famines_opened=49, intervals=1244, food_spoiled=84482, events=3283.
    - **S2 stress** (5 regions → 20 communities, admin off): **cleanup=778ms**, exogamy=894ms, scan=30ms, fission=84ms; callback_total=3135ms, tick_total=3905ms. Fingerprint: community-seasons=96886, famines_opened=12914, intervals=454705, food_spoiled=6041, events=54242.
  - Root-caused the cleanup hotspot: BirthSystem invalidates each community's living cache on every birth (`BirthSystem.hpp:233`), so by the time `cleanup_dead_communities` runs (after exogamy) most caches are dirty → `count_alive` triggers a **full O(people) rescan per community per season**. The index alone doesn't kill this; cleanup must stop re-determining liveness and instead consume a `dead` flag that process_season already computed.
  - **Implemented:** shared `CommunityRef{entity,slot,dead}` + `RegionCommunities` in `EcologyHelpers.hpp`; orchestrator builds one `unordered_map<region,RegionCommunities>` per season (single O(C) `world.each`) and slices each region's slice into process_season / exogamy / cleanup / fission. process_season marks `dead` on empty communities; cleanup consumes the flags (no scan, no refresh). exogamy & fission iterate the slice instead of their own global `ecs.each`. Same archetype order → shuffle/pairing determinism preserved.
  - **Migration-emptying edge** (discussed w/ Ryan): exogamous out-migration can leave the mover's origin community empty, and cleanup now runs off pre-exogamy flags. Per Ryan's call — **free the cell same-season** — exogamy flags an emptied origin community via a new cheap, side-effect-free `has_living_member()` early-exit check (not `count_alive`, whose refresh was mutating lifespans and inflating the world). Also per Ryan: **drop the old per-season lifespan-refresh side effect** (births/deaths/fission already update lifespans), accepting the resulting trajectory change.
  - **Determinism:** intact (S2 byte-identical across re-runs). **Behavior:** changed as expected from dropping the per-season refresh — S2 world grows ~2% (community-seasons 96886→98804, events 54242→55687).
  - **Performance verdict — the optimization does NOT deliver a speedup (rigorous old-vs-new, both rebuilt, 3 runs each):**
    - **S1** (admin on, cs=12003 *identical* old↔new — true apples-to-apples): refresh work (cache+cleanup) 36.1→32.7ms (**−3.4ms, −9%**); cleanup 24.9→0.1 but cache 11.2→32.6 — the hotspot **relocated**, only ~14% was real saving. tick_total ~178→~168 (within run noise). scan flat (1 region).
    - **S2** (stress, cs 96886→98804): cache+cleanup 1137→1179ms (**flat**); cleanup 757→0.8 fully relocated into cache (380→1178). scan 29.5→11.2 (**−18ms, the one real win** = 4×O(R×C)→1×O(C), ~0.5% of runtime). exogamy 879→965 (+86ms, my per-grant `has_living_member` overhead). Per-cs tick 39.6→40.3µs (**~flat, marginally slower**).
    - **Root finding:** the review's premise that cleanup *double-refreshes* is wrong in practice — the cache's dirty-flag already prevents a second rescan; the season's one necessary population rescan just *lands in* the cleanup bucket. Removing cleanup relocates that rescan into process_season; it doesn't eliminate it. Only the global ×R **scan** overhead (~18ms) was genuinely redundant. Net perf ≈ wash (S1 ~−2%, S2 ~+2% per-cs).
    - **Open decision for Ryan:** keep (architectural value — reusable per-region index for Mars/Mercury, cleaner cleanup — but ~flat perf + a behavior change), revert, or keep only the **behavior-preserving index-slice** (removes the ×R scans, keeps cleanup's refresh → byte-identical, captures the real ~18ms, leaves the rescan in place).
  - **Ryan chose the behavior-preserving index-slice (option 3)** — gated on confirming perf matches/exceeds and the code is more coherent. **Implemented & confirmed:**
    - Reverted the behavior-changing parts (the `dead` flag, exogamy emptiness handling, dropped refresh). Kept: one `region_index` built per season in the orchestrator, sliced as `const RegionCommunities&` into all four consumers. `cleanup` iterates the slice but still does its own `refresh`+`count_alive` post-exogamy (so out-migration emptying is caught same-season, exactly as before). Removed now-dead `has_living_member`, the `dead` field, and the unused `region_id` param on `process_season`.
    - **Behavior: byte-identical to pre-change baseline** — S1 (famines=49, intervals=1244, spoiled=84482, events=3283, tables=6216) and S2 (12914, 454705, 6041, 54242, 68252) match exactly; determinism intact.
    - **Perf (clean 4-run, identical community-seasons old↔new): never regresses.** S2 scan **29.5→11.1ms** (the real 4×O(R×C)→1×O(C) win; scales with region count); cleanup/exogamy/tick_total flat (740/869/~3837 vs 757/879/~3836). S1 flat (1 region — nothing to collapse).
    - **Coherence/extensibility:** the duplicated `ecs.each`+`region_id`-filter boilerplate (4 copies) collapses to one index build + a named shared `RegionCommunities` type; every per-region consumer now takes the region's slice via a uniform contract. A new per-region op inside Venus's orchestrator just takes `const RegionCommunities&`. Caveat: the index is local to Venus's tick callback — a *separate* future orchestrator (Mars/Mercury in their own phases) would still build its own, or we'd later promote the index to a world-level singleton; today it's a within-Venus coherence win + a reusable pattern, not yet a cross-layer shared index.
    - Throwaway perf configs (`config/ecology_perftest.json`, `config/world_perftest.json`) left in place; `world_perftest.json` is a *working* 5-region config (unlike the broken `world_3region.json`, tidy-up #5) and is useful for future multi-region perf work. Not committed; pending review.
  - **Committed & pushed** the behavior-preserving index slice: `93401ea` *Refactor: Build region→communities index once per season* (master). (Fixed a botched first commit — used a PowerShell `@'…'@` here-string in the Bash tool, which leaked a literal `@` into the subject; re-committed via `git commit --amend -F` from a temp file.)
  - **Attempted perf #1 — ancestor-set memoization in exogamy (REVERTED, didn't pan out).** Hypothesis: exogamy's O(singles²) pairing re-walks each person's ancestors per candidate via `too_closely_related`; memoize each single's ancestor-id set once per pass. Built it (byte-identical: S1/S2 fingerprints unchanged) — but exogamy did **not** speed up (~957ms vs ~870ms, slightly worse from precompute overhead).
    - **Observed (temp sub-timers) the real split:** exogamy ≈ 970ms is **~93% the collection loop** (`exo_collect` ≈ 900ms), only **~72ms** the pairing. So memoization addressed the wrong 7%. Root cause: `min_unrelated_generations = 1` (rule.json) → ancestor sets are just immediate parents (≤2), so relatedness was already trivially cheap. The real cost is per-community-per-autumn `refresh_living_cache` (full rescan — autumn caches dirty from births) + `count_alive` + `get_eligible_singles` re-walking the whole person tree. **Same structural root as cleanup: the living-cache full-rescan model** — each autumn every community is rescanned in process_season *and again* in exogamy's collection.
    - Lesson (re-)learned: profile before optimizing. Reverted the memoization + instrumentation; tree back to `93401ea`. The genuine exogamy lever is the collection/refresh path (and the broader incremental-living-count idea), not the pairing.
  - **Scoped the incremental-living-count redesign with Ryan; chose Option B (incremental count + lazy tree-order list, phased, byte-identical). Phase 1 attempted and REVERTED — surfaced a blocking structural coupling.**
    - Phase 1 = conception registers `next_birth_tick` directly instead of `invalidate`-ing (a conception doesn't change who's alive *now* — the child is a future Life event — so it shouldn't force a full rescan). Reasoned byte-identical; it was **not**: S1 events 3283→3242, S2 cs 96886→100242, **food_spoiled 6041→8470 (+40%)**, events 54242→56353. And no perf win (callback ~flat — cleanup 757→455 but cache 380→567 + bigger world).
    - **Root cause (the blocker):** the cache's full rescan does double duty — besides rebuilding `living[]` it **refreshes every family + community lifespan Event duration** (`PopulationCache.hpp:120-126`), and `is_community_alive` (fission, `FissionSystem.hpp:250,262`) reads those. **Marriage reparents people between families/communities without updating lifespans** (`create_marriage` has no `update_*_lifespan`), so the sim relies on a later full rescan to reconcile them. Fewer rescans → lifespans refresh at different times → fission settles/caps differently → trajectory diverges (world grows). **Same wall as the cleanup work** — the rescan's lifespan side-effect is load-bearing.
    - **Deeper wrinkle:** reparenting (`child_of`) runs inside the orchestrator's deferred scope (`defer_begin`/`defer_end`), so the new hierarchy isn't visible until season-end — which is *why* the code reconciles lifespans via a later rescan rather than at the transfer site. Eager lifespan updates at marriage would have to contend with that deferral.
    - **Conclusion:** "cheap isolated Phase 1" isn't isolated. A byte-identical incremental cache needs a **prerequisite decoupling** of lifespan maintenance (and/or making `is_community_alive` use actual living count instead of the cached Event duration) — and that's a behavior change in itself. The population-cache ↔ lifespan ↔ fission-liveness ↔ flecs-deferral knot is the real complexity; a byte-identical perf win there is disproportionately expensive. Reverted to `93401ea`; the committed region-index refactor stands as the delivered improvement. **Documented so the next attempt doesn't re-derive this.**
- Magnus3: Added a **Performance** report to the viewer's top row (beside Viewer/Generation/Ecology/Rule) — surfaces where a generation run spent its wall-clock time.
  - **Design fork resolved with Ryan:** engine-sourced breakdown (rebuild magnus3.exe) over server-only log-parsing — the engine already computes a rich per-subsystem timing breakdown (`EcologyTimings`: gather/consume/birth/marriage/fission/exogamy/…) but only *logged* it; the win was exporting that structured data rather than re-measuring or scraping stdout.
  - **C++ (`ExportCommand.hpp`):** new `ExportPerformance` struct, a `performance` field on `ExportData` (sibling to `meta`). Deliberately kept **out of the event-sourced record** — it's non-deterministic (varies run to run) and describes the generator process, not the world, so it never participates in state-at-tick derivation. `run()` now wraps each phase (`generate` / `simulation` / `collect` / `serialize`) in `chrono` timers and lifts the simulation split straight from `ecology_timings()` (+ derived `flecs_overhead` and `outside_ticks` buckets). Serialize is double-passed (measure on pass 1, embed, re-serialize) so its own figure lands in the file; write time is logged only (can't live inside its own file). `reset_ecology_timings()` at the top keeps the singleton clean.
  - **Viewer:** `data.js` passes `raw.performance` through `indexSimulation`; `index.html` gains the Performance toolbar button + a `data-tab="performance"` panel; `main.js` `renderPerformance()` draws three sections — **Phases** (bars relative to total), **Simulation breakdown** (subsystems sorted desc, friendly labels, near-zero buckets dropped, bars relative to `simulation_ms`), and **Summary** (total/entities/events/simulated-years/community-seasons). Files loaded from disk (no `performance` block) show a graceful placeholder. `timeline.css` got `.perf-*` bar-row styles; bumped the `?v=` cache-buster to 7.
  - **Verified:** rebuilt Release; `export -s42 -t920160` emits a correct `performance` block (jq-checked); the bridge server passes it through unchanged (`/api/generate` → `has("performance")` true); screenshotted the rendered report via a throwaway harness + headless Edge — Phases (Simulation dominating) and the sorted breakdown (Gathering→Exogamy→Consumption→Births→…) render cleanly with proportional bars. Harness removed.
  - **Note for larger worlds:** on a tiny 10-yr world world-generation dominates and `flecs_overhead` is the biggest sim chunk (work is trivial); on real 1000-yr runs simulation dominates and the subsystem bars (gathering/exogamy/consumption) become the story — as expected.

Thursday - 06/04/26
Log:
- Magnus3: Reviewed cohesion config questions with Ryan. Confirmed `succession_drop_fraction` (RulerConfig::Cohesion, default 0.25) IS configurable and already present in `config/rule.json` / the `/api/config` payload — it's just not surfaced in the viewer's Rule tab (only Marriage + Identity rows were ever wired with `data-path`; the whole Cohesion block was left out, plausibly because its defaults are flagged "placeholders pending an exponator sweep"). Walked the mechanic: `update_community_cohesion` (LeadershipSystem.hpp:113) applies a *multiplicative* ×(1−drop) the season a new LeadershipHistory entry is stamped (succession, `size>1` excludes the founding election), then accrues `authority_per_season` while a leader is seated; read side `cohesion_modifiers` → `gathering_bonus = cohesion·scale`. Offered to expose the Cohesion block in the Rule tab (deferred pending the leadership-refinement direction below).
- Magnus3: **Leadership-appointment design discussion — Ryan wants Rule to become a self-destabilizing system like Ecology (collapses without external management).**
  - **Current mechanic (why it's "too easy"):** `RuleSystem` runs each season; `update_community_leader` only recomputes when the cached leader has died, and `get_community_leader` deterministically picks the oldest living person in the largest living family. **Appointment is guaranteed and ~instant** — any community with ≥1 living person always yields a candidate, no eligibility gate (no min age, no viability, no cohesion requirement). So an interregnum is at most one season. The 25% succession drop is the only destabilizer and is **self-bounding**: cohesion settles at fixed point `C* = T·authority·(1−drop)/drop` (T = tenure in seasons); tenure variance drives boom-bust but there's no attractor at collapse. Rule has no analogue to Ecology's starvation→famine→extinction.
  - **The refinement (Ryan's idea, developed):** gate/delay appointment on cohesion. On its own that's a *stall*, not a *spiral* — because while leaderless, cohesion is currently **frozen** (only moves via succession-drop or accrual). A true death-spiral needs TWO new pieces: **(A) passive cohesion decay per season regardless of leadership** (the entropy governance holds back), + **(B) cohesion-gated appointment**. Then: bad succession → low cohesion → harder/slower appointment → longer vacancy → decay with no accrual → harder still → cohesion→0, permanent anarchy. **Already has teeth via the existing Rule→Ecology contract:** low cohesion → `gathering_bonus→0` → yield ceiling collapses → Ecology starves the community. Clean separation (Zeus removes the prop, Venus pulls the trigger). A future Zeus DDA directive becomes the external stabilizer — the Rule-side analogue of Fertility Restraint.
  - **Open decisions put to Ryan (awaiting his call before sketching):** (1) mechanism — **deterministic delay** (mandated vacancy `= base + k·max(0, threshold − cohesion)` seasons; recommended — no RNG, tunable, matches "delay" framing) vs **probabilistic seat** (`P=clamp(cohesion/ref)`, needs deterministic per-community RNG) vs **legitimacy accrual** (claim builds, cost rises as cohesion falls); (2) **one-way collapse** (permanent anarchy as the natural attractor, Zeus directive the only way out — recommended, mirrors Ecology) vs a **self-recovery bump** (successful appointment grants a cohesion mandate → boom-bust-with-recovery). Next step once chosen: derive the equilibrium/collapse thresholds so defaults yield a *mix* (some communities survive, some go dark), not all-or-nothing.
- Magnus3: **Big brainstorm expansion — Ryan reframed the whole Rule direction. Stepped back and dissected it. No code yet; this is the architecture being settled before building.** The pile of ideas collapses into two *separable* moves:
  - **Move 1 — make Rule structurally isomorphic to Ecology.** Cohesion becomes a *stock* with an emergent *social carrying capacity* (Ryan's "population ↑ ⇒ cohesion ↓" = a size-scaled drain, the Dunbar/Malthus analogue), and the relief valve for low cohesion is **mitosis (fission)** instead of famine. This turns today's magic-number fission (`family_size_threshold`, `max_families_per_community`, …) into *emergent* behavior. The isomorphism: food↔cohesion, carrying-capacity↔cohesion-capacity, fertility↔authority, famine↔mitosis, Venus-manages-births↔Zeus-manages-authority.
    - **Key subtlety I flagged:** mitosis is **negative feedback (self-regulating)** — splitting a big low-cohesion unit into two smaller higher-cohesion ones *relieves the pressure*, so a mitosis-only world doesn't collapse, it **fragments endlessly into small groups and never sustains large polities**. That's a different "falls apart" than last turn's runaway-collapse-to-death. Still a coherent "needs management" story (Zeus's job = enabling *bigness/stability*), but Ryan should decide: **mitosis-only (fragmentation)** vs **mitosis + a collapse threshold** (a unit too small to split but too low-cohesion to function disperses/dies via the Ecology contract). I lean two thresholds.
    - **Free upgrade once interpersonal compatibility exists:** cohesion-*aware split seams* — fission along the weakest social tie (the family least bound to the others leaves; within a family the thinnest-tie sub-clan peels off) instead of an arbitrary size cut.
  - **Move 2 — change how deities intervene: from bending *global natural laws* to applying *targeted per-entity modifiers* (blessings).** Venus "chooses specific births" rather than "dials global fertility"; Zeus "grants authority blessings to specific people" rather than setting a global authority rate. Ryan's motives: **flavor** (omniscient orchestrator picking entities) + **attribution clarity** (with a global-knob you can't tell divine intervention from natural law because the law itself moved; a per-entity tagged modifier is auditable).
    - **Assessment:** strongly on-brand — the export is already a "complete, auditable, event-sourced record," so a blessing = a recorded fact-on-entity, and the viewer could show natural-vs-divine. **Stays deterministic + single-writer** *if* a blessing is a component owned by the deity's layer and only *read* by the arbiter (arbiter still rolls; deity sets a *targeted* parameter instead of a global one). **Costs:** the deity now needs a per-season *target-selection* step, more per-entity state, and — the hidden scope — **it obsoletes the current global-knob Fertility Restraint**, so adopting it means migrating Venus too (or running two paradigms). Ryan conceded it's "same end result" for Venus in aggregate → the question is purely whether clarity+flavor is worth reworking the shipped directive. I lean: design cohesion to be *compatible* with targeted blessings, but **defer the Venus migration** so it doesn't block the core loop.
  - **Vocabulary pinned (the discussion was blurring four things):** **personality** = fixed per-person substrate (the placeholder O/C/E/A/S traits); **authority** = per-person *capacity to generate order*, resultant `f(personality, age/tenure, divine blessing)` — this is literally the "dynamic authority" RulerConfig already anticipates (+ the dormant `conscientiousness_gathering_bonus`), and it finally makes personality load-bearing; **interpersonal compatibility** = pairwise function of two personalities; **cohesion** = group-level *stock* fed by members' authority+compatibility, drained by size, consumed by the Ecology contract + the fission trigger. Authority/compatibility = inputs; cohesion = accumulator; mitosis + yield-bonus = outputs.
  - **The stack:** (1) personality→authority (per person) → (2) authority+compatibility→family cohesion→community cohesion→[kingdom] (each level +leader authority, −size penalty, +blessing) → (3) low cohesion→mitosis at that level | cohesion→Ecology yield bonus → (4) deities apply targeted per-entity modifiers, provenance recorded.
  - **Risks I raised (Ryan hadn't):** all-pairs interpersonal compatibility is **O(n²)** — must aggregate at the *family* level across a community, not person-level, or it blows the per-season budget guarded in the cache/exogamy work; **scope creep** (3 large efforts — hierarchy, per-person authority, intervention-shift — at once = unreviewable megacommit + long red-build); **replace-vs-coexist** (gate emergent fission behind a toggle like `administration.enabled` and A/B it vs threshold fission via the exponator before ripping the old path out); **determinism** of targeted selection (deterministic per-entity stream + record the choice as an event).
  - **Decisions in dependency order (put to Ryan):** (1) cohesion **stock vs resultant vs hybrid** — gates everything (I lean hybrid: stock fed by aggregated resultant flow, drained by size); (2) **mitosis-only vs mitosis+collapse** (I lean both); (3) **how many levels in v1** (I lean family+community first, interpersonal/kingdom as fast-follows); (4) **authority: accrual-rate-only vs also drives leader selection** (I lean rate-only first); (5) **intervention model now (migrate Venus) vs defer** (I lean defer the migration, keep cohesion compatible). **Strong sequencing rec:** build cohesion-stock + size-drain + mitosis loop FIRST behind a toggle, validate the world-mix with the exponator, then layer authority and targeted-blessings on a proven core. Awaiting Ryan's pick of where to start (#1+#2 vs the intervention-model #5).
- Magnus3: **Brainstorm converged — the Rule model is now settled enough to plan a build. Mitosis-as-relief-valve dropped in favor of individualized food + hunger-driven migration; disparity unified into authority.** Key settled points:
  - **Mitosis is OUT as the relief valve.** Replaced by: individualized (per-family) food distribution → famine targets the underclass → starving families migrate. Grounded in the *actual* `ConsumptionSystem` (today food is **strictly egalitarian**, `per_capita = food/pop`, whole community starves uniformly — the thing we're replacing) and the per-person `Starving`/`StarvationInterval`/`Famine`-Victim scaffolding that already exists. `Migration`/`Movement` event types already exist in the export enum (dormant).
  - **Authority = social worth = per-family food priority + leadership.** Unified — higher authority ⇒ more food allocated to that family. **Disparity is NOT a separate value** — it's *resultant/emergent* from authority (the spread of per-family authority across a community), nudged by leadership. Ryan confirmed: no separate disparity stock.
  - **Families: egalitarian internally, disparity only *between* families; they migrate together.** Rationale: a high-authority head wouldn't let his low-authority child starve — families share in famine (Ryan flagged even *reverse* intra-family distribution as a later option; egalitarian for now). Consequence: **class becomes a lineage phenomenon** (a high-authority family breeds a persistent elite). Open later: social mobility (how new families rise/fall).
  - **Cohesion refined to `legitimacy − disparity-strain`** (not a flat inverse of disparity). This makes the future priest/shaman *mechanically* load-bearing: legitimacy is what lets a society climb the disparity ladder without the underclass fleeing. So disparity + legitimacy + cohesion are one coupled system.
  - **The labor model already encodes Zeus's surplus signal (validated against `GatheringSystem`):** `yield = min(labor·eff, cap·(1+bonus))·var`. **Land-bound regime** (large community at the tract ceiling) = marginal labor produces *nothing* ⇒ free surplus labor + surplus food ⇒ specialists are affordable. **Labor-bound / famine** ⇒ every worker and ration counts ⇒ disparity directly starves the bottom. So optimal disparity *tracks the regime*: Zeus's control law = "raise disparity when land-bound with surplus, lower it when labor-bound or in famine." The "too-low" failure (perfectly egalitarian ⇒ no specialists ⇒ stuck under the Malthusian ceiling) is what makes disparity a genuine double-edged stock, not a "lower is better" knob.
  - **Venus = production (how much food); Zeus = distribution (who eats).** Same famine event — Venus sets its magnitude, Zeus sets its incidence. This is the clean, non-overlapping domain split the whole discussion was hunting for.
  - **Specialists are explicitly DEFERRED to a next phase** (Ryan: not fleshing out now, only "make them *mechanically possible*"). Their benefit (legitimacy-first vs. yield-cap/raise-the-ceiling-first vs. both sequenced) is a deferred decision. Event vocab (`Employment`/`StatusChange`/`Innovation`/`Discovery`/`Teaching`) already anticipates them, dormant.
  - **Next:** define practical, small, validate-first build steps for the *foundation* (per-person authority resultant from personality → family food-priority distribution with an egalitarian baseline → hunger-driven family migration), such that specialists become mechanically possible on top without being built. Plan under discussion.
  - **Agreed phased build plan:** **Phase A** — per-person authority substrate (resultant from personality; behavior-neutral; export + inspector so we can *see* the distribution before wiring consequences). **Phase B** — non-egalitarian food distribution: ConsumptionSystem allocates by family authority with a steepness knob whose zero point = today's egalitarian split (regression-safe); A/B via exponator to confirm an underclass emerges and carries famine mortality. **Phase C** — hunger-driven family migration (relief valve; biggest, deferred until B proven). **Phase D+** — specialists (deferred; A+B make them mechanically possible). Single-writer: authority is Zeus-owned, *read* by Venus's ConsumptionSystem in Phase B (same Rule→Ecology contract as cohesion). Ryan chose to start with **Phase A**.
- Magnus3: **Built Phase A — per-person Authority substrate (behavior-neutral). Done + verified.**
  - **What:** new `Authority{value}` component (per-person, Zeus-owned, ~[0,1]); `compute_authority(traits, RulerConfig::Authority)` in LeadershipSystem.hpp is the *single* formula — a weighted sum of Big Five around a `base=0.5` (extraversion 0.25 primary, conscientiousness 0.15, stability 0.10, openness 0.05, agreeableness 0.0), clamped [0,1]. Wired into PersonGenerator's `generate_traits`/`generate_traits_inherited` (the one choke point all three creation paths — founders, fresh births, inherited births — pass through), so every person gets it at creation. Exported as `entity.authority`; shown in the viewer inspector beside Health. Weights added to `config/rule.json` + `RulerConfig::Authority` (placeholders pending an exponator sweep).
  - **Why personality-only / computed-at-creation:** authority is *resultant* and v1 inputs (personality) are fixed at birth → it's effectively constant now, so compute once (no per-season cost). When dynamic inputs (tenure, divine blessing) arrive, the Rule layer takes over per-season recomputation. Nothing consumes authority yet — this is purely the substrate (Phase B wires it into distribution).
  - **Verified:** rebuilt Release clean. seed 42 / 50yr → 100 persons, authority min 0.18 / max 0.80 / mean 0.49, **all 100 distinct** (good spread around base, sim-born included). **Deterministic:** two runs byte-identical apart from the (intentionally non-deterministic) `performance` block. **Behavior-neutral by construction:** authority is pure arithmetic over already-generated traits — zero new RNG draws — so the SplitMix streams are untouched and the world is unchanged (food_spoiled=3978 etc. matched across runs). Finally makes the placeholder personality traits load-bearing. Committed `f3748a3`.
- Magnus3: **Built Phase B — authority-weighted (non-egalitarian) food distribution. Done + validated; chose to commit the per-season version.**
  - **What:** ConsumptionSystem now allocates community food by *family* authority via a steepness knob (`RulerConfig::Distribution::authority_steepness`, default 0.5). 0 = egalitarian (`food/pop` — the historical model); 1 = strict priority (water-fill: highest-standing families fed toward `need` first, lowest absorb the shortfall → hit zero → death roll); intermediate blends the two. **Families egalitarian internally** (one per-person ration per family, equal to all members — confirmed to Ryan); disparity is strictly *between* families, which is why they'll migrate together in Phase C. Family **standing = MAX member authority** (the most prominent member sets it; mean was tried and rejected — see below). Zeus-owned policy, read by Venus's ConsumptionSystem (Rule→Ecology contract; passed as a float param to keep ConsumptionSystem decoupled from RulerConfig). Knob added to `RulerConfig` + `rule.json`.
  - **Total consumed is independent of steepness** — Venus sets the famine's magnitude, Zeus only its incidence (the clean domain split, now mechanically true).
  - **Validation (seed 7 / 200yr; baseline captured from the Phase A binary first):**
    - **Regression: steepness 0 is BYTE-IDENTICAL** to the egalitarian baseline (same 6 famines / 132 intervals). The zero point preserves the old model exactly. Re-confirmed after the mean→max change (the aggregate is only used on the s>0 path).
    - **Mechanism active (directive OFF via `--no-admin` to surface famines — Ryan's call; famines 6→~300):** egalitarian s=0 → **0 deaths**; strict s=1 → **~410 deaths**. Same total food, concentrated to the top → the bottom families hit zero and die. Famine converts from *universal survivable starvation* into *class-stratified mortality*. Age-at-death collapses 64→~30 under strict.
  - **Honest caveat (the reason we DIDN'T chase further now):** *persistent class lineages* don't yet emerge. Bucketed by individual authority the signal is weak (strict age-at-death low-auth 29.0 vs high-auth 30.4). Three causes: (1) protection is family-level, so *individual* authority is the wrong axis; (2) family standing from *current members* fluctuates season-to-season (births/marriages/deaths reshuffle rank) → no stable dynasties; (3) aggregation compresses spread — individuals 0.18–0.78 (0.60) but family-mean only 0.42–0.55 (0.13); max shifts most families high. A survivorship confound also fooled the interval-count metric (the underclass *dies* → fewer intervals; the protected elite survive and rack up chronic-starvation intervals). **Conclusion: per-season stratification is real & correct (it IS what produces the 410 deaths, and it's exactly what Phase C migration triggers on); visible persistent class structure needs a stable/heritable family-standing stock — which is the disparity-as-stock work already parked for later.**
  - **Decision (Ryan): commit the per-season version as-is** (correct, regression-safe, unblocks Phase C), family standing = max, persistent lineages deferred. Default steepness 0.5 (sweepable via exponator). Not committed yet — pending message approval.
- Magnus3: **Design reflection — tested the Zeus/Venus architecture against esoteric (Hermetic) thought as a critique lens, not decoration.** Ryan asked whether the current approach is esoterically aligned, esp. Hermetic.
  - **Planetary correspondences hold up classically:** Jupiter/Zeus = sovereignty/law/hierarchy (Rule ✓), Venus = generation/fertility/union (Ecology ✓), Saturn = Chronos/limit/boundary (Time-Space backplane ✓), Mercury/Hermes = exchange + *magic* + the literal namesake of Hermeticism (Trade/Magic ✓, self-referential).
  - **Genuinely structural alignments (each tied to a real mechanic):** (1) **Correspondence "as above/so below"** — DDA's deity→world hierarchy + the deliberate Venus↔Zeus isomorphism (food↔cohesion, CC↔cohesion-cap, fertility↔authority, famine↔mitosis) = "same Law, different substance, every plane"; the personality→authority→family→community→kingdom stack is fractal correspondence. (2) **Gender** (Hermetic = generate vs direct, not sex) — we arrived at "Venus produces food / Zeus distributes it" on pure domain-split grounds; that *is* the Gender principle. (3) **Rhythm + Law of Neutralization** — boom-bust (cohesion drop, Malthusian swings) is Rhythm; DDA directives damping it = the adept rising above the pendulum = Wednesday's closed-loop-controller framing (same move). (4) **Polarity "opposites differ in degree"** — `authority_steepness` 0↔1 is textbook (egalitarian/stratified = one mechanism, varying degree). (5) **Cause & Effect "chance is Law not recognized"** — deterministic SplitMix + event-sourced ledger = no true chance; doctrinally on point. (6) **Mentalism** — DDA = intent above mechanism. Vibration (the tick) = real but generic/weak.
  - **Where the lens generated actionable design calls (the payoff):** (a) **tick order — I was WRONG, Ryan caught it.** I claimed Sol→Luna→Mars→Mercury→Zeus→Venus→Saturn was "purely nominal, not the weekday order." It *is* exactly the weekday order (Sun/Mon/Mars/Merc/Jove/Venus/Saturn = the seven days). And the weekday order is the Chaldean (distance/emanation) order *refracted through the heptagram*: name each day for the ruler of its 1st planetary hour, 24≡3 (mod 7) ⇒ each day steps 3 places around the Chaldean ring ⇒ the every-third step draws the heptagram and traces the week. So the tick order is genuinely esoteric (the temporal/hours order), it is NOT a competitor to the Chaldean order but the same seven points connected the other way (heptagon vs inscribed heptagram — a stock Hermetic figure unifying cosmological + temporal order), and the engineering constraint "Zeus before Venus" is just Thursday-before-Friday — the sacred order *hands us* the dependency, no conflict. Open: was the weekday order placed deliberately or did it fall out of independent layer-naming? Either way it's load-bearing, not nominal; Saturn-last = boundary/limit closing each cycle is a clean closing beat. (b) **Reconsider review item #4 (move marriage Venus→Zeus):** esoterically marriage is split — *attraction/pairing/exogamy logic = Venus*, *legitimacy/contract = Jupiter*; better factoring than wholesale relocation, and it mirrors our own production/distribution split. (c) **The blessing/theurgy turn (Move 2) is the *more* esoteric path** — global-law→targeted-recorded-blessing = mechanism→theurgy; gives an independent argument *for* it (vs deferring), since "attribution clarity" IS the natural-law/theurgy distinction restated. (d) **Deepest convergence = "one Law, seven substances":** extract a shared domain-form base class (`stock/capacity/inflow/size-drain/relief-valve/cross-domain-contract`) each planet specializes — the rare case where leaning into the esotericism makes the *code* better (one substrate, seven manifestations).
  - **Honest framing given to Ryan:** most of the fit is post-hoc resonance — the Hermetic frame fits because both it and our design describe self-regulating hierarchical feedback systems; it's trustworthy as a *heuristic* (is each god's act theurgic+recorded? does the form recur at every plane? is the controller neutralizing rhythm not fighting it?), not as authority for any number. Two recs with real teeth: **#b (split marriage by attraction-vs-legitimacy)** and **#d (shared domain-form base)** — both refactors the symbolism and the engineering independently agree on. Awaiting Ryan's pick.
- Magnus3: **External mortality abstraction + cause-of-death visibility.** Phase B made starvation the first-ever non-old-age death, but nothing recorded *why* — Ryan flagged a famine victim (dead at 23) showing only a misleading "Health" value and no cause. Observed a real victim first (seed 42, `--no-admin`): person 116 starved in **22 separate famines** over a 20-yr life, killed by the last — and the only way to recover the cause was scanning all 2519 famines' 84k intervals for one ending at his death tick. Gaps exposed: the link is implicit + expensive, ambiguous (can't tell "starved to death" from "died of old age while starving"), and `Famine.deaths` is a count, not *who*.
  - **"Health" is a confirmed placeholder** (`base_health` static genetic 0.7–1.0 set at birth; `current_health`/`age_category` never updated; nothing reads them for mortality/lifespan) → **relabeled "Constitution"** in the inspector. Real health/condition system is future work.
  - **Design (Ryan picked): a `Mortality{cause, source}` component, NOT a death event.** Death stays the Life-event's end; `Mortality` annotates it. `DeathCause` enum (OldAge implicit default; Starvation; room for Illness/Injury/Violence/Childbirth/Plague). `source` links the causal event (Famine) → victim↔famine nav. Causes are the events; death references them — event-sourced without redundant per-person death events.
  - **Chokepoint:** new `systems/Mortality.hpp::kill_person(person, tick, cause, source)` — the single path for *external* death (truncate Life event + record cause). Separates the *decision* to die (per-system) from *execution+recording* (centralised). Famine refactored to call it; future causes reuse it. Old age stays implicit.
  - **Flecs gotcha → memory:** first cut *added* `Mortality` on death → archetype churn that perturbed iteration order and silently shifted the whole world (seed-42 1000yr 2731→2759 entities). Fixed with the engine's established pattern — **always-present sentinel mutated in place** (like `Starving`): seeded `Mortality{OldAge}` on every person at birth (3 PersonGenerator sites), `kill_person` mutates via `get_mut`. Saved as memory [[feedback-flecs-sentinel-components]] (+ MEMORY.md index), per Ryan's request.
  - **Export + viewer:** export `cause_of_death` (""=alive, else old_age/starvation/…) + `death_source`; inspector "Died" row shows the cause, starvation linking to the killing famine via the existing `data-event-id` handler.
  - **Verified:** rebuilt clean; **self-deterministic** and **byte-identical to the pre-mortality baseline** (always-present sentinel → no churn → 2731 entities / 6654 events / 2519 famines / 84345 intervals all match). Cause recording end-to-end: 947 old_age / 1553 starvation / 115 alive; a victim dead at 8.4 links to Famine id 120 (deaths 4). Committed `3c47229`.
- Magnus3: **Famine visualization — who died + how food was distributed.** Designed the whole thing with Ryan (heatmap with a scrubber drilldown), built in two commits.
  - **The constraint that shaped it:** allocation is computed per season and discarded, and it's *not* reconstructable in the viewer (per-season food isn't exported, only yearly `food_history`). So we record it.
  - **Commit 1 (`f2df347`) — data + casualties.** `FamineParams.distribution`: a per-season timeline of `FamilyRation{family_id, standing, size, ration_pp}` ranked by standing, recorded in ConsumptionSystem on the authority-weighted path (steepness>0; egalitarian famines carry none — keeps the hot path clean). Exported, plus `meta.food_per_person_per_season` as the "need" line. Famine inspector gained a precise **Casualties** list (persons whose recorded `cause_of_death`/`death_source` link here) + tightened the starvation list's "died" marker to use the death link instead of the tick heuristic. **Behavior-neutral** (read-only recording → byte-identical to baseline) + self-deterministic; famine 7212 (3 seasons) shows the disparity in-data: lowest-standing family ration 1 vs 2 for the higher.
  - **Commit 2 (pending) — heatmap + scrubber** (viewer-only). `inspect.js`: a **heatmap** (rows = families ranked by standing, cols = famine seasons, cells colored by ration vs need on a green→amber→red scale, ☠ markers where deaths fell, row height ∝ family size) + a **scrubber drilldown** (click a column → that season's families as ranked ration bars with ‹ ›, exact per-season order — resolves the heatmap's stable-row approximation). Egalitarian famines show a "shared equally" note; pre-feature exports degrade gracefully. CSS `?v=8`.
  - **Time-dimension handling:** heatmap rows fixed by representative (max) standing for readability over the famine; the scrubber gives the exact per-season ranking. Families appearing/dissolving mid-famine → blank cells.
  - **Verified the viz** by screenshotting a throwaway harness (real CSS + the actual render logic, fed famine 2850 = 5 seasons / 6 families / 11 deaths via headless Edge): the amber→red waterline descends toward the low-standing rows, skulls mark the dead families, and the scrubber opens on the worst season (Year 683) with bars down to 0/3. Harness removed. Pending commit-2 message approval.
- Magnus3: **Empirical test — "with Venus's directive ON, do famines only happen during decreasing cohesion?" Answer: no — and the residual famines are still overshoot/overhang famines, not a different mechanism.** Observed (not theorized) on seed-42 1000y, directive on (`fertility_restraint`) vs `--no-admin`. New throwaway analysis scripts under `tools/` (`famine_cohesion.py`, `famine_cause.py`) join each Famine to its community's yearly `food_history` row `[tick,food,pop,resCC,fmod,natCC,cohesion]`.
  - **Cohesion direction is the wrong axis.** Cohesion accrues almost every season (succession is the only drain), so a season-scale "decreasing" test is trivially almost-never. Even on a 5-year window, **54 of 87 famines open while cohesion is *rising*, 33 while falling.** Famine timing tracks population-vs-carrying-capacity, not cohesion's sign.
  - **The trigger, read from the source:** a Famine opens in `ConsumptionSystem` when winter food `< need(3)·pop` — a pure food-vs-pop shortfall. Food = `GatheringSystem` `yield = min(pop·3.5, cap·(1+cohesion_bonus))·variance` each season, `variance ~ U[0.8,1.2]`. `resCC = cap·(1+bonus)/3`, `natCC = cap/3`; **overhang = pop − natCC** = the cohort alive only on cohesion-borrowed capacity.
  - **Classification (directive ON, 87 famines): every single one has pop > natCC; 81/87 have pop above even the cohesion-boosted resCC (ratio 1.0–1.4); zero are labor-bound.** So these ARE carrying-capacity / overshoot famines — population riding at/over the ceiling with ~zero buffer, tipped into recurring mild starvation by below-mean variance draws. They concentrate in **community 6** (lowest land, natCC 24.8) which chronically carries pop 30–43, a standing overhang of +5…+18. Nearly all are 0-death (one 7-death spell at yr 782); the directive makes them mild, not absent.
  - **The cohesion link is real but indirect and backwards from the hypothesis:** cohesion sets the resCC ceiling, and Fertility Restraint **steers fertility toward that cohesion-inflated *resultant* CC** — so as cohesion accrues the target rises, births are licensed, pop climbs to fill (and overshoot) the higher ceiling, and that pop is overhang vs the natural CC. This is exactly the Tier-2 #7 flaw from the 06/01 review ("steers toward resultant CC, manufacturing the overhang it's meant to prevent"), now confirmed in data. Succession cohesion *drops* do matter at the margin (a 25% cut converts headroom→overhang under a standing pop — visible in the falling-slope famine cluster yr 771–782, coh 0.16→0.13), but they're the minority case.
  - **Counterfactual quantifies the directive's actual job:** `--no-admin` → **2740 famines / ~2696 starvation deaths**; directive ON → **87 famines / 7 deaths** (≈31× fewer famines, ≈385× fewer deaths), entities 4727→2614. Same famine *type* in both (OFF is also ~all OVER_RESULTANT, every famine pop > natCC) — the directive changes the *rate and lethality* of overshoot famine, not its mechanism. Net: the premise ("overhang isn't the cause") is the thing to correct — overhang **is** the cause; the directive just suppresses how often/hard the population overshoots.

Friday - 06/05/26
Log:
- Magnus3: **Coherence review of cohesion + Zeus' domain (source-grounded, no code changes).** Re-read the whole Rule layer (RuleSystem orchestrator, LeadershipSystem `compute_authority`/`update_community_leader`/`update_community_cohesion`/`cohesion_modifiers`, RulerConfig, the EcologySystem CC decomposition + GatheringSystem read side, ConsumptionSystem authority-weighted path, and the CommunityGenerator/FissionSystem seeds). Findings, ranked:
  - **(1) "Authority" is overloaded into two disconnected mechanics.** Phase A's per-person `Authority` (from Big Five) feeds *distribution* (Phase B water-fill) but does NOT feed *cohesion accrual*: `update_community_cohesion` adds a flat `cohesion.authority_per_season` (0.002 constant) while a leader is seated, **ignoring who the leader is**. The comments (RulerConfig::Cohesion, CommunityCohesion) narrate "authority = a per-season rate set by the seated leader," but in code the seated leader's actual `Authority.value` has zero effect on cohesion. The obvious connecting move (deferred by design — "v1: authority is a CONSTANT"): scale `authority_per_season` by the leader's `Authority.value`, which finally makes leadership *quality* load-bearing and links the substrate to the stock. Also leaves `PersonalityModifiers.conscientiousness_gathering_bonus` dormant/dead — it should become that authority→rate connection or be removed.
  - **(2) "cohesion" is also overloaded — a pure naming collision.** `FissionSystem::compute_family_fission_threshold` builds a local `avg_cohesion = (agreeableness+stability)/2` that is unrelated to the `CommunityCohesion` stock. Worth renaming (e.g. `family_bondedness`) before it confuses the upcoming cohesion redesign.
  - **(3) Rule is still self-stabilizing, not self-destabilizing (the Thursday target).** Cohesion only moves via succession-drop (`×(1−drop)`) and constant accrual; leader appointment is guaranteed + ~instant (oldest in largest family, no eligibility gate), so vacancies are ≤1 season and cohesion never decays while leaderless. Fixed point `C* ≈ T·authority·(1−drop)/drop` (T = tenure in seasons) is an attractor *away* from collapse — Rule has no analogue to Ecology's starvation spiral. Confirms Thursday's plan: needs (A) passive per-season decay regardless of leadership + (B) cohesion-gated appointment to get a death-spiral.
  - **(4) `cohesion_to_bonus_scale = 1.0`, uncapped → unbounded yield bonus.** `gathering_bonus = cohesion · scale` with no ceiling; long tenures push the bonus well past 1.0 (>100% yield boost), inflating resultant CC far above natural CC. This is the engine behind the documented overshoot famines.
  - **(5) The setpoint positive-feedback loop (Tier-2 #7, empirically confirmed Thu 06/04).** cohesion ↑ → resultant CC ↑ → Venus's Fertility Restraint licenses births toward resultant CC → pop fills/overshoots → overhang vs natural CC → the overshoot famines. The controller steers toward the very ceiling cohesion inflated; should target a CC closer to *natural* (or a damped resultant).
  - **(6) Single-writer seam (Tier-2 #5, still open).** `CommunityCohesion` is Zeus-owned but `set<>({})`-seeded by Venus's `CommunityGenerator` (line 69) and `FissionSystem` (line 302). Benign at zero-init today; a real cross-layer write the moment founding cohesion ≠ 0 (which the authority-driven redesign will want).
  - **(7) Domain-vs-execution split for Marriage (Tier-1 #4 / Hermetic note).** Marriage config lives in RulerConfig (Zeus) but executes inside Venus's orchestrator. The refined call from Thursday: split it — attraction/pairing/exogamy = Venus, legitimacy/contract = Zeus — rather than wholesale relocation; mirrors the production/distribution split.
  - **Net read:** the *substrate* (per-person authority, cohesion-as-stock, the Rule→Ecology yield contract, the distribution knob) is coherent and in place, but the pieces aren't yet wired into one loop — authority→cohesion is the missing link, and the loop that exists is self-stabilizing + uncapped + positively-fed into Venus's setpoint. These line up exactly with the Thursday redesign (authority drives cohesion; add decay + gated appointment to destabilize; damp the setpoint), so the review is less "new bugs" than "here's the shortest path from the current coherent-but-inert substrate to the self-destabilizing Rule layer we scoped."
- Magnus3: **Cohesion-redesign direction settled with Ryan; began the build.** Ryan stated his mental model: cohesion = the ties bonding a community, accruing from leader authority and dropping at leader death; **disparity** = severity of authority differences (instability), feeding appointment speed; without a leader cohesion decays; without the yield bonus famine culls/expels population; Zeus grants authority, which both raises disparity (the eventual specialist-labor benefit) and near-certainly installs the grantee as leader. **"Appoint Leaders" = Zeus's first directive — a positive "pump," the inverse of Venus's fertility-throttle "brake."** The key framing that fell out: **authority is one lever with two opposite-signed channels** — authority→cohesion→yield (fewer famines) vs authority-spread→disparity→concentrated food (deadlier famines) — so Zeus balances order vs inequality exactly as Venus balances growth vs overshoot (brake-on-reinforcing-loop vs pump-on-balancing-loop).
  - **Decisions:** (1) **legitimacy dropped** — Ryan: granting authority ≈ granting legitimacy, so it's a doubled concept; (2) **family cohesion dropped** — its only job was the mitosis relief valve, and mitosis was already cut for migration, so it's orphaned; cohesion stays community-level; (3) appointment is **indirect (via authority grant) but near-certain**; (4) **unified appointment law** (high candidate authority overcomes low cohesion → natural recovery and the divine grant are the same mechanism); instability = disparity (not a separate stock). Proposed (awaiting confirm) that with legitimacy gone **disparity does NOT separately drain cohesion** — its teeth are deadlier-famine (existing) + slower-appointment.
  - **Build sequence agreed:** step 1 = leader authority → cohesion accrual; step 2 = decay-while-leaderless + gated/delayed appointment (the part that creates the spiral; also flips leader selection oldest-in-largest-family → highest-authority so grants can pick leaders); then the directive; then disparity down-levels + migration. Each new mechanic ships at a zero-point that recovers current behavior (byte-identical), exponator-validated before defaults are chosen — the Phase-B idiom.
  - **Built step 1.** New `RulerConfig::Cohesion::leader_authority_coupling` (default 0.0); `update_community_cohesion` now accrues `authority_per_season + coupling·(seated leader's Authority.value)` while a leader is seated. Comments + `rule.json` updated.
    - **Verified byte-identical** at the default: seed 42 / 100y (`del(.performance)`) matches the pre-change baseline exactly (built both binaries via `git stash`). **Mechanism live** at coupling 0.05: world grows 172→476 entities / 214→725 events (higher-authority leaders accrue cohesion faster → bigger yield bonus → higher CC → more population). Reverted coupling to 0.0.
  - **Ryan chose the disparity metric: max − mean** of per-family standing (standing = family's highest-Authority member). **Built step 2 — decay-while-leaderless + gated/delayed appointment.** New `RulerConfig::Appointment` block (`enabled` default false = legacy instant oldest-in-largest-family = byte-identical). When enabled: leader = **highest-Authority living person** (so an authority grant can pick the leader — the lever Zeus's future directive pulls), seated only once vacancy duration ≥ `clamp(base_seasons + k_disparity·disparity − k_cohesion·cohesion − k_authority·candidate_authority, 0, max_seasons)`; a leaderless community loses `decay_per_season` cohesion each season (legacy leaves it frozen). New helpers `get_highest_authority_person`, `compute_community_disparity` (max−mean), `append_leadership_history` (extracted, shared by both paths). Vacancy state folded into `CommunityLeader.vacancy_start` (field-add to an existing component → no archetype churn; default UINT32_MAX = filled). `update_community_leader` now takes `rcfg`; RuleSystem passes it.
    - **Verified byte-identical** at defaults (`enabled=false`): seed 42 / 100y matches the freshly-rebuilt master baseline exactly (full stash/rebuild/compare). **Deterministic when enabled:** two `enabled=true` runs byte-identical.
    - **Viewer: surfaced the appointment delays (per Ryan — "look at this first").** The export already emits per-community `leaders[]` with `end_tick = min(next start, leader death, community end)`, so a gated-appointment delay is already present in the data as a *gap* between one leader's death and the next's seating — but the community ecology chart's goldenrod leader ribbon just left gaps blank (indistinguishable from no-data). Added explicit **interregnum** rendering in `inspect.js` `_paintEcologyCanvas`: crimson hatched spans drawn in the ribbon for each between-leader gap ≥ 1 season (the threshold drops the legacy end-of-season re-election lag, which is always < a season, so the feature is invisible on legacy/`enabled=false` worlds), a `hatchSwatch` legend entry `Interregnum (N)`, and `TICKS_PER_SEASON` imported from constants. **Dropped the trailing-gap case on purpose:** a community's `end_tick` runs to its planned Life-event end, which can sit past both the last leader's death and the simulated period, so a trailing span would be an artifact, not a real vacancy — only the unambiguous in-sim between-leader gaps are drawn.
    - **Verified:** `node --input-type=module --check` clean; generated a demo world (`output/seed42_appointment.json`, seed 42 / 300y, enabled with base 10 / decay 0.02 / k_disp 20 / k_coh 1 / k_auth 1 / coupling 0.01) that produces clear interregnums — Surnholm 8, Laendale 6, Persfell 7 gaps of 5–11 seasons each; screenshotted the real ribbon+interregnum draw via a throwaway harness + headless Chrome (goldenrod tenures, crimson hatch between them, legible at 12px). Harness removed; rule.json restored to `enabled=false`. No cache-buster bump needed (JS-only change; hard-refresh to pick up). Not committed.
    - **Honest caveat — the spiral is wired but not yet shown to *bite* at this scale.** seed 42 / 100y, ~3 communities: strong params (decay 0.05, base 8, k_disparity 40, k_cohesion 2, k_authority 2) moved entities only 172→169 and starvation deaths 0→2; milder params nudged 172→190. The mechanism engages and is param-sensitive, but a genuine death-spiral regime isn't demonstrated — expected, because (a) the run is small/short, (b) `leader_authority_coupling` is still 0 so the "bad leader → low cohesion" channel is off (only vacancy-decay drives it), and (c) once a seat fills cohesion re-accrues, so it's a damped oscillation unless the gate pushes appointment past a community's survival window. **Finding the collapse regime is the exponator's job** (longer 1000y runs + sweep of decay/base/k's, ideally with coupling>0). Not committed — pending message approval. Inspector still recomputes oldest-in-largest for display (`InspectCommand.hpp:118`); should prefer the cached leader once the v2 path is the default (noted, deferred).
- Magnus3: **Committed & pushed Cohesion v2** — `b00ed6d` *Feat: Cohesion v2 — leader-authority accrual + gated appointment* (engine) + `f816a14` *Feat: Show leaderless interregnums on the community leader ribbon (viewer)* (master, origin updated).
- Magnus3: **Appointment-regime tuning exploration (no commit).** Goal: longer, *consequential* leadership gaps so Zeus's "Appoint Leaders" directive is meaningful. Mini-sweep of `RulerConfig::Appointment` (seed 42 / 300y), coupling held at 0 to isolate the gate, corrected metrics (living persons at sim-end via `authority!=null & end_tick>=sim_end`; first pass mis-measured by varying coupling + summing stale dead-community pop).
  - **Finding:** `base_seasons` is a clean linear gap-length dial (mean gap ≈ base + ~0.6 detection lag); off→base8/14/20/24 gives mean gaps 0→8.7→14.6→20.6→24.9 seasons, living-at-end 96→108→89→81→78 (~19% cost at the long end, first starvation deaths only at base 24). **No runaway** at coupling 0 — cohesion sits low (~0.3) so the `−k_coh·cohesion` term is negligible and `required≈base` (constant); the gate re-seats on schedule. Corrected my own theory: the cohesion feedback only *lengthens* gaps once cohesion is already low; with coupling>0 keeping cohesion high it actually makes appointment **instant when healthy / long when sick** — bimodal — which is the "some thrive, some go dark" mix. Recommended either a working config (base~18 + coupling~0.012 + cohesion-sensitive gate) to build the directive against, or a proper exponator sweep scoring for heterogeneity.
- Magnus3: **Cohesion-model + leader-selection redesign discussion → settled, then built the first piece.** Agreed: (1) replace "succession step-drop + decay" with **decline-only-while-leaderless** (quick re-appointment = no loss) — viable and cleaner, but it removes cohesion's only self-bound, so it must be paired with a **cohesion cap = "social carrying capacity"** (Ryan's framing; also fixes the uncapped-yield-bonus flaw). Corrected terminology: the vacancy→decay→harder-appointment spiral is **positive/reinforcing**, not negative, and only a loop if appointment difficulty references cohesion. (2) Make the appointment **delay emergent** rather than a flat `base`: a successor emerges fast when there's a *clear, strong, legitimate* heir, slow when contested — so delay ∝ number of top candidates (contestation) modulated by cohesion. (3) **Authority as a heritable "social demographic"** (the Venus parallel done right: source = inheritance + office-bump, sink = generational decay, chance valve = spontaneous step-up). Same integer rank will drive selection + food priority + Zeus's grant target — finally producing the persistent class lineages Phase B lacked.
  - **Ryan's inheritance decision:** firstborn = highest-rank parent's rank; later children = parent−1; **configurable** (`none` / `first_equal_rest_minus_one` (default) / `all_minus_one`). Personality float stays as tiebreak / step-up bias. Integers chosen for clarity (ties → contested succession). Office-bump, rank-based selection, emergent-delay, recovery = explicitly next phases.
  - **Built the authority-rank substrate (behavior-neutral, Phase-A style — nothing consumes rank yet).** `Authority` gains `int32_t rank` (field-add, no churn); `RulerConfig::Authority` gains `inheritance` + `founder_rank` (3); `compute_inherited_rank()` is the single source of the rule; threaded through `PersonGenerator` (founder leader = first family's first founder seeded at `founder_rank`, others 0; births inherit from the higher-rank parent, firstborn-keeps via a `firstborn` flag captured in `BirthSystem` before `Maternity` is overwritten); exported as `authority_rank` + shown as "Rank" in the inspector.
  - **Verified:** clean Release build; seed 42 / 100y is **byte-identical to committed HEAD after stripping the new `authority_rank` field** (full stash/rebuild/compare) — pure substrate, zero behavior change. Rank pyramid emerges as designed: rank 0=123, 1=20, 2=21, 3=8, with a persistent rank-3 noble *house* (one surname recurring at the top — the founding leader's firstborn line) and decaying cadet branches; rank is decoupled from the personality float (a rank-3 person can sit at 30% authority-value). Not committed — pending message approval.
- Magnus3: **Committed & pushed the rank substrate** — `42b1f88` *Feat: Hereditary integer authority rank + configurable inheritance (substrate)* (engine only; viewer held back for the display work).
- Magnus3: **Viewer — means to display hereditary rank / dynasties** (per Ryan, before proceeding). `inspect.js`: person inspector gains a **Rank** row; the community/family **Members** roster now sorts by rank (desc, oldest-first tiebreak) so a community's ruling house / a family's senior line surfaces at the top, each noble tagged with a gold **rank N** badge (commoners unbadged); famine **Casualties** meta gains rank. `timeline.css`: `.rank-badge` gold pill (+ cache-buster `?v=8`→`9`). **Verified:** `node --check` clean; screenshotted the real roster markup against the real CSS via headless Chrome — the Sesdeirmostril house clusters rank-3 at the top, cadet ranks 2/1 below, rank-0 commoners unbadged; dynasty is legible at a glance. Not committed — pending message approval.
- Magnus3: **Refactor — made hereditary rank the SOLE authority concept; removed the personality-derived authority float ("authority %").** Ryan: two authority concepts muddied things. Deleted `Authority.value` + `compute_authority()` + the `RulerConfig::Authority` Big-Five weight knobs (`base`/`w_*`); `Authority` is now just `{int32_t rank}`. Switched every consumer from the float to rank: leader selection (`get_highest_authority_person` → highest rank), disparity (`compute_community_disparity` → max−mean of family rank), the appointment gate's candidate term, the cohesion-accrual coupling, and **Phase-B food distribution** (family standing = max member *rank*). Export drops the float `authority`, keeps `authority_rank`; `generate_traits*` lost their now-unused `rcfg` param. Viewer: inspector row relabeled "Authority rank" (no %), famine casualties + heatmap/drilldown show `rank N` instead of `N%`.
  - **Behavior change (intended):** distribution now keys on heritable rank, not personality — so class becomes a *lineage* phenomenon mechanically. **Verified:** clean build; `node --check` clean; deterministic (two runs byte-identical). Directive-on world unchanged at seed 42/100y (172 entities — famines don't bite, so the distribution path isn't exercised). Directive-off (famines active): **122 starvation deaths stratify by rank — rank 0=71, 1=22, 2=28, rank 3=1** — the underclass carries the famine, the noble house is nearly immune. Not committed — pending message approval.
- Magnus3: **Committed & pushed** the rank-as-sole-authority refactor — `24c4f91` *Refactor: Make hereditary rank the sole authority; drop the personality float* (engine) + `56f0e98` *Feat: Display authority rank (replacing authority %) in the inspector* (viewer). Also confirmed (per Ryan's viewer observation) that leaders show rank 0 because selection is still legacy oldest-in-largest-family (`appointment.enabled=false`) — rank and leadership are decoupled until the office-bump + rank-based-selection phase wires them together.
- Magnus3: **Rank-based gated leader succession + office-bump; ripped out the `appointment.enabled` toggle.** Per Ryan: there's one Rule system with parameters (and eventually a directive) — succession shouldn't be on/off. Removed `Appointment.enabled` and the entire legacy path (deleted `get_community_leader`, the oldest-in-largest-family selection); `update_community_leader` now always seats the **highest-rank** living person, gated by the same `required = clamp(base + k_disp·disparity − k_coh·cohesion − k_auth·candidate_rank, 0, max)` (all delay params default 0 → instant rank-based succession; raise to introduce spirals). Leaderless cohesion decay is now unconditional (param-driven). **Office-bump:** taking office adds `office_bump` rank (default 1) to the new leader — the source sustaining a dynasty against inheritance's generational decay. `InspectCommand` now reads the cached `CommunityLeader` (shows "Leader: <name> (rank N)") instead of recomputing the legacy leader.
  - **Verified (seed 42 / 100y):** clean build, deterministic. **Resolves Ryan's "leaders show rank 0"** — leaders are now high-rank (12 distinct leaders: 1×rank1, 6×rank4, 5×rank5; was 24×rank0/2×rank3 under legacy). Rank spread climbs via the bump (0:121, 1:5, 2:27, 3:19, 4:11, 5:6 — founders seeded at 3, ruling lines at 4–5). Distinct leaders 26→12: leadership now concentrates in noble lineages rather than churning through the largest household. `inspect community 0` shows "Leader: … (rank 4), Cohesion 0.444 → gathering +44%". Note: office-bump makes a sustained dynasty's rank climb unbounded — a rank cap (cf. the planned cohesion cap) is a future consideration. Not committed — pending message approval.
- Magnus3: **Contested-succession model + interregnums as clickable events + default tuning (one Rule system, no toggle).** Ryan refined the office-bump and surfaced the visibility gap (couldn't see interregnums — they didn't exist at default params=0).
  - **Contestation drives everything (Ryan's model).** A *clear heir* (one living person alone at the top rank) — and the founding election — is seated **instantly**, silently, with **no rank change**. A *contested* succession (≥2 tied at the top rank) opens a gated interregnum (the `clamp(base + k_disp·disparity − k_coh·cohesion − k_auth·candidate_rank, 0, max)` delay, during which cohesion decays → spiral). Resolving a contest is a **"victory"**: recorded as an event **and** the only thing that grants `office_bump` rank. Fixes the "+1 every generation feels bad" creep — rank now grows ONLY from real contests (max rank 5→4 at seed 42/300y). Fixed a founding bug: the first election was being gated (12-season vacancy at tick 1, during which the seeded rank-3 founder could die) — founding is now instant.
  - **Interregnums are now first-class events** (`EventType::GovernanceTransition`). On a victory, `update_community_leader` records the leaderless interval + the contender pool (top-rank living people) + who prevailed, as `InterregnumParams` on the event entity (mirrors `FamineParams`). Exported (`interregnum_seasons`, `appointee_id/rank`, `candidates[]`). Determinism preserved (event entities are inert observers; verified two-run byte-identical).
  - **Default params tuned** for frequent, visible contests: base_seasons 12, decay 0.03, k_disparity 1, k_cohesion 2, k_authority 0.5 (struct + rule.json). seed 42/300y → **22 interregnums, mean 12 seasons (~3 yr), mean 3.3 contenders**, world stable. (Removed the `appointment.enabled` toggle + the legacy oldest-in-largest-family `get_community_leader` in the same arc — one Rule system, parameterized; `InspectCommand` reads the cached leader, shows "Leader: <name> (rank N)".)
  - **Viewer: interregnums are clickable.** The leader-ribbon hatched bands are now driven by the GovernanceTransition events (not gap-reconstruction) and click → the event inspector, which shows **Leaderless N seasons**, **Prevailed: <name> · rank**, and a **Contenders (N)** list (each rank + ✓ for the winner). Added a GovernanceTransition event color. **Verified** via headless-Chrome screenshots of the real markup/CSS: an 8-contender (all rank 1) 12-season contest won by Drurnnul renders correctly. Not committed — pending message approval.
  - **Added the predecessor** (per Ryan) to the interregnum event: `InterregnumParams.predecessor` = the prior leader whose death opened the contest (recovered as the last `LeadershipHistory` entry before the successor is appended), exported as `predecessor_id`; the event inspector now leads with a **Succeeded: <name> · rank** row. Verified: predecessor→appointee resolve to names (Trostcrountmans Sesdeirmostril → Loldrars Sosgrorndaestlirm, 12 seasons), determinism intact.
- Magnus3: **Review verdict — the interregnum→cohesion-crash→overhang-death chain works reliably, and is cleanest with Venus ON.** Built `tools/interregnum_cycles.py`; ran default vs INSTANT-appointment (the Zeus-directive proxy) × Venus on/off × 3 seeds, 1000y. Venus ON: DEFAULT → 66–107 interregnums (6–10/century, ~12 seasons), cohesion craters to **0.000**, 11–18 starvation deaths **~100% interregnum-attributed** (during + within 5y); INSTANT → cohesion never craters (min 0.006), **0–4 deaths**. So removing interregnums removes essentially all the deaths — the directive's effect will read as "deaths vanish." Counter-intuitive but confirmed (cf. Ryan's Venus question): Venus targets *resultant* (cohesion-inflated) CC, parking pop on cohesion-borrowed overhang and suppressing the Malthusian background, so the interregnum cull is nearly the ONLY cause of death → the contrast is clean. Venus OFF buries the signal (1800+ Malthusian deaths, only ~22% interregnum-attributed). Caveat: absolute magnitude is small (~11–18/millennium, ~3 communities) — relative effect stark, absolute trickle.
- Magnus3: **Design — "technology events" as a Mercury foreshadow; first tech = agriculture, raising `cohesion_to_bonus_scale` mid-sim.** Ryan's idea: a light, event-sourced parameter-overlay that flips partway through a run, to explore a *more dramatic* cohesion boost → bigger overhang → bigger rulership-collapse famines → more need for Zeus. Lean toward the real abstraction (not a one-off knob): a timed `Technology` (trigger=year for now; condition-triggered later, e.g. land-bound surplus) carrying parameter *effects* (start: `cohesion_to_bonus_scale ×N`), applied via a Mercury-owned world singleton (`WorldTech`) that a tiny `TechSystem` (runs before Rule, per the Mercury→Zeus tick order) flips on at the trigger, recording a Discovery event; consumers read `effective = config × tech_mult` (single-writer: Mercury writes, Rule/Ecology read). Gives a visible before/after transition in one run (modest cycles → dramatic cycles). Same family as DDA directives (both are runtime parameter overlays) — a unifying "modifier stack" may emerge later.
  - **Narrative (Ryan, logged by request — he likes it):** this is **Mercury's first appearance as a conspiracy to aid Zeus.** Agriculture (Mercury's gift) makes social cohesion far more valuable, which makes society *more* dependent on stable rule, which makes rulership collapse *more* catastrophic — manufacturing the very crisis Zeus's stabilizing directive exists to solve. Mercury's boon secretly underwrites Zeus's relevance. Diabolical, and a clean piece of pantheon politics.
- Magnus3: **Built the Mercury spine — technology events as timed parameter overlays; first tech = agriculture (cohesion-payoff ×N mid-run).** Stand-in to test dramatic dependence on stable rulership (model fidelity TBD — a real agriculture would also bundle yield/CC; revisit). New `MercuryConfig` (agriculture: enabled/year/cohesion_bonus_scale_mult), `WorldTech` singleton (Mercury-owned overlay; cohesion_bonus_scale_mult + agriculture flag), `MercurySystem` orchestrator (registered FIRST, Mercury→Zeus→Venus order; fires the discovery at the trigger year, sets WorldTech, records a `Discovery` event attached to all communities). Consumer: `cohesion_modifiers` now scales the cohesion→yield payoff by `WorldTech.cohesion_bonus_scale_mult` (single-writer: Mercury writes, Rule/Ecology read). Threaded `mercury` through SimConfig (+ `--mercury-config` flag, `config/mercury.json`). Discovery event color added to the viewer. Same family as DDA directives (runtime parameter overlays); a unifying "modifier stack" may emerge later.
  - **Verified (seed 42 / 1000y, agriculture year 300 ×4):** deterministic; one Discovery event at year 300. **Dramatic amplification, visible at the transition in a single run:** overhang max 10→49, **starvation deaths 0 (pre-300) → 207 (post-300)** (vs ~18/millennium at scale 1.0). The post-agriculture cull is **99% interregnum-attributed**. **Zeus-directive proxy (instant appointment) collapses it 207→35** despite a *larger* overhang (max 80) — proving the kills come from cohesion *volatility* at interregnums, not the overhang level. So Mercury manufactures the crisis; Zeus's directive is the visible cure — the "conspiracy to aid Zeus" works mechanically.
  - **Open decision:** agriculture currently defaults **enabled=true @ year 300** in `config/mercury.json`, so default 1000y runs show the regime shift. Fine for exploration; flag whether the shipped default should be on or opt-in. Not committed — pending message approval.
- Magnus3: **Confirmed Mercury/agriculture behavior** (seed 42, 1000y). Trigger year is honored and moves the onset: agriculture@{200,500,800} → Discovery at that year, deaths almost entirely after it (288/185/104 total — fewer the later it lands). Multiplier scales the cull monotonically: cohesion_bonus_scale_mult ×{2,4,8} → {37,207,640} deaths@year300. Disabled → 0 Discovery events, 18 deaths (the scale-1.0 baseline). Deterministic throughout. Committing.
- Magnus3: **Wired Mercury into the viewer** (per Ryan — tweak the agriculture event from the UI; flesh out the next deity). `serve.py`: loads `config/mercury.json` into `/api/config` and passes a `mercury` override on `/api/generate` via `--mercury-config` (same generic per-domain path as world/ecology/rule). `index.html`: new **Mercury** settings tab with an Agriculture fieldset — Discover agriculture (toggle), Discovery year, Cohesion payoff × — all `data-path="mercury.agriculture.*"` (the existing apply/collect JS is generic, so no main.js change). `inspect.js`: the community chart now draws a dashed-cyan **"⚙ tech"** marker at each Discovery tick, so tweaking the year visibly moves the regime shift against the cohesion/famine curves. **Verified end-to-end on a fresh server:** `/api/config` exposes mercury; a `/api/generate` override (agriculture year=120) fires the Discovery at year 120. Screenshotted the Mercury tab (renders cleanly). (Note: any already-running `serve.py` must be restarted to pick up the new config + endpoints.) Not committed — pending message approval.
Saturday - 06/06/26
Log:
- Magnus3: **Formally added Zeus's first directive — `appoint_leaders` (immediate appointment), the Rule-side DDA analogue of Venus's Fertility Restraint.** New `RulerConfig::Administration { enabled, directive }` (mirrors Venus). When active, `update_community_leader` ends every contested succession at once: a divine **random tiebreak** among the tied top-rank pool (`deterministic_select(combine_seeds(community.id, tick), …)`) + `required` forced to 0 — so interregnums collapse to nothing, cohesion never craters, and the rulership-collapse famines never fire. Directive off is unchanged (gated on `enabled`).
  - **Verified (seed 42 / 1000y, agriculture on):** directive OFF → 87 interregnums / 207 deaths; directive ON → **0 interregnums / 11 deaths** (deterministic; even better than the earlier instant-config proxy's 35 — the random immediate anointment fully prevents the craters). The 11 residual are non-interregnum (the big agriculture overhang occasionally famines on variance). OFF matches the prior 87/207 — no regression.
  - **Viewer wired:** Rule tab gains a **Rule · Administration (Zeus)** fieldset (Administer domain toggle + Appoint Leaders directive select, generic `data-path="rule.administration.*"`). Verified end-to-end through the bridge: `/api/config` exposes `rule.administration`; a rule override flips it → 0 interregnums / 11 deaths. So the whole arc is now demonstrable from the UI: Mercury (agriculture) manufactures the rule-dependence crisis, Zeus's directive cures it.
- Magnus3: Clarified/fixed the Mercury UI checkbox. The directive is already reactive (Ryan: Zeus only acts on a contested succession); "selective/targeted intervention" (only the dangerous contests) is the real next refinement, not "make it reactive." The agriculture checkbox read unchecked despite the sim running it because the live `serve.py` was stale (pre-Mercury, so `/api/config` had no `mercury` block → checkbox fell back to its HTML default); the exe reads `config/mercury.json` (enabled=true) directly, hence on-in-sim. Real fix: restart serve.py. Also added a default `checked` to the agriculture checkbox so it reads on (matching the on-by-default config) even before a config load.
- Magnus3: **Added a Stats tab to the viewer** (read-only aggregate, beside Performance). `renderStats(simData)` in main.js computes from the loaded data; six sections: **World** (years, regions, communities alive/total, families, events), **Population** (people ever, living at end, peak [summed yearly food_history], founders, born in-sim, mean age at death), **Deaths** (total + by cause with - Magnus3: **Added a Stats tab to the viewer** (read-only aggregate, beside Performance). `renderStats(simData)` in main.js computes from the loaded data; six sections: **World** (years, regions, communities alive/total, families, events), **Population** (people ever, living at end, peak [summed yearly food_history], founders, born in-sim, mean age at death), **Deaths** (total + by cause with %), **Demographics** (sex/gender/orientation tallies), **Rule (Zeus)** (authority-rank distribution, leader appointments, interregnums + mean seasons), **Events** (marriages, famines, technologies). Reuses the perf-report grid/row styling; toolbar button added at the front of the settings row. Verified on seed 42/1000y (1,415 people; 1,308 deaths = old_age 1,101 / starvation 207; ranks 0–4; 87 interregnums; 1 tech) and screenshotted the rendered tab. Not committed — pending message approval.
- Magnus3: **Directive sweep review — how each deity's directive reshapes the world** (new reusable tool `tools/directive_sweep.py`; sets the three toggles via temp configs + the `--*-config` flags, aggregates the Stats metrics across seeds). 5 seeds × 1000y, default 1-region world.
  - **Cumulative (dev order Venus→Zeus→Mercury, each layered on the last):** base (no directives) = Malthusian hell — 1679 starvation deaths, mean age at death **38**, pop churns (2583 ever / 97 alive). **+Venus (Fertility Restraint):** the famine killer — starvation 1679→**11**, mean age 38→**64**, but holds pop lower (living 97→57). **+Zeus (Appoint Leaders):** stabilizer — interregnums 78→**0**, starvation→1, and pop *recovers* (living 57→89) because stable cohesion lifts sustainable CC. **+Mercury (Agriculture):** growth engine — pop ~doubles (living 89→**216**, peak 105→271) with starvation still ~10, because Zeus already removed the crashes that would make the bigger overhang lethal. Each layer depends on the one before.
  - **Full factorial (all 8 on/off combos).** Leave-one-out from all-on (VZM, starv ~10): **drop Venus (-ZM) → starvation 10→4875** (worst world of all eight — Zeus+Mercury both inflate CC, nothing throttles fertility → massive overshoot); **drop Zeus (V-M) → starvation 10→223, interregnums 0→73** (the agriculture-without-stable-rule catastrophe — confirms the "conspiracy"); **drop Mercury (VZ-) → living 216→89** (Mercury is the optional growth multiplier, not needed to survive). **Key structural finding: the directives are not additive — they split into one BRAKE (Venus, throttles fertility) and two ACCELERATORS (Zeus stability + Mercury yield, both raise carrying capacity). Accelerators without the brake = population bombs (-ZM deadliest); all three together is the only world that is both large and stable.** Caveat: 5 seeds, 1-region (small absolute counts; directions stark).
- Magnus3: **Design vision settled — the pantheon maps to the Tree of Life's three pillars, and that's generative for the unbuilt gods.** Ryan's insight: Mercury and Mars are *both* on the Pillar of Severity — and that aligns the whole layer architecture onto the Tree (accurate sephiroth):
  - **Pillar of Mercy (force / expansion / BUILD):** Venus (Netzach) · Zeus/Jupiter (Chesed) — the accumulators; they *supply* raw flux (generation, order).
  - **Pillar of Severity (form / restriction / DIRECT):** Mars (Geburah) · Mercury (Hod) · Saturn (Binah) — they don't supply flux, they **steer, limit, discriminate** it.
  - **Middle Pillar (balance / consciousness):** Sol/Sun (Tiphareth) · Luna/Moon (Yesod) — exactly the deferred meta-layers (Player, Narrative). That's *why* they're the observer/intervener layers.
  - **Unifying principle: Mercy builds, Severity shapes, the Middle watches.** A world of only Mercy inflates and ossifies; Severity keeps the rising dough from becoming a brick. This revises the earlier brake/accelerator read — agriculture-as-pure-capacity-boost was a *Mercy-flavored placeholder*; the real Severity gods are steering wheels, not pedals.
  - **Mercury reframed — direct the change, don't stabilize it (Hod = the discriminating intellect).** Curate the change-stream: technologies/magic carry a *sign* (mitigating vs destructive), and an unmanaged Mercury lets the destructive outpace the corrective — that's its chaos. Ryan's **lightning-rod** image (love it): the discharge *must* happen (quicksilver, the bolt of sudden transmutation fed by a slowly-charging knowledge/exchange stock); Mercury's job is to **plant the rod where invention should strike**, so the force that has to ground out lands on something useful instead of damaging something valuable.
  - **Mars reframed — the spatula / the judge (Geburah = Severity/Judgment/Might).** Not formless chaos: *directed* destruction aimed at disproportion. Even the pan — cull the over-entrenched dynasty, raid the bloated, churn the frozen — so nothing burns on one side while the other stays raw. He decides *where the heat goes*, he doesn't add or remove it. His chaos is the entrenchment the Mercy gods produce (hereditary rank + accumulating cohesion → frozen aristocracy); Mars restores mobility/renewal. A third kind of necessity: **vitality** (Venus=survival, Zeus=stability/scale, Mercury=growth-direction, Mars=renewal).
  - Honest caveat (as with the prior Hermetic pass): this is *resonance*, trustworthy as a **design compass** ("what should this god DO?") because both the Tree and the engine describe directed flux in self-regulating hierarchies — not authority for any number. Supersedes the prior turn's "Mars = creative destruction" note (sharpened: *directed*, judged severance, not raw chaos).
- Magnus3: **Pre-feature-work housekeeping audit — re-scored the 06/01 pre-expansion review against the current tree (now 3 layers + 3 new viewer tabs), source-grounded.** Both trees clean (engine on `master`, desktop only the log dirty). Of the 06/01 review's 14 items, only **#1 (SimConfig)** and **#3 (region index)** shipped; the rest are open, and the review's own thesis ("each item ~3× harder per layer") is now playing out — Mercury (layer 3) + the Zeus directive + Performance/Stats/Mercury tabs all *worsened* the open items. Verified the three highest-signal ones in source rather than trusting the doc:
  - **#8 fission defer trap (the one live correctness bug) — still unfixed.** `FissionSystem.hpp` reads `get<GridPosition>` (:412) and `get_mut<FoodStores>` (:444-445) right after deferred `.set<>()`, with no `defer_suspend/resume` anywhere in the file. New fission-founded communities likely still get no plot + zero carried food. Needs empirical confirmation (per the review) before a fix.
  - **#2 tick order — worsened.** `World::register_systems` now orders **three** orchestrators (Mercury :173 / Rule :177 / Ecology :180) purely by source-line position, all `OnUpdate`. Was two at review time; Mars makes four. Fix is declarative Flecs `DependsOn` phases.
  - **#9 event IDs — worsened into a latent bug.** Now **seven** hand-partitioned static-local counters; `create_ecology_event` (EcologyHelpers, starts at 1) is shared by the *new* GovernanceTransition (LeadershipSystem) + Discovery (MercurySystem) events, and the generation-side Life events (World/WorldGenerator/PersonGenerator) also start at 1 → the two pools share low IDs. The viewer resolves clickable events by raw id (`inspect.js:274` `data-event-id`), so a governance/discovery click can collide with a Life event. (Famine cross-refs via `death_source` use the 10M range → safe.) Fix: world-scoped counter singleton.
  - **Also still open from the review:** #4 marriage-in-Venus's-loop (Hermetic refinement: split attraction=Venus / legitimacy=Zeus), #5 CommunityCohesion seeded by Venus, #6 family-graph 3-writers-no-owner, #7 setpoint targets *resultant* CC (empirically the overshoot-famine cause), #10 pairing-on-table-order, #11 food_history positional array (now 7 cols), #12 viewer per-layer copy-paste (now Mercury/Stats/Performance tabs to sync → a `LAYER_SPECS` table), #13 DIRECTIVE_DESCRIPTIONS duplicated C++→JS (now 2 directives). Plus Friday's coherence finds: the `avg_cohesion` naming collision in FissionSystem (→ `family_bondedness`) and the uncapped `cohesion_to_bonus_scale` (the "cohesion cap / social carrying capacity" idea). Tier-5 tidy-ups (broken `world_3region.json` fission block, `cap_base` 60↔40 drift, drought doc-drift, dead `process_tick` stub) remain.
  - **Recommendation put to Ryan:** Mars is the next layer, so *now* is again the cheap moment for the compounding structural seams. Proposed grouping into tracks (correctness bug #8 / structural seams #2+#9+#5+#6 / viewer-export #11-13 / Rule-coherence cleanups) — awaiting his pick of which to tackle before feature work.
- Magnus3: **Ryan picked the #8 fission-defer bug track — but stepping back from "fix it" into a full rework of movement/emigration.** Reconstructed the fission system's *intent* from source first: it's a **size-driven demographic pressure valve** — once per *decade* (autumn), the single most-overcrowded family per region (`living ≥ 11 + avg(agreeableness,stability)·5`) splits, 35–50% of members (∝ extraversion+openness) hive off to (1) the same community, (2) another community with room, or (3) a newly-founded community on an empty tract. Only Option 3 models real settlement, and **Option 3 is the broken one** (the defer trap → new community gets no plot + zero carried food).
  - **Source-grounded confirmations Ryan asked for:** spatial model is **World → Regions → Tracts → Plots** (`EcologyComponents.hpp`); a community occupies one tract; **multi-region works** in the engine (world.json just ships 1). **Yield = `min(community_pop·efficiency, tract.yield_cap·(1+cohesion_bonus))·variance`** — community-level, tract-driven, **plot-blind** (`Plot::occupant_slot` is literally "reserved for future use"; nothing statistical reads `FamilyPlot`). So **same-community family split (Option 1) is a statistical no-op**, and even the Option-3 "no plot" half of the bug is cosmetic — only "zero carried food" bites. Fission is also **region-locked** (inter-region migration unbuilt).
  - **Emerging movement model (Ryan, developed together):** *two* motivations → two mechanisms (diffusion folds into distress). A **"Crowded" status** when pop is within ~20% of CC (Venus holds at 10%/setpoint 0.9, so crowding still fires under fertility management → horizontal growth doesn't stop when vertical is throttled). Crowded → families *may* leave; over-CC/leadership-collapse (CC drops → instantly crowded) → *must* leave. **Founding is resource-gated; emigrating to an existing community is cheap and unrestricted** — and cruelly, the upper class doesn't prioritize founding, so they take the cheap existing-community slots, leaving the poor (who can't found) stuck/dead. **Mercury's domain = "Research"** (exploration/knowledge: *which* tract, *when* — routing). Naming convention pinned: letter-count = Tree-of-Life sphere number (Rule=4/Chesed, Player=6/Tiphareth, Ecology=7/Netzach, **Research=8/Hod** ✓).
- Magnus3: **Reviewed how disparity is actually coded → it forced a food-model redesign.** `ConsumptionSystem` disparity is **scarcity-only, memoryless triage**: in surplus it's always egalitarian (everyone eats `need`, leftover banks at the *community* level), in scarcity+steepness>0 it water-fills by family standing (max member rank). **Total consumed is ~steepness-invariant** — disparity changes *who dies, not how much is eaten*; the rich eat to `need` first, the poor get the dregs. Corrected Ryan's "rich hoard uneaten food" intuition (all food *is* eaten in scarcity) but his deeper point holds: **there is no per-family accumulation** → authority-gated food buys *only* famine-survival priority, nothing compounding → that's why persistent class lineages never emerged, and why the founding stake has nothing to gate on.
  - **Decision: move to per-family food stocks (food = wealth).** Disparity relocates from "access to shared pool" → **family income** (same water-fill math, now at production-time, banked per family with memory). Need-capped eating means good times look egalitarian (rich just bank more) while bad times stratify by lineage — and **spoilage caps hoarding → surplus the rich can't store becomes the motive to found.** Settled: income = size-need base + rank tilt (existing `disparity_steepness`), everyone eats (not just laborers — that's a separate future deal), dead-family granary splits evenly, **famine stays per-community grouped by family**. To ship as a **Mercury invention** ("invention of private property") toggleable mid-run like agriculture (both pooled + private paths behind a `WorldTech` flag; pooled path doubles as the future Zeus "Robin Hood" end state). Single-writer/determinism intact; also *simplifies* fission and largely evaporates the #8 defer bug.
  - **Wrote `docs/plans/2026-06-06-per-family-food-stocks.md`** (new docs/plans dir) — designs the wealth substrate; the crowding-trigger + resource-gated-founding movement feature is the next plan that consumes it. Next: implementation plan if the design holds.
- Magnus3: **Reviewed current cohesion mechanics → Ryan reframed cohesion entirely (big late-night brainstorm; digesting, not building).** Cohesion today (`update_community_cohesion`) is a leadership-banked **stock**: +0.002/season while led (flat; `leader_authority_coupling`=0 default), **×0.75 at every succession** (25% multiplicative drop, contested or not), −0.03/season while leaderless (only during contested interregnums). Its sole effect: `gathering_bonus = cohesion · scale(1.0) · WorldTech.mult`, linearly + uncapped raising the tract yield ceiling. Ryan's objection: this makes cohesion meaningless without a leader and gives a perfectly-egalitarian band zero cohesion — both wrong intuitively.
  - **The reframe — cohesion = a *resultant of relationships*, bounded by a cap (not an accumulating stock).** Egalitarian sharing → strong internal relations → high cohesion with NO leader (and it should still yield the bonus — cooperation is inherently productive). **The leader's role = raising the cap** (coordination lets a larger polity cohere → more food → the *first overhang*; lose the leader → lose the cap → CC collapse). **Disparity drains cohesion directly** (relationship damage), and this *cannot happen before authority disparity exists* (egalitarian communities starve together). **Second trap:** during a class-gated famine the "you ate while I starved" damage craters cohesion on top of any leadership crisis — a compounding collapse accelerant distinct from the overhang famine. Open tension Ryan ended on: if cohesion is resultant, how does a leader influence it? — reconciliation: leader raises the ceiling relations fill (and possibly also *dampens* disparity-strain = the useful half of the dropped "legitimacy", unnamed).
  - **The developmental-stages spine (each invention unlocks a movement regime, in dependency order):** (1) baseline Venus — gather→surplus→families grow→hit CC→Malthus, relief = spread to empty land; (2) **Fertility Restraint** — first makes *surplus* possible → the equal/fed/stable equilibrium (floated: gate its availability behind an invention, keep the directive as the control — not changing now); (3) **private property (Mercury)** — per-family wealth → surplus-funded founding + class; (4) **leadership/authority (Zeus)** — raises cohesion cap → overhang + class-gated starvation → underclass flee. Emergent gating: no fertility control → no surplus → only distress migration; class-sorted "unfounded claims" don't exist until authority (before it, who-flees is random/even).
  - **New concepts introduced (not in code):** **"areas" — 1 community per area, configurable** (replaces the arbitrary `max_communities_per_region`=3; solves "I never wanted all ~64 tracts occupied" → a peaked world is a dozen-ish communities, major+minor; needs spatial definition — fixed partition vs min-spacing). **Founded vs "unfounded"/Hail-Mary settlement** (surplus-staked reliable vs desperate under-resourced with real failure chance — the underclass's last resort; gives the deferred founding-danger a home). **Relations** — community↔community (surplus-founded child → good relations w/ parent; flight-founded → resentment) and family↔family ("I'm starving, you have food?" the big negative); good relations → raise cohesion cap. Start simple (parent↔child founding sentiment); flag O(n²) risk if family-pairwise.
  - **Verdict (mine):** more coherent than the starting point — the cohesion recast makes the whole pantheon click and subsumes the parked disparity→cohesion link + half of legitimacy. **The one decision everything hangs off: confirm cohesion-as-relationship-resultant (leader = cap-raiser).** When confirmed, this graduates the per-family-stocks doc into a broader "developmental stages / cohesion-as-relations" design doc with per-family stocks as one stage inside it. Ryan to sleep on it.

Sunday - 06/07/26
Log:
- Magnus3: **Picked the cohesion-as-relationships brainstorm back up; grounded it in the actual yield/labor math + empirical nondirected-world tests to answer Ryan's gating question — "is there a food surplus before CC/Malthus?" (because amicable surplus-funded founding only matters if surplus exists).** Source-grounded the math first (GatheringSystem, ConsumptionSystem, ecology.json), then ran the binary.
  - **The surplus law (drilled into the real constants).** `yield = min(pop·efficiency, cap·(1+bonus))·variance`; `efficiency=3.5`, `need=3`, `variance ~ U[0.8,1.2]` (mean 1.0), `cap = cap_base(40)·terrain`. **Surplus is structural because efficiency(3.5) > need(3).** Three regimes: **(1) labor-bound** (pop < `cap·(1+bonus)/efficiency` = `0.857·natCC`): per-capita surplus = `3.5·var − 3`, **mean +0.5/person/season (+17%)**, positive whenever var>0.857 (~86% of seasons) — this is the founding/growth phase, surplus is *maximal* here. **(2) land-bound-but-sub-CC** (0.857·natCC < pop < natCC): total surplus tapers linearly to zero as pop fills the tract. **(3) overshoot** (pop > `natCC = cap·mean_var/need`): mean deficit → famine; only sustainable on a cohesion-borrowed `resCC`. So the surplus window = everything below natCC; the Malthusian ceiling is exactly where surplus vanishes.
  - **Empirical (nondirected = `--no-admin`, single community `world_1com.json`, seeds 42/7/123, 80yr).** Surplus is **real but a brief transient**: stored food/person sits at **3.5–5** (vs need 3) for the first **~5–8 years**, then collapses to ~0 permanently. Two reasons it's short: **(a) founders start at ~60–80% of natCC already** (pop 10–13 vs natCC ~16.5 — there is *no* "tiny founding, slowly fill" phase in the current config; they arrive near-crowded), and **(b) unrestrained fertility (0.6/adult/yr) blows past natCC by year ~6–8**, after which pop pins at **1.2–1.5×natCC** by repeated famine-cull and food/pp bounces 0–1 forever. 80yr food_spoiled=687, 212 famines.
  - **Fertility Restraint converts the transient surplus into a permanent one (confirms the developmental-stages dependency).** Same world, Venus ON (setpoint 0.9): pop **parks at ~15 (0.9·natCC) for the whole run**, food/pp holds **3–4.5 indefinitely**, famines **212→7**, and **food_spoiled 687→5728 (8.3×)** — the now-permanent surplus has nowhere to go, so it rots. **That spoiled food is precisely the un-storable surplus the per-family-stocks doc wants to redirect into founding** (spoilage = the founding motive, now quantified). Also: shelf_life=32 days = **2 seasons**, so the pooled bank self-caps at ~1–1.5 seasons' surplus (~3–5/pp) regardless — can't hoard a big granary under the current model.
  - **The payoff for the cohesion reframe:** the two relationship-cohesion channels map cleanly onto the two regimes — **amicable surplus-funded founding (+cohesion for both)** is only accessible in the *surplus* regime, and **disparity-famine resentment ("you ate while I starved", −cohesion)** only fires in the *scarcity/overshoot* regime. **Consequence: without Venus, a community spends almost its entire life in scarcity (surplus is only the first ~6 yr) → the amicable channel is essentially unreachable and the relationship model would be resentment-dominated.** So surplus-founding-cohesion is *gated on fertility management existing first* — the developmental-stages spine, now empirically grounded. Flagged to Ryan: if he wants the gradual "populate → feel crowded" narrative, founding pop must start small vs natCC (ties to last night's configurable "areas").
  - Noted doc-drift to fix later: `ConsumptionSystem.hpp:43-44` comment says "consumption only fires in Winter" but the orchestrator runs gather/spoil/consume/births **every season** (only marriage=autumn, history=winter are season-gated).
- Magnus3: **Audited default founder generation + reproductive viability (source-grounded: world.json, WorldGenerator, FamilyGenerator, PersonGenerator, BirthSystem, MarriageSystem).** Answering Ryan's "what are the founder numbers, and can founders start non-viable?"
  - **Numbers (default `config/world.json`):** **1 region** ("Heartland"), **3 communities/region**, **3 families/community** = 9 families world-wide. Each family = **3–5 members** (`FamilyGenerator::member_count` = `3 + select(0..2)`, uniform, mean 4). So **9–15 founders/community** (mean 12 — matches the empirical year-0 pops 10–13 from the surplus test), **27–45 world-wide**. Founder ages uniform **18–45 yr** (`founding.min/max_age_years`), born pre-tick-0.
  - **No pairing is enforced.** Each founder's **sex** (Venus: F 0.49 / M 0.505 / intersex ~0.005), **gender**, and **orientation** are **independent per-person seed draws** in `generate_demographics` — nothing guarantees a fertile pair. So **yes, a founding can be non-viable.**
  - **But the viability unit is the COMMUNITY, not the family** — and the bar is low. Reproduction (`BirthSystem`) needs only: ≥1 **natal Sex::Female** aged [15,45) + ≥1 **adult natal Sex::Male** *anywhere in the community*. Marriage is **not** required (`get_spouse` husband is preferred as father, but absent that, paternity falls back to a random adult male in the community — "marriage is a factor, not the gate"); **orientation/gender gate only *marriage*, not births**; intersex gestation is deferred (v1). Births happen in the mother's family but the sire is community-scoped, so an all-male or all-female *family* is harmless as long as the community has both sexes (marriage is exogamous/cross-family anyway).
  - **So non-viability is rare at default sizes but real:** per community of n founders, P(no male)≈0.495ⁿ + P(no fertile female)≈0.51ⁿ → ~0.3% each at n=9, ~0.05% at n=12; a few-percent chance *somewhere* across many worlds. **The risk scales up sharply as founding pop shrinks** — directly relevant to the gradual-"populate→feel crowded" narrative (smaller founders vs natCC), which would want either a viability guarantee at generation or accepting occasional stillborn foundings. Offered Ryan an empirical seed-sweep to measure the actual non-viable-founding rate if he wants the real number.
