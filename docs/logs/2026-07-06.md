# 07/06/26 - 07/12/26

Week Goals:
- [ ]

Accomplished:
-

Saturday - 07/11/26
Daily Goals:
- [ ] Magnus3: pre-refactor determinism hardening (world-scoped event IDs, ordered region iteration, golden-checksum corpus)
Log:
- **Magnus3: General architecture/direction review — full-project pass (source, docs/logs history, viewer/tools), Magnus2 consulted for lineage.** Verdict: the macrostate model is proven — the directive A/Bs are the evidence (Fertility Restraint ~2,740 → 87 famines; Appoint Leaders 87 → 0 interregnums; Crowd Relief 3 → 10 communities) — and the microstate substrate largely *exists but is dormant*: persons are full entities (kinship, temperament, mortality causes, famine intervals), the event schema already defines unemitted types (Meeting/Rivalry/Estrangement/Illness/…), `RelationshipSeed`/`SkillTraining`/`SocialPosition` are schema-only, and the viewer already reconstructs per-person arcs. Microstate is an emission/curation problem, not a modeling problem. Magnus2 comparison: Magnus3 *surpasses* it on social depth (M2's entities were plants/animals only; Rule/Narrative/Player were roadmap) — the only regression is interactive surface, and the June-17 "Generation is the app shell" decision already answers that.
  - **Three pre-refactor hazards flagged** (beyond the standing 06/01 review, of which only SimConfig + region-index shipped): (1) nine process-global `static` event-ID counters feed RNG via `Life.id` (`BirthSystem` woman_seed, `ConsumptionSystem` death seed) under `unordered_map` iteration at the top of the ecology loop — the forbidden iteration-order pattern, latent per-build, pressed on by any refactor that reorders creation; (2) Venus is the executor god-object (marriage, fission, Mercury founding all run in her orchestrator; the 7-phase tick order is really 3 phases); (3) rosters never shrink — `FamilyMembers` keeps the dead, per-season scans are O(ever-born), the long-run scaling wall. Also: the three DDA directives are ad-hoc string dispatches, not yet the (intent, implementation) scaffold — factoring deliberately held until the directive model settles (gunas, below).
  - **Theogonus refactor viability:** sound on this base (7 of 9 cards are built law-forms; "off" as rates-to-zero fits the sentinel rule). One collision flagged: single-tract pantheon-sized seeding meets the 06/07 non-viable-founding stats (~0.3% at 9 founders, rising sharply as the founding pop shrinks) — with role-bearing gods sterile and Sol the sole progenitor, reproductive viability of the mortal line needs to be a decision, not a surprise.
- **Direction (Ryan): the engine generates stories about people — every individual modeled as faithfully as any other, never an NPC or a number in a mob/army.** The target: RimWorld-style emergent microstate experiences by *diegetic* means, with Crusader Kings' long-term scope but without CK's sin of abstracting the low folk; "macrostate" = the divine domain, the long term, Heaven. Discussion developed the mortal inversion from existing structures (Ryan: same page): Heaven/Earth doubles as a perspective split; Saturn is the *diegetic* storyteller (the coins are the incident stream — what RimWorld bolts on as Cassandra, the cosmology derives); the intent/receipt bus is the mortal-play protocol (a mortal player = an entity whose intents are exogenous, arbitrated like everyone else's — no special-casing; Sol runs first as the reserved slot); the soul pool gives player continuity across deaths; **"no faceless multitudes"** as a binding invariant to state before Mars (every event resolves to named participants — famines already carry per-person starvation intervals); day-grain texture via deterministic *elaboration on observation* (pure function of seed+event, one-way, never writes back; the cat-1/cat-3 dial decides what's simulated vs derived).
- **Magnus3: Hardening begun — dispatched to agents.** (1) World-scoped ID allocator singleton replacing the nine static counters (also fixes the two-Worlds-per-process counter bleed in `cmd_generate`); (2) ordered region iteration (`SpaceState.regions` unordered_map → deterministic order) + sweep for other order-sensitive unordered iteration; (3) `tools/golden/golden.py` — record/verify harness over a seeds × configs checksum matrix (baseline to be recorded *after* the C++ changes land, since IDs feed RNG and trajectories will move). Codified the **creation-time seeding exemption** to single-writer in `Magnus3/CLAUDE.md` (generator may initialize any component at creation; post-creation writes belong to the owner) — was folklore, now text.
- **Magnus3: `tools/golden/golden.py` landed — the golden-checksum record/verify harness.** Follows exponator conventions (deep-merge overlays in a tempdir, absolute `-c`/`--world-config`/`--rule-config` paths so cwd never matters, `runs/` layout). Default matrix: seeds {7, 42, 99, 1234, 31337} × 100y + seed 42 × 250y + one cell each for the `world_1com`/`world_3region` variants (8 cells); a built-in 2-cell `smoke` matrix for tool self-tests; `record` captures build info (exe mtime/size, `--version`, git HEAD + dirty flag). Verified end-to-end off a scratchpad exe copy (concurrent rebuild in flight): record → verify 2/2 PASS exit 0; tampered baseline → FAIL exit 1; matrix/baseline shape mismatch correctly reported. No baseline recorded yet — deliberately waits for the ID/iteration hardening to land, since those changes move trajectories. Repo untouched beyond the new `tools/golden/golden.py`; nothing committed.
- **Directive direction (Ryan): the directive layer is the lever for divine *behavior* — how the gods handle their levers; function over naming.** Fertility Restraint is just the simple case of the intended *moodiness* the gods subject mortals (and each other) to. The moodiness should be modeled on the **gunas — tamas / sattva / rajas** — with the *expression* of a state gaining variety as the sim gains actuators (Venus can show displeasure by letting a community starve, or by sending lions). **Luna moderates/encourages the gods' states — for the plot.** Discussion in progress; scaffold factoring waits on it.
- **Magnus3: Determinism hardening landed (agent-executed, verified).** (1) **World-scoped ID allocation:** new `IdAllocator` singleton + `next_id()` helper (`EventComponents.hpp`), replacing all nine function-local `static` counters (bases 1/100k/200k/10M) with one monotonic counter — cross-category ID collisions (a governance click resolving to a Life event) now structurally impossible, and `cmd_generate`'s second-World-continues-first's-counters bug fixed. Verified against flecs 4.0.3 that singleton `get_mut` is never deferred, so increments are safe inside `.immediate()`/defer scopes. (2) **Ordered iteration:** `SpaceState.regions` + `RegionSpace.plot_grids` `unordered_map → std::map` (the regions loop was the top-level orchestrator iteration with ID minting inside — hash order silently drove trajectories); `ExportEvent::params → std::map`, making the content checksum fully container/hash-independent. Swept the rest: remaining unordered containers are lookup/membership-only or perf-block (checksum-excluded). Verified: seed-42 100y twice → identical `953ae454ecc41d33`; seed-99 differs; 250y clean; all 485 exported event IDs distinct.
- **Magnus3: the golden harness caught a real bug on its first record — the config silent-fallback trap.** Both variant cells checksummed identical to default: a config file that *exists but fails to parse* (the 06/01-flagged stale `fission` block in `world_1com`/`world_3region`) silently fell back to the default world while the log claimed the file was loaded — every experiment ever run "with" those variants actually ran defaults. Fixed: all four domain `load()`s now **throw on parse error** with glaze caret diagnostics (missing file still → struct defaults, documented); `main.cpp` catches → `LOG_ERROR` + exit 1; removed the dead `fission` blocks from both variants (never in effect; reinstate as ecology overlays if the caps are wanted). Verified: default checksums unchanged (happy path untouched); variants now genuinely diverge (seed-7 100y: 1com → 244 entities, 3region → 856); unknown-key and BOM'd configs exit 1 with diagnostics. **First real golden baseline recorded: 8/8 verify PASS.**
- **Committed + pushed both repos (Ryan approved):** Magnus3 `f059cf8` (IdAllocator + ordered iteration), `99b4d90` (loud config parse failure + dead fission blocks dropped), `f704451` (golden harness + first baseline), `e400382` (creation-seeding exemption); magnus-docs `58ddbe6` (this log).
- **Disposition data model drafted — `docs/research/2026-07-11-directive-disposition-model.md`.** Ryan's decisions locked: moods **per-deity**; **discrete** guna states (tamas/sattva/rajas); a **gradient value** (signed charge, [-1,+1], sattva the centered band) determining when the switch flips; influenced by **both world-state and Luna**. Model: intent · disposition · repertoire factoring; `DivineDisposition {mood, charge, since_tick}` owned by the deity's own layer (sentinel component); `NarrativePressure {push}` as Luna's sole write surface (her binding constraint honored; ships defaulted-0 before Luna's writer exists); seasonal charge update = decay + per-deity `domain_signal` + Luna push; hysteresis (enter/exit thresholds + min episode length) so moods are episodes; **transitions are graded rolls within a ramp, not hard steps** (the 05/28 step-function remedy applied at birth — each shift a cat-1 divine act with a causality edge); episodes are `DispositionShift` events (Famine-spell pattern) → export/viewer/chronicles; expression table (sattva = law as written, tamas = runtime suspension distinct from config-off, rajas = escalated gain + repertoire selection); repertoire registry with guna-tagged expressions (v1 = existing knobs re-seated; lions/plague/storm as later rajas entries, named victims always); `disposition.enabled:false` pins sattva = exact current behavior (golden corpus guards the claim). Open: per-deity `domain_signal` (the character question, first to settle), rajas selection among multiple expressions, deity-Temperament linkage to thresholds, Luna's flatness metric.
- **Proceeding to implementation (Ryan): disposition lands on Venus, Zeus, Mercury, and Mars.** Two standing directives set: **(1) keep the four domain gods at structural parity going forward** — every divine mechanism lands on all four, even where a god has little content (Mars enters as scaffold for this reason); **(2) no compatibility concerns** — the project is "super, super greenfield," completely flexible, great sweeping changes welcome (per-build determinism remains the only contract). Doc's compatibility framing removed accordingly; implementation dispatched (Opus agent) with provisional domain signals marked as placeholder wiring — the per-god character pass stays open.
- **Roster compaction dispatched in parallel (Ryan) — isolated worktree.** Second Opus agent, in a git worktree off master so it can't collide with the disposition work: make per-season scans O(alive) instead of O(ever-born) (`FamilyMembers` retains the dead forever; `refresh_living_cache`/leadership scans walk full historical rosters). Constraints given: history stays reconstructible from events (4D export invariant — no silent export-semantics changes), family death + fission-threshold semantics decided deliberately, deterministic removal; verification includes before/after 1000-year timings with the EcologyTimings breakdown. Diff to be reconciled onto the disposition changes when both land.
- **Magnus3: Divine Disposition landed on all four gods (agent-executed, verified).** New `DispositionComponents.hpp` (Guna, `DivineDisposition`, `NarrativePressure`, Mercury/Mars deity tags), `DispositionConfig.hpp` (shared per-deity block), `laws/Disposition.hpp` (charge update, hysteresis + graded-ramp roll, expression + provisional signals), `MarsSystem.hpp` + `MarsConfig` + `config/mars.json` — **Mars enters as scaffold** (deity entity, tick slot before Mercury, `--mars-config`, serve.py plumbing, `Technology::War` unbuilt so validate-config seats his Heaven params; Mercury also gets his first deity *entity*). Episodes are `DispositionShift` events (open/extend/close, Famine-spell pattern; no mutable mood fields exported — 4D invariant held; initial mood implied, first event fires on first transition). Expression v1: tamas = runtime suspension of the deity's administration; rajas = gain escalation (Venus setpoint tighten, Mercury crowd_fraction lower; Zeus sattva-equivalent, no natural gain). validate-config: 160 leaves, all owned (77 Earth / 83 Heaven), disposition leaves prefix-resolved (ecology→BirthControl, rule→Leadership, mercury→Crowding, mars→War).
  - **Verified:** build clean; seed-42 100y twice → `92a59bf206f73765`; A/B disposition-off → different checksum AND 0 episodes (bites + gates); 250y no thrash; **golden re-recorded, 8/8 PASS**. Seed-42's one honest episode: **Mercury → Rajas at ~yr 14, sustained** — real unrelievable crowding in a saturated single-region world. **Zeus and Venus stay sattva because their own directives null their weather** (appoint_leaders keeps interregnums ~0; restraint keeps famine down) — the optimizer suppresses the god's mood, unprompted thesis material. Agent's design catches: domain signals filter to living communities (else Zeus miscounts extinct ones and spuriously enrages); Mercury's crowding *gauge* reads the base crowd_fraction while only the founding *action* uses the mood-modulated one (no self-referential signal lock). Open: episode *exit* coded but unobserved in this seed/config (Mercury's crowding never relieves); Mars signal = 0 (Luna-only mover, unbuilt); `NarrativePressure` writerless by design.
- **Roster compaction: completed, verified — then LOST to worktree auto-clean; re-implementation dispatched from the surviving report.** The worktree agent finished with a proven result (split roster: historical `members` + same-order-subset `living` + `last_death_tick`; **byte-identical 1000y checksum `029bc3b4b3ac9c44` → pure perf refactor; sim ~2.1× faster** — cleanup 315→37ms, cache 250→41ms, exogamy 271→81ms; remaining top costs genuinely O(alive)) but left its changes uncommitted in its worktree, whose root the harness auto-cleaned on completion; the transcript file was also empty. Nothing recoverable. Lesson memorialized (worktree agents must commit to their scratch branch before finishing). Recovery: the agent's report is a complete spec with hard byte-targets — a fresh agent is re-implementing on the disposition tree, acceptance test = reproducing the disposition checksums exactly (`92a59bf206f73765` seed-42 100y + golden verify 8/8 against the existing baseline, no re-record) since the design is behavior-preserving. Disposition changes held pre-staged in the git index so the two land as separate commits.
- **Green light (Ryan, heading to bed): once the re-implement lands and commits, proceed in parallel on the chronicle view and the Theogonus Nova refactor, judgment calls as needed.** Night-shift plan: chronicle (viewer-only, disjoint files) dispatched immediately alongside the roster re-implementation; Theogonus slice 1 queues behind the compaction commit (engine-wide, would collide), dispatched with a written slice plan in `docs/plans/`.
- **Roster compaction re-implemented, byte-perfect.** Same 7-file footprint, zero spec deviations; seed-42 100y = `92a59bf206f73765` ✓, **golden verify 8/8 against the existing baseline (no re-record)** ✓, **seed-42 1000y byte-identical before/after (`f1721a43f84d0822`)** ✓; sim **~2.2×** (1575.9 → 714.9 ms; cleanup 346→40, cache 277→43, exogamy 274→78); soak healthy (844 alive of 10,363 ever-born ≈ 12:1; families born and dying; `roster_kill` exercised by 7 external deaths). The same-order-subset property held under the disposition code without adjustment.
- **Committed + pushed Magnus3:** `175a169` Feat: Divine Disposition (guna) model on Venus/Zeus/Mercury/Mars; Mars layer scaffold (32 files) · `a4ee26e` Perf: split family rosters into historical + living, per-season scans O(alive) (~2.2× sim). Recovery worktree + branches removed; tree clean.
- **Theogonus Nova slice 1 planned + dispatched.** Plan at `docs/plans/2026-07-11-theogonus-slice-1.md`: re-partition genesis into coins 0–4 + downstream with the sent/chosen agency line; **staged mortality is the one real mechanical change** (starvation rides coin 1; senescence rides coin 2, assigned retroactively-deterministically to all living when it fires); regroup-don't-rename (config paths stable so exponator/serve/viewer survive; viewer sync deferred); defaults keep all coins on (world stays rich), new `genesis` config variant staggers the walk for per-prefix verification; single-tract default world. Deferred untouched, per Ryan's own open list: ascension/pantheon-filling (spec says not worked out; gods stay pre-created), prestige, tyranny/dynamic personality, per-family stockpiles (pre-community pooling kept as flagged approximation), Luna; agriculture ownership discrepancy (Theogonus: Venus; registry: Mercury) flagged not resolved.
- **Theogonus slice 1 landed (agent-executed, fully verified).** The 11-tech tree is now the 8-coin registry (`world_and_people` / `hunger_and_toil` / `death_and_birth` / `leadership_and_community` / `marriage_and_family` / crowding / agriculture / war) with a `TechAgency` sent/chosen axis; Bearing World retired into Hunger and Toil; Birth Control folded into Death and Birth as its lever; prereqs per spec (1⟂2, both→3; 4 needs only 0 — its curse merely manifests alongside 3). **Staged mortality live:** golden age is deathless via a finite `DEATHLESS_DURATION` sentinel (~4e9 ticks, overflow-safe, idempotently re-armable); `arm_senescence` in the genesis sequencer finalizes every living Life to a seed-derived age-aware span on a mid-run coin-2 fire (already-old founders die on a short seeded residual, no one-tick mass death). Default world now single-tract; new `world_genesis.json` + `mercury_genesis.json` stagger the walk (y0/2/4/10/12/20/30). `technologies.md`/`.csv` rewritten to the coin tree.
  - **Verified:** build + validate-config clean (154 leaves, 77 Earth / 77 Heaven); determinism ×2 on default (`838561fac0567eca`) and genesis (`7a8a33dc1777d3a9`); golden re-recorded 8/8; 1000y soak healthy (10,416 ever-born, 867 alive); genesis 250y shows the staged walk (Discoveries y2/4/10/12/20/30; first Famine y2, Pregnancy y4.4, Marriage y12.5). **Per-prefix proof:** coin 0 → 12 souls, zero events; +1 → Famine w/ 0 births/0 senescence (and a lean-tract world: 12 starvation / 0 old-age / 0 births — starvation gates independently); +2 → full demography; +3 → leaders seated; +4 → 9 marriages + rank inheritance flips (seed-7 A/B: `{0:27,1:1,2:2}` vs all-zero). Retroactive senescence airtight: all old-age end_ticks ≥ the fire tick.
  - Judgment calls: single documentary `owner` (deciding deity) + co-owner in the doc rather than a second enum; deathless as large-finite not UINT32_MAX; retroactive arming lives in the genesis sequencer and only for mid-run fires; Venus's birth-control gate implicit (rate already 0). Rough edges: pooling-with-coin-3-off approximation stands; genesis not in the golden matrix (golden.py lacks a --mercury-config hook); **viewer generation cards now bind stale schedule keys** — full viewer-sync TODO list captured in the agent report for a follow-up task; chronicle agent warned mid-flight that Generate may fail loudly with the old keys, with a route-interception workaround.
- **Chronicle view landed (agent-executed, Playwright-verified) — the mortal camera works.** New `viewer/js/chronicle.js` + `chronicle.css`; "Read the Chronicle" button in the person inspect panel; deep-linking (names inside a chronicle open that person's chronicle, timeline+inspector sync). Deterministic prose engine: entries tagged birth/life/context/death/legacy — things that happened *to* the person vs. quieter indented lines they *lived through* (community famines, contested successions, **DispositionShift god-mood turnings as era color**); mulberry32 seeded from person id picks among template variants (per-person texture, byte-stable per world); life phases derived from milestones; overlapping starvation intervals merged into "spells" with a one-line summary past two (chronic-famine communities stay readable); temperament characterization from dominant suit + spirit. Three chronicles verified in full (a starvation death at 66 after ~30 endured famines; a founding exodus bride; a leader who prevailed over 7 rivals) — no undefined/raw ids/tick leaks/impossible statements; six prose bugs found-and-fixed in critical read; 4 screenshots confirm no clipping and the main viewer intact. Hardened against the new deathless sentinel (untested against a new-build world — pre-refactor exe used for test data). Confirmed independently: **Generate is broken by the coin-key drift** (posts old `mercury.technologies.*` keys → fatal unknown_key), verified via the drag-drop path instead; viewer-sync dispatched next.
- **Viewer synced to the coins — Generate repaired (agent-executed, Playwright-verified, zero console errors).** The 11 old cards → 7 coin cards on the new schedule keys (coin 0 keeps the Frame's un-toggleable substrate rule; identity/founder-age params fold into it per `TECH_PARAM_BINDINGS`); **agency badges** (SENT gold / CHOSEN green); the formerly-disabled chosen coins are now real toggles. **Deathless-sentinel clamp** centralized in `data.js` `indexSimulation` (`end_tick > meta.ticks` → clamp + `alive_at_end` flag): no death cap on bars, "still living at world end" in inspect — verified on a death_and_birth-OFF world (HTTP 200, 0 deaths, no 43,000-year bars). Staggered walk via cards works (hunger y2 / death y4 → 4 Discoveries). **Chronicle passed its first new-build test unchanged** (sentinel guards + `DEITY_WEATHER` fallback held; Mercury→Rajas mood line renders). Bonus fix: `serve.py` `HTTPServer` → `ThreadingHTTPServer` (single-threaded bridge deadlocked under browser keep-alive). Six deity lanes render incl. Mercury + Mars.
- **Committed + pushed Magnus3 (Ryan approved):** `dd5c48b` Feat: Theogonus Nova slice 1 — coin registry, staged mortality, agency axis, genesis walk (23 files) · `0d49a12` Feat: Chronicle view (the mortal camera); generation cards synced to the coin registry (9 files).
- **Direction pivot opened (Ryan): coopt Magnus3 as a worldbuilding tool for role-playing games — develop purely in that direction for now.** His diagnosis: stuck in the weeds of the divine layer the way Magnus2 got stuck in the instantiated/earth layer — small-detail back-and-forth instead of momentum. Playing as god/mortal *mechanically in the engine* is shelved, to return later. The vision: generate a world (skim seeds), pick it as a setting; pick a time period; choose generated people as PCs and edit them in a player-creation stage (generalized as an **entity editor** usable in a creative/free mode); rewrite the world antagonistically with diegetic material (the nearby wizard made evil, made to hate the PCs' town); run adventures with Magnus worlds as the stage. Dev focus shifts from "gods automatically adjust the world state" to **"a tool for rich, interconnected, on-the-fly editable world states."** A **Luna referee/assistant-DM agent** holds all the levers to adjust world state on the fly AND suggests project changes to make the process easier/more exciting — that suggestion loop *is* the development loop (play → collect capability gaps → apply → play again), like the MTG-CLI playtest loop. Venus/Zeus/Mercury/Mars as low-activity consulting agents for their domains. An **LLM adapter for the player experience in exact parity with player options** (per MTG-CLI's decision-shape consolidation refactor), with free-form text allowed since it's an RPG (interpreted by Ryan + Luna). Perhaps a fork, with docs rewritten to the new purpose. Discussion opened before any changes.
- **Pivot settled (Ryan): all in — no fork, reframe the docs in place; the design "serves all purposes neatly."** Session 0 with Claude as Luna ASAP; but first, the minimal editor tooling to change a generated world state before and during a session ("play with the data like the toy soldiers we've made") — the first shake yields the most low-hanging fruit. One-API-two-clients confirmed as the invariant; Ryan plays DM for now and **swaps to the mortal seat once the system is built enough**; player seats get an LLM adapter for playtesting + room for a heuristic adapter.
- **Stage contract authored + wave-one dispatch (3 agents, disjoint files).** `Magnus3/docs/design/stage.md` written (by Claude, the load-bearing piece): campaign file = recipe + edit log (`world_state = resolve(export(seed, config), edits)`; export regenerated and checksum-verified — build drift detected, not absorbed); **six edit ops** (set/note/tag/link/event/create; authored events carry providence `caused_by: Sol/Luna`; authored ids namespaced `a1…`); `now_tick` = the chosen present, everything after it is **the unwritten future — Luna's notes**, excluded from player-facing projections; HTTP API (viewer's client) + `stage` CLI (Luna's client) as thin peers over the same functions; Decision shape + seat adapters per MTG-CLI's decider pattern. Dispatched: **(A)** stage package + serve.py endpoints + CLI incl. `skim` seed-blurbs (Opus); **(C)** `tools/seats/` — Decision/context/base + LLM adapter mirroring MTG-Commander's conventions + seeded heuristic + demo transcript (Sonnet); **(D)** docs reframe of README/CLAUDE.md/pantheon.md — clean reframe, no scars, theology stands as the setting's cosmology (Opus). Wave two (viewer entity editor against the API) queues behind A.
- **Docs reframed (agent D landed).** README opens on the tool's purpose + the full flow, gains "The Stage" section + Chronicle feature line; CLAUDE.md opens on the purpose + the play-loop-as-dev-loop, keeps every binding rule verbatim and gains one new binding bullet — **the stage invariant** (`world_state = resolve(export(seed, config), edit-log)`; one edit API, no private paths; engine stays a pure generator; undo = truncation); pantheon.md statuses retuned (Sol = "the DM's hand", Luna = "assistant-DM agent", Mars = scaffold), theology intact as the setting's cosmology, "unbuilt gods" → "The work ahead". Agent also fixed two stale statuses beyond its brief with justification (Mercury/Zeus → "built", matching code). Verified: no self-explanation, no pivot-scars, no claims contradicting src/.
- **Stage layer landed (agent A, fully verified).** `tools/stage/` package: `model.py` (campaign IO, strict six-op validation, undo-by-truncation, derived `effective_party()` so party changes replay pure), `resolve.py` (pure resolve + `person_facts` clipped to now_tick), `export_source.py` (exe regeneration, checksum verify — **build drift = loud StageError**, never silently absorbed), `session.py` (**one `StageSession` object both clients drive — parity is structural**), `skim.py`, `stage.py` CLI (raw `edit` + convenience verbs incl. `now`; `@file` JSON args for the PowerShell boundary); serve.py gains the nine endpoints, `/api/generate` intact. Verified: `new --seed 42` checksum = golden baseline; all six ops; `show` proves overlays + facts; undo reverses; save→open→resolve byte-identical (SHA-compared); now_tick clips facts ("Still living as of year 20", children 11→1); HTTP round-trip 200s; malformed op → CLI exit 1 / HTTP 400; `skim --seeds 1..8` produces the shelf. Notable spec-silent decisions: now_tick defaults to sim end (whole history = the past, DM lowers it to open the unwritten future); authored ids stamped at append (`a1…`, on the create edit) so the file is self-referential; disk export cache write-only by design (regeneration is the drift detector). Wave two (viewer editor) dispatched.
- **Player seats landed (agent C, verified).** `tools/seats/`: Decision/Option/Response shapes + validation; `build_pc_context` (identity, temperament-as-character-read, family, community, lived-through history — **hard-clipped to now_tick**, proven two ways: no record with the PC's id anywhere in context, own death-tick string absent from serialization; parents' pre-now deaths correctly present); `Seat` base with response validation; **LLMSeat mirroring MTG-Commander's conventions exactly** (bare `Anthropic()` env-key client, `.env` setdefault pattern, `claude-sonnet-5` decision tier, forced tool-call per decision kind so illegal responses are unrepresentable, cached system block, degrade-to-heuristic on API failure); **HeuristicSeat** seeded from (world checksum, person id, ordinal) — demo'd temperament-true (hot Swords PC picks "bold and reckless", cold Cups PC picks "cautious"); JSONL transcript. Demo ran both seats live on seed-42 PCs at now=year 90; parity asserted (Decision objects byte-identical before/after every seat call); heuristic determinism proven across separate OS processes. Export-schema findings flagged for later: `end_tick` unclamped past sim horizon; `community_id`/`current_spouse_id` are final-snapshot not time-indexed (spousal history reconstructed from Marriage events; community history simplified); children imply partners that Marriage events undercount; per-season starvation-interval duplication (context caps recent entries).
- **Viewer entity editor landed (agent, wave 2, Playwright-verified 9/9, zero console errors).** New `campaign.js` (CampaignClient/Controller — single mutation→refresh funnel preserving zoom/scroll/playhead); campaign bar in the toolbar with **DM/Luna author toggle**; writable inspector Edit section (rename/note/tag/link/party/author-event) + Annotations read-view + authored/party badges; edit-log drawer with Undo + create-entity form; authored entities get gold-dashed bars + an "Authored" layout section; authored events get a diamond mark + their own track; cyan dashed **NOW** marker; plain Generate stays read-only (edits gated to campaign mode). Exact op payloads documented — CLI/UI parity auditable. Full rehearsal verified: rename→Montbael the Grey, note/tag/link, party ★s, the Luna comet (which **appears woven into Montbael's chronicle** — "Year 17, Spring — A comet crosses the sky (Luna)"), create/undo The Leaning Tower, save→reload→open restores all 7 edits. Bug found+fixed: tickless authored entities NaN-poisoned the maxTick reduce.
  - **API gap caught by the parity invariant: now_tick isn't reachable through the one API** — the CLI's `stage now` mutates directly (a private path) and the spec-shaped `set campaign now_tick` edit is a silent no-op (`effective_party()` has no now_tick sibling). Agent correctly declined to wire a no-op button. Gap-closer dispatched: `effective_now_tick()` replay, CLI rerouted through the edit op (now undoable), viewer set-now button, save keeps base+log pure.
- **now_tick gap closed (agent, verified CLI + HTTP + viewer).** `effective_now_tick()` mirrors `effective_party()` (base value untouched on disk, edits replay at read — verified: file keeps base 9216000 while `open` reports effective 4000000); CLI `stage now` reroutes through the edit op (undoable — `show` flips a post-now death to "Still living as of year 43" and back on undo); viewer gains Set Now at the playhead through the same funnel (year 43 → 3,962,880 exactly, undo reverts); zero console errors; six-op regression green.
- **Wave complete — final integration pass:** golden verify **8/8 PASS** (engine untouched under the entire stage stack, as designed); `.gitignore` gains stage runtime artifacts (cache/.current/campaigns/_scratch). Stage v1 is whole: contract (`docs/design/stage.md`) → stage package + CLI + HTTP API → player seats (LLM/heuristic, parity-proven) → viewer entity editor (second client) → docs reframed. Session 0 is unblocked: skim, pick, play.
- **Committed + pushed (Ryan approved):** Magnus3 `ac327e0` (Stage) · `4d70a84` (player seats) · `7f1a08b` (viewer entity editor) · `7102d09` (docs reframe); magnus-docs `18acb0b` (log).
- **Flavor round (Ryan): before proceeding, methodically derive what the model needs for a stereotypical fantasy scene** — travelers of varying professions in a tavern when undead from the necromancer's evil city appear. Significant, no shortcuts; must fit diegetically; community relations, undead, combat, classes. **Luna delegated authority to answer outstanding model questions (prestige etc.) and author new design.**
- **Design authored (Luna): `docs/research/2026-07-11-tavern-scene-model.md`.** Scene decomposed into six seats: **Callings** (Mercury·Zeus chosen coin; suit→calling families Maker/Blade/Keeper/Adept; affinity weights, Zeus arbitrates); **⚖L prestige answered** — event-sourced witnessed acts, per-person + community-scoped (elsewhere it travels only as story), decays, dies with holder until Family inherits it; leader-by-prestige; renown = prestige within a calling (fantasy "level" = deeds); **⚖L groundedness answered** — earth-affinity × spirit, office erodes spirit (dynamic personality's first live driver); **Roads and Rests** (Mercury; routes derived from founding lineage + marriage webs; journeys via the dormant LocationLog; venues as scene generators — a tavern is mechanically "people from different towns co-located tonight"); **Stance** (Zeus-derived per-pair standing from kinship density vs. grievance events — explainable hostility with dates); **Mars v1** (generation-grain raids with named-only outcomes; session-grain authored resolution + small seeded resolver; domain signal = unavenged grievance + entrenchment, per his own pantheon spec); **Necromancy** — Mercury's first *signed destructive discovery* striking a person instead of the lightning rod → the necromancer (a corrupted calling, not a species); **undead = specific named dead reanimated** (soul stays in the pool; the horror is recognition), grave-raids as the grievance engine, second death = Saturn collects; **the evil city needs zero new mechanics** — necromancer-led community + prestige scoping (revered at home, monster abroad) + hostile stances = "evil" as a reputation gradient with receipts. Build order F1–F5 (callings/prestige → stance → Mars → necromancy → routes/venues); **everything stage-authorable today** with the six ops, so session 0 doesn't wait on any slice.
- **Green light (Ryan): design approved ("great design") — run the slice train F1→F5, session 0 deferred until F5 lands.** Sequential dispatch (slices share engine files); F1 (Callings + prestige) dispatched to Opus.
- **Standing commit approval granted (Ryan) for the slice train:** each slice commits as it passes verification, message pattern `Feat: F<n> - <slice name>`.
- **F2 committed (`55b1640`, pushed); F3 (Mars) dispatched** — first real grievance sources (raids), famine-desperation ignition for the raid↔hostility loop, named-only casualties, HealthState live, Keepers healing, Mars's domain signal replacing the placeholder.
- **F1 landed + committed (`e9f8c38`, pushed).** `Technology::Callings` (Zeus, chosen, prereq Leadership; 164 leaves / 9 techs, validate-config clean); `CallingType` {None,Gatherer,Maker,Blade,Keeper,Adept} always-present, assigned at coming-of-age by a pure-from-person-seed divine roll — Gatherer weight `base/(1+surplus_seasons/margin)` (specialization is what surplus buys), calling weights ∝ suit affinity; retroactive assignment at coin fire (senescence-arming pattern). `Prestige{value}` with **extensible `credit_prestige` hook** (practice 0.01/season, office 0.05/season the dominant differentiator, famine-surplus autumn credit, ×0.98/yr decay — all PROVISIONAL; agent rebalanced famine credit 0.50→0.05 when it swamped office). **Leader selection = highest_prestige (new default)**, epsilon tie-band = contested; rank kept for food distribution (seam commented); office prestige-credit replaces rank office_bump under prestige mode. Export: calling+calling_tick as permanent fact; prestige NOT exported (4D invariant — survives as `appointee_prestige` on GovernanceTransitions, making leader choices explainable). Verified: determinism ×2; coin A/B clean (off = all none, rank fallback, 80 tenures either way); distribution sane (Gatherer plurality 53%; Adept heat +0.061 vs Maker −0.173; Keeper coldest, Blade wettest); leaders 2.1–6.3 prestige vs peers 0.7–1.7; retroactive fire deterministic; 1000y soak bounded (max 9.27, mean 1.81); **golden re-recorded 8/8**; chronicle gains the "took up the craft" line. Seams left: rank/prestige unification, per-community prestige scoping (F2+), heritability (Family coin).
- **F2 landed (Community Stance).** Zeus-owned `RelationsState` singleton — a `std::map` keyed by ordered community-id pair holding directional grievance + a derived symmetric `Stance` (allied/friendly/wary/hostile), recomputed seasonally in Zeus's phase (`RelationsSystem::update_relations`, gated on the Leadership coin). Warmth = `region_baseline + lineage_weight·[parent/daughter] + kinship_weight·n/(n+halfsat) − grievance_weight·max(directions)`, thresholds→stance with a hysteresis margin; **grievance decays `forgiveness`/yr**. Kinship counts living cross-**birth**-community marriages (exogamy's product), lineage reads the founding `CausedBy` edge. **Extensible `record_grievance(world, aggressor, victim, weight, src)` hook** for F3/F4 (no generated sources yet — verified via a `rule.stance.test_grievance` build knob, defaults OFF). **First derivation seats the initial stance silently** (no shift-event storm at coin activation / colony founding); subsequent changes emit a `StanceShift` event (both communities as participants; params carry old/new + the dominant term — lineage/kinship/grievance — so the chronicle says *why*). **Feedback loop:** exogamy candidate formation gated by a deterministic per-pair stance multiplier (allied/friendly certain, wary halved, hostile≈0.02) — hostility starves kinship, which entrenches hostility (the loop Mars answers in F3). Export: StanceShift events (authoritative history); a **non-authoritative final-state `relations` snapshot** (top-level, off the community entity per the 4D invariant) for the inspect panel. Viewer: StanceShift color; community inspect "Neighbours" panel (stance per neighbor, click-to-jump); chronicle context line ("In these years Persfell and Laendale turned hostile"). Config `rule.stance.*` (19 leaves) under the Leadership coin.
  - **Verified:** build + validate-config clean (183 leaves, Leadership 32→51); determinism ×2 (seed 42 `71390635e71b44d4`, seed 99 differs); **warmth side (real data):** Surnholm→its fission daughters read *allied* via lineage (0.9), pairs with ≥2 living cross-marriages *friendly* (warmth 0.50, hysteresis exact), strangers *wary* (0.30); **grievance side (injected, stable 3-community world):** Persfell↔Laendale go friendly→**hostile** at Y30 (grievance-dominant StanceShift, warmth −0.5) → **cross-marriages starve** (baseline forms one at Y40, injected run forms none Y30–42) → **forgiveness softens hostile→wary at Y36** (grievance 0.8⁶) → recovers to friendly Y72; **coin gating:** Leadership off → 0 StanceShift, 0 relations; **1000y soak:** no crash (~1.5s), 83% friendly/wary + lineage-allied + 0 hostile (no sources), 7.2 shifts/pair/millennium (no flapping — hysteresis holds); **golden re-recorded 8/8**; viewer neighbor + chronicle contract confirmed headless. Seams left: cross-region stances, sibling-colony lineage, per-community prestige scoping — all still deferred; real grievance sources arrive with F3 (Mars raids).
- **F3 landed (Mars: raids and combat) — the loop closes.** `Technology::War` (Mars, chosen, prereq Leadership) activated: default on year 0; off = zero raid machinery + Mars's domain signal pinned to 0 (pure disposition scaffold). Generation-grain raids in `MarsSystem` (registered before Mercury/Zeus/Venus — acts on last-season stance/famine, one-tick back-edge), resolved to **named outcomes only** on Mars's own RNG stream: **reprisal** raids (grievance-holder answers, stance-gated hostile / wary+floor, modulated by Mars's mood — tamas festers, rajas answers every insult harsher) and **desperation** raids (a famished community strikes its nearest non-allied neighbor for food regardless of stance — hunger is the ignition source). Party = Blades-first + hot-tempered able adults, capped; defense = the victim's whole able militia; `combat_power = age_curve × calling_mult(Blade 2.0) × (1+heat·0.4) × current_health`; outcome = seeded roll on the strength ratio (Success / Repelled / Rout); **casualties scale with the ENGAGEMENT (min party/militia)** — party-scale, not army-scale — weighted toward the weakest; injuries cut `current_health`; food stolen on success (bounded FoodStores transfer); **grievance recorded to the victim** (feeds F2's stance loop), **prestige credited** (defenders on a repel, raiders at home on a success, ×2 for Blades) — both through F1's `credit_prestige`. **HealthState now live** (`current_health` seeded=base, wounded by raids, regenerated each season faster where **Keepers** live, who earn healing-prestige; multiplies combat power). **Mars's domain signal** replaces the 0.0 placeholder = the world's outstanding grievance (saturating, minus a peace-drift): rajas in a hot feud, tamas in a long peace. `Mortality` gains cause **Violence** (source = the Raid). New `RaidParams`+`RaidCombatant` (replaces the WarParams stub). Config `mars.raid.*`/`mars.healing.*` (37 leaves, Earth) + disposition (14, Heaven), all PROVISIONAL. Export: Raid events with full params + named roster (side/fate/prestige); `current_health` non-authoritative final-state snapshot; Violence cause. Viewer additive: Raid color, raid detail panel (named roster, outcome, spoils, strengths, prestige), community timeline strip, chronicle raid + violence-death lines. New `tools/stage/combat.py` — the session-grain table die mirroring `combat_power` + a seeded 1v1 resolver for Luna.
  - **Verified (10/10):** build + validate-config clean (220 leaves, War 51); determinism ×2 (golden record→verify **8/8 PASS**; seed 99 ≠ seed 42); **coin A/B** war-off → 0 raids, 0 Violence deaths, Mars 100% sattva (0 shifts), all current_health==base; **the loop end-to-end on real data** (world_3region seed 7): Y40 Droufell(famished) desperation-raids Ranvale→rout (raider slain) → StanceShift wary→**hostile** (grievance-dominant) → Y42 Ranvale **reprisal** back→rout, **Mars now rajas** (signal live); **named-outcomes invariant airtight** — across three 1000y soaks every Violence death resolves to a fate-2 raid combatant with a chronicle, death_source→Raid, **0 anonymous** (the one "extra" violence row is a pre-existing export double-listing, not a raid gap); **prestige** — Blade defenders earned +0.60 for repelling; **healing** — a wounded gatherer recovered 0.626→0.766(+1s)→0.976(+3s), Keeper A/B at +3s: keepers-ON fully healed vs keepers-OFF still 0.866; **frequency (1000y):** fat default 1.9/century (populations survive), scarce world_3region 5.5–8.7/century — notable events, not constant war; **combat.py** deterministic ×2, Blade beats Gatherer 65%, wounded Blade (power 0.63<0.80) beatable. Viewer browser-verified: raid detail panel renders the named roster w/ callings+fate+prestige; chronicle renders the raid death line ("killed when raiders from Larndale fell upon Urgvale"); zero console errors. Docs: technologies.md/.csv updated (War built, params, off-expression, prereq).
  - **Tuning notes (PROVISIONAL):** reprisal_base_chance 0.045/season, desperation 0.02/season, engagement-capped casualties, Mars signal gain 0.60/halfsat 3.0/drift 0.04 (feud→rajas, peace→tamas). **Seams left:** army-scale war parked (raids stay party-scale via the engagement cap); health→non-violent mortality seam left commented (a floored person is feeble, not dying); cross-region raids wait on F2's cross-region stances; Prestige/FoodStores are cross-layer writes through the shared credit_prestige/theft chokepoints (documented, not single-writer violations — Mars runs first so Venus/Zeus read fresh); combat.py constants copied from MarsConfig (drift acceptable v1, noted). **NOT committed** (awaiting approval).
- **Export double-listing fixed (the pre-existing bug F3 verification flagged).** The `entities` array double-listed persons — 119 in a `world_3region` seed-7 1000y export (the "one extra violence row" from F3 was one of these). **Observed first:** every dup is a `person`, both copies byte-identical *including `parent_id`* (the current family), so it's one person listed twice in one family's roster, not a two-collection-path overlap. **Root cause** in `perform_fission` (`FissionSystem.hpp`): the mover-set build guarded the *spouse* push against re-adds but not the *direct* push, so when both members of a couple were independently selected as movers (order P, then S) — push P → add spouse S → push S again unguarded — S landed in the new family's `FamilyMembers{movers,movers}` twice. Confirmed empirically: all 119 dups are married, 100 still share a family at sim-end. **Fix:** route both pushes through one deduping `add_mover` lambda; byte-identical mover order in every non-buggy case, dup dropped in the buggy ones. It also corrects a real side effect — the inflated `movers.size()` had over-counted the moved-food fraction transferred to the colonizing family.
  - **Verified:** rebuild + re-export → 16459 entities, 0 duplicate ids (was 16860 / 119 dups; the larger drop is the expected trajectory cascade off the food-fraction correction); determinism ×2 (`bfcb8790edb9c32b`); golden `verify` failed 7/8 (legitimate output change — only seed31337-100y never hit the case in 100y), **re-recorded → 8/8 PASS**. **NOT committed** (working tree: `FissionSystem.hpp` + re-recorded `baselines.json`; the two belong in one commit to keep build/baseline consistent).
- **Dedupe fix committed + pushed (`279c604`, Ryan's separate session; reviewed from the main session and confirmed surgical** — exactly the 2 files, none of F4's in-flight work swept in; the moved-food over-count correction is a real behavior fix beyond the cosmetic dup). One caveat noted: that baseline was recorded against a tree carrying F4's uncommitted sources; F4's own final golden record/verify on the completed tree supersedes it. F3 note corrected: the dup count was 119, not 19.
- **F4 committed (`37b4cfd`, pushed; stray serve logs untracked + ignored). F5 (Roads and Rests — routes, journeys, venues) dispatched: the last slice before session 0.** Its acceptance test is the scene itself: a `stage scene` query assembling "who is at this community tonight" — locals and named travelers of varying callings — on a necromancer world.
- **F5 landed + committed (`ab81ea3`, pushed) — THE TRAIN IS COMPLETE; session 0 unblocked.** `Technology::Roads` (Mercury, chosen, Mitigating, prereq Leadership; 263 leaves / 11 techs). **Routes** derived from F2's own inputs (lineage 0.6, kinship saturation, distance decay; open ≥0.5 AND stance≠Hostile — hostility closes roads, dated `RouteChange` events; verified: Breiford↔Froholm hostile y87 → road closed; **the necromancer's Caestead ends with every incident route closed** — the evil city isolates itself per-wronged-neighbor, no special-casing). **Journeys** = pure event overlay (one `Movement` event per journey, traveler/origin/destination + span; current location derivable at any tick — chose this over writing the dormant LocationLog, keeping every home scan intact and the 4D record clean); calling-skewed propensity verified (maker 1.54 > keeper 1.08 > adept 0.91 > blade 0.85 > gatherer 0.29 per-capita); rare by design (~2–6 concurrent travelers/season worldwide); **travelers defend the host community when raided** (`mars_defenders` includes visitors). **Venues**: taverns found at route-degree ≥2 + surplus ≥1.0 seasons/head + a living Maker keeper (deterministic names — *The Aersluld House*, *Fremholm Wayhouse*; keeper succession on death). **`stage scene <community> [--tick|--year/--season]`** assembles locals + travelers-in-residence with age/calling/home/temperament-read + overlays — Luna's table query.
  - **THE SCENE OCCURRED NATURALLY** (seed 8, year 143, tick 13,224,960): traveler Finstenfeird Mormdem of Gruford lodged at *The Roungith Hearth* in Treistead — kept by his kinsman Lirmmoum Mormdem — when a Graves raid from the evil city raised 3 of Treistead's dead that very tick; he stood in the defense ("came through unhurt") and his chronicle carries the journey, the tavern, and the fight. 19 present: 18 locals + 1 traveler, callings mixed (adepts/blades/keeper/maker/gatherers). Campaign `TavernFinale` saved.
  - Verified: determinism ×2; coin A/B (roads off = 0 routes/journeys/venues, pre-F5-shaped); 1000y soak (16,380 persons / 37 communities / 32 venues / 12,670 journeys); **golden re-recorded 8/8**; chronicle journey + alehouse-founding lines quoted; zero console errors on a full roads world. Seams: traveler labor stays home (double-presence accepted at journey rarity, self-healing vs double-kill); scene "locals" use final community_id (community-at-T replay deferred); evil-city isolation proportional to neighbors wronged; **viewer coin-card gap now spans four coins** (Callings/War/Necromancy/Roads have no settings cards — flagged as follow-up); markets/shrines deliberately unbuilt; export gained `routes` field → prior campaigns report build drift until re-stamped (TavernFinale re-stamped).
- **Go-wide round (Ryan): before session 0, broaden the content — more threats, community types, flavor** (his list: demigod divine offspring; natural beasts — lions, bears, dragons; mad wizards with towers and experiments away from society; dungeons; more divine interactivity; plus whatever Luna adds). Take our time, great effort. **And absolutely vital: the work must be extremely repeatable** — eliminate content-addition friction NOW, before sessions pile content on continuously (the decided dev cycle).
- **Design authored (Luna): `docs/research/2026-07-11-the-wide-world.md` + `Magnus3/docs/design/content-playbook.md`.** Half one (load-bearing): **three generalizations make content cheap** — **Beings** (undead generalize: any named non-person actor; new kind = registry row + control law), **Sites** (venues generalize: tavern/tower/shrine/den/**ruin**), **the signed-discovery table** (necromancy generalizes: corrupted-adept archetypes — beastcaller, plaguewright, stormcaller — each with emissions; mitigating counter-arts). Half two, the catalog seated: **beasts** as Venus's wild children (rajasic Venus finally sends the promised lions; hunts as Blade prestige; dragons apex + hoards); **dungeons = ruins with chronicles** (dead communities/towers persist as sites, beasts den, undead linger, the hoard is the dead's goods + story — zero new theology); **wizard towers** via discovery table + prestige-collapse exile; **demigods = Solblood** ⚖L (Theogonus-licensed: Sol's fertile line carries the spark — ⚠ flagged: needs Ryan's Sol-grounding decision before building, since Theogonus seeding isn't in-engine); **divine interactivity expression-side only** (omens from DispositionShifts read by Keepers, shrines as sites, blessings/curses as providence events — table-authorable already); **community archetypes** (derived labels: trade hub/shrine town/martial/frontier/evil city/ruin) + **raider cultures** as a second generated antagonist. **The pipeline (the vital half): content-playbook.md** (the distilled F-train checklist + fixed verification table every slice follows — prompts shrink to spec+playbook+deltas) and **`tools/lint_content.py`** (static cross-checks: every EventType has export name + viewer color + chronicle handler; every coin a card; kills the drift class caught four times today). Waves W0–W6, session-runnable at every point.
- **W0 dispatched (parallel, disjoint):** Beings framework + lion proof (Opus — undead migrate onto the framework; lion = Venus's rajas expression + road predation + hunts; success criterion: kind #3 = registry row + control law or the framework failed); content lint (Sonnet — expected to flag the four missing cards as proof it works).
- **Standing commit approval extended to the wave train (Ryan): `Feat: W<n> - <name>` per green slice.**
- **Blooded Lines designed (Ryan's direction — one of his original ideas for the engine, predating it):** other gods can, rarely, spawn children blessed with domain-specific unique abilities, **inheritable** — Ten Shadows / Kekkei Genkai texture; he wants the moment "the Six Eyes user is born for the first time in hundreds of years" and the world dramatically changing. **Theology revised** (logged): role-bearing-god sterility relaxed — Sol remains progenitor of the *common* line; the gods' rare children carry named bloodlines. Design doc `docs/research/2026-07-11-blooded-lines.md`: the **carrier/manifest split** is the core (carriers silent for generations, potency diluting ~0.85/gen; expression rolled at birth ∝ potency × rarity tuned to once-in-centuries; **the Manifestation is a world-visible omen-grade event**); divine conception = rare cat-1 on each god's stream, disposition-colored, providence edge carries the divine parent (the lineage/providence split doing its born job); four v1 gifts through existing hooks — Venus **the Evergreen Blood** (life aura), Zeus **the Crowned Blood** (born sovereign), Mercury **the Open Eye** (the knowing; discoveries strike near them safely — the living lightning rod), Mars **the Red Hand** (war prodigy); Family-coin breeding tension noted (bloodline dynasties = the entrenchment Mars answers); carriers hidden from PC-facing context (unwritten-future discipline), visible to DM/Luna — **carrier PCs are loaded guns for sessions**. Promoted to **W1** (waves reordered; wide-world doc updated). Open: do families know their blood (v1 no); gift depth; ascension interplay (parked); cross-line unions (stronger line wins v1).
- **Content lint landed (W0, Sonnet): `tools/lint_content.py` + `lint_allow.txt`** — stdlib, ~0.08s, exit-1-on-gap; checks every EventType for export-name/color/chronicle coverage, every coin for a settings card, schedule completeness, calling/being-kind name tables, death-cause chronicle handling, TechParams block sanity. **Proved itself in real time:** the Beings framework landed mid-task from the parallel session and the lint caught `lion` missing its viewer color immediately. Final table: 8 real GAPs (lion color — W0a will close it; pre-existing Pregnancy/Movement/RouteChange colors + Pregnancy/Discovery/Crowding/RouteChange chronicle handlers — to triage at W0 wrap: patch or allowlist-with-reason), 25 allowlisted with code-grounded reasons (Life never becomes a standalone event; 21 scaffold EventTypes verified never-constructed; substrate coin cardless by design; the `none` calling guarded out of UI). Failure detection + allowlist behavior both demonstrated. **Also visible in its table: the coin-card session's fix is in the working tree (10/11 OK)** — to be committed separately from W0 when the tree is sorted.
- **W0 landed + committed (`5ef41ef`, pushed; card session's `d9922be` confirmed committed+pushed between waves).** **Beings framework proven by the lion**: `BeingComponents/BeingHelpers/BeastsSystem`; Undead migrated onto the framework (F4 behavior re-verified post-migration: 4 necromancers / 148 undead / 47 grave-raids / release cascade intact on seed 7); **the extension contract held — kind #3 costs a registry row + control law + chronicle template + color; the lion needed ZERO kind-specific export code** (the generalized being walk carries kind/name/ticks/territory/combat/end generically; only extension data like `WasInLife` adds a branch). `Technology::Beasts` (Venus, **Sent**, prereq Hunger and Toil, 284 leaves clean); named lions sparse on wilderness tracts (aged out via their own existence event); **Venus's rajas surges the wild ×2.5 / tamas ×0.25 — her promised second instrument delivered**; predation rare-by-design (road travelers first, margins second; ~9/century in a lean 3-region world, fat worlds ≈0; a story, not a toll); hunts Blade-led with killing-blow prestige (composition verified: **year 25, the lion Trarclaw took traveler Drandfrurd on the road to Derholm; Derholm's hunt slew Trarclaw the same year, Blade Drandgirs Omjildkant +1.4 prestige for the killing blow**). Determinism ×2, coin A/B, no-faceless (0 unnamed anywhere), golden re-recorded 8/8. Hunt arbiter = Venus (the beast's life is hers), documented cross-layer seam: Venus wounds Mars-owned HealthState on a maul, same shape as Mars moving FoodStores.
  - **W0 wrap (Luna):** lint gaps triaged to zero — beasts coin card added to index.html (Venus tab, cloned from d9922be's pattern); Pregnancy/Movement/RouteChange event colors added; four chronicle heuristic misses allowlisted with code-grounded reasons (births narrated via child links; genesis discoveries world-scale with the necromancer strike narrated separately; crowding as Mercury-mood era context; route changes told through journey lines). **`lint_content.py` exit 0, fully green.** The pipeline is now real: playbook + lint + frameworks. **W1 (Blooded Lines) dispatched.**
- **Dungeon/ruin direction (Ryan): diegetic creation is the rule — "we don't really ever want to pull anything out of thin air if we can help it, the bigger the thing, more so."** A city falls → it IS the ruin, inhabited by threats that genuinely moved in, containing treasure genuinely created in the timeline; dungeon types: fallen places, mad-wizard constructions, natural caves. **Codified in the playbook as invariant #6 ("Nothing from thin air")** beside no-faceless-multitudes, with the one exemption stated: Saturn's creation is itself a diegetic act, so worldgen-seeded terrain/caves qualify. W2 design updated (wide-world doc): three dungeon kinds (fallen places incl. dead wizards' towers via a generic site lifecycle; creation-seeded hollows where beasts den first; inhabitants always *arrive*, never spawn in place) + **Works** — the honest treasure: rare named durable goods a Maker produces at a life's peak, with maker/date/place provenance, inherited/carried/left behind at the fall; dragons later gather them. Loot becomes a story hook with a maker's name, not a number.
- **Performance regression flagged (Ryan): ~5 min to export a 1000y 3-region world on the W1 in-flight build** — vs ~1.5s at F2 and ~11s at W0. Not inherent — accumulated per-season debt from six content slices (suspects: O(pairs)-per-season Relations/Roads/Mars scans, event-history rescans in necromancy grief scoring and beast targeting, per-god full-population sweeps, and whatever W1 added; also most new orchestrators lack timing zones — EcologyTimings only covers Venus). W1 agent messaged mid-flight: shrink soaks, report where time goes, fix only its OWN accidental quadratics. **Perf interlude queued as the next slice after W1 lands, before W2**: instrument every orchestrator with a timing breakdown, profile seed-7 world_3region 1000y (Tracy available via --profile), rank and rip the top bottlenecks — behavior-preserving where possible (checksum-verified, the roster-compaction pattern), greenfield where not. Target: back toward seconds.
- **Update (Ryan): the W1 agent found + fixed the bug it introduced (the 5-min spike); now ~20+s for 1000y — reasonable, but a full performance review still wanted**, with a moment of reflection on bottlenecks introduced or previously unseen; feature work may run concurrently. Plan set: when W1 commits → **perf review on the main tree** (instrument all orchestrators with EcologyTimings-style breakdowns — perf data stays in the checksum-excluded block; profile sim AND export/serialize separately, the payload has grown hugely; audit the four debt classes: per-season pair loops recomputable incrementally, history rescans needing running ledgers, full-population sweeps that should reuse PopulationCache, and the known O(families-ever) community-children seam left by the roster compaction; fix top offenders checksum-verified where cheap) **+ W2 Sites concurrently in an isolated worktree, with the commit-to-scratch-branch lesson applied**, rebased onto the perf-fixed master after. **Playbook gains a standing Perf row**: every slice reports 1000y wall+sim ms vs the previous slice; >1.5× jumps must be explained or fixed before shipping. Aim: single-digit seconds.
- **F4 landed (Necromancy and the undead) — the slice that composes F1+F2+F3.** `Technology::Necromancy` (Mercury, chosen, prereqs **Callings AND Death and Birth**; `TechSign::Destructive` — new `TechSign{Neutral,Mitigating,Destructive}` field on `TechInfo`, agriculture retagged Mitigating). Default enabled year 0, but **"on" means POSSIBLE, not present**: emergence is **grief-gated and conditional, not calendar-driven**. New `NecromancySystem` runs inside Mercury (after the tech loop, gated on the coin) in four passes: **release** (a dead necromancer frees the whole horde — the second death, all at once), **emergence** (score candidate living adult Adepts by GRIEF = kin lost to famine/violence in a recent window, recency-weighted, times renown; the most-aggrieved who crosses the threshold draws Mercury's own roll → a **Discovery(Destructive)** with the struck Adept as Primary; at most `max_concurrent`=1 living), **reanimate** (raise local recent dead on a cadence, horde-capped), **recount labor**. An **undead is a specific dead person reanimated**: standalone entity carrying `WasInLife{person}` + `Undead{necromancer, home_community, raised_tick, second_death_tick, cause}`, and **it IS its own Life-shaped unlife event** (start=raising, open until the second death truncates it — the house pattern; skipped by the export's standalone-event walk, emitted by a dedicated undead walk as type "undead", named for the body that walks). **Grave-raids** ride the existing Mars machinery: new `RaidMotive::Graves` — when local graves thin (raisable dead below `local_graves_floor`), the necromancer's community raids the nearest non-allied neighbor **with** raisable dead; on success the victim's recent dead are carried off and raised (no food stolen), and a **desecration grievance** (per grave, weighted **higher than a death**) feeds F2's stance loop. Undead **add gather-labor but don't eat** (Mercury stamps per-community `UndeadLabor{count, labor_units}`, Venus's `GatheringSystem` folds `labor_units·efficiency` into labor — consumption still counts only living, so the community out-produces the living) and **field fixed combat power** on their side of a raid (aggressor or defender); undead casualties are second deaths (Destroyed). `Necromancer{since_tick,discovery_id,raised_total}` always-present sentinel on every person (since_tick 0 = not one); `NecromancerState` world singleton (the O(1) living-necromancer handle). New files: `components/NecromancyComponents.hpp`, `systems/NecromancyHelpers.hpp`, `systems/NecromancySystem.hpp`. Config `mercury.necromancy.*` (21 leaves, Earth, ALL PROVISIONAL) under the new tech. **No alignment flag anywhere** — "evil" stays a reputation gradient (prestige scoping + grievance); grep confirms no `alignment`/`is_evil`/`morality` in src.
  - **Verified:** build + **validate-config clean** (243 leaves / 10 techs, Necromancy owns 23 = 21 params + 2 schedule). **Determinism ×2** (world_3region seed 7 → `d9e343d1537895c6` twice; seed 99 differs). **Coin A/B:** necromancy off → 0 strikes / 0 undead / 0 grave-raids; and on a **no-emergence seed (42) ON vs OFF the entire world is byte-identical** (entities/events/areas/relations all identical; the only checksum delta is the folded config flag itself) — the machinery is **truly inert until a strike**. **Emergence incidence:** natural in **3 of 8 seeds** (7, 13, 31337 at 300y world_3region; ~1-in-3 lean worlds; default single-region config also emerges one for seed 7 at 300y). **The full chain, seed 7, by name:** Adept **Dangund Jaerddrurmleigor** struck Y203 after burying 2 of his children → Discovery(Destructive) → raised **26** of the dead (each a real `WasInLife` person: Koustforcroun, Contdrast, …) → grave-raided **Urgvale** from **Rerdale** (19 grave-raids across the world, 3 graves/success, horde grows 2→16 fielded) → **desecration grievance → StanceShift Rerdale↔Urgvale wary→hostile** (dominant=grievance) at exactly the grave-raid onset → reprisals → undead fielded (fixed power) → **second deaths: 30 Destroyed (in raids) + 42 Released** (necromancer deaths) → **5 successive necromancers** over 300y (max_concurrent=1 holds; the release/re-emergence cycle). **No-faceless invariant airtight:** across seeds 7/13/31337 every undead resolves to a named `WasInLife` person entity — **0 anonymous raisings**. **Undead labor** confirmed applied (an isolated `labor_fraction=0` A/B on seed 7 — the fraction feeds no RNG, so the strike/raisings/raids are identical — shows a +5-food delta exactly when the horde peaks at depleted living pop; muted overall because the lean communities are largely **land-bound**, so the surplus horror needs a labor-bound tuning to dominate — honest seam). **1000y pop sanity:** seed 7 on ~1350 alive vs off ~1390; seed 42 on ~2318 vs off ~2272 — **no extinction, no snowball** (horde cap 24 + one-necromancer rule hold). **Golden re-recorded → 8/8 PASS.** **Leadership:** the necromancer did not become leader in seed 7 (killed fast as a defender, no vacancy opened during his short reign) — reported honestly; the prestige-selection path is wired (surplus credit + office), it just did not fire in this world.
  - **Chronicle (the deliverable's heart) — three quotes, rendered by the real `chronicle.js` against the seed-7 export (verified headless via Node on the actual ES modules):** *(a) the strike* — "After burying 2 of his children, Dangund Jaerddrurmleigor **began to dig**. The bolt of Mercury that should have struck a rod struck him instead, and he learned the raising of the dead — the first destructive art, and his own damnation." *(b) a raised person's postscript* — "17 years after she was buried, **Drothhan Nethhounddaendhens's body was raised from the grave** by Koustforcroun Nethhounddaendhens and made to walk again — the soul long gone, only the body and the name." … "At last Drothhan Nethhounddaendhens was struck down again in the fighting: **laid to rest a second time, and the wound closed.**" *(c) a defender* — "The **dead-raisers** of Rerdale came for the graves of Urgvale, and Feindgrendnoth Kalbrandtreint **stood among those who fought to keep their own dead in the ground**" (and, chillingly, that same defender is later raised by the next necromancer — the horror recurses). Necromancer chronicles also gain the raisings summary + the release line ("the 13 dead he had bound fell where they stood — the second death, all at once, and Saturn gathered them at last"). Viewer additive: undead entity color; `undeadByWasInLife`/`undeadByNecromancer` indices in data.js; grave-raid vs food-raid distinguished in the raid prose; Discovery carries `sign`. Docs: technologies.md/.csv gain coin 9 + the parameter mapping. Housekeeping: `serve.err.log`/`serve.out.log` added to `.gitignore` and deleted from the tree.
  - **Export vs 4D:** the undead's **unlife = its Life event** (start=raising, end=second death) — same "a person IS their Life event" pattern, on a non-person; emitted by a dedicated undead walk, never double-listed (the standalone-event walk skips Life). `is_necromancer`+`necromancer_since_tick` on the person and the undead's `was_in_life_id`/`necromancer_id`/`raised_tick`/`second_death_tick`/`second_death_cause` are permanent facts (bucket 1). `UndeadLabor` is not exported (a derived per-season gauge, like the caches). **Seams left:** the surplus-horror ("well-fed evil city") needs a labor-bound community to dominate (land-bound lean worlds mute it — a tuning question, mechanism correct); necromancer-as-leader is wired but demands a longer-lived necromancer + a vacancy to fire; grave-raids stay same-region (inherit F2's cross-region seam); the emergence incidence (~1-in-3) is threshold-tunable if rarer is wanted. **All config PROVISIONAL** pending an exponator sweep. **NOT committed** (awaiting approval; code repo + docs repo are two commits per convention).
- **W1 landed (Blooded Lines) — the founding vision, built.** `Technology::BloodedLines` (Saturn, **Sent**, prereq Death and Birth; config seated at `world.bloodlines.*` — Saturn's own domain, coin 0 — prefix-resolved to the coin like the `disposition` blocks; validate-config clean, 15 leaves incl. schedule). New: `components/BloodlineComponents.hpp` (`DivineBlood{line,potency,expressed,generation}` always-present birth-fixed sentinel on every person, `BloodlinesState` singleton = pending conceptions + gift magnitudes, `BloodlineGifts` POD), `config/BloodlinesConfig.hpp`, `systems/BloodlinesHelpers.hpp` (inheritance dilution, the expression roll, the gift predicates), `systems/BloodlinesSystem.hpp` (the conception orchestrator in the **Saturn slot**). **The carrier/manifest split is the spine:** divine conception is a rare per-god seasonal roll on each god's own stream (mood-coloured parent choice — a Keeper's cradle for a sattvic Venus, a Blade for a rajasic Mars, PROVISIONAL); the chosen woman's next child carries the line at potency 1.0 + a **providence edge** (`BlessedBy` the line-god — the divine-parent side, the lineage/providence split doing its born job); inheritance is max(parents)×decay (0.85), stronger line wins a cross-line union; **expression rolls once at birth on the line-god's stream, chance = potency² × rarity.** The **squared potency** was the key tuning insight — a line spreads to *every* descendant (stronger blood always wins), so carriers proliferate toward the whole population while potency dilutes; a linear chance let that growing diluted tail manifest constantly (~10/1000y). Squaring concentrates manifestations near the divine conception and silences the far end. The **four gifts, all through EXISTING hooks, active only while the bearer lives:** Evergreen (Venus) — `mars_heal_community` × heal, consumption starvation-death roll ×, `beasts_season` targeting skips the community/bearer; Crowned (Zeus) — `credit_prestige` × (magnetism), `update_community_cohesion` bonus while leading, `update_community_leader` contest-seize; Open Eye (Mercury) — `MercurySystem` schedule year-pull + `necromancy_emergence` **lightning rod** (a strike in the bearer's community grounds out as an absorbed Discovery, no Necromancer); Red Hand (Mars) — `mars_combat_power`/`beast_hunter_power` ×4, halved injury. **Export:** `DivineBlood` fields as birth-fixed person facts (4D bucket 1, like Calling), a new `Manifestation` event (line/potency/generation; child Primary, line-god Witness, community Context). Viewer: coin card in the Saturn panel, `Manifestation` saffron color, a **biggest-voice chronicle handler** (gates on `divine_expressed` — carriers stay silent). **Scoping verified airtight:** PC context (`tools/seats/context.py`) never reads the blood (left untouched; a carrier's context greps clean of every blood term); the chronicle narrates only the expressed (a carrier is never a Manifestation Primary); DM/Luna `person_facts`/`scene_facts` DO show the latent line ("Carries the Crowned Blood, latent" vs "MANIFEST — bears the Crowned Blood").
  - **A perf bug I introduced, found + fixed (the 5-min spike):** `crowned_cohesion_per_season` was seeded at **5.0** — but normal cohesion accrues at `authority_per_season`=0.002/season, so +5 is ~2500× base. A Crowned leader drove cohesion to 900+ → yield → carrying capacity → **population explosion** (one community 24→1638 people in 65y), tanking the sim (and it was the cause of the slow seeds, not a pre-existing regression as feared). Fixed to **0.01** (~5× base): 1000y×3region seed 42 went **5 min → 21 s**, the previously-SLOW seed 23 **>40 s → 5.5 s**. The other W1 scan I added (Open Eye's world-wide living-bearer scan, once per season) is now gated to run only while genesis techs remain unfired (a no-op in the default all-on schedule). The remaining per-season additions (`community_has_living_bearer` for Evergreen, coin-gated) are O(living), not quadratic; coin-off ≈ coin-on at 200y.
  - **Verified:** build + validate-config clean; `lint_content.py` **exit 0**; **determinism ×2** (seed 42 500y → `ebe990940a730038` twice, seed 7 differs); **coin A/B** (off → 0 carriers / 0 manifestations, machinery inert; checksums differ); **no-faceless** (every Manifestation participant resolves); **golden re-recorded 8/8 PASS**; **soak** 8 seeds × 1000y no crash. **Incidence** (8×1000y world_3region, `expression_rarity=0.004`): **17 manifestations / 8000 seed-years = ~2.1 per 1000y, range 1–4, median 2** — "once in centuries"; 53 conceptions (~6.6/1000y), all four lines appearing. Tuning trail: linear × 0.01 → ~10/1000y (the diluted-tail problem) → **potency² × 0.006** → ~2/1000y → **× 0.004** to centre the band (kept 0.004 over 0.003 so manifestations stay reliably present for play). **THE MOMENT (seed 23, 1000y):** the Crowned line began with **Bindtrint Murnfrarn** (Zeus's own child by a mortal mother, potency 1.0) and passed **silent through seven generations of carriers** (Reithseimmoust 0.850 → Hustkaestjaens 0.723 → Honsbrardhourd 0.614 → Fraersceld 0.522 → Sinfrul 0.444 → Fudournmoth 0.377, all `expressed=false`, the world never knowing) until year 239, when **Rersgrestnaeld Caecrocreintael** (gen-7, potency 0.321) woke the Crowned Blood — chronicle: *"In the 239th year of the world, for the first time in an age, the Crowned Blood was born sovereign in a child of Draefell: Rersgrestnaeld Caecrocreintael, 7th-generation of a line the world never knew it carried."* He took Draefell's seat (magnetism) and held it y247–296, making it **the most cohesive community in a region of 31** (mean 1.82 / peak 2.97 vs ~0.9 for its neighbours) — one person bending a macro trajectory. Carrier-silence confirmed on his father Fudournmoth (gen-6 carrier): PC context clean, chronicle silent, DM view shows the latent line. **Seams (honest):** the Open Eye's early-tech gift is INERT under the default all-on schedule (nothing left to pull early — only visible under a staggered genesis config); the **lightning-rod** and a Red-Hand-in-battle need a rare natural co-occurrence (a grief-broken Adept, or an active feud, in a bearer's community) that an 8-seed sweep didn't surface — verified by code path, not live data. **Perf note:** a full breakdown wants EcologyTimings-style zones in the new orchestrators (deferred to the queued perf pass); the one W1 scan worth revisiting there is `community_has_living_bearer` running every community-season even when no bearer exists in the world (a global living-bearer counter would gate it). **NOT committed** (awaiting approval).
- **W1 committed (`d47c894`, pushed) under the standing wave-train approval.**
- **Concurrent pair dispatched:** (1) **Performance review** on the main tree (Opus): instrument every orchestrator FIRST (EcologyTimings idiom world-wide, timings into the checksum-excluded perf block — instrumentation itself proven checksum-neutral), sim vs collect/serialize split on two seeds, ranked table before any fix, then the four debt classes audited with **each behavior-preserving fix proving byte-identical checksums on both seeds** (the roster-compaction discipline); the before-table becomes the playbook Perf row's standing baseline; target single-digit seconds. (2) **W2 Sites/ruins/caves/Works** (Opus, isolated worktree, branch `w2-sites`) — **commit-to-scratch-branch explicitly required, with the exact worktree-add incantation for the nested-repo trap** (both lessons from the lost roster slice applied); nothing-from-thin-air named as the slice's soul; venues migrate onto Sites; Works = Maker-made provenanced treasure left in ruins at the fall; caves seeded at creation (Saturn's exemption); beasts prefer dens; merge/rebase onto the perf-fixed master afterward.
- **Performance review landed + committed (`fef459f`, pushed; +`29733c6` chore untracking an accidentally-swept agent worktree gitlink, `.claude/worktrees/` now ignored).** **21.2s → 5.8s (seed 42), 13.3s → 4.0s (seed 7) — 3.7×/3.3×, EVERY fix byte-identical on both seeds, golden verify 8/8 with NO re-record** (zero behavior change). Instrumentation first: every orchestrator now self-times into the export's checksum-excluded `performance.subsystems` (instrumentation itself proven checksum-neutral); the old picture (Venus-only) hid 91% in `flecs_overhead` — which resolved into **archetype-table walking linear in table count**. The three fixes: **(1) per-birth `CausedBy(mother/father)` pairs removed** — every unique parent-pair minted a persistent archetype (4D record never deletes) → 56,633 tables walked every season-tick → now **917**; lineage lives in `Parentage` (export reads it sorted by raw id, reproducing the old id-sequence exactly — byte-identical); **(2) necromancy emergence's per-season full-population `children_of` rebuild → an incremental reverse-child index** maintained at the birth site (8,354ms → 258ms); **(3) global per-line bearer rosters** gate the bloodline-gift checks (no more full-roster walks to find nobody). Export/serialize measured small (~0.4s) and left alone. **Load-bearing constraint discovered and now in CLAUDE.md as a binding rule: RNG seeds ride raw flecs entity ids — registering ANY new component shifts every id and the whole trajectory** (fine in feature slices/greenfield; forbidden in behavior-preserving changes — store new state on existing singletons; prefer value components over unique-target relationship pairs). Playbook Perf row now carries the standing baseline (5.5s sim/917 tables s42; 3.8s/976 s7) + the fragmentation warning. Proposals deliberately not taken (listed with expected wins ~0.5–1s each): shared kinship-count pass (coin-gating fragility), journeys/callings PopulationCache reuse, wounded ledger for mars.heal, living-Adept set. The `community.children()` worry re-measured a non-issue (roster compaction keeps it small). W2 (in worktree) will need a rebase onto this before merge.
- **W2 landed on branch `w2-sites` (commits `9519168`+`4c78541` — the commit-to-scratch-branch discipline held; nothing lost).** Sites framework (`SiteKind{Venue,Cave,Ruin,Tower-reserved}`; a site's Life event IS its existence); **venues migrated** (30/30 keeper histories preserved, tavern scene intact); **ruins** = a Site created at the community's fall (a `Fall` event dates it; FoodStores cleared — a ruin keeps no larder; representation: separate lightweight entity referencing the persisting dead community — no mid-sim archetype churn); **caves** seeded at worldgen (Saturn's act), **beasts prefer dens** ("the lion of the Caerp Hollow", `BeingBinding::Site`); **Works** — a renowned Maker forges once in a life ("the chalice Sarmlon, forged in year 178 by Peldmaerdjir…"), custody chains as events (forged/inherited/came-to-rest), **left in the ruin at the fall**. Seating: **no new coin** (caves/ruins under `world.sites.*` — substrate/consequence; Works under `rule.callings.works.*` — a Maker's masterpiece); 304 leaves clean; lint exit 0; golden 8/8; perf 1.04× on its (pre-perf-review) base. **Emergent texture, exactly right: hoarded ruins are RARE** — forging needs prosperity, prosperous towns rarely fully fall, so most ruins are empty failed hamlets and a treasure-ruin is a true find. **Real bug found+fixed:** fission colonies carry a placeholder 1-tick Life until populated, so naive fall-detection flagged thriving newborn colonies as ruins (23 false → 2 genuine); fall now requires population AND a genuinely-ended Life. Nothing-from-thin-air grep-audited clean. Scene on a ruin renders the hoard with provenance. Note: browser visual smoke was inconclusive (harness instability) — quick manual viewer check recommended post-merge.
- **Merge dispatched (Opus): squash-merge `w2-sites` onto the perf-fixed master**, resolving both intents (keep all perf structures — Parentage-sorted lineage export, incremental indices, bearer rosters, timing zones — carry W2 content through them; `sites_season` gains its own zone), then the full playbook table re-run on the merged build (incl. table-count ~900 sanity — if 56k returns, the CausedBy fix was lost; perf row vs the new 5.5s baseline). Working tree left staged, uncommitted; main session commits.
- **W2 merged + committed (`a9febb7`, pushed); scratch worktrees/branches cleaned.** The squash-merge hit **zero conflict markers** (disjoint regions), semantically verified per hotspot: perf structures all survived (**938 tables** — the CausedBy/Parentage fix held; necromancy still ~271ms), all W2 content through them; the agent's one manual resolution gave `sites_season` its own `venus.sites` timing zone across four files (naming inconsistency with unprefixed `beasts` flagged — cosmetic, deferred). Post-merge verification full-green: 304 leaves, lint exit 0, determinism ×2, sites A/B inert, golden re-recorded 8/8, **perf 5.7s sim / 938 tables = 1.04× of baseline**; compositions re-proven on the merged build (Brurmaw + Vounmaw denned in The Caerp Hollow; the ruins of Aerdstead holding the chalice Sarmlon, custody forged→inherited×3→came-to-rest; Pardmere Wayhouse with 22 named patrons via the real `stage scene`). **W3 (Beasts full — tiers, dragons + hoards) dispatched.**
- **W3 landed + committed (`022de69`, pushed).** **The framework report card, as W0 promised: a beast tier = data + a control-law delta** — wolf and bear each cost 2 enum/registry lines + an ~11-line config block + a ~6-line `beast_tier()` arm + naming/color/bindings; everything else (presence, predation, hunts, export, scene, chronicle via `beastNoun`, lint) shared. Tiers: wolves (maul-heavy, bias to struggling towns' margins) > lions > bears (solitary, power 9, den-first) >> **the dragon** (power 30, arrival max 1/millennium — a slain dragon is NOT replaced; lifespan 400±150y; **⚖ outside Venus's mood surge, owned decision: her rajas is weather, a dragon is a calamity on its own clock** — mood-scaling would make a once-a-millennium omen a symptom of her temper). Distribution across 8×1000y: wolves>lions>bears everywhere; dragons in 3/8 worlds, 0–1 each; populations survive (seed 99 took 26 dragon raids and ends highest). **The dragon story composed naturally (seed 99, no nudge): the dragon Farsligath came out of the wild y278 and took the ruins of Vintstead for its lair → 87 years of named-casualty raids on Heiholm → the idol Beishif carried off to the hoard (custody `taken_by_dragon` — gathered, never spawned) → slain y365 by a warband of 18 led by Genperddein Leinslusjith — of the very family the dragon had culled** — hoard came-to-rest, the den now a dragon-flavored treasure-ruin with full provenance. Red Hand × dragon verified by code path + demonstrated under a nudged config ("Trufous Grourdlus… struck the blow that felled it" — an expressed bearer's ×4 against a worthy target). Verification all green: 335 leaves, lint PASS, determinism ×2, coin A/B fully inert (Sites unaffected), no-faceless (874 beasts named, 1752 participants), golden 8/8, **perf 1.09× (6.2s) / 940 tables (+2, value components only)**. Seams: beasts subsystem 330→827ms (two cheap wins noted if it ever matters); aged-out dragons leave hoards as taken-by-dragon without settle events (elapse hook absent); the inherited-hoard lair (dragon claiming a ruin that still holds Works) supported but didn't roll in 8 seeds. **W4 (Wizard towers) dispatched.**
- **Session review (Ryan): what is the player's experience — what decisions, how answered, how do answers touch the world?** Assessment delivered: the seat/knowledge-scoping/consequence machinery is real (Decision shape validated, contexts now_tick-clipped, authored events weave into chronicles); decisions are AUTHORED not generated (the referee model); answers touch the world only through DM/Luna translating them into stage edits — by design, but the connective tissue has gaps, ranked: **(1) a real leak found during the review — PC contexts passed through ALL DM notes/tags/links, so "secretly evil" would reach the PC and the LLM playing them**; (2) no decision→consequence linkage (transcript and edit log unlinked); (3) decisions not part of the campaign file; (4) no human seat yet; (5) no viewer decision surface. Items 1–3 proposed as a "session record" slice.
- **Levers review (Ryan): what influence do DM/Luna hold in-game?** Assessment: over FACTS, absolute (the six ops compose to retcon-grade power — `set` can end a life; `event` forges even divine acts; undo is a rewind); hidden levers — **now_tick as time-on-rails** (the generated future is Luna's script; advancing now reveals it scene by scene, divergences authored against the ghost of what would have been), consultable gods, forged providence; the hard boundary — **no simulation during a session, so no edit cascades** (an authored grievance doesn't flip stances; consequences are records, never reactions; coherence is manual bookkeeping). Proposed: **consequence macros — "Luna's spellbook"** (kill/wound/embitter/bestow/bind/advance — compound ops compiling to the six primitives, one-API invariant intact) + the session-record items as a "Luna's table kit" slice, offered for after W4.
- **W4 landed + committed (`ed033b6`, pushed).** The discovery table: necromancer becomes row #0 beside **beastcaller** and **plaguewright** (shared grief-gated emergence, weighted archetype roll, Open Eye lightning-rod guards ALL archetypes by construction); **exile**: a strike craters standing (−8 prestige) and the wizard withdraws to a wilderness **Tower** (SiteKind::Tower claimed at last; residence = value-component marker, structural membership untouched — kin/lineage/Works stay coherent; home raids/hunts skip a resident via able-adult guards); **experiments**: beastcaller binds/loses wild beasts ("Rinttooth escaped Paergeind's tower and denned in the wilds, a menace to Laendale"), plaguewright looses Plague on nearby towns (named victims, Illness sentinel, `DeathCause::Plague`; **4 living Keepers drove severity to 0 — emergent containment**), necromancer grave-raids from the tower (F4 loop intact); **expeditions**: per-tower grievance ledger (⚖ not community-pair stance — a tower is not a polity) → threshold → Mars-idiom march ("The people of Centvale marched on the Silent Tower of Paennir and cut Freirsnaewourd down… his death unbound his legions"); slain wizard's tower → Ruin via the W2 fall path (both fall paths shown). ⚖ coin kept `necromancy` (regroup-don't-rename; the table lives at `mercury.wizards.*` under it). Verified full-green: 365 leaves, lint exit 0, determinism ×2, coin A/B inert, no-faceless, golden 8/8, **perf ~1.01× (6.28s/951)**; 4 wizards/1000y — era-defining, not weather. Seams: lone wizard's mouth/hands still count in home aggregate ecology (negligible); max_concurrent>1 advisory; lightning-rod verified by construction. **W5 (omens/shrines/blessings) dispatched.**
- **The seat-context leak fixed + committed (`d381a7d`, pushed; surgical — W5 in flight in the same tree).** `_overlay` now publishes to PCs only `known:`-prefixed notes (prefix stripped); tags/links stay world-legible; the unfiltered margin remains DM/Luna-only (`person_facts`/`scene_facts`). Verified: secret filtered, published note passes, demo green. **Bonus catch from the demo's paranoid self-check:** in today's regenerated world the demo PC *leads* their community, and the leader record carried the PC's own id — now normalized to a self-reference (`{"self": true}`, rendered "which you yourself lead"); the no-own-id scoping rule holds again.
- **Spellbook blessed + perturbation UN-DEFERRED (Ryan).** His two directions: (1) Luna's spellbook = **codified concept-level changes** (templates applying a concept, not a value), mintable mid-session, and — the deep point — **a source of future game mechanics**: a repeated change shape "probably means something and should be legible to the rest of the engine"; (2) applying/flattening edits — "taking this earth and informing heaven" — should be tackled now; his read: the deferral was too-many-questions, not technical difficulty, and what's needed is hardening of the existing model. **Confirmed right, with the dissolving insight: REPLAY IS THE LOAD** — the engine regenerates to any tick cheaply and at that moment all live state (ledgers, charges, singletons) exists in memory by construction; no save format, no state import. And the two ideas are one design: **macros are what give edits engine semantics** (a raw event is unmappable; `kill`/`embitter`/`bestow` know both their record shape and their mutation).
- **Contract authored (Luna): `docs/research/2026-07-11-spellbook-and-perturbation.md`.** Spellbook: macro = {name, params, compile→primitives, semantics}; casts append compiled PRIMITIVES to the edit log with macro provenance (one-API invariant intact; undo removes the cast group); v1 book kill/wound/heal/embitter/reconcile/bestow/bind/release/exile/bless/curse/advance; minted macros stored in the campaign, record_only until taught; **usage ledger + promotion report** ("most-cast record-only macros = the engine's next mechanics, demonstrated by play"). Perturbation: `continue(seed, config, perturbations@T, years)` — replay to season-boundary T → apply script via the existing chokepoints (kill_person/record_grievance/custody/generators — the hardening pays) → simulate on (stances recompute, the provoked raid actually comes — "the engine handles itself; that is the point") → export full 4D; recipe becomes a **chain of edit-epochs absorbed at T1, T2…**, deterministic end to end; `stage advance --years N` re-stamps the campaign, absorbed edits kept-but-skipped. ⚖: all perturbations apply AT the boundary (past-dated keep their stated tick in the record; "the timeline heals" mid-replay injection documented as v2, not v1 — it would rewrite events the table witnessed); note/tag stay stage-only; authored ids remapped at absorption. **Acceptance gate: the null test** — an empty perturbation at T must reproduce the plain run byte-identically.
- **Both dispatched concurrently:** Spellbook (Opus, main tree, tools/stage only — scope-fenced against W5); Perturbation (Opus, isolated worktree, branch `perturbation`, commit-to-scratch-branch discipline; engine `continue` mode + applier; merged after W5 lands).
- **Spellbook landed + committed (`976a9fe`, pushed; surgical — W5 in flight).** `tools/stage/spellbook.py` + additions to model/stage + 61 unit checks, all green, no exe needed. The 12 codified spells compile to grounded primitives stamped `macro:{name, params, cast_id}` (log stays primitives; **undo is cast-aware — removes the whole group**; re-cast byte-identical). **Minting proven live:** a `haunt` macro defined mid-session, cast 3×, survived save/open, and `spellbook report` flagged it as the sole promotion candidate — "teach the engine this" (Ryan's macros-become-mechanics loop, working day one; ledger counts lifetime casts incl. undone — gap-log semantics). **Perturbation compiler emits the versioned script** (`magnus3.perturbation` v1: one entry per cast using its semantics token; death-shaped bare sets route as kill_person; note/tag/bare-link skipped; authored ids flagged for remap) — demonstrated on a mixed log (kill/embitter/bestow/flavor/note/create → 5 entries, note absent). Absorption bookkeeping implemented + tested (mark_absorbed_through / id-remap); `stage advance` deliberately waits for the engine half. `/api/cast` + 3 sibling endpoints documented-not-implemented (serve.py owned by W5 this hour) — a pure-call stitch on the shared StageSession, parity structural. Bonus: the old campaign file correctly refused on **BUILD DRIFT** — the determinism guard doing its job across today's engine changes.
- **The Map round (Ryan): a general map improvement agent — content AND appearance; enhance the bare-bones conceptual skeleton.** His method, explicit: consider the archetypal fantasy maps (Wheel of Time, LotR, Malazan, ASOIAF), extract what makes those worlds unique ON THE MAP, then **work those elements backward into the model** — not "what can our model already draw."
- **Derivation authored (Luna): `docs/research/2026-07-11-the-map.md`.** The archetypes read for their load-bearing elements: LotR (ranges as walls the plot must cross; rivers as roads AND borders; the cartographic hand as identity), WoT (the wall-with-one-pass making cultures; **the danger gradient** — the Blight as distance-from-safety made visible; depopulated vastness wearing older ruins), Malazan (deep time layered under the present; the honesty of the unmapped), ASOIAF (**geography as politics** — chokepoints deciding wars, climate gradients hardening identities, ruins with names and curses). Extracted into the elements table, each seated: coastline/sea + mountain-ranges-with-passes + rivers = **new worldgen** (Saturn's creation act, seeded noise — the sanctioned exemption); contiguous biomes + the danger pale = derivation over existing multipliers/distance; **sites, routes, and style = pure cartography — this week already built their content** (W2 ruins/towers/shrines, F5 routes). The killer wiring: passes shape F5 route pathing → chokepoints → isolation → F2 stance divergence → F3 raid geography — **the whole social engine gains terrain**. Two slices: M1 geography (engine; acceptance test = the chokepoint composition) + M2 cartography (viewer; acceptance test = the glance test: "a stranger shown only the map should want to play in it").
- **Dispatched (Opus, isolated worktree, branch `map-geography`, commit discipline):** M1+M2 as one coherent slice — terrain synthesis (dependency-free seeded noise: coastline silhouette, impassable ranges with 1–3 passes, greedy-descent rivers, contiguous biomes), **GeoFeature naming** ("the Weeping Pass" voice), consumers wired (settlement avoids sea/peaks + favors banks; routes respect walls/passes; beasts read biome affinity + a wilderness field; caves prefer hills), `world.geography.*` under coin 0; the viewer minimap grows into THE MAP (parchment-dark ground, peak glyphs, tree clusters, river threads, danger-pale wash, site glyphs incl. dragon lairs, drawn routes, italic feature labels, three zooms, click-through to inspect). Merges after W5 + perturbation land. Three agents now in flight (W5 main tree · perturbation worktree · map worktree).
- **Perturbation landed on branch `perturbation` (`af98f7a`; commit discipline held).** **THE NULL TEST PASSED FIRST — an empty perturbation reproduces the plain run byte-for-byte at 60y AND 1000y** (the pause fires right after the season tick reaching T; default args make plain `export` a pure no-op — golden 8/8 against EXISTING baselines, no re-record). All 15 op kinds; tolerant reader (record-only skipped-with-note; unknown mechanical kinds/fields/versions abort LOUDLY at construction before any replay); the `perturbation` meta block (T, script hash, id-remap, flavor sidecar, per-entry log) checksum-excluded. **The demonstration is Earth-informs-Heaven in miniature (seed 42, kill @ y60 → continue to y100, deterministic ×2):** the killed leader's community held a real succession; the embittered pair turned friendly→hostile (grievance-dominant) then decayed to wary — derived stance turning on injected grievance; **the created stranger Silbrand married, conceived, took the gatherer calling, and was ELECTED LEADER** — authored state treated as fully ordinary; the flavor comet injected at its stated tick; **ghost-hunt clean** (the killed leader in zero post-T events). Notable: the two new event kinds ride enum-only (no registered component → no id shift). Seams for the stitch: existing-entity references resolve by name/raw-id/person-seed NOT base-export id (the absorption contract's one open item — a stable per-entity key recommended); set whitelist name/temperament/prestige/health; create_place cave|tower|ruin (community-create deferred loudly); bless/curse riders prestige/health v1. Merges after W5 (shared files: EventComponents, colors, chronicle).
- **W5 landed + committed (`836d55a`, pushed).** Expression-side divine content on all four gods at parity, dispositions untouched — only their legible face. **Omens**: per-community Keeper readings at real mood shifts, god-flavored portent tables ("crows gathering black on every rooftop over Laendale, and said the god Mars had gone heavy and distant"), shrine-reliability bonus, Keeper prestige. **Shrines**: SiteKind::Shrine, founded diegetically — ⚖ dedicated to the god whose domain owned the trial the community outlived (FamineOutlived→Venus with the retained Famine event as provenance; Manifestation→the line-god, giving four-god dedication parity; MonsterSlain/PlagueContained named but deferred — need a producer-side trial board); keeper terms, steady keeper prestige. **⚖ The worship whisper INCLUDED, tiny + gated:** a living shrine pulls its god's charge toward sattva (`min(0.010, 0.0015·shrines)`, folded into decay) — **A/B: measurable, not flattening** (Venus 6→2 episodes under 10 shrines; she keeps her weather); a world that builds shrines calms its gods — mortals' first touch on Heaven. **Providence**: deeply-sattvic gods bless their domain's most-renowned hand (Venus→Keeper, Zeus→leader, Mercury→Adept, Mars→Blade), deeply-rajasic curse the least-renowned; riders ±prestige/±health (+ Venus fertility touch); ~7–11/millennium — rare as intended. **Authored-vs-emitted proven same-shape**: a Luna-authored Blessing renders in the same register as a god-emitted one. All rows green (409 leaves; lint PASS; determinism ×2; orthogonal A/B knobs isolate correctly; golden re-recorded 8/8; 961 tables ≈ +1%; soak sane: ~1300 omens / ~10 shrines / ~10 providence per millennium). Seams: shrine-keeper succession v1-lite; BlessedBy mid-sim add bounded (~10/millennium, birth-path precedent).
- **Perturbation merge dispatched (Opus, W2-drill):** squash `perturbation` onto W5's master; ⚖ collision to converge — the perturbation's bless/curse `Providence` event vs W5's `Blessing`/`Curse` types (brief: prefer one vocabulary, the perturbation's ops emitting W5's types — authored and emitted and PERTURBED all sharing one chronicle voice); **the null test re-gated post-merge**; golden must verify against W5 baselines with NO re-record (continue must not disturb export).
- **Perturbation merged + committed (`54224ca`, pushed; branch cleaned).** ⚖ converged: `EventType::Providence` dropped (grep-confirmed unreferenced), the perturbation's bless/curse emit **W5's `Blessing`/`Curse`** — authored, emitted, and perturbed providence now share ONE chronicle voice ("The god Ecology looked with favor on Ounsgrarstreis Sesdeirmostril and blessed him — a grace that told in the years that followed", a DM-cast bless w/ +12 prestige rider, verified live in the viewer); `Chronicle` kept for flavor records. **Null test re-passed post-merge byte-identical at 60y AND 1000y; golden 8/8 vs W5 baselines with NO re-record** (continue doesn't disturb export); exact 961 tables; demo re-ran with null-baseline contrast (the kill IS the cause: null world's same leader dies old_age at y89; the embitter-caused StanceShift absent in null); Halvard the Wanderer lived an ordinary 37-year created life; ghost-hunt clean; loud-failure regression green.
- **The stitch dispatched (Opus, main tree): `stage advance --years N` end-to-end** — the id bridge (export-id → person-seed/name keys, ambiguity fails loud), invoke `continue`, absorb + remap + re-stamp, **the campaign recipe becomes an epoch chain** (`campaign.epochs[]`; export_source learns to regenerate chained sources — build-drift guard per epoch); serve.py gains /api/cast, /api/spellbook(+report), /api/perturbations, /api/advance; gates: chain determinism ×2, a second epoch composes, the empty advance = the null test through the stage layer, ambiguous-name refusal.
- **Omens review (Ryan): do omens foresee the determined future — warn of a famine/raid, offering escape from foreseen fate?** Assessment: as built, omens are **barometers, not oracles** (they read the mood that statistically shapes the future; no event foresight; record-only, mortals never react). Three tiers surfaced: **(1) fate-reading omens** — the divine-determinism taxonomy licenses true, paradox-free reads of category-3 facts (dice already cast: senescence dates, prereq-satisfied schedule years) — cheap, unbuilt; **(2) the session layer — Ryan's exact scenario is playable TODAY**: the unwritten future is the seer's script, and the perturbation makes averting foreseen fate mechanically real (foresee → strive → advance → diverge, provable against the null baseline); **(3) generation-time event-prophecy** — possible via two-pass generation exactly while omens stay consequence-free; mortal reaction makes prophecy a fixed-point problem (self-fulfilling/self-defeating oracles as emergent phenomena — thesis-adjacent, the observer changing the observed).
- **Ryan: build tier 1, park tier 3.** Parked-with-plaque in the-wide-world.md ("Parked — generation-time prophecy (the fixed point)"). **Fate-reading omens dispatched (Opus, main tree src/ — disjoint from the stitch's tools/stage footprint):** two invariants as the slice's soul — **TRUTH (prophecy accuracy must equal 1.0**, forcing truth-safe read shapes: the death-read is a BOUND ("will not see the Nth winter") robust to violent preemption since senescence is an upper bound under external mortality; conceived births EXCLUDED per the cancellation landmine; discovery-reads only when prereqs already on) and **INERTNESS (pure-hash selection, no shared RNG; ON/OFF A/B must differ ONLY by the foretelling records**, byte-compared). The payoff line: when the foretold fact lands, its chronicle line gains "as had been read in the omens" (render-side cross-reference).
- **The Map landed (M1 geography + M2 cartography), on `map-geography` (b885bcd).** Worked backward from the archetypal fantasy maps: mountain ranges as walls, passes as chokepoints, rivers as roads-and-borders, the danger pale, named geography. `TerrainGenerator.hpp` — dependency-free value-noise fBm — synthesizes per-tract `Terrain` (Sea/Coast/Plain/Forest/Hills/Mountain/Swamp/Desert) onto the existing tract grid at world-gen: a shaped coastline, 1 mountain range as a connected impassable chain that divides a region with a seeded pass, rivers by greedy descent (banks flagged), contiguous biome fields. GeoFeatures ("the Storm Range", "the Cold Pass", "the Keirnp Gulf") on a SpaceState registry — no entities, no id shift. An all-pairs Dijkstra tract-distance matrix (river-valley discount, swamp penalty) is the chokepoint substrate.
  - **The wiring — terrain earns its keep:** terrain drives `base_fertility → yield_cap` (settlement favours banks/coast, hard-avoids sea/peaks — save a town founded *in* the pass, which is the point); routes read TERRAIN path distance not Euclidean, so a wall collapses cross-wall routes; cross-community marriage gains a reach gate, so kinship and stance diverge across the wall vs along it; beasts read biome affinity + a derived wilderness field; caves prefer uplands. Config `world.geography.*` under coin 0 (prefix-bound in TechParams); export extends with per-tract terrain + GeoFeatures, folded into the checksum.
  - **M2 — the minimap became THE MAP:** parchment terrain, hachured peaks, tree-cluster forests, river threads, coast outline, swamp/desert stipple, the danger-pale wash, drawn roads, site glyphs (town/ruin/tower/cave/venue/dragon), italic geo labels with greedy collision avoidance, world→region→tract zoom, click-through to inspect — dark-parchment palette. Glance test: passes (Playwright screenshots at 3 zooms + world overview, no console errors).
  - **Verified:** determinism ×2 identical; settlement sanity (0 towns drowned/on peaks across seeds — the "mountain" towns sit in passes, by design); geography A/B (off = inert pre-Map world); **the chokepoint composition — seed 13, Coastmarch: the Storm Range split by the Cold Pass, within-side routes 6/6 open vs cross-wall 1/9, stance warmth 0.67 within vs 1.56 across** (Fraefell holds the gap); beast enrichment (wolf Forest 1.73×, bear Mountain 2.03× / Hills 1.69×); season loop ≈ baseline (geography on 4741ms ≈ off 4980ms, seed 7 1000y — the cost is worldgen-time, ~6ms); golden 8/8 re-recorded; validate-config + lint clean. Follow-up: chronicle/scene naming the pass a journey crosses (not cheap — needs journey→pass mapping). ⚖ chokepoint social teeth (agent): exogamy was distance-blind, so a wall wouldn't diverge stance — a terrain reach gate on cross-community marriage closes the causal chain. Map merge queued behind the stitch + foretellings (its base predates W5/perturbation; shared files: WorldConfig, TechParams, ExportCommand, colors/data).
- **Map v2 dispatched (Ryan's verdict on the first pass: "a great first pass... a bit rough as an end product... too low resolution. And not very styled. It's a bit ugly, even if it's been dramatically improved").** Mission: move the space model off the strict 8×8 grid while keeping the region→area→tract→plot hierarchy and every consumer working; render at "a fantasy map you'd frame" quality. Branch `map-v2` from `map-geography` (its merge to master still queued), master (W5 + perturbation, `54224ca`) merged in — two conflicts, both mechanical: data.js (routes index vs perturbation block — both kept) and golden baselines (taken from master; re-record comes with the slice anyway). Merged tree builds clean; baseline determinism/perf pair running. Two Explore agents sweeping (1) every engine consumer of SpaceState/grid/path_cells/GridPosition, (2) the full viewer map pipeline — the spatial-model decision (finer uniform grid vs one continuous world grid vs Voronoi) gets made against that catalog.
- **Map verdict (Ryan, from screenshots): "a great first pass… a bit rough as an end product"** — changes not as drastic as hoped; the strict 8×8 grid shows through ("too low resolution… not very styled… a bit ugly, even if it's been dramatically improved"). Direction: **rethink the space model off the strict 8×8 grid**, keep subdivision (the model approach is sound), make it look like a fantasy map; **population scale-up allowed** to suit a realistic map. **Map v2 spawned to a dedicated separate session** (chip; own worktree/branch, Sonnet-heavy grunt work): evaluate finer grids vs Voronoi/Lloyd (Azgaar-style) as the tract layer, keep every consumer working (settlement/routes/beasts/marriage-reach/wilderness), re-prove the chokepoint composition on the new model, and render "a map you'd frame" (marching-squares + Chaikin coastlines, painterly dark-theme terrain, cartographic type). This session compacts after the in-flight agents (stitch + fate-omens) land and the map-geography branch merges.
- **The stitch landed + committed (`49f2114`, pushed; surgical — foretellings still in flight): `stage advance --years N` end-to-end, THE LOOP CLOSED.** The id-bridge (export-id → name keys, loud collision refusal; authored ids pass through for engine remap; flavor events injected as Chronicle records); `advance.py` compile→continue→absorb→re-stamp→epoch with **write-temp-then-swap** (a rejected non-whitelisted set left the campaign untouched, proven); **the epoch chain persisted** (`campaign.epochs[]`, per-epoch script hash + tail checksum; chained regeneration with a build-drift guard per epoch); `state()` resolves unabsorbed only (no double-apply); `[absorbed]` log markers; serve.py's 5 endpoints. **78/78 tests. Null test through the stage layer byte-equal; chain determinism ×2; a 2nd epoch composes.** End-to-end: Gurmfin killed @y60 → succession; embitter → 9 post-T StanceShifts; **Vesh the Wanderer (a1→297) marries at y78**; the green comet rides the flavor sidecar into the chronicle. Seams: strict per-epoch-T mechanics awaits the engine's multi-(script,T) replay (single-epoch fully faithful); set whitelist growth = engine change; export ids shift across an advance (reference by name, as designed).
- **⚖ Map lineage decision (main session): `map-geography` stays UNMERGED to master** — the Map v2 session branched from it and merged master@54224ca into `map-v2`, so v1 lives in v2's lineage; v2 owns the map's future and lands it whole (avoids double-reconciliation of the same conflicts). Master intentionally carries no map until v2 lands. Compaction plan updated: this session wraps once fate-omens lands + commits.
- **Map v2 session: baseline + consumer sweep + THE DECISION.** Merged tree deterministic ×2 (seed 42 1000y world_3region → `ce4ae890a11651c5` twice), 6.5s export wall. Sweep findings that shaped the design: `tract_dist` (the all-pairs N² matrix — the scaling wall at fine resolution) has exactly TWO consumers, marriage reach + route adjacency, both community↔community → per-community Dijkstra fields are a clean drop-in; everything else (Mars/beasts/wizards/fission) is raw Euclidean on GridPosition, terrain-blind; routes pair same-region ONLY and store no path; plot-key arithmetic (`y*W+x`) in four places; viewer minimap has one integration seam (MinimapPanel), no layer caching, venue glyphs dead (no grid pos), shrine/wolf/lion/bear exported but never drawn, and the whole map lives scrolling in a 260px drawer.
  - **Decision (design doc: `docs/research/2026-07-11-the-map-v2.md`): ONE CONTINUOUS WORLD GRID** (~96×64 for 3 regions, config-scaled), regions as contiguous partitions grown from seeded capitals by terrain-aware Dijkstra (borders hug ranges — geography as politics for free); per-community distance fields computed at founding replace the matrix; social pairing (routes/marriage/Mars) goes world-wide, distance-gated — region-only grouping was an artifact of disconnected space, and the pass becomes the road between two region-cultures (the chokepoint made world-real). Voronoi rejected: every consumer is (x,y)-lattice-addressed, and the organic look comes at render time from marching squares + Chaikin over cell data. Finer per-region grids rejected: keeps the three-postage-stamps structure — the structural half of "not drastic enough." Hierarchy intact: region=partition → area=flood-fill within region → tract=world cell → plot unchanged. Synthesis v2: domain-warped fBm continent with closed silhouette, ranges as coast-to-coast momentum ridge-walks (8-connected blocks 4-connected passage) with passes at low saddles, rivers with RECORDED ordered courses, wilderness by multi-source BFS; export goes columnar for the world terrain block, routes gain drawn paths (A* at export), rivers export as polylines.

Sunday - 07/12/26
Log:
- **Fate-reading omens landed + committed (`7ae0ba2`, pushed; golden re-recorded + verified at commit).** Both invariants proven. **TRUTH: prophecy accuracy 1.000000** across 3 seeds × 1000y and a 4,645-foretelling stress test — death-bounds hold under preemption by construction (the foretold winter sits at-or-after senescence; external mortality only truncates earlier — shown live: **Koundfrenddist Nuntreir, foretold not to see her fourth winter, killed by the bear Trulbruin three winters early — the bound TRUE**); coming-change reads only on prereq-satisfied schedule years (the sequencer meets them or the Open Eye beats them); birth reads excluded per the cancellation landmine. **INERTNESS: byte-wise A/B — entities and non-foretelling events identical to the fate-off world, 0 differ; only the records added.** The agent's first cut failed instructively: live Foretelling entities shifted every raw flecs id and thus the trajectory (the CLAUDE.md rule biting in the wild) — rebuilt as **buffer-on-a-registered-singleton, materialize-at-export**. ⚖ seated as shared `world.fate` under coin 0 (fate reads what's already written — Saturn's clock, the genesis arc — whoever wrote it; one read per community-season, pure-hash, no deity stream); ⚖ `keeper_prestige` defaults 0.0 deliberately (credit would bend leader selection and break inertness; the lever is wired-but-guarded for worlds accepting divergence). The cross-references render: *"Banveirm Fuldlourmfraendtaent died of old age, 50 years old, **as had been read in the omens**"*; a coming-change reading closes *"and so it came to pass."* 1.3 foretellings per community per century — solemn, not weather. 413 leaves; lint PASS; **961 tables unchanged (zero fragmentation)**; fate cost ≈56ms/millennium.
- **Session wrap (pre-compaction).** All in-flight work landed and committed; master at `7ae0ba2`, clean, pushed. The day's arc, end to end: architecture review → determinism hardening + golden harness → divine dispositions (gunas) on four gods → roster compaction (~2.2×, byte-identical) → Theogonus slice 1 (the coins) → the Chronicle → **the stage pivot** (worldbuilding tool; campaign = recipe + edit log; player seats; entity editor) → the F-train (callings/prestige · stance · Mars raids · necromancy · roads/taverns; the tavern scene occurred naturally on seed 8) → the content pipeline (playbook + lint + Beings/Sites frameworks) → Blooded Lines (the Manifestation) → the perf review (3.7×, the raw-ids rule) → ruins/Works · beast tiers + the dragon · wizard towers · omens/shrines/providence → **Luna's spellbook** (concept macros, minting, promotion ledger) → **the Perturbation + `stage advance`** (Earth informs Heaven; the epoch chain; null test byte-equal end to end) → fate-reading omens (prophecy that cannot lie) → the Map v1 (chokepoints socially load-bearing) with **Map v2 running in its own session** (one continuous world grid decided). Remaining before session zero: Map v2 lands; W6 (community archetypes/raiders/skim upgrade) optional; then we skim, pick a world, and play.
- **W6 dispatched (Ryan: "let's get W6 started") — the last wave of the-wide-world (Opus, main tree, master @ `7ae0ba2`; map-v2 untouched in its own worktree).** Three parts per the spec: **community archetypes** as derived labels, never stored flags (trade hub · shrine town · martial town · frontier · evil city · ruin — computed from calling mix, venues/sites, route degree, stance web, raid history; deterministic derivation with fixed tie-breaks; preference stated for export-time derivation so viewer/skim/scene read one truth); **raider cultures** as a seeded generation knob — a Blade-skewed community whose economy is raiding, riding the EXISTING Mars raid machinery (propensity, not a parallel system), founding diegetic (Saturn's-creation exemption or emergence-by-drift, agent's call, documented); **skim/scene upgrade** so a DM shopping for a world sees "an evil city, two trade hubs, a raider culture in the east" at a glance and scenes take archetype color. Full playbook table demanded (lint · determinism ×2 · coin A/B with a named raider community as evidence · no-faceless · soak · golden re-record · perf · archetype spot-check argued from underlying data · skim/scene output shown). Standing wave-train commit approval applies (`Feat: W6 - …`, explicit staging, push on green).
- **Map v2 session: the engine core + the cartography engine, both written.**
  - **Engine core committed to `map-v2` (`1171ca5`):** EcologyComponents space v2 (WorldGrid on SpaceState; Tract gains region_id/elevation; RegionSpace = biome + areas + ordered tract list + capital; `community_fields` + 5-arg `path_cells` with the Euclidean fallback), TerrainGenerator full rewrite (domain-warped fBm continent with a seeded elliptical mask — closed silhouette; ranges as coast-to-coast momentum ridge-walks, stamped 8-connected so they block 4-connected passage, passes punched at the lowest natural saddles, foothill lift for relief; rivers with RECORDED source→mouth courses and tributary joins; `partition_regions` — farthest-point capitals over terrain-aware distance + simultaneous multi-source Dijkstra growth, so realm borders hug walls and waters; `derive_biome` off the dealt ground; `compute_community_field`; `route_path` A* for drawn roads; major-components-only feature naming + swamp/desert name tables), AreaGenerator v2 (region-confined flood fill, farthest-point seeds, terrain-derived area types, ~14 areas/realm = fission's room to grow), Community/Plot/World-Generator restructure (synthesize once → partition → per-realm seating; wilderness + founder fields world-level after all realms), config v2 (world_grid 96×64 / 56×40; geography knobs rescaled ~×4; `passes_per_range`). **Consumer migration dispatched (Opus, same worktree):** the 12-file sweep per the catalog + ExportCommand contract v2 (columnar world terrain block, world-level geo_features, routes gain drawn `path`) + cells-knob rescale, gated on build/determinism ×2/settlement sanity/geography A/B, with an ASCII-map sanity print demanded.
  - **Cartography v2 first cut committed (`3d85e9b`), built against the spec'd contract while the engine migrates:** new `viewer/js/maprender.js` — MapPainter (marching squares over the sea mask + Chaikin ×3 coasts, even-odd so inland lakes stay water; per-pixel painterly land pass = 4-cell tone blend × NW hillshade × danger-pale wash × parchment grain, routed through a scratch canvas because putImageData ignores clips; depth-shaded sea + wave dashes; hachured peaks with shaded SE flanks; gold pass-gates; tree/hill/swamp/dune glyph fields; linked+smoothed region borders; LabelEngine with greedy collision + principal-axis range labels). `minimap.js` rewritten as camera views over ONE world render (world→realm→tract; statics cached per scale — the playhead re-render repaints only the dynamic overlay; full glyph set incl. previously-undrawn shrines, beast-den paw marks, and the dragon-lair wing mark; compass rose, scale bar, neatline, seed-and-year cartouche; the drawer auto-grows and gains ⛶ expand). Same MinimapPanel public API — main.js untouched; the legacy area-map fallback kept for geography-off exports, now windowed by region bbox. Noted for the final land: master moved again (fate-omens `7ae0ba2`, W6 in flight) — map-v2 takes one more master merge before review.
- **W6 landed + committed (`34133c1`, pushed; golden re-recorded + verified at commit) — the-wide-world train complete.** Three parts, all on the existing machinery.
  - **Community archetypes — derived labels, never stored flags.** ⚖ Derivation lives export-time in C++ (`annotate_archetypes`, a post-pass over the ASSEMBLED export — the same data skim/scene read, so one truth), baked onto the community entity as a non-authoritative final-state snapshot (like `relations`/`routes`/`current_health`), folded into the checksum, deterministic (ordered walk, fixed thresholds). Labels: **evil_city** (a living necromancer resident) · **trade_hub** (open-route degree ≥ 4 — a genuine crossroads; the first cut at ≥ 2 caught 26/30 and was raised) · **martial_town** (Blade fraction ≥ 0.30 OR ≥ 2 raids led) · **shrine_town** (a standing shrine) · **frontier** (route degree ≤ 1 in a multi-community region — the web's edge) · **ruin** (fell before the horizon). A community carries every label that fits; the primary (display) is the first by a fixed priority. Thresholds are documented PROVISIONAL constants (a labelling convention, not simulation dynamics — no config surface).
  - **Raider cultures — a seeded raid-economy community type**, the second generated antagonist beside the necromancer's evil city. `RaiderCulture` always-present sentinel on every community (off by default; birth-fixed, no single-writer owner — like `calling`/`is_necromancer`). ⚖ Founding is Saturn's-creation-act seeding (the caves exemption): a rare few flipped at world-gen (deterministic id-sorted walk, `found_chance` roll, `max_per_world` cap) AND inherited by fission colonies (drift — a raider's daughter is raised to the custom), so 2 world-gen founders became a 6-town raider dynasty by y150 on seed 42. Manifests through EXISTING machinery, no parallel system: Blade-skewed callings (the skew rides the component so Zeus's Callings roll reads it without seeing Mars config), **plunder raids** (new `RaidMotive::Plunder` — its economy IS raiding, so it strikes the nearest non-allied neighbour without waiting on grievance or famine) + harsher reprisals/desperation (Mars multipliers), and the grievances those seed turning neighbours hostile (the Rule stance loop, unchanged). Config `mars.raider_cultures.*` under the War coin (7 leaves bound + validated); `enabled=false` keeps the pre-slice shape.
  - **skim/scene upgrade.** skim's blurb gained an archetype tail — "raiders: Paedale, Runtmere; 2 trade hubs; 3 martial" (seed 5) — so a DM shopping seeds reads a world's character at a glance; scene colours to its town — *"a raider culture - its economy is raiding; a martial town - it breeds fighters and its feuds run hot (also: frontier)"* — a tavern in a martial town reads differently from one at a crossroads. chronicle voices the plunder motive distinctly ("as their custom demands").
  - **Verification (full playbook table).** Build clean; lint PASS; validate-config OK (420 leaves, the 7 raider leaves Earth-scoped under War). Determinism ×2 identical (`909003…`→ threshold tune →`595bc12…`, byte-stable per build); distinct seeds differ. **Coin A/B on seed 42 world_3region 150y: OFF = 0 raider cultures / 0 plunder raids / 2 total raids (a quiet baseline); ON = 6 raider cultures / 178 plunder raids / 228 total (incl. 46 grievance reprisals the plunders seeded — composition with the old Mars+stance loop, named).** No-faceless: 1,863 named plunder combatants, 0 anonymous. Soak 1000y no crash (seed 42 default sim 2.1s / tables 697; world_3region 2,115 raids over a millennium ≈ 2/yr — notable, not weather; tables 1005 ≈ the 976 baseline, zero fragmentation — the sentinel shares the community archetype, no 1:1 pair split). Golden 8/8 re-recorded + verified (generation touched — RaiderCulture on every community shifts ids; greenfield). Archetype spot-check argued from data (Surnholm trade_hub+martial via deg 5 + raid-active; Persfell martial via 3 raids led; Purwick trade_hub+shrine via deg 4 + a shrine). Seams: raider seeding runs at world-gen regardless of the War-coin schedule (raiders exist but don't raid if War is off — documented); archetypes are a final-state snapshot (scene colours with the end-state label, not as-of the queried tick — the underlying facts stay time-correct).
- **W6 raider rebalance queued (map-session feedback → Ryan's texture call: DRIFT + HARD CAP).** The map-v2 session measured W6 at world scale: on the continuous grid (seed 42, 1000y, world_3region, merged tree) ONE seeded founder (Surnholm) grew a 6-town raider lineage → 1,957 plunder + 556 reprisal = 2,561 raids and **158 community falls per millennium** (pre-W6 same world: 54 raids, 4 falls). Diagnosis confirmed in source: worldgen seeding respects `max_per_world`, but `FissionSystem` copies the parent's `RaiderCulture` into every daughter unconditionally — inheritance is destiny, and nothing counts LIVING raiders; W6's soak validated frequencies on the old compact world where fission was area-starved. The world survives (37 towns, 42k persons all-time) but wears permanent raid pressure — a plague, not "a second antagonist" (the config comment's own standard). ⚖ Ryan: **inheritance becomes a probability (~0.35, drift not destiny) AND a world-wide cap on living raider communities (~3–4) blocks inheritance when full** — dynasties can rise and fall (a fallen raider town frees a slot), a plague is structurally impossible. Lands AFTER map-v2 merges (shared touch points: FissionSystem, MarsConfig; and the tuning must be measured against big-world numbers). New leaves under `mars.raider_cultures.*`: `inherit_chance`, `max_living`.
- **Map v2 COMPLETE on `map-v2` (12 commits ahead of master, tip `b80e985`; push to origin rejected by the pre-receive hook — branch is local, ready for Ryan's review/merge).** The whole arc, engine to frame:
  - **The debugging trail that made the composition true (each found by measuring, not guessing):** (1) biome fractions were shares of the map RECTANGLE, not the land — forests ate the world (53 Plain cells); land-share fix restored real provinces. (2) Fission settled UNREACHABLE ISLANDS (the area picker was reachability-blind) and the parent-lineage term alone held "routes" open across open sea — fixed with the walkable-colony rule (the exodus walks; the isles wait for boats) + no-land-path-no-road in roads_strength + routes close when an endpoint town dies. (3) The first cross-realm baseline (0.10) sat below the wary threshold — cross-realm pairs defaulted HOSTILE, and the exogamy gate (hostile ×0.02) cold-start-deadlocked the pass corridor; raised to 0.18 = default Wary, hostility earned by grievance only. (4) THE BIG ONE: ranges didn't reach the sea, so every wall could be walked around — all 38 towns one side, composition vacuous. Walls now run WATER TO WATER (destined-sea mask computed pre-carve; ridge ends extend into the sea as rocky headlands; foothill lift excluded from destined-sea so no collar re-opens the tip). (5) Passes were punched at the lowest saddles — which sat at the COASTS, gates onto the surf (one onto an empty pocket). Passes now punched POST-sea and PROVEN: opposite land flanks in DIFFERENT components of the sealed world, both ≥ 20 cells, else refused (fallback: one flanked gate per range so every named wall carries a way through).
  - **The chokepoint composition, re-proven world-scale (seed 42, 1000y, world_3region, merged tree):** the two walls + sea split the land into **4 sides holding 17/8/4/8 towns**; **16 cross-side open routes, ALL 16 threading a named pass, ZERO leaks** — the Wolf's Notch carries 7 roads, the Red Way 5, the Lonely Pass 5, the Last Gate 2 (measured by sealing pass cells, componentizing, and walking every exported route path). **Stance diverges across the wall**: same-side mean rank 1.39 vs cross-side 1.11; same-side runs friendly/allied (64 of 197), cross-side sits wary (425 of 465) with the pass-corridor pairs that built kinship going friendly/allied (39) — the gate is where cross-realm society flows. Settlement sanity: 37 towns, 0 on sea/wall.
  - **Cartography debugging (Playwright screenshot loop, every bug found by LOOKING):** marching-squares saddle cases were oriented opposite the other 14 (chains broke at every saddle — islands closed, the continent didn't); the exactly-0.5 corner degeneracy needed a SEAWARD epsilon (landward pushed the isoline through the map edge and an open chord bisected the continent); putImageData ignores clips (land pass now rides a scratch canvas); hillshade was lit from the SE (flipped to NW, warm on the lit slope); sea labels anchor at each sea's own DEEPEST water (a C-shaped bay's centroid is on shore); labels clamp inside the neatline; A* road staircases get collinear-simplify + Chaikin ×3; world zoom shows highways (strength ≥ 0.62), realm zoom every lane; out-of-world is sea, never void; **no playhead → the world AS IT STANDS at end-of-sim** (157 dead wizards' towers fall dark; the 158 ruins remain — the map wears the world's history, Malazan-style).
  - **The full playbook table on the FINAL merged tree (map-v2 + W6 + fate + the stitch):** build clean · validate-config **433 leaves** OK · lint PASS · **determinism ×2 both seeds** (42 → `ce016db7c9b8f605` twice, 7 → `1f37181156e5ff3d` twice) · **geography A/B** (off → no terrain block, 0 features, 55 towns on the flat partition — machinery inert) · **golden re-recorded + verified 8/8** · W6-on-the-world-grid sanity (archetypes derive: 18 trade hubs / 10 martial / 4 shrine towns; no raiders on this seed roll post-merge trajectories) · real-app Generate flow console-clean end to end (found+fixed en route: the Generation tab still posted `region_grid_width` — now world-grid fields; `parseFloat('')` NaN-override guard) · Playwright screenshots at all three zooms, console clean. **Perf row, honest: 13.2s wall / 11.1s sim / 1,029 tables (baseline 6.5s / ~961 tables) — 2.0× sim time carrying 2.2× population (42,627 vs 19,003 persons all-time, 37 vs 23 towns) on 17.6× more land (3,379 land tracts vs 192). Per-person cost DOWN; the growth is the sanctioned scale-up.** Tables +68 = value components only, zero fragmentation.
  - **Seams, stated:** Mars targeting stays terrain-blind Euclidean (raids cross walls the roads won't — the known v1 seam, untouched per scope); Mars reprisals remain region-scoped while desperation/graves targets went world-wide; wilderness field is founding-time static (the pale doesn't recede as colonies spread); offshore isles stay unsettled until the world has boats (the walkable-colony rule — a feature wearing a seam's name); beasts/wizard ranges are Euclidean cell radii rescaled ~×4 (config PROVISIONAL); the map harness (`viewer/maptest.html` + `tools`-side shot scripts) is dev-only and untracked.
- **Map v2 MERGED to master (Ryan: "let's merge to master please") — fast-forward, landed whole, PUSHED (`a838d99` + manifest chore `82467d1`).** The landing hit one snag worth recording: an early branch commit had accidentally swept `viewer/maptest-data.json` — a **223MB test export** — into history (`git add viewer` sweeping the harness data; untracked one commit later, but the blob rode the pack), and GitHub's 100MB cap rejected every push. ⚖ Ryan chose the in-place strip over squash-landing: `git filter-branch --index-filter` over the never-pushed range only (nothing published was rewritten — origin had rejected every push containing it), backup-tagged first, **final tree verified byte-identical** (`git diff` empty) and the blob gone from `rev-list --objects` before pushing. Post-push verification on the master checkout's own build: 433 leaves clean, checksum reproduced exactly (`ce016db7c9b8f605`), **golden verify 8/8**. Cleanup: `map-v2` + `map-geography` branches deleted (both fully in master's ancestry — v1 lives in the lineage as decided), scratch worktree removed, backup tag + `refs/original` dropped after verification. Lesson for the conventions pile: never `git add` a directory that holds generated data — the harness now has a `.gitignore` line, and the 223MB blob cost a history rewrite to undo. The raider-drift rebalance (queued behind this merge) is now unblocked.
- **Raider rebalance dispatched (Opus, main tree, master @ `82467d1`)** — the decided drift-+-hard-cap shape: `inherit_chance` (~0.35, pure-hash roll off the daughter's seed) + `max_living` (world-wide cap on living raider communities, id-sorted count at the inheritance site; a fallen raider town frees a slot), both under `mars.raider_cultures.*`/War. Tuning target measured not guessed: OFF / pre-fix ON / post-fix ON on the merged world (seed 42 + one more) — a standout dynasty of 2–4 living raider towns, raids notable-not-weather, falls in the tens not 158; `inherit_chance=1.0` + huge cap must reproduce the old plague (proving the levers are the only change). Full playbook table; golden re-record.
- **The next big push named (Ryan): THE END OF THE WORLD, then THE AGES.** Part I — engineered end states ("too many of the wrong technologies, too many vengeful gods at once, undead kill everything") driving the world into a simplified, smaller state where everyone is dead. Part II — stacked eras in one world file, apocalypse transitions between, much state inherited: more time for Works and ruins to accumulate; different technologies/levels/whole categories per era, leaving marks — the found weapon the current era cannot forge, the lost spell, runes with impossible architecture. Assessment developed together: **an apocalypse is containment losing** (the wide-world's immune loops — Keepers/expeditions/hunts/reprisals — overwhelmed past thresholds; W6's raider metastasis was the accidental proof of concept, patched as a bug — this push makes world-death authored physics); the three dooms read off EXISTING ledgers (TechSign imbalance · deep-rajas concurrence · undead:living ratio); the dead world is a product, not a failure screen (the richest adventure site the engine can produce); **an era transition is the epoch chain wearing a crown** — the turning as an engine-authored perturbation, Saturn's second creation act over used ground; the residue function = "what does an age leave behind" (land, layered ruins — the Malazan element earned honestly, cross-era Work provenance, lingering dragons/undead, bloodlines through the fire, chronicle→legend); **lost arts need no mechanism**: coin hands dealt per era + (era, coin, level) stamps on Works/Sites — a thing is lost when its stamp falls outside the current hand. ⚖ Ryan to every open axis: **"yes, and…" — always configurable**: successors are remnants OR the same race respawned OR **a different race entirely** ("era of the Jaghut, era of men; era of elves, era of men" — no forced order, men need not come last, high tech need not follow low); gods carry across the turning or open fresh; fire and the quiet fade both in the endings palette. **Contract authored: `docs/research/2026-07-12-the-end-of-the-world.md`** with the wave table — E1 doom ledger (byte-inert) · E2 cascades + seals (mortal/sealed worlds; sealed = byte-identical to today) · E3 the terminal event + dead-world export · E4 the turning spine (`eras[]`; era-1-only run byte-identical to a plain run) · E5 the peoples registry · E6 the hands and the stamps. Train runs behind the raider rebalance.
- **THE NIGHT ORDERS (Ryan, heading to sleep): five pushes, fully autonomous, blanket commit approval for the session, "a lot of work done tonight," Sonnet/Opus delegation, no check-ins between stages.** Push 1 = the End (E1–E3). Push 2 = the Turning (E4–E6), proceed when ready. **Push 3 = the interconnection review**: survey the last few days' additions and better interconnect them — "we want our gains to compound, for our features to have synergy… how can we use what we already have to create a more rich and emergent system?" **Push 4 = another content phase**: look at the SHAPES added (the capability for types of content) and add significantly more — vampires, machines, magic fleshed out significantly, combat fleshed out significantly, "and a lot of other stuff we can think of. Get creative." **Push 5 = the tool review**: the project re-examined as the thing it pivoted to — a tool for Ryan+Luna to DM games for players human or LLM; the in-session toolset, and a sturdy gap-documentation system applied iteratively between games/campaigns; named gaps to start: combat mechanics, skill checks, richer town maps and town content. Luna's stated interpretations (vetoable in the morning): "machines" = constructs/automata — an Age of Machines is what coin categories were built for, a dormant construct in a ruin is a Work whose art is lost; "combat" split = sim-depth in Push 4 (how fights resolve, wounds, weapons-as-Works mattering), table-facing mechanics in Push 5 (initiative, checks, the PC in danger).
- **Raider rebalance landed + committed (`b94cdfc`, pushed) — drift + hard cap, the plague patched into a dynasty.** Two new leaves under `mars.raider_cultures.*` (War coin, Earth): **`inherit_chance` (0.35)** — a raider parent's fission daughter is raised to the custom only on a pure-hash roll seeded from the daughter's OWN community seed + a fixed salt (`0x0D21F7`, no shared RNG stream touched — drift, not destiny); **`max_living` (2)** — a world-wide cap on LIVING raider communities, counted at the inheritance site (a plain sum — order-independent, so deterministic without a sort, matching the sibling `community_count` walk). ⚖ **When the cap is full the drift roll is skipped entirely** (the cheaper deterministic shape — no hash spent); a fallen raider town frees a slot, so a plague is structurally impossible. World-gen seeding (`max_per_world`) untouched. The old unconditional `RaiderCulture` copy in `FissionSystem::found_new_community` is now gated on `src_rc->is_raider` + the cap + the roll.
  - **Tuning measured, not guessed (seed 42 / 8 / 99, 1000y world_3region, OFF / pre-fix ON / post-fix ON).** The pre-fix lever (`inherit_chance=1.0` + huge `max_living`) **reproduces the map-v2 plague to the digit** — seed 42: **2,561 raids (1,957 plunder + 556 reprisal) / 158 falls / 6 raiders** — proving the two leaves are the *only* change to the mechanism. OFF = the pre-W6 shape (seed 42: **54 raids / 4 falls / 0 raiders**). ⚖ **`max_living` tuned to 2, not the mooted 3–4:** seed 99 carries TWO world-gen founders (`max_per_world=2`), so a cap of 3 admits a drift daughter and climbs back to a residual plague (**1,780 raids / 161 falls** — worse than the reference 158); cap 2 holds it at **1,217 / 95**. Post-fix, all three raider-bearing seeds converge to the wanted texture: **seed 42 = 1,118 raids / 72 falls / 2 living; seed 8 = 615 / 49 / 2-ever-1-living (Saerholm rose at y4, fell y23 — a dynasty that rose and fell); seed 99 = 1,217 / 95 / 2 living.** A standout antagonist of 1–2 living raider towns with teeth, raids ~1/yr not 2.5/yr, falls in the tens — not weather, not a plague. `inherit_chance` stays a live lever on the common single-founder seeds (42/8, where drift adds or doesn't add the second town); on the rarer two-founder seed (99) the cap governs and drift is moot.
  - **Playbook table, all rows.** Build clean (Release); `lint_content.py` PASS; **validate-config 435 leaves OK** (the 2 new leaves Earth-scoped under War; a bad-typed value throws loud — `parse_number_failure` caret → exit 1). **Determinism ×2** identical (`22019a9838abaf87` twice, seed 42 post-fix; distinct seeds differ). **A/B** as above (OFF = pre-slice results; `enabled=false` inert — its checksum differs only by the two new config-serialization leaves, results byte-for-byte the pre-W6 shape). **No-faceless unchanged** — the fix emits NO events (drift is a creation-time seeding decision), 0 anonymous across 56,636 combatant rows / 3,249 named violence deaths. **Soak** 1000y no crash, counts above. **Golden** verify → 0/8 moved (two new config leaves shift every checksum + real trajectory moves on raider seeds; greenfield) → **re-recorded → 8/8 PASS** (baselines + record/verify manifests in the commit). **Perf** in family — seed 42 1000y world_3region **sim 10.5s / 1,016 tables** vs the post-map-v2 baseline ~11.1s / 1,029; RaiderCulture was already a sentinel, so zero new component, zero fragmentation. Honest seam: the residual raid magnitude (~1k/millennium at 2 towns) is set by the *existing* `raid_chance` (0.10/season), not the two new leaves — "a few hundred" is only reachable at 1 town, which would forfeit the dynasty; shipped 2 as the floor of "2–4 living towns" with the plague structurally sealed.
- **E1 — The doom ledger landed + committed (`86f135c`, pushed; golden re-recorded + verified 8/8) — Push 1 opens.** The three world-pressure dooms, each read off a ledger the engine already keeps, and — the whole contract — **DERIVED at export from the assembled events + final entities, never recorded at sim time.** No component registered, no entity created for the ledger, so raw flecs ids don't shift: the fate-omens lesson applied one level cleaner (that slice had to buffer NEW records on a singleton; E1 records nothing — the history the 4D views already reconstruct yields all three). ⚖ `derive_doom()` sits beside `annotate_archetypes` as a post-pass over `ExportData`, writing only `data.doom`, folded into the checksum. Yearly series (1000 points/millennium) + final + all-time-peak-with-year, per doom:
  - **wrong_arts** — net dark = cumulative destructive − mitigating strikes (the TechSign on every Discovery), a monotone step per strike.
  - **wrath_concurrence** — gods in rajas at once, reconstructed from DispositionShift episodes (each mood-1 event spans `[start, start+duration)`; a deity holds one mood at a time so per-deity ±1 edges never double-count). Granularity seam stated: mood-level, not charge-depth ("deep-rajas" collapses to rajas — the record's exported granularity, exact for the concurrence count); a world *configured* to open a god in rajas before its first shift would under-count until that shift (default worlds open sattva → exact).
  - **dead_ratio** — undead : living, both counted by interval deltas (same alive test the top-bar stats use).
- **THE INERTNESS GATE — byte-wise A/B, passed hard.** Pre-slice `b94cdfc` vs post-slice export, same seed+config (42, 1000y, world_3region): strip `doom` + `content_checksum` + `performance`, canonicalize, sha256 → **`cb61019fa9c8293f` on both**; 45,093 entities and 133,742 events identical; the ONLY addition is the `doom` block. Trajectory untouched, proven at the byte.
- **The doom history reads true (soak, seed 42 1000y world_3region).** wrong_arts climbs from the **first necromancy strike (~Y63)** to a final net-dark **+71** (72 destructive vs 1 mitigating — this world's arts ran almost purely dark); dead_ratio first stirs at **~Y77, dated to necromancer emergence**, peaks **1.05% @ Y222** (the undead surge Y216–220 in the raw record) and returns to 0 as expeditions fell them (final undead 0, living 3,353 — containment HELD, exactly what E1 must show without steering); wrath_concurrence hits **3 gods in rajas at once @ Y751** — a real "three gods burn" moment out of only 4 shift episodes. Every metric traces to a named cause in the world's own history.
- **Surfaced in all three clients.** Export: the `doom` block (checksum-folded). **Viewer**: a `Doom` toolbar button opens a bottom-drawer panel (new `doom.js`, sibling to Community/Minimap, mutual-exclusion wired) — three SVG sparklines that track the playhead with a marker + the value AT that year + a plain reading; screenshot-verified via Playwright on a generated world (the drawer moving to Y221 read *"the arts run dark / two gods burn / the dead walk few"* live), console clean. **skim**: a doom tail — silent on a calm world (40y seeds 42–45: nothing), naming what rises on a dying one (300y seed 42: *"gods have co-raged"*; the 1000y 3-region world: *"the arts run dark (72d/1m); three gods have burned at once"*). Windows gotcha caught in test: a `‖`/en-dash separator crashed the cp1252 console — swapped to ASCII.
  - **Playbook table, all rows.** Build clean (Release); `lint_content.py` PASS; **validate-config 435 leaves** (unchanged — E1 added no config leaves and no EventTypes; it measures what exists). **Determinism ×2** identical (`8b3bc63321916faf` twice, seed 42 post-slice; distinct seeds/worlds differ). **Golden** verify → 0/8 (the doom block reshapes every checksum; greenfield) → **re-recorded → 8/8 PASS** (baselines + record/verify manifests in the commit). **Perf**: the derivation is `O(entities+events+years)` — collect went 394.3ms → 397.0ms on the 1000y 3-region export (~3ms, within noise), sim time unchanged (10.4s), zero new flecs tables (no components). Milliseconds, not seconds, as required.
  - **Seams, stated:** wrath granularity is mood-level (charge magnitude isn't in the record — noted above); the sample grid is year-END boundaries (a rajas episode that opens and closes inside one year between samples could be missed, though episodes run multi-season so yearly sampling is sound); dead_ratio uses the person `end_tick` alive test (inclusive, matching the top-bar stats); the natural seed-42 world never approaches dead_ratio 1.0 — E1 only *measures* the slope, E2's cascades/seals will drive it past the thresholds.

- **E2 — Cascades and the seals landed + committed (`23ffb71`, pushed; golden re-recorded + verified 8/8) — the world becomes mortal.** E1 measured the three dooms; E2 lets them **steer**, wired through the EXISTING containment machinery with **no new registered component** (that would shift every raw flecs id and break the seal) — the whole slice's sim-time state is **one field** (`net_dark` on the already-registered `NecromancerState`) plus two live reads (rajas count across the four gods; undead:living census each season). The three cascades, each a threshold past which the existing loop starts LOSING (a slope, not a switch):
  - **The wrong arts (Mercury).** Past a net-dark strike imbalance the arts spiral: the grief bar drops (`eff_grief = grief_threshold / tilt`), the bolt fires more readily (strike chance tilts up), and the Open-Eye ward is forgotten a growing fraction of the time (a seeded wither roll bypasses the rod). Steered on the live `net_dark` counter (destructive − mitigating Discoveries, the same quantity E1 derives). Darkness begets darkness: each landed strike lifts net_dark → lifts the tilt → lands more. **The grief bar was the real lever** — a first pass tilting only strike-chance produced FEWER strikes (throughput is grief/candidate-bound, not slot-bound — a crank test proved 72→459 when the grief bar drops); adding the grief-threshold drop gave the runaway.
  - **The wrath concurrence (all four gods).** When `gods_threshold` burn in rajas at once, the return to calm slows (the whole decay pull — `charge.decay` AND the shrine sattva-ward, load-bearing in reverse — suppressed) and the world-signal gain that drives rajas is amplified, so every god stays deeper and longer in the expression it already runs. **Four-god parity for free**: one edit in the shared `update_disposition` law, every god runs it. No Zeus-instrument gap to fill — the shared substrate carries all four.
  - **The dead outnumber the living (Mercury).** Past a dead:living ratio the raising quickens (cadence gate bypassed, per-event budget grows with the imbalance); containment is not switched off but **OVERWHELMED** — expeditions ride living communities (party = fraction of living able adults), so as the world empties the containment shrinks on its own. Divide-by-zero guarded (living 0 with undead present → ratio fully past threshold, never a divide).
  - **The seals.** `world.doom.*` seated under Saturn's substrate (coin 0, the `fate`/`geography`/`bloodlines` precedent — the world's mortality is substrate, whoever triggers it), prefix-bound `doom_owner`, Heaven-scoped. **Per-doom `sealed` defaults TRUE** — an unconfigured world is immortal. New `src/config/DoomConfig.hpp` (13 leaves).
  - **Voicing the slope.** Two new EventTypes `DoomCascade`/`DoomRelent`, DERIVED post-sim from the ledger with hysteresis (recede band 0.6× — a crossing is a turn, not a flicker), **synthesized only for an UNSEALED doom** (the Foretelling idiom — no live entity, byte-inert), Primary = the World deity (no-faceless). Colours (apocalypse-red / slate relief) + chronicle handlers land with the slice.
- **THE SEAL GATE — sealed world byte-identical to pre-slice master at 1000y.** Built the pre-slice exe (`86f135c`) in a throwaway worktree; pre-slice vs sealed, seed 42 1000y world_3region, strip checksum+perf, canonicalize, sha256 → **`72cad07a27dfa1bd` on both**; **45,093 entities / 133,742 events identical** (E1's own inertness numbers, reproduced). 100y ×2 seeds (42, 7) also byte-identical. The `&&` short-circuit means a sealed world never even calls the concurrence count or the ratio census — zero overhead, zero behaviour.
- **THE CASCADE GATE — each end reachable, measurable, deterministic (seed 42, 1000y world_3region), on E1's OWN ledger.** Driven configs added (`config/doom_arts.json` · `doom_wrath.json` · `doom_undead.json` + `doom_undead_mercury.json` companion):
  - **wrong_arts:** sealed net-dark climbs 3→8→14→30→**71**; driven steepens 3→13→87→180→**330** (4.6×) after the year-162 onset. The slope visibly diverges past the crossing.
  - **wrath_concurrence:** sealed sits at **2 gods (971 of 1000 years)**, touches 3 for only 4y; driven **locks at 3–4 gods burning together — 527y at 3, 257y at 4, peak all four** — after the year-25 onset. The gods stoke each other and will not come down.
  - **dead_ratio:** sealed peak **0.010** (undead horde-capped at 24 vs 3,353 living, 107 expeditions/57 repelled); driven peak **0.262**, **796 expeditions with 722 REPELLED (91% failure vs the sealed 53%)**, **22,815 undead raised** (vs 195), 184 falls (vs 72). Containment overwhelmed, mortals fighting all the way down — the ledger oscillates as each necromancer falls and the horde releases, the peaks climbing (7 cascade episodes past the 0.1 threshold where sealed never clears 0.01). Determinism ×2 on the driven wrath config identical (`34169cd082b2e8f3`).
  - **Robustness:** an all-three-unsealed extreme config + a single-community collapse both soaked 1000y with no crash / no divide-by-zero (1com dead_ratio peaked 0.892 — undead nearly outnumbering the living — and survived).
- **Chronicle, rendered by the real chronicle.js (viewer driven via the drop path, modules imported, person chronicles rendered):**
  - *"In the 25th year of the world, the gods burned in wrath together and would not be calmed — no shrine could cool them faster than they stoked one another."*
  - *"In the 162th year of the world, the arts ran dark past mending — the wards were forgotten faster than they were found, and darkness begot darkness."*
  - *"In the 125th year of the world, the dead began to outnumber the living — the graves filled faster than the living could put them back down."* / (recede) *"…the living held the line against the dead once more, and the graves stayed quiet."*
- **Playbook table, all rows.** Build clean (Release); `lint_content.py` PASS (DoomCascade/DoomRelent export+colour+chronicle all OK); **validate-config 448 leaves OK** (13 new `world.doom.*` all Heaven under coin 0; a bad-typed value throws loud — `parse_number_failure` caret → exit 1). Determinism ×2 identical. Seal + cascade gates above. **Golden** verify → 0/8 (the config now serialises the doom leaves → every checksum shifts by the config-serialisation delta only, results byte-identical per the seal proof; the raider-fix precedent) → **re-recorded → 8/8 PASS** (baselines + record/verify manifests in the commit). **Perf**: sealed 1000y world_3region **sim 10.5s** (baseline ~10.4s), in family; the doom reads are a per-season O(gods)+O(communities) census gated behind `!sealed`, off entirely on a default world.
- **Seams, stated:** the wrong-arts runaway is grief-throughput-driven (the wizard slot cap of 3 is untouched — a genuinely slot-bound world would need the multi-slot refactor E2 deliberately skipped); the driven wrath config uses `gods_threshold=2` to provoke early (the struct default stays the canon 3 — "three at once is the end"); the dead cascade's honest texture is an OSCILLATING ledger (the single-necromancer horde releases on his death and rebuilds — containment keeps winning battles while losing the war), not a monotone climb — reaching a sustained dead>living would want either multi-necromancer support or undead that outlive their binder, both E3+ territory; the crossing events are post-hoc (a doom that opens and closes between year-end samples could be missed, as with E1's sampling); the driven dead demo needs the companion mercury config (horde_cap/corpse-window/grave-raid cranked) since a default necromancer can never out-raise 3,353 living.

- **E3 — The terminal event landed + committed (`0f361d1`, pushed; golden re-recorded + verified 8/8) — Push 1 (the End) closes.** The world's death is now an event with a cause and a named age, DERIVED at export from the assembled ledger (byte-inert, the E1/E2 idiom carried to its conclusion — no component registered, no entity created, so raw ids never shift). A new `terminus` block + a `WorldEnded` EventType (full treatment: export name, viewer colour `#3B0A0A`, chronicle handler; lint enforces). `derive_terminus()` sits beside `derive_doom` as a post-pass over `ExportData` — census the living per year, find the last death, read the dominant unsealed doom's engagement (value/threshold) over the death's final years, seed the age name from a per-cause bank.
  - **The three design calls, all consulted with May (Saturn — the tomb-threshold, the whole arc) and endorsed.** (1) **The epilogue is the horizon, not a hard stop, no config knob**: the sim already runs to its stated end regardless of population (an emptying world only ticks *cheaper*), so the post-death years ARE the quiet epilogue — May: "Death is a threshold, not a stop. The world does not end when the last breath goes; it empties… let it run quiet to the horizon. The silence after is data." (2) **Age naming: unnamed until earned** — the name belongs to the death, not the birth; a world that never cascaded and never died carries no `terminus` at all (May: "leave the stone blank. Blank is honest."); sealed at death, never revised. (3) **The guttering names no killer** — a death with no author states the plain fact ("the last hearth guttered out, its last town falling to no one in particular"), not an invented villain. Age banks drawn from May's own word-list (womb-and-tomb register): undead → *the Age of Crows / Ash / the Unburied*; wrong-arts → *the Unmade / the Hollowing / the Sundered Ward*; wrath → *the Burning / the Devouring / the Reaping*; guttered → *the Long Grey / the Quiet Emptying / the Last Hearth*.
  - **THE KEY FINDING — only the MONOTONIC doom can be a slow death's cause.** wrong_arts is cumulative net-dark strikes (they never un-happen), so it reads its accumulated value even at the horizon when two survivors remain — it is *guaranteed* cascading at the death if it ever crossed. The reactive dooms (wrath, dead) cascade in the world's populous MIDDLE and RECEDE as it quiets: a necromancer's horde towers over the last few living (dead_ratio spikes ~Y42–70), then releases when he dies *centuries before* the final human, so "the dead outnumbered the living" cannot be the death-*year* cause of a guttering — exactly the seam E2 flagged ("undead that outlive their binder, E3+ territory"), now confirmed at the byte. This makes wrong_arts the reliable doom-caused death and gives the guttering its own honest first-class ending.
  - **THE SEAL GATE — at its strongest yet.** The `terminus` is folded into the checksum ONLY when present, and E3 adds *no* config leaves — so a surviving sealed world is **byte-identical** to pre-slice master (seed 42 1000y: `034f95bc17e1ee00` on pre-slice `23ffb71` and post-slice alike; not merely canonical-equal — the whole digest). A naturally-dying default world (seed 31337, a fragile 13-soul world that gutters by Y75 under *default* config) has an **identical engine trajectory** (strip the derived overlays, canonicalize → `8058c8de9685f584` pre==post, 63 entities / 20 events), with only the derived terminal overlay added — E3 doing exactly its job, not steering. Golden 8/8 (only seed31337 moved, gaining its terminus; the other 7 identical).
  - **THE DEATH GATE — two full deaths, distinct cause + distinct age (`config/dying_*`).** The E2 configs don't kill (undead are free *labor* — a food boon that makes the necro city the LAST to fall; wrath maxed all four gods for ~975y and the world lived) — expected, and the task's licence to strengthen. What it took: a below-replacement birth (0.10) + short lifespans (28–40y, the churn) so the world establishes then the deaths outrun the births; undead *labor OFF* so the necromancy boom can't sustain it. **Death A — wrong_arts → "the Hollowing", died Y156** (net-dark 4, engagement 4×, an 844-year epilogue), deterministic ×2 (`dbaa71b50d149239`; seed 7 differs). **Death B — the SAME world with the seal back on → guttered "the Quiet Emptying"** (`6f4f0ca0f1546c12`): identical population dynamics, wrong_arts sealed → no cause → the honest guttering. One world, one seal flipped, two causes, two age-banks — the machinery differentiates.
  - **THE PRODUCT GATE — the dead export is a navigable product.** The Hollowing world (Playwright, headless): loads with **zero console errors**; top bar reads "0 people, 0 communities, 201 deaths"; the timeline wears its ruins ("the ruins of Surnholm", "the ruins of the Leaning Tower of Mernhae"), the ownerless Work "Grilwae" spans to the horizon, beings render. Census: **13 ruins, 10 fallen communities, 3 Works — all ownerless with custody chains intact** (forged → inherited through named holders → `came_to_rest` at holder 0 in the ruin where the last hand died), **905 beast entities (9 still walking the empty world at the horizon), 140 undead recorded** (0 walking — released when their necromancers fell, honest: a wrong-arts death leaves ruins and beasts, not a standing horde). The **doom drawer surfaces the terminus banner** in red ("The world ended — the Hollowing / Year 156: the arts had run past all mending / 844 quiet years followed…"); the **chronicle renders the final chapter** ("*And so the world ended, in the 156th year — the Hollowing, when the arts had run past all mending. After it the record keeps only the quiet: the ruins standing, the dead walking where they would, and no living hand to write the years down.*"); **skim announces the dead world FIRST** ("DEAD — ended year 156, the Hollowing, the arts had run past all mending"), so a DM shopping seeds can find them.
  - **THE DOOM-AWARE OMENS — presentiment, render-side, byte-inert.** A Keeper's *existing* omen reading that falls inside a doom's cascade window now speaks the rising doom (barometer of present state, tier-3 future-knowledge stays parked). Verified on the E2 wrath world (3,177 omens inside the cascade window): "*…read the shape of a throne in the thunderheads over Nosdale, and said the god Ruler had gone heavy and distant — and in it, the wrath of the gods rising in one another, past all cooling.*" No new events (no parallel narration); sealed worlds have no windows, untouched.
  - **Playbook table, all rows.** Build clean (Release); `lint_content.py` PASS (WorldEnded OK on export/colour/chronicle); **validate-config unchanged** (E3 added no config leaves); **determinism ×2** identical on the dying world; **golden 8/8**; **perf** — E3 is export-only (the terminus derivation is O(entities+years), milliseconds), and the *dying* world is **16× cheaper** than the sealed one (277ms vs 4418ms total, 616 vs 717 flecs tables — an emptying world gets cheaper, as the push promised); sealed 1000y sits at 4.4s in family, no regression.
  - **Seams, stated:** the dead-outnumber ("Age of Crows") death is NOT landable as a death-*year* cause via the current release mechanic (horde falls with its necromancer centuries before the last human) — it remains a real, demonstrable *cascade* (E2), just not a terminal *cause*; a wrong-arts death leaves no walking undead at the horizon (same reason); beasts-at-horizon are thin on a small dying world (9) — proportionate, not a bug; the cause is read over a 5-year window ending at the death to catch a reactive doom's terminal crest, so a doom that spikes and fully recedes >5y before the end won't be named (the guttering catches it); the two proven deaths share one substrate (birth/lifespan) and differ only by the seal — a clean A/B, but both are wrong-arts-adjacent worlds, so wrath/dead as terminal *causes* stay unproven-as-death (argued from the machinery, not shown).
- **Push 1 CLOSED (E1 `86f135c` · E2 `23ffb71` · E3 `0f361d1`). E4 (the turning spine) dispatched — Push 2 open.** Concurrently, **Push 3's two read-only surveys returned** (Sonnet): the ENGINE FEATURE GRAPH (every mechanism's reads/writes/consumers; islands: venues — the code's own comment calls them "a scene generator" yet nothing mechanical reads them; tower emissions never taint standing; the wilderness field has one consumer — plus 15 ranked candidate edges and four asymmetries, the deepest being *grievance has many doors in and one slow door out* and *divine favor flows down but renown never beckons Heaven back*) and the SURFACE MAP (buried treasure: deities have NO inspect panel in a god-simulator; dragons/shrines/towers/undead have no viewer address; Discovery events narrated nowhere; the PC context omits the PC's own calling, their town's archetype/feuds/shrine/roads, and the last omen read at its altar; 12 lever gaps a DM cannot cast — raid/plague/appoint/awaken/create_being — handed to Push 5).
- **Push 3 contract authored + committed (`485a38e`): `docs/research/2026-07-12-the-weave.md`.** The principle: emergence lives in CLOSED LOOPS, not new nouns — every slice is an edge between mechanisms that already run, or a window onto data that already exports. Six slices: **S1 War & Renown** (raider reputation precedes the first raid; prestige-aware raid targeting; raiders loot Works via the dragon's carry-off idiom; Mars raids towers) · **S2 Blood & Blessing** (the blood tilts its bearer's own calling roll; Mars's line runs truer among raiders; Evergreen blunts plague; RENOWN BECKONS HEAVEN — deeds weight the divine-blood conception roll, and providence finally sees shrines) · **S3 The Living Road** (venues become mechanical: journey co-presence seeds the marriage pool — the road network writes kinship writes stance writes war; journeys read the danger pale; being territory taxes routes and marches) · **S4 Grievance & Grace** (wizard emissions taint the exile's home; siege-stress feeds Adept grief — the evil city the war MADE; reconciliation events give feuds a real exit) · **S5 The Faces of Heaven** (deity drill-down, addresses for the nameless hosts, Discovery narrated, Mars's missing weather voice) · **S6 The Knowing Player** (the PC context learns what any resident knows). S1–S4 Opus engine slices, S5–S6 Sonnet surface slices; the acceptance test per edge = the story it enables actually appearing in a soak world's chronicle.

- **E4 — The turning spine landed + committed (Magnus3 `4f1f57d`, master; golden re-recorded + verified 8/8) — Push 2 (the Ages) opens.** A world config gains `world.eras[]` — a list of turnings (a `boundary_year` + a `succession` mode), N turnings → N+1 ages, empty by default = today's single ageless world. At each boundary the new `src/core/EraDirector.hpp` runs THE TURNING **in memory, between ticks** — the pause-then-act idiom generalised from the Perturbation: `Simulation::run` now takes a SORTED pause schedule + `on_pause(T)` (the single-pause perturbation became a one-element list; the null default is unchanged), and `ExportCommand` feeds it the season-snapped era boundaries. One seed, one run, one continuous deterministic trajectory end to end.
  - **The design's spine — no new registered component, so the null gate holds by construction.** The turning creates entities AT the boundary (new founders — sanctioned, the post-turning zone is new by design) but registers NOTHING: the Turning event is EXPORT-SYNTHESISED (like `WorldEnded`/`DoomCascade` — no live entity, the pre-boundary trajectory untouched), the `eras` export block is DERIVED at collect from the EraDirector's out-of-band turning record + the doom ledger + the terminus (the E1/E2/E3 idiom), and the only new enum values (`DeathCause::Cataclysm`, `EventType::Turning`) are inert unless a turning fires. `register_components()` is untouched — the one thing that would shift every raw flecs id.
  - **The residue function v1 — what an age leaves behind, mostly for FREE through the existing paths.** The land is immutable (same map across ages — the payoff). **Sites** fall through the EXISTING fall path: killing a town's people at T ends their Life events at T, and the town is ruled a Ruin dated to the turning — layered ages of ruins. **Works** cross the boundary unbroken with zero new code: a fallen town's Works `came_to_rest` in its ruin (the E3 custody idiom), a survivor's stay held. **Beings** carry over (the turning kills only PERSONS — a bear that remembers the last age; undead follow their binder rules — a binder felled in the turning releases its dead). The whole prior-age record (people, events, chronicle) stays.
  - **Succession v1 (same race — peoples are E5).** **remnant** — the survivors ARE the next age: `survival_fraction` of the living communities carry (by living population, ties by id), their people/families/bloodlines/leaders firsthand; the rest fall into the layered ruins. **respawn** — Saturn's second creation act: everyone alive dies of Cataclysm (the turning IS the cataclysm — a 762-death spike all at year 50 in the 1000y soak), their towns fall, and NEW founder communities are seeded worldgen-style (a per-era SALTED seed, so era 2 is a distinct people, not a copy) on the same map. The fallen towns' high-fertility ground is FREED so the founders settle it — **the town built on the bones of the old** (verified: 4 of 9 era-2 towns founded ON an old ruin's tract). **Dead-world-at-boundary decided + documented:** remnant on an already-empty world degrades to NOTHING (the age turns and is named, but nothing living carries — the dead stay dead); respawn always reseeds (dead or alive — a dead world can begin again). **Gods v1 CARRY** (dispositions/Theogonus untouched at the turning — continuity; the fresh-theogony option parked for E6's hands).
  - **Age accounting.** Each age is named by what ENDED it — a doom in cascade at its boundary lends its register (reusing `terminus_age_name`, seeded per-age so each names apart), else a turning-flavoured name keyed to succession (respawn → *the Age of the Broken Wheel / the Sundering / the Buried Kings*; remnant → *the Thinning / the Long Winnowing / the Few*). The `eras[]` export block carries every age's absolute-tick span + sealed name + census (fell/founded/carried/lost); the final age ends per E3 (terminus, or unnamed-if-uneventful). Clients keep absolute ticks and render era-relative years off the table.
  - **THE NULL GATE — no-eras byte-identical to pre-slice master (`0f361d1`), the strongest form.** Built the pre-slice exe in a throwaway worktree; default config, seed 42 100y, strip `content_checksum` (the config-serialisation delta — `world.eras` now serialises) + `performance`, canonicalise → **byte-identical, 587 entities / 980 events**, no `eras` key on either side. **Gate 4b:** a two-era run's PRE-boundary record matches the plain run's same years exactly — **1,064 pre-boundary entities and 1,193 fully-pre-boundary events identical** (the turning acts only AT T; the boundary stillbirths of gestating unborn are stamped at T, never pre-boundary, so nothing before the wall moves).
  - **THE RESIDUE GATE, item by item on a real two-era respawn world (seed 42, 100y, boundary Y50).** **35 named era-1 ruins dated exactly to the turning** ("the ruins of Surnholm", …). A **Work chain crossing unbroken**: *Murnrei* (a loom) forged Y26 → inherited Y38 (Ruthfros → Bountfurn Tihorddroth) → `came_to_rest` at Y50, the turning. **Long-lived beings alive in both ages** (bear Crishide [38y..57y], lions across the wall). **Era-2 founders correctly shaped** — 9 new communities, 4 on old ground; a remnant world instead shows **594 era-2 births whose parent was born pre-turning** (firsthand descent traceable). **908 era-1 people preserved.** **Both modes demonstrated** (one world each) + a **three-era smoke** (turnings Y34 respawn → Y67 remnant → open; era table shows three ages, both boundaries chain).
  - **THE FALL-PATH FIX (the one real engine wrestle, stated honestly).** The engine has a PRE-EXISTING quirk — an emptied town can keep a deathless projected Life event and never formally "fall" (20 of 36 communities in a *plain* world_3region carry deathless ends). A cataclysm kill takes the population cache's death fast-path, which never re-derives the community lifespan, so the fall guard deferred the ruin forever; and even forcing the span at the turning, continued-sim bookkeeping re-inflated it. Two-part resolution: (1) the EraDirector settles families + community to T and rules the empty town a Ruin **through `sites_fall` directly** (still the existing fall function, just triggered by the turning, marked ruined so the season scan skips it), AND (2) an export-time clamp — a community with a Fall event reads as ended at its fall — which is a **no-op for a plain world** (a town that falls already ends at its fall tick; plain checksum unmoved, `8692f944…`), correcting only turning-felled towns. Result: 35/35 fallers ruin at the boundary, no phantom-living ruins.
  - **Chain determinism ×2** identical (respawn seed 42 → `69ee1889c9b7d3f8` twice; seed 7 differs). **No-faceless** — every Turning event carries the World deity (Primary). **Soak** — a two-era 1000y run, sane, no crash at the boundary: age 1 fell 35 / founded 9 / lost 747, era 2 recovered over 950 years to pop 5,182 (57,768 entities, 173,733 events). **Golden** verify → 0/8 (config-serialisation delta only, results byte-identical per the null gate) → **re-recorded → 8/8 PASS** (baselines + manifests in the commit). **Perf** — a no-eras 1000y world_3region sits at **sim 10.5s / 1,016 tables** (in family); the turning's apply is a one-time sub-second cost (35 kills + 9 foundings); a respawn world costs more overall (20.2s) only because era 2 is a fuller LIVING world (more community-seasons), not turning overhead. `lint_content.py` PASS · `validate-config` clean (the one new leaf `world.eras`, Heaven-scoped under coin 0).
  - **The chronicle voices the turning (the single heaviest line in a multi-age world), rendered by the real chronicle.js (Playwright, viewer drop path, console clean):** *"And so the First Age ended, in the 40th year of the world — the Age of the Broken Wheel. The living were swept away — 57 souls — and 5 towns fell to ruin; and over their bones 2 new settlements were raised — a people who would find the world before them only in the ground beneath their feet."* / (remnant) *"And so the Second Age ended… — the Thinning. 10 towns fell, but 10 carried through the turning, and their kin remembered the age before as the elders' own firsthand tale."* / (a cataclysm death) *"The turning of the age took Gurmfin Kurscanfrourn, in the Age of the Broken Wheel at 34 years old — swept out with the world that had been."* The **timeline walls each age** with an amber turning-line + the ended age's name-pill (screenshotted: two walls, "the Age of the Broken Wheel" / "the Thinning"); **skim announces the age FIRST** — *"Second Age, year 50; the First ended in the Age of the Broken Wheel."*
  - **Seams, stated:** the deathless-community quirk is pre-existing and untouched for towns the turning does not fell (an empty-but-not-fallen plain-world town still carries a deathless end — E4 only settles the ones it fells); the boundary stillbirth of a gestating unborn exists for a single tick at T (swept in the womb — the alternative, letting an in-flight birth land in the emptied town, repopulated it); a `uint32` tick horizon caps the run at ~46 millennium-ages (not a realistic config); `perturb` + `eras` on one `continue` run merge their pause schedules but have not been exercised together (eras drives the plain `export` path — the common case); demo configs `config/ages_{respawn,remnant,three}.json` ship as worked examples (not in the golden matrix). E5 (the peoples — different-race succession + a Peoples registry) and E6 (the hands and stamps — coin hands per era, lost-art derivation) remain; E4 architected neither out (succession is a string, the gods'-carry is a v1 default with the fresh-theogony seat noted).

- **E5 — The peoples landed + committed (Magnus3 `aa50632`, master; golden re-recorded + verified 8/8) — Push 2, wave two.** A world config gains `world.peoples[]` (a light registry — each folk = a NAMING VOICE + modifiers on dials that already exist, no new mechanic) and `world.first_people`; each `world.eras[]` turning gains a `people` key naming the successor folk. **A world that names no people is byte-identical to a pre-E5 one** — one implicit "Men", no voice, neutral modifiers. Ships an ORIGINAL default bank (`config/ages_peoples.json`, 4 folk in Magnus3's own register): **the Elder Folk** (long-lived ×2.2, slow to bear ×0.55, lean Keeper/Adept; a liquid l/r/n/th tongue — *Naelaseleiel, Silarintheolas, Aelidor, Seirloth*), **the Deep Folk** (sturdy ×1.5, Maker/Blade; a hard guttural bank — *khaz-/gron-/-grum/-delve*), **the Small Folk** (teeming ×1.6, homely; *-low/-well/-burrow*), and plain **Men** (the voiceless baseline). Fully renamable — Jaghut was Ryan's example of the FEELING, not a name to ship.
  - **The mechanism — a `uint16 people` index on `PersonIdentity`/`CommunityIdentity` (a field on an ALREADY-registered component, so no entity id shifts — the raw-id rule honoured; the null path never moves).** A `PeopleStyle` resolved from config threads through the generators: the voice draws the given name (`PersonGenerator`), the surname (`FamilyGenerator` — the strongest signal, it carries the whole line), and the place name (`CommunityGenerator`); the modifiers fold into `estimate_lifespan` (×lifespan\_mult, the SAME factor re-applied in `arm_senescence` so retro-armed founders match), the community's per-season conception RATE (×fertility\_mult — folded into the rate, NOT the applicant weight: the arbiter grants `k = rate·n`, so a community-uniform tint must move the rate to move the count), and the coming-of-age `roll_calling` weights (×maker/blade/keeper/adept tints). Empty registry ⇒ neutral style (voice off, ×1.0, exact identity). `EraDirector` walks the peoples chain in boundary order — a turning with no people key keeps the incumbent (so E4's people-less respawn stays same-race), a keyed respawn ushers in ANY folk (no forced order); a **remnant that names a DIFFERENT people is a loud config error** (survivors keep their blood). `validate_peoples` throws before a single entity is generated (unknown key / remnant mismatch, both verified loud); `validate-config` clean (two Heaven-scoped leaves, `world.peoples` + `world.first_people`, coin 0).
  - **THE NULL GATE — no-peoples/no-eras byte-identical to pre-slice master (`4f1f57d`), E4's method exactly.** Built the pre-slice exe in the shared tree (stash + rebuild), default config seed 42 1000y, strip `content_checksum` + `performance`, canonicalise → **BYTE-IDENTICAL, 20,462 entities / 60,808 events**; zero `people` keys leaked (the per-entity field is `std::optional`, omitted when no peoples configured); the ONLY checksum delta is `m_config` now serialising two empty leaves (`peoples: []`, `first_people: ""`) — the seal gate, unchanged perf (sim ~4.0s, 717 tables, identical to master).
  - **THE FOREIGNNESS GATE, on a real elder→men→elder world (`config/ages_peoples.json`, seed 42, turnings Y40/Y80).** **Two audibly different tongues, 10 names each** — Elder: *Naelaseleiel · Silarintheolas · Laloriarin · Thalethaelyrcelywen · Ileinliathreael* / Men: *Haestlegel · Dralddraem · Teisttreil · Trirdmeistsant · Frurnceild*; places *Aelidor/Seirloth/Vaewmar* vs *Tasholm/Branstead/Dandale*. **Lifespan measured** — Elder mean **140.1y** vs Men **61.1y** (×2.29 ≈ the ×2.2 dial). **Fertility measured** on three mono-people 30y worlds (same seed, isolated from CC dynamics) — Elder (×0.55) **265 births**, Men **350**, Small (×1.6) **376**. **Calling mix** — Elder keeper+adept **34%** vs Men **19%** (the tints land). **An era-1 elder ruin surfaced in the men's age reads as another folk's work** (`resolve.py` scene): *"Built by The Elder Folk, in the First Age — their script is not yours."* (dated to the town's FOUNDING age, not its fall); a hoard Work carries *", of The Elder Folk"*. **Both directions in one world** (elder→men AND men→elder) satisfy no-forced-order.
  - **The surfaces name the folk.** Chronicle Turning line (rendered, both directions): *"…and over their bones 9 new settlements were raised — **Men**, who would know **The Elder Folk** only from their stones."* / *"…raised — **The Elder Folk**, who would know **Men** only from their stones."* **skim** — *"Third Age, year 40, the age of The Elder Folk; the Second ended in the Sundering, **Men's**."* **Viewer** (Playwright, `/api/generate` intercepted with the peoples export, console clean 0 errors): the age pills carry the people (*"the Age of the Broken Wheel · The Elder Folk"*, *"the Sundering · Men"*), and a person panel shows the folk as an amber chip (*Aelerinthyrinviil Iliarinylaelsilyr · The Elder Folk*). `lint_content.py` PASS.
  - **Chain determinism ×2** identical (peoples seed 42 → `2176af0d66acf5f8` twice; seed 7 differs). **No-faceless** — 0 empty person/community names; the 12 unresolved `ShrineFounded` participants are a PRE-EXISTING quirk (the master-identical null world carries the same class, 4 of them), not an E5 regression. **Soak** — a two-people 1000y run sane, no crash at either boundary (46,879 entities, 179,631 events; 41,520 Elder / 725 Men in the elder-heavy final age). **Golden** verify → 0/8 (the baseline was still E3's, never re-recorded through E4 — the config-serialisation delta; results byte-identical per the null gate) → **re-recorded → 8/8 PASS** (baselines + manifests in the commit).
  - **Seams, stated:** the elder voice's multi-char syllables make some surnames long (*Siarinaelywenyleiwenveiiel*) — reads as foreign, tune the banks to taste; per-community fertility keys off `CommunityIdentity.people` (correct because a town is founded by one folk and its births inherit it — mixed-people towns don't exist today); the E5 modifiers fold into the SAME dials E4/W-slices own (no new arbiter), so a heavily-tinted world is still bounded by carrying capacity (fertility differences wash toward equilibrium at CC, visible in the growth phase); skim's age-line reads the horizon's `current_index` (baked at export), not an arbitrary present. E6 (the hands and the stamps) remains.

- **E6 — The hands and the stamps landed + committed (Magnus3 `eabaa0d`, master; golden re-recorded + verified 8/8) — Push 2 (the Ages) CLOSED.** Theogonus deals each age a HAND of coins, and *lost* needs no mechanism: a config gains `world.first_hand` and a `hand[]` on each `world.eras[]` turning (each entry a coin key + a `level`); at a turning the new hand's coins are dealt and the coins it drops are REVOKED from `WorldTech` — every system gated on `wt->on(coin)` simply stops, the arts end, nothing new needs to know why. Every Work and Site is stamped **(era, coin, level)** at creation, and a thing is LOST-ART — *derived at export, never stored* — when its stamp falls outside the age the world ended in. **A world that deals no hand is trajectory-identical to a pre-E6 one** (the full hand, absolute schedules, nothing ever lost); the one growth is the serialised-empty `world.first_hand` leaf + the stamp fields on Works/Sites (derived enrichment). Ships a worked gate world (`config/ages_hands.json`: Age I holds war at depth 2 + necromancy, no roads; Age II drops necromancy, war to depth 1, deals roads).
  - **The mechanism — new fields on the ALREADY-registered `WorldTech` singleton (`current_era`, `era_start_tick`, `has_hands`, `hand_level[]`) + a `MakeStamp{era,coin,level}` field on the ALREADY-registered `WorkIdentity`/`Site` (the raw-id rule honoured; no entity id shifts, the null path never moves).** A new `config/HandRegistry.hpp` (mirroring `PeopleRegistry`) resolves per-age hands: empty `first_hand` ⇒ the full hand; an empty turning hand ⇒ INHERIT the closing age's; `validate_hands` throws before generation on an unknown coin key or a level < 1. The genesis sequencer (`MercurySystem`) gains two gates — a coin outside the current hand never fires, and the schedule is **era-RELATIVE** (`tick − era_start_tick`), so a newly-dealt coin discovers in the NEW age's own years; both are inert on the null path (`has_hands` false, `era_start_tick` 0 ⇒ the pre-E6 absolute schedule byte-for-byte). `EraDirector::apply_hand` runs at the turning (after the fall + succession, so the closing age's ruins stamp their OWN era): revoke the dropped coins, set the new `hand_level`, `era_start_tick = T`, bump `current_era`. The stamp's coin is read at each creation site — **Blade → War, other Works → Callings, Tower → Necromancy, Venue → Roads, cave/ruin/shrine → none** (a thing made by no art stamps `coin=none` honestly). **Level v1 = the stamp only** — the depth STAMPS what is made and drives the lost-art reading; no quality dial is wired yet (the stamp is the point; deeper-level mechanics parked as the spec invited). **Fresh-theogony stayed a documented seat** (gods carry, E4's default — the hand change was the load-bearing half).
  - **THE NULL GATE — no-hands, WITH and WITHOUT eras, trajectory byte-identical to pre-slice master (`aa50632`), E4/E5's method (stash + rebuild the pre-slice exe, diff).** Default (no eras), seed 42 100y: **587 entities / 980 events identical**, zero `impossible_now`, the ONLY delta the stamp fields on 20 Works/Sites (checksum stable at `afe85d61e12b3050`). Ages (eras, no hands), seed 42 100y: **2,042 entities / 2,282 events identical, the `ages[]` array identical**, delta = stamps on 128 Works/Sites + the new (empty/false) eras-summary fields (`has_hands:false`, `hand:[]`, `lost_at_end:[]`). Optional stamp fields are omitted on every non-Work/Site entity (glaze skips null members).
  - **THE LOST-ART GATE — `config/ages_hands.json`, seed 42, respawn turning Y150, 300y.** (a) a **necromancy Tower made in Age I survives to Y300** and reads *"the art of necromancy is not held in this age"*; (b) the war Blade **Rourjur (forged Age I, depth 2)** reads *"war-craft of the second depth; this age holds the first"* (the level rule); (c) the derived world list is correct — **`lost_arts: [necromancy]`, holds 12 of 13** (war is HELD at depth 1, so it is a per-Work reading, not a lost art); (d) **the revocation is real — ZERO necromancy events at tick > boundary** (Reanimation 0, GraveRaid 0; the one Y150 strike is Age I's closing season, before the turning bites — the E4 pause-then-act idiom), while **war continues in Age II** (raids 146 → 3 across the turning, war held at depth 1).
  - **Era-relative schedules shown** — with `mercury.roads.year = 20`, Roads (newly dealt to Age II, blocked in Age I by the hand gate) fired at **world-year 170 = Age-II-relative year 20**, not world-year 20: the Second Age discovers roads in ITS own years.
  - **Determinism ×2** (`ages_hands` seed 42 → `5444ca419452eebb` twice; seed 7 → `2d97e33e22bcc7fc`, differs). **No-faceless / soak** — a two-era different-hands 1000y run sane, no crash at the turning (**49,231 entities / 143,327 events; 0 nameless persons, 0 faceless participant refs**). **validate-config** clean (452 leaves; `world.first_hand` [Heaven]); the loud-throws verified (unknown coin `sorcery` → exit 1; `war` at level 0 → exit 1). **lint** PASS. **Golden** verify → 0/8 (the stamp fields moved every checksum — greenfield) → **re-recorded → 8/8 PASS** (baselines + manifest in the commit). **Perf** — null default seed 42 **4.0s / 717 tables**, seed 7 **3.8s / 701** (below the standing baseline; E6 registers NO new component, so no archetype fragmentation).
  - **The surfaces voice it.** **skim** — *"Second Age, year 150, the age of Men; the First ended in the Age of the Broken Wheel; holds 12 of 13 coins; the art of necromancy is lost."* **scene** (`resolve.py`) — a hoard Work reads its wonder, *"the blade Rourjur, forged in year 68 by Traesnans Grontdrindcroustnaens — war-craft of the second depth; this age holds the first,"* and a lost-necromancy tower carries its own `impossible_now`. **Chronicle** — the Turning line names what went with the age (`lost_at_end` per turning). **Viewer** (the hands world generated through the live bridge, console clean 0 errors) — the Work inspect panel shows **"Made by: war · Age 1 · depth 2"** and a violet **"✦ impossible now"** lost-art badge titled with the reason; verified via DOM (the timeline canvas's live render loop times out the screenshot capture in-harness — a tooling seam, not an E6 one).
  - **Seams, stated:** level v1 stamps but does not yet forge a *greater* Work (no quality dial — parked); venues stamp the Roads coin (a road-rest IS Roads-craft — so a lost-roads age reads its taverns impossible-now, honest; none of the gate worlds drop roads); revocation lands AFTER the turning's own season tick (the E4 idiom), so a strike in the closing season fires as the age's last act (dated ≤ boundary) and revocation bites strictly after; a hand *can* technically drop a substrate coin (0–2) with no guard — a degenerate config the gate worlds never write; the per-turning `lost_at_end` names only FULLY-absent coins (a depth reduction is a per-Work reading, not a lost art). **Push 2 (the Ages: E4 spine · E5 peoples · E6 hands) is closed.**

- **S1 — War & Renown landed + committed (Magnus3 `8c90059`, master; golden re-recorded + verified 8/8) — Push 3 (the Weave) opens.** The first interconnection slice: four edges wiring EXISTING mechanisms into closed loops around raiders — no new system, no new EventType, **no new registered flecs component** (the raw-id rule; the trajectory only shifts because the logic did, not because ids moved). Every new config leaf PROVISIONAL + loud-throw (validate-config 324 Earth / 144 Heaven, clean); lint PASS.
  - **Edge 1 — raider reputation precedes the raid.** `RelationsSystem`'s stance warmth gains a flat cold term when either community is a known raider culture (`rule.stance.raider_reputation_penalty`, default 0.12, read off the birth-fixed `RaiderCulture` component — the same one that already crosses into Zeus's layer for the Callings blade-skew, so no Mars config threads across the boundary). A new `StanceTerm::Reputation` carries the "why". **Design call:** the penalty lives in `rule.stance.*` (a warmth weight, beside grievance) not `mars.*` — its home is where the warmth terms are evaluated (Zeus's Relations pass), keeping the config beside its siblings.
  - **Edge 2 — targeting reads renown and wealth.** `mars_choose_target` folds `FoodStores` + summed community prestige (temptation), the most-renowned resident Blade's prestige (deterrence), and a squared-distance kernel (nearer still easier) into a DETERMINISTIC weighted pick on Mars's stream, seeded per raid; drives the desperation + plunder target selection (`mars.raid.targeting.*`; `enabled=false` restores the pure-nearest pick). Reprisals still walk grievance pairs.
  - **Edge 3 — raiders loot Works (the sack).** The dragon's carry-off idiom reused: on a successful MATERIAL raid (Reprisal/Desperation/Plunder — Graves takes corpses, not treasure) a held Work may be carried off to a named surviving raider — **new `CustodyCause::TakenInRaid`** — into the raider community's holdings (`mars.raid.loot_chance`, default 0.14). **Design call + the one real correctness wrestle:** a looted Work must not teleport home, so `SitesSystem`'s custody inheritance AND the fall sweep are now **residence-based, not maker-home-based** (a Work displaced from its maker's home is inherited within its current holder's household where it sits, and rests in THAT town's ruin when it falls; for a never-displaced Work residence == maker-home, so the change is a byte-identical no-op — proven below). This makes spoils inheritable in the raider community and lootable BACK the other way.
  - **Edge 4 — Mars raids towers.** A raider culture marches on a wizard's Tower in reach FOR PLUNDER — **new `RaidMotive::Tower`**, the "victim" being the tower SITE (placeSpan renders its name). The party meets the wizard + its bound forces (Mars models the wizard's defense with its OWN constant — a hunt does not borrow Mars config, a tower-raid does not borrow Mercury's), sacks the tower's holdings, and may slay the wizard — at which the EXISTING slain-wizard→ruin path fires for free via **Mercury's `wizards_release` later the same tick** (Mars just `kill_person`s the wizard; no layer inversion). Earns prestige, NO grievance (a tower is nobody's polity — the F4 precedent). Rare + reach-gated + raider-only (`mars.raider_cultures.tower_*`).
  - **THE A/B — all four knobs off reproduces pre-slice RESULTS byte-for-byte.** The checksum can't match across the boundary (it bundles the config struct, which grew), so I hashed ONLY the results (entities+events, config/meta/perf excluded): with `reputation=0`, `targeting.enabled=false`, `loot_chance=0`, `tower_raid_chance=0`, the 1000y world_3region results are **byte-identical to the pre-slice HEAD exe** — seed 42 `851155a82efb86f0` == `851155a82efb86f0` (45,093 entities / 133,742 events, every event-type count identical), seed 7 `f0fd8b6a22e2eae3` == same. Each edge is guarded by its own knob (the guards proven inert). **Isolation:** reputation ALONE barely moves counts (raids 1118→1018); edges 2/4 drive the drop (norep→721) — smart targeting reshapes WHO is raided and tower-raids consume raider raid-slots, cascading to raider demographics.
  - **THE STORY GATE — all four rendered by the real chronicle.js (Node harness over the export; seed 42 world_3region, 1000y).**
    - (a) **reputation before the raid:** *"In these years Jarford and Surnholm turned hostile — Surnholm being a people of raiders."* (year 60) — and Surnholm and Jarford **NEVER once raided each other**: the raider's reputation drove hostility with zero raids between them (23 such reputation-dominant shifts, all onto the raider Surnholm).
    - (b) **the wealthy target over the near one** (env-gated debug, since community stores aren't exported — capture then removed, does not affect outputs): aggressor 563 repeatedly chose victim 693 (**d²=16, stores up to 96, renown 8.0**) OVER the adjacent nearest 628 (**d²=2, stores ~70**) — 787 non-nearest picks in 200y; distance overridden by wealth + renown.
    - (c) **a Work looted in a sack, custody crossing communities:** *"Creilddustrous Fuldlourmfraendtaent carried off the blade Donfron from the hearth of Fostbunnurm Lirsjeth in the sack of Groudale, in the year 145."* The blade **Donfron**'s chain: forged in **Groudale** (y77) → inherited there → **sacked to Surnholm** (y145, TakenInRaid) → inherited within Surnholm → **sacked AGAIN, out of Surnholm to Laendale** (y174) → inherited in Laendale for the remaining eight centuries. Three communities, two sacks, the take-back-the-other-way dynamic live.
    - (d) **a tower raided by raiders:** *"Loswirm Fuldlourmfraendtaent marched with the raiders of Surnholm to storm the Leaning Tower of Heimfre, the mad wizard's hold, and they carried the day."* / *"Endcrild Sesdeirmostril was cut down defending the Leaning Tower of Heimfre against the raiders of Surnholm, 66 years old."* (45 tower raids, 14 wizard-slain → the tower falls to a ruin via the existing path).
  - **Gates.** Determinism ×2 identical (`1a4187003269da59` twice; seeds 7/99 differ). **No-faceless** — the sack names raider/work/prior-holder/place, the tower raid names the party + the wizard. **Soak (seed 42 world_3region, 1000y):** raiders did NOT re-metastasize — **raider communities 2→1, raids 1118→714, falls 72→64** (the safe direction; motives plunder 390 / reprisal 244 / tower 45 / desperation 35; 13 sacks — Works circulating through war, a handful per millennium as intended). A raider-free world (seed 7) is barely touched (38 raids vs 40 — the edges are raider-gated). **Golden** re-recorded → verify **8/8 PASS** (baselines + manifests in the commit). **Perf** in family — default 1000y **4.3s / 721 tables** (pre-slice 717; +4 is not fragmentation — zero new components), world_3region **~11.8s**.
  - **Seams, stated:** the raid-count drop (1118→714) is a real emergent consequence of edges 2/4 reshaping raider demographics, not a suppression bug — left as the PROVISIONAL default (play tunes it); a wizard's tower rarely HOLDS a Work (wizards are Adepts, not Makers), so tower-raid loot is usually empty — the tower-raid's value is the wizard-slay→ruin + prestige, and looting "whatever custody it holds" (grimoire-less v1) is honest per spec; the reputation penalty tips a same-region raider pair to the cold edge of Wary and a cross-region raider pair to Hostile without locking near pairs hostile (0.30−0.12=0.18); a tower-raid's `victim_community` field carries the tower SITE entity (an entity is an entity — placeSpan renders it, archetype-annotation counts the aggressor only). Wealth halfsats are set against observed soak magnitudes (median stores low-thousands, veteran prestige ~1–4), PROVISIONAL. S2 (Blood & Blessing) is next.
- **S5+S6 — the surface pair MERGED to master (`6c96f18`; squash from the isolated worktree branch, auto-merge clean, `src/` confirmed untouched, lint PASS + seat demo green on the merged tree; branch + worktree cleaned).** Built parallel to S1 under commit discipline. **S5 — The Faces of Heaven:** the gods get their drill-down at last (deity inspect branch: domain, current mood + since, full guna history, providence given, shrines dedicated — event-sourced via three new data.js indexes); the nameless hosts get ADDRESSES (shrines/towers/undead as timeline rows under their communities, dragons/lions/wolves/bears as region-level rows; each with a real inspect panel — a dragon shows its den, kills, and HOARD; an undead its in-life, binder, and second death); four missing chronicle voices land — **Discovery narrated** ("In the 300th year of the world, Agriculture came into the world"), **Mars finally has weather** ("forge-smoke hung over the settlements, spear-points going out to settle old scores under red dusks"), Crowding ("the press of too many mouths… the crowding that comes before a famine"), RouteChange ("In these years a road opened between Sunsfell and Rathmere"). **S6 — The Knowing Player:** `build_pc_context` learns what any resident knows — own calling + authority rank, raider-culture flag + a tick-safe trade-hub signal, venue/shrine presence, open roads, neighbor stances, the last omen read in-community, and an `ambient_dread` cue from doom-cascade windows — with the slice's best judgment call being what it REFUSED to surface: the derived archetype labels are final-state-only and would leak the future, so roads/stances are reconstructed tick-correct from the event trail instead (RouteChange/StanceShift), and archetypes stay DM-side. Existing secrecy rules verified intact (known:-filter, no post-now leaks). Weave scorecard: S1+S5+S6 landed, S2–S4 to go.
- **T1 — The Surviving Campaign landed + committed (Magnus3 `509da17`, master; Python-only, `tools/stage/` — footprint-disjoint from the engine waves, no golden, no build).** The Table's fix-class first wave. The rehearsal's sharpest finding was a defect, not a gap: **`advance` silently broke every id-based reference in the campaign.** Export ids shift across an advance (by design), the mechanical casts ride the name/seed bridge (by design) — but the party roster and every stage-only note/tag/link are id-anchored and got NEITHER remapped NOR re-attached. One advance and the party pointed at strangers; the DM's hooks evaporated. The between-sessions loop was destroying session prep.
  - **Reference remapping across the advance (`tools/stage/reconcile.py`, new).** Before the old export is replaced, every id-anchored campaign reference — party members, note/tag/link targets — is keyed by the SAME identity the cast bridge uses: person-seed where the export carries it (this build's does not), else name unique-within-type, **loud on a collision** (never a guess). Authored refs (`a1`) ride the engine's own `id_remap`. After the new export loads, keys resolve → new ids; the party is rewritten and the overlay edits are re-attached (fresh unabsorbed copies preserving text/tag/kind/provenance, `reanchor:{from,epoch}` stamped), so `resolve` re-applies them over the new export. The remap is recorded on the epoch entry (`reference_remap`). Runs AFTER absorption so the copies land above the watermark; a SECOND advance re-keys the prior epoch's copies — **remap-of-remap composes** (proven).
  - **The stale policy — honest, never silent.** A reference whose key is truly gone from the new export (a vanished target) is KEPT: the re-anchored edit is marked `stale:true` (a new envelope key; `resolve` skips it, the DM's content survives) and a loud warning is emitted. A vanished party member is dropped from the roster with a named warning rather than left pointing at a stranger. People persist dead in the export, so a party member who DIED still re-keys by name and rides through (verified — a dead necromancer stayed on the roster with his hook intact).
  - **Fail at the right end.** A `create` the perturbation bridge can't carry — a being (undead/dragon/beast) — is now **refused as it enters the log**, at cast time, with a clear message pointing at T5, instead of being discovered later blocking the advance. person/place creates still pass. The guard lives in `model.Campaign.append_edit`, so both clients (CLI + serve.py) inherit it (parity).
  - **The change digest.** After a successful advance the CLI prints, and the epoch stores (`epoch.digest`, so the viewer/API surface it), a compact digest of what happened while you were away — deaths (with cause), births, marriages, raids, leadership turnover, route + stance shifts, and world-line falls — scoped to the party and its communities, read straight from the new export's event/entity data.
  - **Verification (all green).** `test_spellbook.py` extended +5 (**103/103**): the rehearsal defect replayed green (party → same people under new ids, hooks re-attached, remap on the epoch, the baked `set` NOT re-anchored); the stale case (kept + marked + warned, resolve tolerant); the cast-time being refusal (person/place still work); the digest (a known death+marriage surfaced); the chain (remap-of-remap, exactly one live overlay after two advances). All prior stage/spellbook tests intact. **End-to-end against the real exe** (seed 42, 100y, Release build — not rebuilt): party `[19,23,26]` → `[17,22,25]` = the SAME three Kurscanfrourn, the note/tag/protects-link all re-attached to the new ids; the embitter cast between Surnholm and Mirmford landed and its **raids + route/stance consequences appear in the digest** (the Earth-informs-Heaven loop closing), and a party member's death surfaced unprompted — *"Veinscoultaens Kurscanfrourn died in year 58 (beast)."* Acceptance met.
  - **Seams, stated:** re-attachment grows the log by one absorbed + one live copy per overlay per advance (append-only-honest, old copies marked `[absorbed]`, provenance in `reanchor.from`) — bounded but not free; a stale overlay is terminal (not re-multiplied on later advances); the digest reads the new export's full history and caps each category (~8) to stay dozens-of-lines; person-seed keying is future-proofed but this build's export carries no seed, so keys fall to name+type today (the same exposure the cast bridge already has — an ambiguous name refuses loudly at BOTH ends). Table waves T2–T5 (the session verbs, skirmish, town-knows, missing casts) to go.
- **T2 — The Session Verbs landed + committed (Magnus3 `696eaf6`, master; Python-only, `tools/stage/` + `serve.py` — footprint-disjoint from the engine waves, no golden, no build).** The Table's second wave: the session's actual resolution primitive, the player handout, and the dev loop's inbox.
  - **check** (`tools/stage/check.py`, new codified spellbook macro): five attributes (might/wits/grace/craft/lore), each a documented formula over calling + temperament mirroring combat.py's own `CALLING_MULT` idiom — a per-calling multiplier converted to a d20 modifier, three of the five also taking a trait term (heat for might, spirit for wits, moisture for grace; craft/lore stay calling-only, no trait carries a documented tie). The die is DETERMINISTIC — seeded on (export checksum, person, a per-campaign check ordinal = `campaign.cast_count("check")`, a pure read of the ledger that only advances once a cast actually appends) — mirrors `seats/heuristic.py`'s `seeded_random` idiom exactly; **replay-safety falls out of the log**, not a separate idempotency mechanism (an uncommitted attempt never bumps the ordinal, so a retry reproduces the identical roll). Advantage/disadvantage draw a second d20 off the same stream, take max/min. Margin-vs-DC bands to critical/success/partial/failure (a natural 1 caps at partial, a natural 20 floors at critical), plus a one-line narration hook. DC guidance ships in the command's own `--help` (trivial 8 / easy 10 / hard 14 / heroic 18). The attempt casts as `check` (record_only semantics — a single Chronicle event carrying person/attribute/dc/roll/band); undo removes it like any other cast.
  - **sheet** (`tools/stage/sheet.py`, new): the one-command player handout — name/age/people/calling/temperament-as-descriptor (raw numbers only under `--full`)/authority+offices/kin living-or-dead/marriage/children/held Works (custody read off the same Custody-event participant shape `bestow` already writes, with any impossible-now wonder line riding along)/prestige-as-renown-words/all five check modifiers/a derived combat rating (reused `combat.py` outright)/community+archetype flags/active tags+notes ONLY if authored `known:` — the exact convention `seats/context.py`'s `_overlay` already enforces for a PC's own seat context; a sheet is DM-run but PLAYER-facing content, so it inherits the same promise. Tick-correct throughout (`person_facts` + a couple of `resolve.py`'s own tick-safety helpers reused directly, not re-derived). Renown is a documented honest proxy (rank/offices/held-Works, with the export's measurement-only `measure_prestige` folded in as a soft nudge) — `ExportCommand.hpp` itself documents there is no durable prestige field by design ("deliberately NOT exported as a mutable field").
  - **gap + gaps report** (`tools/stage/gap.py`, new): `campaign.gaps[]` — plain-sentence session friction logged against the campaign's present, DM-tool metadata that never touches the edit-op vocabulary (no undo, survives `advance` untouched — it was never id-anchored to begin with). `gaps report [--all-campaigns]` aggregates newest-first across campaign files and folds in the spellbook's own promotion-candidate report (record_only macros cast often with no engine meaning) as "macro-shaped gaps" — the dev loop's inbox made real: session → gap ledger → build → next session.
  - **serve.py**: thin HTTP wrappers for all three (`GET /api/sheet/<id>`, `POST /api/check`, `POST /api/gap`, `GET /api/gaps/report`) — the same `StageSession` both clients drive, parity by construction, not a promise.
  - **A latent bug caught along the way.** `to_engine_script`'s macro-vs-bare-op discriminator (`macro and sem != "record_only"`) mis-fired on exactly the case check's own perturbation-bridge test exercises: a record_only macro's primitive still carries `macro` provenance but compiles to a bare-op-shaped entry (semantics `record_event`/`set_field`/...), so it was routed into the mechanical-macro branch and rejected for lacking a `_MACRO_WIRE` entry — silently blocking ANY record_only macro (including the pre-existing minted `haunt` pattern) from ever surviving an advance, just never exercised by a prior test. Fixed by switching the discriminator to the entry's own `op_kind` — what `compile_perturbations` actually uses to tell the two apart.
  - **Verification.** `test_spellbook.py` extended +51 (**154/154**): check's determinism (a pure function of checksum/person/ordinal), the ordinal-only-advances-on-a-real-cast property, provenance + cast-aware undo, bad attribute/mode rejection, advantage beating straight on average over a 300-sample seeded run (13.54 vs 10.57; disadvantage 6.98), and the perturbation-bridge fix itself (a check rides an advance as a name-bridged Chronicle record). sheet's tick-safety (a future-dated death fixture never leaks), the `known:` filter, and a full render over a person with offices + a held Work. gap's append/validate/round-trip and a two-campaign aggregate report with a promotion candidate folded in. **End-to-end against the real exe** (seed 42, 100y, Release — not rebuilt): `check 23 might --dc 14` produced two real rolls on two invocations (d20[11]+5=16 SUCCESS, d20[8]+5=13 PARTIAL — different ordinals, different results); `sheet 23` rendered a full player handout (calling/temperament/renown/offices/checks/combat rating/community); two gaps logged + a `gaps report` aggregate; and the whole loop proven — cast a check, `stage advance --years 5`, and the check's Chronicle record landed baked into the new export verbatim: *"Seistdars Kurscanfrourn attempted a might check vs DC 12: d20[9] +5 = 14 -- success. testing a check riding an advance."* serve.py's four new endpoints exercised live over HTTP (sheet/check/gap/gaps-report, all 200s).
  - **Seams, stated:** renown/combat-rating words are tuned-by-feel, not measured against play; prestige has no durable engine field by design, so the renown proxy leans on offices+Works+rank (honest, not authoritative); the gap ledger's "scene context if recently queried" nice-to-have was skipped per the brief's own allowance — the CLI is stateless between calls, and tracking it would add real session-state machinery for thin payoff. T3–T5 (skirmish, town-knows, missing casts) to go.
- **S2 — Blood & Blessing landed + committed (Magnus3 `3ea3c7c`, master; golden re-recorded + verified 8/8, pushed).** The second Weave slice: FIVE edges wiring existing mechanisms into closed loops around divine blood, callings, plague, and providence — no new system, no new EventType, **no new registered flecs component** (the raw-id rule; the trajectory shifts only because the logic did). Landed on top of the parallel Table waves (`696eaf6`) — T1/T2 are Python-only (`tools/stage/` + `serve.py`), footprint-disjoint from the engine, so the A/B against the pre-slice-`6c96f18` engine holds. Every new leaf PROVISIONAL + loud-throw (validate-config 329 Earth / 148 Heaven, clean); lint PASS.
  - **Edge 1 — the blood wants a calling.** An EXPRESSED bearer's own coming-of-age roll tilts toward its line's preferred calling (Red Hand→Blade, Evergreen→Keeper, Open Eye→Adept; Crowned tilts to the office, no calling lean). `roll_calling` gains a weight MULTIPLIER on that one calling (`rule.callings.bloodline_calling_lean`, default 4.0) keyed on `expressed_line(person)` — affinity/surplus still predispose, a lean not a lock. **Design call:** the knob lives in `rule.callings.*` where the term is EVALUATED (the S1 precedent), not in the bloodlines block.
  - **Edge 2 — Mars runs truer among raiders.** A Red Hand conception weights up when the candidate woman dwells in a `RaiderCulture` community (`world.bloodlines.redhand_raider_weight`, default 5.0) — the war-god favors the people who live by the blade. Additive, Red Hand line only.
  - **Edge 3 — Evergreen blunts plague.** A struck community holding a living Evergreen bearer softens the outbreak's per-season severity (`world.bloodlines.gifts.evergreen_plague_severity_mult`, default 0.5), reusing Venus's famine-mercy pattern exactly (`community_has_living_bearer` + a gift mult) — the life-aura that blunts famine blunts plague. **Design call:** the mult rides the bloodline-gifts bundle (beside `evergreen_famine_death_mult`), the recognized cross-system gift exception, though it's read in Mercury's plaguewright emit.
  - **Edge 4 — renown beckons Heaven (the upward half of the favor loop).** A household's DEEDS draw the gods' eye: the candidate woman's household prestige (her current family's living members' summed `Prestige`) adds a SATURATING weight to the divine-conception choice (`world.bloodlines.renown_weight`/`renown_halfsat`, 3.0/4.0) — beyond Crowned's static `Authority.rank` snapshot. Renown raises the odds, **never buys the gift** (the per-season conception roll is untouched — only WHO is chosen moves).
  - **Edge 5 — providence sees shrines (the top-down half).** When a god BLESSES, a candidate whose community keeps a living shrine to THAT god has its renown weighted up (`<domain>.expression.providence.shrine_bless_weight`, default 1.6) — worship nudges whose deeds Heaven notices. Curses stay shrine-neutral (a shrine doesn't attract wrath). Closes the loop edge 4 opened bottom-up.
  - **Edge 6 (omens read grievance) DEFERRED — an honest null, not shipped inert.** Threading grievance into the War-omen chance changed **ZERO outcomes** across 6-world sweeps at BOTH the default read_chance (0.80) AND a stress probe (floor lowered to 0.30, reliability 0.6): War omens are gated on the rare Mars mood-turn (~8/world) and simply don't coincide with communities actively holding grievance (which is small — max ~1.7, mostly ~0, and yearly-decaying). Built it, measured it, removed it (code + config), documented the why. Deferred to when grievance sources are richer — S4 adds grievance doors (raids/plagues/beasts feeding it), which may make this loop viable.
  - **THE A/B — all five knobs off reproduces pre-slice RESULTS byte-for-byte.** `content_checksum` bundles the config struct (which grew), so it can't match across the boundary — hashed the RESULT sections alone (entities+events+areas+relations+routes+doom+…, config/meta/perf excluded): with the five knobs at their inert values (lean 1.0, raider 0, plague-mult 1.0, renown 0, shrine 1.0), **all 8 golden cells are byte-identical to the pre-slice exe** (e.g. seed42-100y `0158585088469f86` == `0158585088469f86`, world_3region `deb531b7d4b9742a` == same). The proof also caught the trap: the first A/B pass "failed" 8/8 because it compared `content_checksum` (which folds config) — the results were identical all along, the digest just carried the new config text.
  - **THE STORY GATE** (measurement worlds at elevated bloodline density for sample size — the conception/expression rates only give the mechanism a countable N; the WHO-redistribution is the point; prose rendered by the real chronicle.js via the live viewer bridge).
    - (a) **the calling lean** — among expressed bearers, the preferred-calling rate roughly TRIPLES: Red Hand→Blade **34.3% on vs 11.4% off**, Evergreen→Keeper **35.1% vs 9.7%**, Open Eye→Adept **29.0% vs 10.0%** (629/612/682 bearers).
    - (b) **Red Hand clusters in raiders** — gen-0 Red Hand conceptions in raider communities **22.9% on** (above the ~19% blind expectation) **vs 14.8% off** (at/below blind). Modest, directional — the war-god favors raiders, doesn't monopolize them.
    - (c) **plague blunted** — of 180 paired plagues (edge-off is inert, so plagues pair by id), **21 struck a living-Evergreen-bearer town**, severity **halved 0.100→0.050** (the 0.5 mult exactly); deaths negligible in both worlds (2/2 — severity is the operative measure at these magnitudes).
    - (d) **renown correlates** — divine-conception mothers' mean final prestige **1.35× the population of fertile women on vs 1.24× off** (n≈279/265). Modest by design — final prestige is a noisy proxy and Crowned's rank-weighting already lifts the baseline; the +8% increment isolates renown's hand. Renown raises the odds, doesn't buy the gift.
    - (e) **shrine-town blessings** — the share of blessings landing in a shrine-town of the blessing god **DOUBLED: 16.0% on vs 8.4% off** (630 blessings).
    - **The chronicle showcase (seed 7, Narsheinskound Crendpeinsfrim)** — one life carrying edges 1, d, and e: *"In the 32th year of the world, for the first time in an age, the Evergreen Blood quickened in a child of Fremholm: Narsheinskound Crendpeinsfrim, Venus's own child by a mortal mother."* → *"Narsheinskound Crendpeinsfrim turned to healing and the keeping of rites."* (the Keeper lean) → she led Fraeholm 37 years → *"raised and kept the Shrine of Fraeholm, dedicated to the god Ecology, where the god's blood had looked out of a child."* → *"The god Ecology looked with favor on Narsheinskound Crendpeinsfrim and blessed her — a grace that told in the years that followed."* (the shrine's own god, thrice). And the honest counterexample: **Craensdreistseth Crisdraenstaes**, a Red Hand bearer of a stony Earth temperament, took the Gatherer's path anyway (*"went to the gathering, one working hand among many"*) — the lean is a lean, not a lock — while Ursmford *"raised the Shrine of Ursmford to the god Mars, where the god's blood had looked out of a child."*
  - **Gates.** Determinism ×2 identical (same seed byte-equal; distinct seeds differ). **No-faceless** — every S2-touched event type (Manifestation/Blessing/Curse/Omen/Plague) has zero unnamed participant slots. **The gift stays RARE and is NOT inflated** — the per-season conception roll is untouched, so counts shift per-seed but stay era-defining (default world_3region 1000y: seed42 **4→4**, seed7 **0→1**, seed99 **11→4**; aggregate 15→9 — the weights redistribute WHO, not HOW MANY). **Golden** re-recorded → verify **8/8 PASS** (baselines + record/verify manifests in the commit). **Perf** in family — default 1000y **4.36s / 721 tables** (== pre-slice), world_3region **11.98s / 1012 tables** (pre-slice 1012 exactly — zero table inflation, no new components).
  - **Seams, stated:** edge 4's signal is modest and its evidence leans on `measure_prestige` — a temporary export field added ONLY for the measurement (adds no flecs component, so trajectories were byte-identical) and reverted before ship, so nothing durable exports prestige (the T2 sheet proxy's premise holds); edge 2's clustering is real but small (raider communities are a handful, and the conception picks one mother among all fertile women — the tilt is a nudge above chance); the elevated measurement rates would crash export if Crowned conception is boosted (its cohesion-per-bearer gift booms population past the serializer's comfort at 120k+ entities) — the measurement configs leave Crowned at default and it never bit a shipped world; edge 3's death delta is 0 at these severities because the halved drain still recovers before the death floor — the blunting is honest at the severity level, not the mortality level in calm worlds; the chronicle's *"for the first time in an age"* reads oddly in an elevated measurement world (239 manifestations) but is accurate at shipped rates. **Weave scorecard: S1+S2 landed (engine), S5+S6 landed (surface); S3 (the living road) + S4 (grievance & grace) to go.**
- **T4 — The Town Knows landed + committed (Magnus3 `c57d878`, master; Python-only — `tools/stage/{lore,town}.py` + `stage.py`/`serve.py` + `tools/seats/context.py`, footprint-disjoint from the engine, no build, no golden, pushed).** Investigation — a core session mode with zero support until now — gets its primitive: a community's **belief-set** at the campaign's present, and a seeded **town sheet** the DM can open a scene on. Two deliverables, both pure over the resolved state.
  - **`stage lore <community>` — the belief-set, graded.** Every event the town witnessed or took part in reads **known** (events located there via the event's own community fields/roles, plus any event a resident participated in — a local's marriage, a local taken on the road). A neighbour's MAJOR events (raid · fall · work forged · tower/shrine raised · discovery · omen · famine · plague) **travel the roads** and arrive graded by stance + distance: **rumored** over an open friendly/wary road within a couple of hops (true but vague, honest recency), **distorted** from a hostile neighbour OR ≥3 hops (the game of telephone), **absent** when no open road reaches the source at all. **News travels roads**: the open-route graph is reconstructed tick-correct from the RouteChange trail (BFS for hops), stance from the StanceShift trail — both the exact S6/context.py idiom (never the export's non-authoritative final-state `routes`/`relations`, which would leak a turn that hasn't happened yet). So nothing post-now, nothing un-turned, leaks.
  - **The distortion is deterministic and honest.** A distorted telling is seeded on (campaign content checksum, event id) → same world, same twist every time; it wears a hedge ("They say…") and an inflated menace, but the **underlying event stays real** — the DM-grain item keeps the true `summary` beside the twisted `rendered` telling (distortion never invents, only garbles).
  - **`--about <entity>`** filters the belief-set to items touching one entity and reports **who here holds each grade** — resident participants hold a local event *witnessed*; the leader and Keeper hold local lore, and *have heard* a rumour; the gatherers who walk the roads carry rumours home. ("who knows about the tower?" finally answers.) **`--as-pc <person>`** routes the same query through the seat gate (`build_pc_context`): the PC gets a `local_lore` block of what they plausibly know, with **the DM-grain truth behind a distorted rumour stripped** — a distorted seat item exposes only the twisted telling it believes, never the real event or its source. Pure context enrichment, no new Decision kind (the invariant held).
  - **The ambient-dread fix.** The known honest limit — ambient dread keyed off DoomCascade EVENTS, which a **sealed world never emits** even as it darkens — is closed: `context.py` gained `ambient_dread_from_doom(state, now)`, reading the export's **doom BLOCK series** (E1's three yearly `dead_ratio`/`wrath_concurrence`/`wrong_arts` series) and raising dread on a rising SLOPE at the present (rise weighted by how far over each metric's floor it already stands), keyed to the doom climbing hardest. `build_pc_context` now prefers the slope cue and falls back to the cascade-window one — so a sealed-but-darkening world whispers where the cascade never fires.
  - **`stage town <community>` — the DM's scene-opener,** all derived + seeded: **notable spots** (the real venue/shrine/tower + 3 flavour spots — the well, the gate, the mill — sampled deterministically from the world checksum + community id, STABLE per world); **current mood** from the last ~3 years of LOCAL events (a raid ON the town reads *raw*, a famine *hungry*, a neighbour's fall *shaken*, a raid BY it *emboldened*, a sour omen *uneasy*, a wedding season *merry*, else *settled* — a raided town and a quiet one open different scenes); **who matters** (leader · taverner · Keeper · a notable blade/adept/elder, and any [NECROMANCER]-class or DM-tag flag, DM-grain); **table talk** (3 seeded snippets drawn FROM the lore pool — real events, phrased as overheard talk, never invented); **hooks** (an open grievance with a hostile neighbour · a recent fall nearby · a resident away on the road and overdue). `--player` filters every field through the seat rules (no necromancer/DM flags, public read only).
  - **serve.py**: thin `GET /api/lore` + `GET /api/town` wrappers on the same `StageSession` the CLI drives (the T2 idiom — one API, two clients, parity by construction).
  - **Verification.** `test_spellbook.py` extended +35 (**189/189**, all 154 prior intact): the grading model on a raid+stances fixture (local→known, allied neighbour→rumored, hostile neighbour→distorted, honest summary kept); the roads gate (an isolated town with no open route receives nothing, and DOES once a road opens); tick-correct stance reconstruction from a StanceShift trail (allied before a year-90 turn, hostile after); `--about` holders (the Keeper witnessed the local raid; an unreachable town holds nothing); `--as-pc` leaks nothing (the `known:` note reaches the PC, the secret note never does, the post-now raid never appears, a distorted seat item carries no DM-grain source/truth); distortion deterministic + honest (identical twice, real event underneath, different checksum → different twist); the town sheet renders and its mood shifts raided↔quiet, spots stable across builds, table talk traces to real event ids, `--player` drops the necromancer flag; and the doom-slope dread (a sealed-but-rising `dead_ratio` raises a line at the present, silent at year 50, silent with no doom block). **End-to-end against the real exe** (seed 42, 100y, Release — not rebuilt, cache hit): `lore Ithmmere` = 20 known / 27 rumoured / 42 distorted, the distortions traceable to true raids from hostile Mirmford 2 hops off (*"It is whispered that Mirmford raided Teimstead for plunder… and none who went to look came back the same"* — TRUE beneath); `lore Ithmmere --about Mirmford` returned five KNOWN raids on the town with their witnessing residents (leader, blade, gatherers); `town Mirmford --player` opened a raider martial town reading *raw* off a recent raid, its tavern + burying-ground + gate, its leader/taverner/blade, three table-talk lines and a grievance-with-Feimvale hook; `lore --as-pc` delivered the seat-safe pool. **Seams, stated:** the travel model is a clean open-route BFS with stance-and-hops grading (a documented convention, not a diffusion sim — a rumour "arrives" at the present, its recency told honestly, but the *tick* it would have reached each town isn't modelled — news is present-or-absent, not en-route); mood reads LOCAL events only, so a town ringed by war it never suffered directly still reads *settled* (the grievance surfaces in hooks, not mood); flavour spots and table-talk selection are seeded per-world for stability, not drawn from real geography; distortion twists are a small hedge+menace table (deterministic, honest, but not yet varied by doom/era). T3 (the skirmish) + T5 (the missing casts) remain of the Table.

- **S3 - The Living Road landed + committed (Magnus3 `3185730`, master; golden re-recorded + verified 8/8, pushed).** The third Weave slice: THREE edges wiring existing mechanisms into closed loops around venues, journeys, terrain, and Beings - no new system, no new EventType, **no new registered flecs component** (the raw-id rule; the shift is the logic, not the ids). New surface is a value struct (`VisitRecord`) + a field on the already-registered `JourneyBoard` singleton, and one pure RNG-free helper header (`DangerField.hpp`). 12 leaves PROVISIONAL + loud-throw (validate-config **341 Earth / 148 Heaven**, clean); lint PASS.
  - **Edge 1 - journey co-presence seeds the marriage pool (the road WRITES kinship).** A cross-community pair who have actually been co-present - one journeyed to the other's town, or both were at a venue town within an overlapping stay (the keeper counts as a resident) - has its exogamy distance-gate lifted toward certainty (`reach' = 1 - (1-reach)(1-boost)`, `mercury.roads.copresence.reach_boost` 0.75). Co-presence is remembered in a **bounded, ordered, tick-stamped visitor ledger on `JourneyBoard`** (host-keyed `std::map`, window 12 seasons, cap 48 - Mercury appends on departure, evicts each season; not exported, reconstructible from the Movement stream). The marriage is stamped with the meeting place (the venue, or the town) as a **Witness participant** - so a Witness on a Marriage IS the "met on the road" tell the chronicle reads. **Design call:** all co-presence knobs live under `mercury.roads.copresence.*` (Mercury owns the road that writes the kinship); the marriage pass reads Mercury's config the way Mars already reads Mercury's necromancy block. 0 = off (pre-slice exogamy - the A/B).
  - **Edge 2 - journeys read the danger pale.** Departure chance `*= (1 - route_danger * mercury.roads.journeys.danger_penalty` 0.6`)`, where `route_danger` folds the wilderness field + living beasts along the span (`span_danger`: an integer-sampled, order-independent MAX over the hazard list, no RNG - so an all-off config is byte-identical). The destination is now chosen BEFORE the departure roll so the corridor can be read; both are pure keyed draws, so their values are order-independent and the A/B holds exactly. The existing predation-on-travellers hook then makes the crossings that DO happen costlier.
  - **Edge 3 - Being territory taxes routes and marches.** `roads_strength` and Mars's `mars_choose_target` lose weight for a span overlooked by a living apex Being (a dragon) or a dread wizard-tower (`mercury.roads.routes.danger_penalty` 0.6 / `mars.raid.targeting.danger_penalty` 0.6, sharing the `mercury.roads.danger` geometry read via `DangerField.hpp`) - monsters reshape the trade network and war without a new system. Guarded to zero cost when no apex hazard exists (the common case).
  - **THE A/B - all knobs off reproduces pre-slice RESULTS byte-for-byte.** `content_checksum` bundles the (grown) config so it can't match across the boundary; hashed the result sections alone - all 7 cells (7/42/99/1234x100y, 42x250y, world_1com, world_3region) **byte-identical to the saved pre-slice exe** (the same trap S2 named: the checksum differed, the results never did).
  - **THE STORY GATE** (soak seed42 world_3region 1000y, ON vs S3-off; prose via the real chronicle.js):
    - (a) **co-presence** - **133 "met on the road" marriages ON vs 0 OFF** (~1% of cross-community marriages: a real, minor channel - single travellers are rare, so it stays a garnish by construction; distance still matters). The quote, one life carrying edge 1 whole: *"Mistfrount Grontdrindcroustnaens and Nirstarn Rirneirnjeind were married, and Nirstarn came to live among his people... - a match made of the road, for they had met at The Cantin Hearth"* (and from her side *"...she having met Mistfrount at The Cantin Hearth while travelling"*), with the trigger journey **Vaenfell -> Jarford the same year** in the Movement data (*"Mistfrount took the road to Jarford, and wintered there"*).
    - (b) **danger** - journeys drop **monotonically with route wilderness: -4% (safest quartile) -> -16% -> -25% -> -29% (wildest)**; **145 of 240 predations** struck travellers on the road (the crossings that happen cost more).
    - (c) **dragon** - same seed+dragon, routes-danger ON vs OFF, snapshot at y55 (wyrm alive): near-lair route **mean strength ON 0.110 vs OFF 0.146 (-25%)** while FAR/control routes are **identical (0.229 vs 0.229)** - the dragon thins ONLY the roads it overlooks.
  - **THE KINSHIP CASCADE (the payoff).** Community pairs with >=2 co-presence marriages run **82% warm stance vs 18% across all cross-community pairs** - the road writing peace exactly where it runs. Rare in volume, concentrated in effect.
  - **Gates.** Determinism x2 identical (distinct seeds differ). **No-faceless** - all 133 Witness participants named (venues + communities), zero anonymous. **Marriage rate NOT inflated** - total marriages ON **35,424** vs OFF **35,455** (the boost redistributes WHO weds, not how many). **Golden** re-recorded -> verify **8/8 PASS** (manifests in the commit). **Perf** in family - same-build on/off ratio **1.21x default** (6.53s vs 5.39s sim) / **1.23x world_3region** (13.76s vs 11.20s), `flecs_overhead` flat (**no table inflation** - no new components); absolute wall-times were inflated by the concurrent Table agent, so the on/off ratio is the honest, contention-free measure.
  - **Seams, stated:** co-presence is journey-limited so it is a garnish channel by design - its power is the cascade, not volume; edge 2 suppresses ~26% of journeys world-wide (wilderness blankets the gaps between sparse towns) which ALSO thins co-presence opportunity - edges 1 and 2 pull against each other, honestly; the danger span is crow-flies Euclidean, not the terrain path; route danger is `O(C^2 x hazards x samples)` per season when apex hazards exist (towers accumulate over a millennium), the one place a much larger world would want a spatial prune. **Weave scorecard: S1+S2+S3 landed (engine), S5+S6 landed (surface); S4 (grievance & grace) to go.**

- **S4 - Grievance and Grace landed + committed (Magnus3 `392cc15`, master; golden re-recorded + verified 8/8, pushed) - THE WEAVE'S ENGINE SLICES ARE CLOSED.** The asymmetry S4 named: *grievance has many doors in and one slow door out*. S4 gives it the missing doors AND the missing exit. One new `EventType::Reconciliation` (full treatment - color, export, chronicle; lint PASS), one new `CustodyCause::Returned`, 11 config leaves (validate-config **352 Earth / 148 Heaven**, clean); **no new registered flecs component** (PairRecord latches + the `siege_grief` receipt are plain fields on already-registered singletons/param structs - the raw-id rule; tables 1016 vs 1014 off, zero fragmentation).
  - **Door IN 1 - wizard emissions taint the home.** Every tower emission (plague · escaped beast · grave-raid) that accrues to the tower ledger ALSO records REAL grievance (`record_grievance`, feeding Zeus's stance exactly as a raid does) against the exile's HOME community - "everyone knows whose adept it was" - scaled by `mercury.wizards.home_taint_fraction` (0.25; the home bred the wizard, it did not send the plague). Folded into `wiz_accrue_grievance` so it covers ALL emission types by construction; a raising that would grieve the home town itself self-cancels in the aggressor==victim guard. **Measured:** over 1000y world_3region, **35 emissions (20 plagues + 15 escaped beasts) tainted a different home town, 21.5 grievance injected across 17 pairs.** 0 = off (only the tower ledger moves - the A/B).
  - **Door IN 2 - siege-stress breeds necromancers.** A community's INBOUND grievance (the chronic raiding it has SUFFERED, summed over the pairs where it is victim) lifts its Adepts' grief-scores at emergence (`mercury.necromancy.siege_grief_coeff` 0.2, cap 1.5) - the evil city the war MADE, not only where a lone mourner turned. Built once from Zeus's ledger (O(pairs)), read O(1) per candidate. **Tuned as a NUDGE, not an override (the S2 discipline - redistribute WHO, don't inflate):** first cut at coeff 0.6/cap 4.0 inflated emergence 1.56x (97 vs 62 wizards); dropped to 0.2/1.5 = **63 vs 62 over 1000y, dead in family**, while the DISTRIBUTION shifts toward the besieged. The exile event carries the causal receipt (`TowerFoundedParams.siege_grief`, exported only when >0 so a siege-off world adds nothing - the A/B): **58/63 emergences war-made; Surnholm, a war-torn hub, bred 5 of the top 8** (the necromancer y130, siege_grief 0.74). 0 = off (grief is kin-loss only - the A/B).
  - **Door OUT - reconciliation (three grace channels, each a chronicle-visible `EventType::Reconciliation` + a grievance write-down, all folded into Zeus's seasonal pair loop, O(pairs), no RNG):** **(C) intermarriage** - a sustained living cross-marriage web forgives the old wrong (`intermarriage_unions` 5, re-arms when NEW marriages push past the last fire); **(B) work returned** - the S1 sack chain run BACKWARD: a warm (Friendly+) or kin-linked pair carries a looted Work home (`CustodyCause::Returned`, communal at the town it was restored to, still lootable), big write-down + a WorkPassed(returned) so the custody chain stays honest; **(A) shared trial** - both communities outlive the SAME famine together (latched on Venus's `CurrentFamine`, fires on the joint survival), a partial thaw. All three fired in the soak: **14 reconciliations (6 intermarriage / 5 work-returned / 3 shared-trial), 24.5 grievance forgiven, 0 with all channels off.**
  - **THE STORY GATE (soak seed42 world_3region 1000y, ON vs S4-off; every quote rendered by the REAL chronicle.js via a DOM-stub node harness):**
    - **(a) a wizard's plague taints his home's standing** - the cleanest case has ZERO raids between the pair: **Taeldale and Groudale, 0 raids between them, 3.25 grievance injected purely by a resident's adept's emissions**, turned hostile at y138 (`dominant=grievance`, warmth -0.575): *"In these years Taeldale and Groudale turned hostile."* And the emission itself: *"Bomtamroul Tihorddroth loosed a plague on Persfell, and 3 fell ill"* (of Surnholm - tainting Surnholm's standing).
    - **(b) a war-made necromancer** - the `siege_grief` receipt on the exile event makes it exact: **58 of 63 emergences carry siege pressure**, Surnholm (the raided hub) breeding necromancer after necromancer (y130 siege_grief 0.74, y541/736/838 more) - the town the war made.
    - **(c) a reconciliation laid to rest, all three channels quoted with the write-down measured:** work-returned *"In these years Surnholm and Groudale were reconciled, for Donfron, taken long ago in a raid, was carried home again - and the old feud was laid to rest"* (forgiven 2.53); intermarriage *"In these years, so many marriages had crossed between Surnholm and Persfell that the old feud was at last laid to rest"* (forgiven 2.59); shared-trial *"A famine fell on Surnholm and Cirwick alike about this time, and outliving it together thawed the feud between them"* (forgiven 0.81).
    - **(d) edge 4 (omens read grievance) RE-TESTED and DROPPED - an honest null, removed cleanly (no trace, per S2).** S2's null was "War omens (~8/world) don't coincide with grievance"; now they DO (118 War omens, grievance enriched by doors 1-2). But a NEW ceiling replaces it: the 0.80 base read chance is near-saturated. A non-invasive measurement (byte-identical to baseline) found that over an ENTIRE MILLENNIUM only **6 War-omen opportunities are "flippable"** (a grieved community whose base roll missed) out of 150 - no headroom. Built the probe, measured it, removed it (code + include), documented the why.
  - **THE FEUD LIFECYCLE (ignite -> burn -> END, measured before/after).** ON vs OFF: **more feuds closed (198 vs 190), fewer left open at the horizon (17 vs 21), median duration 12y -> 9y** - feuds now truly end, not only decay. Modest by design (grace is rare - 14 moments over a millennium, each a named chronicle event, texture not silence).
  - **A/B - all knobs off = byte-identical to the pre-slice exe.** Result sections (checksum bundles the grown config, so hashed the results alone) **byte-identical across 5 cells** (7/42/99/1234x100y, 42x250y) AND world_3region - the same discipline S1-S3 held. THE CRASH FOUND EN ROUTE: a `Returned` communal Work (holder null) fell through SitesSystem's inheritance pass into `current_family(null)` when displaced - a real trajectory-dependent segfault (surfaced at coeff 0.2, y165-175); fixed by treating `Returned` as settled/communal there (like `CameToRest`, but at a living town - still lootable, so a re-taken Work can re-ignite the feud).
  - **Gates.** Build clean; lint PASS; validate-config 352/148 clean. Determinism ×2 identical (golden verify re-runs prove it). No-faceless: 48 Reconciliation + returned-WorkPassed participants, 0 anonymous. Soak 1000y no crash (43,849 entities / 121,717 events). Golden re-recorded -> **verify 8/8 PASS** (manifests in the commit). **Perf in family - same-build on/off sim 13.6s vs 13.5s (1.01x), tables 1016 vs 1014** (no fragmentation - no new components). Emergence rates in family (wizards 63 vs 62; the ~1.3x on plagues/expeditions is small-count trajectory wobble on ~20 rare events, not knob inflation).
  - **Seams, stated:** the `siege_grief` values are modest (0.5-0.74 vs the 5.0 threshold) - siege rarely SOLELY manufactures a wizard, it tips the marginal war-torn candidate (net +1 wizard, many redistributed), which IS "in family" by construction but means the door-2 signal is a nudge not a flood; shared-trial reads only co-FAMINE (not plague/beast waves) - a documented narrowing of the "same trial" idiom to Venus's cheap per-community signal; a returned Work rests communally forever unless re-looted (no living heir claims it - simpler than routing it back into the maker's line when the return target isn't the forge-home); reconciliation is sparse (grace is rare), so its feud-duration effect is real but modest. **Weave scorecard: S1+S2+S3+S4 landed (engine - THE ENGINE SLICES ARE COMPLETE); S5+S6 landed (surface). The Weave is closed.**

- **THE MERGE TRAIN - four parallel-worktree feature branches landed on master IN ORDER (T5 -> N2 -> N4 -> N5), each squash-merged with its own drill (build/lint/validate-config/determinism x2/A-B knob), then one golden re-record and a composition soak.** The night's content wave (Push 4's shapes) coming home. Every landing built clean Release, lint PASS, validate-config clean; determinism x2 identical per build; no golden re-record per-landing (greenfield re-rolls, gated once at the end). Final master `8088af5`.
  - **T5 - The missing casts (`64376a1`).** 13 new perturbation op-kinds (raid/install/depose/route/marry/famine/relieve/plague/awaken/grant/bless_line/curse_line/record_event) + 12 spellbook macros - the casts that drive an EXISTING chokepoint at the pause point. **Near-clean merge** (footprint `src/core/Perturbation.hpp` + `tools/stage/*`, touched by nobody else - zero conflicts). Drill: 227/227 spellbook tests; cast verified end-to-end on the `continue` path - a `famine` op emptied Surnholm's granary and opened a real Famine event at the stated tick (the engine resolved the starvation on resume), `record_event` injected a Chronicle flavor without abort. Plain-path determinism `0385c6fa3249e515` (final tree).
  - **N2 - Arms and scars (`2e97c43`).** Weapons borne in combat, scars/maiming (a permanent `base_health` cut), duels at a raid's joint, veterancy. **Git auto-merged textually - but the semantic convergence with S3+S4 was VERIFIED by hand, not trusted:** MarsSystem carries BOTH S3's danger-tax targeting (`mars_choose_target` danger_penalty) AND N2's combat internals (weapon index, veterancy, the Duel resolution) - non-overlapping regions, both live; `EventType` woke `Duel` (N2) beside `Reconciliation` (S4); `CustodyCause` runs sequential `TakenInRaid=4`/`Returned=5` (both kept); `HealthState` gained scars/maimed/battles, `Work` gained battles/triumphs (the blade legend); colors.js + chronicle.js carry both Duel and Reconciliation. The lint allowlist's "Duel never constructed" line retired (N2 fires it for real - real color+chronicle now). Det `0a87dbe715ad4496`. **A/B on `arms.enabled`:** ON = 9 duels/100y with scars+maiming; OFF = 0 duels / 0 maimed / 0 scars, the pre-N2 aggregate-combat class held exactly.
  - **N4 - The made (`2664a9b`).** The MachineCraft coin: a renowned Maker forges a CONSTRUCT (a `BeingKind::Construct=5`) - a Work that stands up and serves, outlives its master (the anti-vampire), stands sentinel over its fallen town, and winds DORMANT when the coin leaves the age's hand (the EraDirector hook). **First coin of the registry convergence - auto-merged; `Technology::MachineCraft` appended after `BloodedLines`, sequential no-collision, registry still topologically sorted (prereq Callings precedes).** 3 new EventTypes (ConstructForged/Service/Dormant) with full export+color+chronicle (lint PASS); TechParams + schedule + coin card all carried. Det `d335d752da8efcc6`. **A/B:** demo config ON = 3 constructs forged/300y; `mercury_no_machine` OFF = 0. Coin-absent is the pre-N4 byte-class by construction.
  - **N5 - The salt road (`5daa342`) - THE REGISTRY CONVERGENCE, resolved by hand.** The Seafaring coin pays the walkable-colony debt ("the isles wait for boats"): coastal fishing (a Venus food channel, new `FoodType::Fish=1`), sea-aware route fields when the coin fires, island settlement across the strait, ships as community Works (`WorkKind::Ship=6`), and wrecks (`SiteKind::Wreck=5` + a `Shipwreck` event drowning a NAMED crew, `DeathCause::Sea`). **FIVE real conflicts - the two coins meeting in every shared table:** `Technology.hpp` (both coins in the enum AND the registry, MachineCraft then Seafaring, each before Count), `EventComponents.hpp` (both the ConstructDormant and the Wreck param blocks - the merge had truncated ConstructDormantParams, rebuilt from both branches), `MercuryConfig.hpp` (both schedule Entries + both `schedule_for` cases), `MercurySystem.hpp` (both includes), `colors.js` (construct + wreck). **Every six-family enum extended collision-free** (EventType/SiteKind/WorkKind/DeathCause/BeingKind/FoodType - each value unique and sequential). **Season-loop call order documented:** `seafaring_open` fires once inside the tech-firing block when the Seafaring coin lands (a one-time sea-aware field recompute); `constructs_season` runs in the per-season cadence after roads (necromancy -> roads -> constructs) - two well-defined points, no ambiguity. Det `0385c6fa3249e515`. **A/B on `seafaring`:** ON = 31 ships / 21 shipwrecks / fishing (300y default - the default world has coast); OFF = 0 ships / 0 wrecks / 0 fish, the landlocked class held.
  - **Post-train - golden, soak, composition.** Golden verified FAIL first (0/8, greenfield re-roll expected - seed42-100y `a2df...` -> `0385c6fa3249e515`), re-recorded ONCE, re-verified **8/8 PASS** (`Test:` commit `8088af5`, baselines + manifests). **One 1000y soak (seed 42, default): no crash, checksum `5d303c495cda98ff`, 22,154 entities / 61,563 events / 21 communities / 20,220 persons** - all four features alive TOGETHER in one world: **N2** 257 duels / 262 maimed / 1,253 scarred / legendary blades up to **209 battles + 137 triumphs** borne amid **1,379 raids** (the "raiders with legendary blades" texture, real); **N4** 3 constructs forged; **N5** 127 ships / 121 wreck sites / 121 Shipwreck events / **307 sea deaths** - composing beside 349 undead + 123 wizard-towers + 126 ruins without extinction or snowball. **Perf: 7.0s wall / 6.04s sim** - ~1.1-1.2x the perf-review standing baseline (5.8s wall seed42 default), comfortably under the 1.5x composition-flag threshold; the four slices stack with no quadratic blowup (all additions are value components / derived reads, no table fragmentation). **Cleanup:** the four branches + their four worktrees removed, pruned; main tree clean at `8088af5`. (Left untouched: `n1-vampires`, `n3-the-grimoire`, `n6-the-unquiet` worktrees - other agents' in-flight work, not this train's.)
  - **Seams, stated:** the merge trusted git's textual auto-merge on N2/N4 only after hand-verifying every enum/registry/switch coexistence (the raw-id rule makes a silent enum-order slip a trajectory shift, so the convergence was audited, not assumed); N4's `machine_craft` defaults to year 0 (available from world open) while N5's `seafaring` defaults to year 100 - both design defaults, carried as-is; the wreck SITE surfaces in the export as `type:"wreck"` (121, matching the events), not a `site_kind` field - a reader convention worth noting; the S3-absolute 1000y baseline isn't separately recorded in the logs, so perf is stated against the nearest recorded reference (the perf-review 5.8s) - honestly ~1.2x, not a precise S3 ratio.
- **T3 — The skirmish landed + committed (Magnus3 `e591bc2`, master; 277/277 spellbook tests; pushed) — THE TABLE'S FIVE WAVES ARE ALL LANDED.** Table combat over named scene participants, the last of the-table.md's T1–T5. `stage skirmish <a-ids…> vs <b-ids…> [--seed-note] [--seat] [--label-a/-b]` (+ `POST /api/skirmish`, the T2/T4 endpoint idiom). Footprint `tools/stage/` + `tools/seats/` + `serve.py` only — the two sibling worktrees (map-v2, others) untouched; near-clean, no conflicts on pull.
  - **The mirror upgrade (the point).** `tools/stage/combat.py` — the session-grain die — was mirroring the PRE-N2 core; it now mirrors the merged `mars_combat_power` (MarsSystem.hpp:168-188): the old `age_curve × calling × (1+heat·0.40) × current_health` **times the two N2 factors** — veterancy `1+min(0.50, battles×0.05)` (mars_veterancy_factor :130-134) and a borne blade-Work's bonus `1+min(1.20, 0.35 + triumphs×0.08)` (mars_borne_weapon :153) — read straight off the export's new fields (`battles`, work `custody` + `work_triumphs`). Scars/maiming need no factor — they already dropped `current_health` via `base_health`, exactly as in the engine. `borne_blade()` builds the weapon index from BOTH the export custody array (the sim's source) and a table `bestow` Custody event, so sim and table agree on who is armed. **Mirror parity spot-checked in tests against the documented arithmetic for 5 fighters.**
  - **The fight model** (`tools/stage/skirmish.py`). Seeded initiative (age-curve × trait); rounds of exchanges depleting an hp pool derived from `HealthState` (hp starts at `current_health`, ceiling `base_health`; a fighter's EFFECTIVE power scales with hp, so a wound weakens him as `current_health` does in the engine); party-vs-group over uneven sides; a per-calling special, one per fighter (**a blade presses** 1.6×, **a keeper steadies** the line, **an adept works a disorienting art**, else a rally); a felled fighter is SLAIN or left grievously wounded on a seeded lethality roll; a side YIELDS when its remaining strength collapses past a morale floor. Deterministic on (campaign checksum, the `skirmish` cast-log ordinal, the sorted ids, a seed-note) — the check idiom; a re-fight reproduces, a different seed-note diverges.
  - **Seat parity.** Each round's player-facing choice (attack whom / defend / aid / flee / the special) is a frozen `Decision` built from the scene through `build_pc_context` (now-scoped, secret-filtered — enemies shown by COARSE condition, never an exact hp number), the identical object an LLM / heuristic / human seat sees; **resolution rolls AFTER the choice, engine-side** — seat-independent, un-rerollable. The heuristic seat is wired through a full fight in the tests; the LLM adapter gets the same shape by construction.
  - **Writeback — the point.** `perform_skirmish` casts ONE record_only `skirmish` macro (spellbook, `_c_skirmish`, + a `raw` param kind for the precomputed outcome) whose primitives are the fight's **wounds** (set health + Wound event), the **fallen** (the kill shape — set end_tick/cause_of_death + Death event), and a **Skirmish Chronicle record** naming both sides + the turning moment. One `cast_id`, so T1's cast-aware **undo removes the whole fight**; the deaths ride the perturbation bridge like any `kill`, so an `advance` simulates around a fight that really happened. One general fix fell out: the perturbation compiler now **skips a `set end_tick` op** — a resolve-only lifespan marker, not an engine-writable field (a death rides via its paired cause_of_death → kill_person); the codified `kill` sidesteps it by being non-record_only, but a record_only composite emits it as a raw op, and the real advance rejected the unsupported set until this landed.
  - **Verification (test_spellbook.py, 277/277 — was 227).** Determinism (same choices identical, different differ — proven with two seats that always-attack vs always-defend). **A legendary blade shifts Ath's win-rate 0.52 → 0.98 (+0.46); a 20-battle veteran +0.34; a maiming (cur .55/base .65) −0.52** — the mirror's factors visibly decide who is dangerous. 3v5 resolves sanely with a morale break exercised. Writeback: the log holds the wound/kill casts + the record, all named; undo drops the whole cast group. A heuristic seat plays a full fight through Decisions (stream reproducible; no enemy hp leaks). The death compiles to a `kill` the engine carries + the record rides as injected flavor.
  - **End-to-end on a REAL seed-42 export** (fresh campaign, the saga campaign was build-drifted by N2). A 3-vs-7 at **Surnholm** (year 90), the party's blade **Veinscoultaens bearing the blade Kilbeir** (power 1.66) against Fuldlourm raiders incl. a **13-battle veteran gatherer** (power 1.30): resolved over **6 rounds, four fallen**, the raiders carried the day when the Surnholm party yielded. The writeback appended **19 primitives under one `cast1`** (3 wounds, 4 named kills, the Skirmish record); the four fallen read dead in the resolved table. A real **`stage advance --years 2`** then applied 15/15 entries and **carried all four deaths** into the re-simulated world (each dead of violence at the fight tick, found by the name-bridge). The between-sessions loop closes on a fight that happened.
  - **Seams, stated:** a fighter of ~0 combat power (a child / the very frail, `age_curve→0`) deals ~0 damage — faithful but reads oddly in a transcript; the Red Hand war-prodigy bloodline factor and undead fixed-power fielding are NOT mirrored (niche divine cases the table does not yet reach); the fight's tuning constants (DMG_BASE, morale/lethality rates) are session-grain provisional, copied not shared; `skirmish` is record_only so it shows in the promotion report though its bites are mechanical (the death primitives already ride the bridge — the semantics is honestly record_only at the macro level, exactly like a bare cause_of_death set). **the-table scorecard: T1 (surviving campaign) + T2 (check/sheet/gap) + T3 (the skirmish) + T4 (the town knows) + T5 (the missing casts) — ALL FIVE WAVES LANDED. The Table is complete.**
- **MERGE TRAIN #2 — the three remaining new-tenant branches landed on master IN ORDER (N6 → N3 → N1), each squash-merged with its own drill (build/lint/validate-config/determinism ×2/A-B off-class/signature), then one golden re-record + a composition soak. Push 4's last shapes come home.** All three branched before the T5/N2/N4/N5/T3 train, so conflicts were expected and enum collisions guaranteed; the convergence was resolved by hand and audited, never trusted to git's textual merge. Final master `2e72f6b`.
  - **The enum convergence (this train's heart).** Master already carried N4's `BeingKind::Construct=5`, N5's `WorkKind::Ship=6` / `SiteKind::Wreck=5` / `DeathCause::Sea` / `FoodType::Fish`, and N2/S4's `Duel`/`Shipwreck`/`Construct*`/`Reconciliation` + `CustodyCause::TakenInRaid`/`Returned`. Each incoming branch's enum values were renumbered to APPEND after what master held, deterministically, no collisions: **`BeingKind` — Construct=5 (N4), Ghost=6 (N6), Vampire=7 (N1)**; **`WorkKind` — Ship=6 (N5), Grimoire=7 (N3)**; **`DeathCause` — …Sea (N5), Vampire (N1)**; every new `EventType` appended to the union. Then every dependent table was converged — export name switches, `colors.js`, `chronicle.js` handlers, inspect panels, TechParams, config structs, and `being_counts_as_dead` (Undead|Vampire). Each branch's enum literals were grepped after renumbering to catch hardcoded values (the forge-roll bound in SitesSystem was the one that mattered — kept `WorkKind::Ship` so the roll still spans Blade..Loom, excluding both Ship and Grimoire).
  - **N6 — The unquiet (`8a09445`).** Ghosts (Zeus/Relations — grievance's supernatural client) + cursed Works (Venus/Sites — the curse rider on things). A `BeingKind::Ghost` rises where a pair's grievance stays HIGH and a recent violent dead anchors it; it only BE SEEN (combat 0) until laid to rest by S4's reconciliation (the wrong repaid / the Work carried home) or, with hysteresis, by grievance decaying to the floor. A Work is proven cursed when its custody chain shows CONSECUTIVE holders slain by violence — a modest health shade on the living holder, the chain the indictment. Three EventTypes (Haunting/LaidToRest/WorkCursed); state on RelationsState/SitesState fields (no new component). **Conflicts (4): BeingComponents (enum+registry — Ghost→6), EventComponents (the Unquiet param block beside N4/N5's Made+Seafaring blocks), colors.js, ExportCommand (cursed_origin beside dormant_tick).** Det ×2 `41731b251b79734f`. A/B off (`rule.stance.unquiet` + `rule.callings.works.curse`) → 0 ghosts / 0 cursed, 21 shipwrecks unaffected. 300y signature: **8 ghosts, all arced rose→rest; 2 cursed Works** — coexisting with 21 shipwrecks / 3 constructs / 71 duels / 2 reconciliations.
  - **N3 — The grimoire (`3452487`).** Magic as material culture. Grimoires — a resident wizard WRITES their art down as a `WorkKind::Grimoire` (medium: text), riding all Work custody machinery, its coin the art (Necromancy) so E6's stamp makes it a LOST SPELL in a later age. `EventType::Teaching` woken from its placeholder (a Maker takes an apprentice — a craft's lineage). A new `EventType::Ritual` — a Keeper's warding rite at a shrine, a wizard's dark rite at a tower. **Conflicts (5): SiteComponents (WorkKind enum+noun — Grimoire→7), SiteHelpers (`work_coin` unioned Grimoire→Necromancy + Blade→War + Ship→Seafaring), SitesSystem (two — the forge-roll bound, and a big structural one), ExportCommand, colors.js.** **The judgment call:** N3's teaching pass and N6's cursed-Works pass both appended a "Movement" pass at the tail of the same SitesSystem function; N6's curse pass early-returns on `!curse.enabled`, so the teaching pass was placed BEFORE it — otherwise disabling cursed Works would silently disable teaching, coupling two independent A/B knobs. Det ×2 `f8416bee76f1d952`. A/B off (`mercury.lore.enabled`) → 0 teaching / 0 ritual / 0 grimoire, WorkForged unaffected. Signature: **1 grimoire at 1000y** (a handful per millennium, as designed); 138 Teaching + 2 Ritual at 300y.
  - **N1 — Vampires (`6ce831a`) — the third apex tenant, and the missing mechanic for the Age of Crows.** A `BeingKind::Vampire` is the SELF-BOUND undead: a dead person risen with no necromancer's leash — keeps its name/face/grudges, stops aging, and NEVER releases (so unlike a bound undead it does not fall when its binder dies — the thing that lets the dead outnumber the living AT a world's death). Rare diegetic origins (a strike gone deeper, a cursed violent death, the dark gift given); a hunger-scaled feeding loop; a parlor vampire drains subtly and persists as the unaging elder until COUNTED and expelled to a lair; the stake party is containment. Five EventTypes (VampireRisen/Feeding/Embrace/VampireRevealed/Staking) + `DeathCause::Vampire`; new files VampireComponents / VampiresSystem (single writer); driven configs `crows_*` + `eras_vamp_*`. **Conflicts (6): BeingComponents (enum+registry — Vampire→7), EntityComponents (DeathCause), MercuryConfig (Vampires block beside N3's Lore), TechParams (26 vampire leaves beside 12 lore leaves), chronicle.js (two — the being arc + the death-line), ExportCommand (two — territory anchoring + DeathCause name).** **The territory-anchoring convergence:** N6 made a ghost community-anchored; N1 generalized to key off what the territory IS (a vampire is community-anchored as a PARLOR, region-anchored as a LAIR) — kept both (a ghost's town has CommunityIdentity, and Ghost kept explicit in the OR for zero doubt). `being_counts_as_dead` tallies Undead|Vampire so the E2 dead census counts them together. Det ×2 `8a2084c7124dfb91`. A/B off (`mercury.vampires.enabled`) → 0 vampires, undead (57) unaffected. Signature (300y default): **5 vampires rise (1 by embrace — the bite-line), 22 feedings / 19 kills, 7 stake parties.**
  - **THE AGE-OF-CROWS RE-PROOF (the renumbering's real test).** Ran N1's `crows_*` config on the fully merged tree (the vampire-takeover tuning): **the world dies with `cause: "dead"` — "the dead outnumbered the living", dominant_doom 2, "the Age of the Unburied", death year 22, final_living 0, final_undead 23.** The Vampire=7 renumbering did NOT break the dead census — vampires still count as dead through `being_counts_as_dead`, and the E2 doom ledger tallies them with the undead exactly as before.
  - **Golden.** Verify FAILED 0/8 first (greenfield re-roll — new components shift ids, new events fire), re-recorded ONCE, re-verified **8/8 PASS** (`Test:` commit `2e72f6b`, baselines committed).
  - **The composition soak (1000y, seed 42, default): no crash, checksum `00fe73fd48f5ac7f`, 22,638 entities / 64,045 events** — every tenant of both trains alive TOGETHER in one world. **This train:** 23 ghosts (23 Haunting → **23 LaidToRest, every ghost that rose was laid to rest**, riding 24 reconciliations), 26 cursed Works, 19 vampires (18 risen, 68 feedings, **33 stake parties** — the towns fight back, 1 embrace), 8 grimoires, 32 rituals, **1,658 Teaching** (the craft lineages thread everywhere). **Prior guard, composing beside them:** 110 ships/wrecks + 270 sea deaths, 3 constructs, 215 duels amid 1,311 raids, 120 wizard-towers, 193 foretellings, beasts (172 wolf / 88 lion / 35 bear), 329 undead, 122 ruins, 756 Works, 4 shrines. **Deaths by cause: 3,107 violence, 270 sea, 92 vampire, 51 beast.** The interactions the design wanted are present and legible: a cursed blade is proven by the very raids and duels that slay its bearers; a ghost rises at a wronged town and is laid to rest when a looted Work is carried home; vampires rise and are answered by stake parties without tipping the default world to an Age of Crows (that needs the `crows_*` tuning). Rates read as a living world, not a monster carnival — the rarest tenants (grimoires 8, constructs 3) are era-defining, the mid tenants (ghosts/vampires/rituals/cursed-works ~20-30) uncommon, and only **Teaching (1,658) reads as frequent** — but it is the gentlest new event (no combat, no death; a craft's quiet inheritance), texture not spectacle. **One honest seam for a morning: 68 Feeding events vs 92 vampire-caused deaths don't reconcile at a glance** (embraces kill too, but not the whole gap) — worth a look, not a blocker.
  - **Gates.** All three build clean Release (absolute build path); lint PASS; validate-config OK each. Determinism ×2 byte-identical per build. **277/277 spellbook tests green** — the table layer survived the merges. **Perf: 1000y default 7.74s exe-internal / 8.4s wall vs the 5.8s reference ≈ 1.33× — elevated (three new seasonal passes; Sites now 1.77s) but comfortably under the 1.5× (8.7s) composition-flag line.**
  - **Cleanup.** The three branches deleted; worktrees removed + pruned (`git worktree list` = main only, `git status` clean, master `2e72f6b`). One benign leftover: the `n6-worktree` temp directory (under AppData/Temp, outside the repo) held an OS file lock and could not be deleted — git's worktree registration is already pruned, so it is a stale temp folder for OS eviction, not a repo concern.
  - **Seams, stated:** the teaching-before-curse ordering in SitesSystem is a deliberate call to keep two A/B knobs independent (documented in code); grimoires are genuinely rare (0 at 300y/seed42, 1 at 1000y) so their signature was proven at the millennium horizon, not the 300y spot-run; VampireRevealed fired 0 at 300y (the parlor-reveal needs 40+ unaged years — vampires get staked first at that horizon) but the mechanism is exercised in the crows re-proof; the Feeding/vampire-death count mismatch above is unexamined. **Push 4 (New Tenants) COMPLETE — N2 · N4 · N5 · N6 · N3 · N1 all landed; six new tenants (arms, the made, the salt road, the unquiet, the grimoire, vampires) live in one world.**
- **THE NIGHT SHIFT WRAPS — ALL FIVE PUSHES COMPLETE.** Master `2e72f6b`, docs `c2e202f`, both clean, both pushed; one worktree (main), all scratch branches and worktrees gone; golden 8/8 on the final tree; 277/277 table tests; composition soak green at 1.33× perf. The ledger of the night, in order: the raider rebalance (drift+cap) → **Push 1, the End** (E1 doom ledger byte-inert · E2 cascades+seals, worlds mortal · E3 the terminal event — "the Hollowing", the dead world a navigable product) → **Push 2, the Ages** (E4 the turning spine · E5 the peoples, another folk's work · E6 the hands and stamps, lost arts derived never stored) → **Push 3, the Weave** (surveys → contract → S1 war&renown · S2 blood&blessing · S3 the living road, 82% warm vs 18% — the road writes peace · S4 grievance&grace, feuds truly end · S5 the faces of heaven · S6 the knowing player) → **Push 5, the Table** (the Luna rehearsal → contract → T1 the surviving campaign · T2 check/sheet/gap · T4 lore + the town · T5 thirteen casts · T3 the skirmish — sim and table agree on who is dangerous) → **Push 4, the New Tenants** (N2 arms&scars · N4 the made, Grernfra 918 years at its labour · N5 the salt road, the isles wake · N6 the unquiet, ghosts laid to rest by returned Works · N3 the grimoire, spells no living hand can read · N1 vampires — **the Age of Crows landable at last**, closing E3's seam) → two merge trains converging every enum and coin. Morning looks, honestly queued: the Feeding/vampire-death count gap; Teaching volume (1,658/millennium — texture or weather?); vampire seed-variance (an exponator sweep would steady the rates); sea-route polylines don't render on the drawn map; the composition perf at 1.33× (S3's danger field is the known heavy edge). Contracts for everything live in docs/research/2026-07-12-*. **Next: session zero.**
- **SESSION ZERO — played, retired, and worth it.** The first table ran on seed 45 ("TheShrinesUnderWrath", 500y): Cursford, a frontier shrine town in a merry wedding season; the hook (Ryan's pick of four): **the masterless made thing** — Mandmath, 400-year construct of famine-struck Saefell, walks nineteen days over the high wild way on grinding knees and kneels at the harvest-goddess's shrine: a petition of presence from a servant that does not know how to ask. Three Sonnet seats (adept/blade/gatherer) played two beats of genuinely fine table — the adept's critical lore read ("kneeling is PETITION; only plain questions bind; 'I hear you', not 'I receive you' — the latch stays shut"), the blade turning his back on the construct to face the real danger ("It came to kneel. I'll cut the next fool who forgets that"; "That's not a coincidence; that's an address"), the gatherer reading the water-marked fords in the mud. Real dice (a crit and a partial), real casts, the gap ledger working live (8 gaps: skim config-file support, a 'stage world' dossier, scene ids, stage party, being event-histories, exported food stores, offered-checks/crit-cost-structure/declare-intent). **Then the session's real finding, from the DM, critical: Luna ran the table as an occluded pipeline** — prompts, seat responses, dice, NPC voicing, and world edits invisible to Ryan; his whole surface one three-option prompt; "I'm lost... [you played] by yourself, rather than as my assistant." The campaign is retired (too far gone to be his), the lesson institutionalized: **`docs/design/luna-protocol.md`** (the beat contract — every beat renders verbatim in chat: table text, each seat's words, dice, world edits, LUNA'S SECRETS ALWAYS VISIBLE TO THE DM, levers offered, STOP by default; stage-setting as a sequence of DM decisions with materials shown before they become world-fact; UX collected inline per-turn, never deferred — the seats' transcripts proved non-durable) and **`docs/design/luna-compendium.md`** (Ryan's ask: the toy box a non-expert Luna needs — cosmology in one page, all 15 coins, the full bestiary vampire-to-construct, Works/sites/lore mechanics, the dooms and the Ages, the census habit: what CAN exist, how to spot it, how to bring what's absent). Committed `fc2cbae` + `83339bb`. Next session starts from the protocol, at the DM's pace.

- **L1 — THE GENERATED COMPENDIUM landed + committed (Magnus3 `bc73bec`, pushed; 277/277 table tests, 33/33 new compendium tests, lint PASS).** The Living Toybox's KNOW layer: `docs/design/luna-compendium.md` is no longer hand-written — its toy-box body is GENERATED from the engine's own registries, so it can never drift from the code. Ryan's intent made mechanical: a new concept lands in code, describes itself once, and appears in the compendium forever; lint makes forgetting impossible. New footprint `tools/compendium/` (generate.py, concepts.json, preamble.md, test_compendium.py) + one verb in `tools/stage/stage.py` + a `[compendium]` section in `tools/lint_content.py`. Python only, no engine changes, no golden impact.
  - **The concept registry (`concepts.json`, 109 entries).** One entry per concept keyed `<kind>/<name>` — blurb (table-facing), spot (how to recognize it in a world's data), bring (how to introduce it when absent: a cast / a world-gen knob / emergence), knobs. Seeded FULLY from the hand-written compendium — every concept it carried got its entry, the careful prose preserved. Ten code-backed kinds (coin 15 · being 8 · work 8 · site 6 · line 4 · doom 3 · macro 26 · calling 5 · wizard 3 · custody 6) plus free prose kinds (social/archetype/art/treasure/heaven/era/mechanic) for the knowledge that has no single registry.
  - **The generator (`generate.py`).** Parses the registries from source with the SAME regex-over-text idiom as lint_content.py (no build, stdlib only, `Technology.hpp`/`BeingComponents.hpp`/`SiteComponents.hpp`/`BloodlineComponents.hpp`/`DoomConfig.hpp`/`EntityComponents.hpp`/`NecromancyComponents.hpp`/`spellbook.py`), joins with concepts.json, and emits a two-layer doc: a static hand-curated cosmology preamble (`preamble.md`) + a GENERATED body below a `do-not-hand-edit` marker. A registry row with no concept renders a loud `[!] UNDESCRIBED` stub. Deterministic ordering (registry order for code-backed kinds, name order for free) so a regeneration diff is meaningful; idempotent (regen twice → identical, body hash `7b057690…`).
  - **The lint guard (staleness both ways + doc-drift).** `[compendium]` fails if any enforced registry row lacks a concept (`compendium`), if a concept names a row the engine dropped (`compendium_stale`), or if the checked-in doc is stale vs a fresh generation (`compendium_fresh`, a body-hash compare). Proven live: deleting `being/vampire` → two GAPs (the missing entry + the doc-drift), restored → PASS. Allowlist idiom carried (`check:item # reason`).
  - **The stage verb.** `stage compendium` prints the toy-box summary; `--regen` rewrites the doc; `--world <campaign>` renders the CENSUS — each concept annotated with this world's reality (present with counts / past-in-the-record / absent-and-how-to-bring), from the campaign's resolved state. The protocol's census habit is now one command. Census on **TheShrinesUnderWrath** (seed 45, 500y): constructs PRESENT (3), wrecks PRESENT (40 sites), ruins/caves/shrines/venues standing, beasts alive (lion/wolf/bear), works all extant in hoards — but **no living dragon (1 in the record), no vampires, no ghosts, no undead**; towers read PAST (both fell — the 2 standing ruins ARE those fallen towers, an honest census correction to the pre-run guess); Evergreen blood manifest in 1 of 3,510 carriers; the three dooms measured-but-sealed. Each absent tenant names its Bring (create_being for a vampire, the machine_craft knob for a construct, emergence for a ghost).
  - **Docs.** `luna-protocol.md` updated: the compendium reference now says it is generated (`stage compendium --regen`), stage-setting's world-shopping census cites `stage compendium --world`, and the instruments table gains the toy-box row.
  - **What changed vs the hand-written doc:** equivalent knowledge, reorganized as generated sections (coins · people&towns · beings · wizards&arts · works · sites · heaven · the end&ages · mechanics). The cosmology one-pager survives verbatim as the static preamble. Two mechanics that were prose-only in the original got their own dedicated entries in the rebuild (legendary/cursed Works; stamps & lost arts). Every coin, being, work, site, line, doom, and cast the original named is present, now each with an explicit spot/bring.
  - **Seams, stated:** the census reads the export's `type` string for beings/sites and `work_kind`/`divine_line`/`wizard_archetype` fields — a deathless thing (a Work in a hoard, a ruin) is counted alive via `end_tick >= now` because the export clamps a still-living Life-event to the world's final tick; coin presence is NOT censused per-coin (coins are config/substrate, described with their knobs, not counted like furniture — matching the protocol's census list); calling/wizard/custody are enforced beyond the spec's floor (they parse cleanly and it strengthens the guard), while archetype labels are described as free prose (their push_back extraction is fragile enough not to gate a lint on). L2–L5 of the Living Toybox (the proto kit, session logging, the normalization report, the concept menu) remain queued behind this — L1 defines the concept registry they reference.

- **L2–L4 — THE IMPROVISE→NORMALIZE LOOP CLOSES landed + committed (Magnus3 `ed56262`, pushed; 277/277 spellbook + 33/33 compendium + 70/70 new proto tests, lint PASS).** The Living Toybox's middle three layers, Ryan's ask made mechanical: a Luna sets a "spell" up in one line mid-session, its whole life is captured with provenance, and after the session a report hands a build agent the brief to normalize it native — "we don't want any of our creative energy to go to waste." The griffin is the canonical case, run end-to-end for real. Python only; footprint `tools/stage/` (model/spellbook/gap/stage + new `proto.py`, `sessionfile.py`, `test_proto.py`) + `docs/design/luna-protocol.md`; no engine changes, no golden impact.
  - **The mechanism that makes protos free (L2): the engine doesn't need to know about griffins for a griffin to work at the table — it only needs a body to ride.** `stage proto new griffin --rides lion --name-in-world "the Shrine-Wing" --concept "…" --fields '{…}'` spawns a REAL lion through the EXISTING create machinery (so T5's bridge already carries it through an `advance` — nothing new to bridge) and records a **proto envelope** in `campaign.protos[]`: name, concept line, what it rides (+ the created entity's authored id), fields, born-at (tick/year + session/beat), and a provenance tag `proto:<name>`. Ride targets: a being kind (undead/lion/wolf/bear/dragon), `person`, `place:<cave|tower|ruin>`, `work` and `record` (record-only envelopes — a faction, a weather, a ritual form; the engine has no mid-session Work generator, so `work` captures the intended WorkKind for the brief without pretending to spawn one — a stated seam).
  - **Provenance without a mutable ledger (the design's spine).** Three optional envelope keys added to `validate_edit` (`proto`/`session`/`beat`) — pure audit metadata, never touched by resolve. A proto's **usage ledger is DERIVED from the edit log** (every edit carrying the proto tag OR touching the ridden entity), never stored — so undo, which is truncation, can never corrupt it: undoing the creating cast drops the create edit and the proto reads `dead` again (derived from whether its creating edit still stands); undoing a tagged cast drops it from the ledger for free. `--proto <name>` stamps a cast/note/event into the ledger; a minted macro bound `--proto` at mint time carries it implicitly (a griffin's signature move in the griffin's record). `stage proto show`/`list` render the whole recorded life.
  - **The session file (L3) — the durable transcript.** `stage session start [--title]` opens `campaigns/<name>.sessions/NNN-<date>.md` and points the campaign at it; `stage session beat --file <b>` (robust on Windows; else stdin) appends the protocol's rendered block and auto-numbers beats; `stage session end` closes it with a wrap stub + the session's gaps folded in. While a session is open, **every new edit/gap/proto/mint is auto-stamped `{session, beat}`** (the beat counter lives on the campaign) — the record cross-references itself, and born-at refs render in `proto show` and the report. Closed campaigns stamp nothing (byte-identical to a pre-L3 log — the 277 existing determinism checks unaffected). `luna-protocol.md` updated: rendering a beat is now TWO writes (chat shows it, `session beat` keeps it — the seats' transcripts aren't durable), plus session start/end in the instruments table and the stage-setting/wrap sequences.
  - **The normalization report (L4) — the loop's exit ramp, formatted for a build agent.** `stage protos report [--all-campaigns]`: per proto — the envelope, the full usage ledger (with tick + session/beat refs), **session-file excerpts** (the beats it was referenced in, trimmed at the next heading), and a **pre-filled brief**: proposed native shape inferred from what it rode (a being → `BeingKind::Griffin` registry row + a control law in its owning god's layer; place → SiteKind; work → WorkKind; person → a calling/preset; record → a system hook), observed behaviors grouped from the ledger, open design questions tailored to the ride, and a **DRAFT `concepts.json` entry** (keyed `being/griffin`, blurb/spot/bring/knobs + a `census.absent` hint) so L1's compendium is ready to receive the concept the moment it lands. The **spellbook promotion report folds in** as a second section (record-only mints cast often — protos and mints are one loop at two grains). The envelope records the **migration anchor** (ridden entity id + tag) so a FUTURE proto→native cast is possible — noted as the designed seam, NOT built.
  - **The griffin, end-to-end on the REAL TheShrinesUnderWrath (seed 45, 500y, export regenerated from cache).** `session start` → beat 1 (a shadow over the ruined shrines) → `proto new griffin --rides lion` (entity `a1` "the Shrine-Wing", born s1b1) → a `known:` note + a Predation event, both `--proto griffin` → beat 2 → `session end`. `protos report` rendered the whole brief: ledger of 3 (born/note/event, each `<s1b1>`), the born-beat excerpt pulled from the session file, `BeingKind::Griffin` as the proposed shape, the four open questions (apex/wild? dens where? bindable? power?), the pasteable `being/griffin` draft entry with `census.absent: castable`, the migration anchor (`a1` / `proto:griffin`), and the folded-in promotion section catching the campaign's 2 prior `check` casts. **The campaign was restored byte-for-byte afterward** (backed up, restored, sessions dir removed — left exactly as found; 5 edits, 8 gaps, no protos/session keys).
  - **Gates.** 70/70 new proto tests (being-ride: envelope+entity+texture-fields+auto-linked ledger+compile-perturbations carries the lion as `create_being`; record-ride: envelope-only, `--proto` ledgers, untagged casts don't leak; undo→dead+round-trip clean; session start→beat×2→gap+proto born-refs→end wrap+gaps; parse_beats stops at the Wrap heading; report brief all-sections + promotion fold + draft-kind mapping per ride). 277/277 spellbook + 33/33 compendium still green (the new optional envelope keys and session-open stamping are inert unless used). `lint_content.py` PASS — protos are campaign-side, never `concepts.json` entries until normalized, so L1's compendium guard is untouched.
  - **Seams, stated:** the ledger's "touches the ridden entity" is a structural match (target/from/to/participants/entity_id == the authored id) — a cast that affects the proto only obliquely (a community-wide event near its den) won't auto-link unless flagged `--proto`; `check --proto` was not wired (checks are person-only and a griffin-lion isn't a person, so the auto-touch path already covers a person-riding proto — the explicit flag lives on cast/note/event); a `work` proto rides nothing yet (no mid-session Work generator) so its envelope is record-only with the WorkKind captured for the brief; the migration cast (proto→native, re-keying the ridden entity to the real kind post-normalization) is a designed seam with the anchor recorded, deliberately not built. **Living Toybox scorecard: L1 (the generated compendium) + L2 (the proto kit) + L3 (the session file) + L4 (the normalization report) landed. L5 (the concept menu — `stage new --concepts`) remains queued; the registry makes it cheap later.**

- **L5 — THE CONCEPT MENU landed + committed (Magnus3 `0d707a6`, pushed; 277/277 spellbook + 33/33 compendium + 70/70 proto + 33/33 new gating tests, lint PASS) — THE LIVING TOYBOX IS COMPLETE.** The pick-and-choose layer L1's registry was built to make cheap: `stage menu` renders the whole toy box as a furniture catalog (blurb, default state, lever), and `stage new --concepts "<spec>"` compiles a DM's picks straight into the config overrides `new` already accepted. Python only; footprint `tools/compendium/` (new `gating.json`, `gating.py`, `test_gating.py`) + `tools/stage/stage.py` + `docs/design/luna-protocol.md`; no engine changes, no golden impact.
  - **The gating map (`gating.json`, 109 entries — one per `concepts.json` row, none missing/stale).** Three shapes: **toggle** (34 concepts — a single leaf, two values: all 14 non-substrate coins' `mercury.technologies.<key>.enabled`, `mercury.vampires.enabled`, `mars.raider_cultures.enabled`, `rule.stance.unquiet.enabled` (ghosts!), the three `world.doom.<x>.sealed` seals (enable = UNSEAL), the four `world.bloodlines.<god>.conception_chance_per_year` lines (evergreen/crowned/open_eye/red_hand, individually zeroable), `world.sites.shrines.enabled`, `rule.expression.omens/providence.enabled`, `world.fate.enabled`, `mercury.lore.enabled` (grimoire/rituals/teaching, shared), and the three `mercury.wizards.weight_<archetype>` discovery-table weights (necromancer/beastcaller/plaguewright, individually zeroable to 0.0)); **preset** (2 — `era/turnings` and `era/peoples`, too structured for a bool: named templates with `{param}` substitution over `world.eras`/`world.peoples`/`world.first_people`, a `disable` shape resetting to empty); **none** (73 — substrate/always-on/emergent/rides-another-concept's-coin, each with a stated reason, e.g. lion/wolf/bear/dragon share `coin/beasts`'s single lever, macros are engine primitives, archetypes are derived labels). Every leaf was grepped against the real structs first (`WorldConfig`/`MercuryConfig`/`MarsConfig`/`BloodlinesConfig`/`DoomConfig`), then proven against the ACTUAL build's `validate-config` leaf listing — no guessing.
  - **The spec grammar.** Comma-separated tokens: `-name` disables a default-on toggle, `+name` enables a default-off one, `+name:preset[@param]` fires a named preset (`+name@param` uses the concept's default preset), `-name` on a preset concept resets to its off shape. `name` resolves bare ("necromancy") when it names exactly one concept across every kind, else qualified ("coin/necromancy", disambiguating from "wizard/necromancer"); a `gating: none` concept or an unknown/ambiguous/typo'd name is refused LOUDLY (near-miss suggestions via `difflib`, e.g. `-necromancie` → "did you mean coin/necromancy, wizard/necromancer?"). Compiled overrides MERGE with an explicit `--config-overrides` (the hand-authored JSON wins on conflict), echo before generation, and stamp the campaign (`meta.concept_spec` + `meta.concepts_applied`) — the furniture list is part of the recipe record, same as seed/years. One Windows/argparse gotcha documented in `--help`: a spec starting with `-` needs the `=` form (`--concepts=-necromancy`) or argparse mistakes it for a flag.
  - **The staleness guard (the L1 spirit, applied to config).** `test_gating.py` proves gating.json <-> concepts.json coverage both ways (no missing, no stale — mirrors L1's `coverage()`), then asks the BUILT `magnus3.exe validate-config` for its own live leaf reflection (the ground truth per the research doc — "validate-config is the truth") and asserts every gating leaf is in it; a renamed/dropped leaf fails loudly instead of silently drifting.
  - **The before/after demonstration (seed 10, 400y — chosen because the DEFAULT world actually rolls necromancers, unlike the first several seeds scouted).** Default: `necromancer PAST (0 alive / 16 in the record)`, `undead PAST (0 alive / 156 in the record)`, checksum `e05d8f9711d847ae`. `--concepts=-necromancy`: `necromancer ABSENT`, `undead ABSENT` — genuinely zero EVER, not just none alive now — checksum `35a9490f5f4af750` (differs, as it must). `meta.concept_spec == "-necromancy"`, `meta.concepts_applied == ["coin/necromancy"]`. The menu and the census agree: a gated-off concept reads exactly the "absent — needs a world-gen knob" story `concepts.json`'s own `census.absent` already told for it.
  - **Enable + preset, and refusal, both proven live.** `--concepts="+vampire,+turnings:two-age@40"` compiled `{mercury.vampires.enabled: true, world.eras: [{boundary_year: 40, succession: "respawn"}]}`, echoed, generated. `--concepts="-necromancie"` (typo) refused with the suggestion list before any generation ran.
  - **Docs.** `luna-protocol.md`: stage-setting step 1 now opens with `stage menu` (offer the furniture, don't impose it) before world-shopping; the instruments table gains `menu` and `new --concepts`; a short grammar recap follows the table.
  - **Seams, stated:** a handful of compound concepts (`treasure/legendary_and_cursed`, `archetype/raider_culture`'s sibling label-only archetypes, `site/cave`) were deliberately left `gating: none` rather than wired to a lever that would silently disable MORE than the one concept (e.g. `world.sites.enabled` also kills ruins and forged Works) — a partial/misleading lever felt worse than an honest "no clean lever in v1"; eras/peoples presets are intentionally thin (two era presets, one peoples preset) per the spec's own "fine for v1" allowance, with arbitrary JSON still available via `--config-overrides` for anything richer; the individual being kinds (lion/wolf/bear/dragon/undead/construct) have no per-species toggle, only their shared coin — the registry would need a genuinely new per-kind config leaf to go further, out of scope for a stage-layer slice. **Living Toybox scorecard: L1 (the generated compendium) + L2 (the proto kit) + L3 (the session file) + L4 (the normalization report) + L5 (the concept menu) — ALL FIVE LAYERS LANDED. The improvise→normalize loop, and the pick-and-choose future behind it, are both real.**
- **The compendium brief + inquire landed + committed (Magnus3 `c7ce6a7`, pushed; 277/277 spellbook + 168/168 compendium + 70/70 proto + 33/33 gating tests, lint PASS) — the summary/inquire model, ahead of the compendium's continued growth.** Ryan's diagnosis: `luna-compendium.md` reads beautifully but is too many tokens to hand a fresh delegate-Luna whole, and it only grows. Split the KNOW layer into an always-loaded BRIEF + on-demand full entries; Python only, footprint `tools/compendium/generate.py` + `tools/stage/stage.py` + `docs/design/luna-protocol.md`, no engine changes.
  - **`stage compendium --brief` — the agent-facing index.** The cosmology preamble compressed to 9 invariant lines (determinism/edit-log/casts-only, the gods in one line, the gunas in one line, the coins in one line — full prose stays in `preamble.md`/the doc), then ONE line per concept: `kind/name -- <hook> [tag]`, section-grouped in the doc's own order. **139 lines total** (well under the 150 target) covering all 109 concepts. Each concept got a hand-tuned `hook` field seeded into `concepts.json` (8-14 words, compressing its blurb) rather than a truncation heuristic — quality over derivation, and the lint now requires it (`compendium_hook`, same idiom as the existing `compendium`/`compendium_stale`/`compendium_fresh` rows). The bracket tag (`gated`/`always`/`emergent`/`castable`) is DERIVED, not stored — `brief_tag()` reads `concepts.json`'s own `census.absent` first (castable/needs_knob→gated/emergent), falling back to `gating.json`'s shape (toggle/preset→gated, `none`+emergent→emergent, else always) — 47 always, 41 gated, 16 emergent, 5 castable across the 109.
  - **`stage compendium <name>` — inquire, fuzzy.** `find_concept()` resolves exact key (`being/vampire`) → bare unambiguous name (`vampire`) → substring against key or title (`grimoi`→`work/grimoire`) → near-miss suggestion (`vampyre`→"did you mean being/vampire?"); an ambiguous bare/substring name (`blade`→`calling/blade`+`work/blade`; `necro`→`coin/necromancy`+`wizard/necromancer`) lists every match rather than guessing. A resolved concept renders its full entry (title/hook/blurb/spot/bring/knobs + the concrete gating lever, e.g. `mercury.vampires.enabled (enable=True, disable=False)`).
  - **Composition with `--world`.** `--brief --world <c>` appends a terse `(PRESENT|PAST|ABSENT)` marker to every countable concept's line (reusing the existing `census_for()`); `<name> --world <c>` appends the FULL census line to the inquired entry instead. Verified against **TheShrinesUnderWrath**: `being/construct -- ... [gated]  (PRESENT)` (3 alive) and `being/vampire -- ... [castable]  (ABSENT)` — the brief's static tag plus the world marker together read exactly "vampires absent-castable" without duplicating the full counts (those stay in inquire's `census_for()` line, e.g. `ABSENT -- CASTABLE (SEE BRING)`).
  - **Token sanity.** Brief ≈ 12,459 chars / ~3,114 tokens (chars/4) vs the full doc's ≈ 38,620 chars / ~9,655 tokens — **the brief is ~3.1x smaller**, small enough to always load; inquire pulls in the rest only as the table needs it.
  - **Docs.** `luna-protocol.md`'s opening now tells a fresh Luna to load `stage compendium --brief` (not the full doc) and inquire per concept (`stage compendium <name>`) as the table needs it; `luna-compendium.md` reframed as the human-facing reference. Instruments table gained two rows (brief, inquire) beside the existing summary/census row.
  - **Gates.** `docs/design/luna-compendium.md` untouched byte-for-byte (`hook` isn't read by `render_body()`, so `compendium_fresh` stays green without a regen — confirmed via `--check`); regen re-hashes to the same `7b057690…` body. 135 new checks added to `test_compendium.py` (hook coverage + word-count range, tag classification, brief size/coverage/determinism, brief+world composition, find_concept's five paths, render_full_entry) — all green alongside the untouched 277/70/33.
  - **Seams, stated:** the brief's bracket tag is a coarse single-word classifier (a concept with both a toggle AND a castable census, like `being/vampire`, reports `castable` — the census wins by design, since "how do I bring this into MY world right now" reads more useful to an agent than "does the world-gen default include it"); the cosmology compression is a hand-written literal, not derived (no single source shorter than `preamble.md` itself to compress from, so it can drift from the full preamble if that prose changes — worth a lint someday, not built); `generate.py` deliberately does not import `gating.py` (stage.py wires the gating map in), keeping the KNOW layer's only new dependency on `gating.json`'s static shape, not the exe-backed staleness machinery.
- **THE LIVING TOYBOX COMPLETE (L1 `bc73bec` · L2-L4 `ed56262` · L5 `0d707a6`; 413 tests green; both repos clean/pushed) — and the day closes.** The full arc of the daylight session: session zero played and retired (the occlusion lesson) → the Luna protocol + compendium institutionalized → Ryan's direction ("generated, programmatically extendable; ad-hoc spells easily normalized; no creative energy wasted") → the improvise→normalize loop built end to end: KNOW (`stage compendium`, self-describing, lint-gated) · PLAY (`stage proto new griffin --rides lion` — the engine sees a lion, the table sees a griffin, advance carries it) · KEEP (`stage session start/beat/end`; every gap/proto/mint stamped with its birth beat) · NORMALIZE (`stage protos report` — the brief a build agent receives, draft compendium entry included) · CHOOSE (`stage menu` + `new --concepts` — 34 concepts toggleable, the necromancer-free world proven: 16→0). Next session starts at the protocol's stage-setting sequence, at the DM's pace, with the whole toybox on the table.
- **`stage world` landed + committed (Magnus3 `2b5ccaa`, pushed; 70/70 proto + 277/277 spellbook + 59/59 new world tests + 168/168 compendium + 33/33 gating, lint PASS) — gap #2 closed.** The DM's own gap from session zero, verbatim: "no one-command world dossier: DM had to hand-script community lists, being/site censuses, and doom summaries against raw state." Tonight's ask ("understand the world a bit before choosing a time and place") is the same verb. New file `tools/stage/world.py` (six reusable census functions + renderers) + one verb in `stage.py`; Python only, no engine changes.
  - **Six sections, one screen-and-a-bit.** The header (seed/years/present + the ages line — per age: name, people, span, how it ended + fallen/lost counts, lost arts if hands differ); the living world (every standing town WITH IDS — name, region, archetypes, raider flag, leader name+id, venue/shrine marks — plus a routes one-liner: hub count + isolation notes); the record's weight (per-age fallen-community counts straight off the engine's own `eras[].communities_fell`, ruins/caves/towers-standing-vs-fallen/wrecks/shrines tallies, Works: total/legendary/cursed/ownerless-in-a-ruin); the menaces (beings by kind, apex ones — dragon/vampire/construct/ghost/undead — named AND located when alive; active wizards; raider cultures); the heavens (the three doom series final+peak in readable years, each deity's current mood from its latest `DispositionShift`, the last omen read anywhere in the world — the-table.md's "last omens" closed here, foretellings outstanding at `now`); threads (3-6 auto-surfaced hooks with entity ids — most-aggrieved living pair, a recent famine, an unclaimed legendary Work, a lost-art relic, an outstanding foretelling, the oldest living thing, with isolated-town/largest-town fillers so a sparse world still hits 3).
  - **One real bug caught by the two-world drill, fixed before landing.** A construct's `necromancer_id` field (a controlling PERSON id, reused loosely across being kinds) was being looked up AS A PLACE by the first `_locate_being` draft — a construct at Couford was misnamed "at Lanfrans Mintcruscrordaerd" (its binder's own name). Fixed to consult only den/tower/community place-ids; a synthetic regression test (`test_menace_census_locates_correctly`) pins it so the binder's name can never leak into a location field again.
  - **A second seam, caught the same way: a stale cross-age relation.** In the two-age verify world, `relations` (a final-state-only snapshot) carried a hostile pairing naming Truwick — an Elder-Folk town that fell in the Age of Old Bones' respawn 350 years before `now`. The aggrieved-pair thread now requires BOTH communities alive at `now`; the synthetic test seeds exactly this case (a fallen `Old Sarn` with a stale hostile grievance) and proves it never surfaces.
  - **Verified on both required worlds.** `TheShrinesUnderWrath` (seed 45, 500y, single age, checksum `43cf1293c0a66cf9`): compact, 18 towns, 6 trade hubs, 6 legendary Works, no towers standing (both fell), one construct census correctly locates to its town once the bug above was fixed. `TheAgeOfOldBones` (seed 204, 500y, two ages — already live from the session, rendered against it per instruction rather than a throwaway; checksum `c0c355aaa30a0193`): the Age of Old Bones (Elder Folk, years 0-150, ended in respawn, 18 communities fell / 499 lost) then the ongoing Second Age (Men) — the two-age story reads correctly, Elder ruins housing constructs under Men's towns, no lost arts (this world's turning didn't gate a coin hand). Full seed-204 output captured verbatim in the session report.
  - **Tests + docs.** `test_world.py` (new, 59 checks) covers all six census functions + thread surfacing over a hand-built synthetic two-age state, same idiom as `test_proto.py`/`test_spellbook.py`. `luna-protocol.md`'s world-shopping stage-setting step now calls out `stage world` (render the whole dossier once a seed is picked, before drilling into any one town) and the instruments table gained a row.
  - **Landed alongside, not by this work: `stage compendium --brief`/inquire (`c7ce6a7`)** — a concurrent agent's slice, pulled in clean before this one started (no conflicts; disjoint footprint).
  - **Seams, stated:** the aggrieved-pair and famine threads read the raw `relations`/event trail rather than a fully tick-correct reconstruction (lore.py's stance/route graphs ARE tick-correct and are reused for routes/isolation, but relations' grievance floats are a final-state snapshot — the alive-both-ends guard above is the practical fix, not a general tick-correct rebuild); "a lost-art relic in a reachable ruin" and "an outstanding foretelling" are real thread types wired and tested, but neither world drilled fired them (no impossible-now Works, no future-dated foretellings survived to `now` in either) — proven only synthetically; `--full` expands every section but was not exercised against a very large soak world for a legibility check (both verify worlds are modest, 18-45 communities).

- **The Table view landed + committed (Magnus3 `b3618b1`, pushed; 43/43 new sessionview + 70/70 proto + 277/277 spellbook + 59/59 world + 168/168 compendium + 33/33 gating = 650 checks green, lint PASS) — the DM's gap from tonight closed.** The DM's own words: "I can't seem to see the exact prompt given to the agents in the Claude UI… I have to go into the finished agents log to view their response in whole. I want to be able to parse all the details, precisely, that's the fun in this." The session runs through seat-agents whose prompts/responses the harness occludes; now OUR viewer renders the session as first-class UI. Python + viewer JS; footprint `tools/stage/sessionfile.py` + `stage.py` + new `test_sessionview.py`, `serve.py`, `viewer/` (new `table.js` + index/main/css wiring), `docs/design/luna-protocol.md` — no engine changes.
  - **Structured beats (L3 grows a twin).** `stage session beat` now writes, ALONGSIDE the markdown block, a JSONL sidecar `NNN-<date>.beats.jsonl` — one record per beat, `{beat, ts_realtime, tick, segments:[{kind, label?, text}]}` over seven kinds `table | seat_prompt | seat_response | dice | world | luna | levers` (`label` = the character name on seat segments). Beats append STRUCTURED via `session beat --json <file>` (preferred: `{"segments":[…], "tick"?}`; a leading `@` allowed) which renders a readable markdown twin AND writes the JSONL; a rendered markdown block via `--file`/stdin still works, parsed into segments best-effort (`[TABLE]/[SEATS]/[DICE]/[WORLD]/[LUNA]/[LEVERS]` + labeled `[SEAT: Name]/[PROMPT: Name]` heads, unknown heads like `[INVENTION]` fold into the surrounding segment). Markdown-only beats written before the sidecar existed are backfilled from their markdown on first read — the .md is NEVER rewritten (backfill only appends to the sidecar), so it's safe on a live session; backfilled records carry `ts_realtime: null` + `backfilled: true`.
  - **Two thin endpoints, on the shared session.** `/api/sessions` (the campaign's sessions: number, title, date, beat count, open/closed, newest first) and `/api/session/<n>` (that session's structured beats, backfilling the sidecar on first read) — same one-DM-tool idiom as `/api/sheet`/`/api/lore`.
  - **The Table tab.** Appears beside Stats/Viewer/Performance when an opened campaign has sessions (revealed on campaign load, not on any world). Renders the session as BEAT CARDS: each segment its own collapsible `<details>` with a per-kind left rail — table narration as prose (accent-blue rail), seat prompts vs responses visually distinct (blue vs green rails, character-name chips), dice as a mono roll line, world edits as cast chips, luna reasoning gold-railed and italic, levers as a footer. A session selector shows when there's more than one; a live-poll (4s) runs while a session is open and the tab is on screen, so beats land during play without a reload. **`luna` segments respect the DM/Luna author toggle** — visible in DM mode, hidden entirely (CSS, no re-fetch) in Luna(player) mode.
  - **Protocol update.** `luna-protocol.md`'s beat contract now says beats are appended STRUCTURED (`session beat --json`), seat prompts AND responses go in VERBATIM as their own segments, and the Table tab is where the DM reads the living session; markdown remains the durable human-readable twin. Instruments table row updated.
  - **Live proof on the REAL TheAgeOfOldBones (seed 204, 500y), the session open.** Fresh server on :8099 (the DM's :8097 left untouched), campaign opened through the viewer's own Open flow → Table tab revealed → session 1 rendered as two cards, beat 1's NARRATION/WORLD/DICE/LUNA/LEVERS verbatim, marked "from markdown" (backfilled). Toggle test: Luna mode → both luna segments in the DOM but 0 visible, beat 1 shows table/world/dice/levers only; back to DM → luna returns. Poll test: a transient beat 3 appended via `session beat --json` (6 segments, both seat chips "Fernfrouskal", a real-time stamp, dice as a roll line) appeared on the poll WITHOUT a reload (meta → "3 beats"); then **removed cleanly — all three touched files (.md, .beats.jsonl, .campaign.json) restored byte-exact by SHA256, beat counter back to 2, the poll dropping the browser back to two cards.** Console clean throughout (screenshots time out on the heavy timeline canvas; verified precisely via DOM instead). The beat-1 JSONL backfill sidecar is left in place — it's derived, and the campaign files aren't git-tracked, so nothing of the test is in the commit.
  - **Seams, stated:** the markdown-header parser is best-effort — a legacy markdown block with no `[HEADER]` tokens yields no segments (there's no kind for bare prose), and the `[SEATS]` legacy header maps to a single unlabeled `seat_response` (per-character splitting only happens when beats are authored structured or with labeled `[SEAT: Name]` heads); backfilled beats have no realtime timestamp or tick (unknown from markdown alone), shown as "from markdown"; the poll is a full re-fetch gated by a cheap signature (`n:count:last-ts`) so an unchanged open session re-renders nothing, but it does re-fetch every 4s while visible (no server push); screenshot verification was DOM-only because the viewer's canvas stalls the screenshot path (a pre-existing viewer trait, not introduced here).
